schema_version: 1.0.0
meta:
  project: PixEagle
  generated_from: /home/alireza/PixEagle/configs/config_default.yaml
  generated_at: '2026-01-05T07:08:32.858647'
  description: Auto-generated schema for PixEagle configuration
categories:
  video:
    display_name: Video Input
    icon: videocam
    order: 1
  network:
    display_name: Network & Streaming
    icon: router
    order: 2
  tracking:
    display_name: Tracking
    icon: gps_fixed
    order: 3
  detection:
    display_name: Detection
    icon: search
    order: 4
  follower:
    display_name: Followers
    icon: navigation
    order: 5
  safety:
    display_name: Safety
    icon: shield
    order: 6
  control:
    display_name: Control
    icon: tune
    order: 7
  processing:
    display_name: Processing
    icon: auto_fix_high
    order: 8
  display:
    display_name: Display & Debug
    icon: tv
    order: 9
sections:
  VideoSource:
    display_name: Video Source
    category: video
    icon: videocam
    parameters:
      VIDEO_SOURCE_TYPE:
        type: string
        default: VIDEO_FILE
        description: Primary video source selection
        options:
        - value: VIDEO_FILE
          label: VIDEO_FILE
        - value: USB_CAMERA
          label: USB_CAMERA
        - value: RTSP_OPENCV
          label: RTSP_OPENCV
        - value: RTSP_STREAM
          label: RTSP_STREAM
        - value: UDP_STREAM
          label: UDP_STREAM
        - value: HTTP_STREAM
          label: HTTP_STREAM
        - value: CSI_CAMERA
          label: CSI_CAMERA
        - value: CUSTOM_GSTREAMER
          label: CUSTOM_GSTREAMER
        reload_tier: system_restart
      CAPTURE_WIDTH:
        type: integer
        default: 640
        description: Width in pixels (lower = less CPU)
        min: 0
        max: 10000
        unit: lower = less cpu
        reload_tier: system_restart
      CAPTURE_HEIGHT:
        type: integer
        default: 480
        description: Height in pixels
        min: 0
        max: 10000
        unit: px
        reload_tier: system_restart
      CAPTURE_FPS:
        type: integer
        default: 30
        description: Target FPS (actual may vary by source)
        min: 0
        max: 100
        unit: actual may vary by source
        reload_tier: system_restart
      DEFAULT_FPS:
        type: integer
        default: 30
        description: Fallback if source doesn't report FPS
        min: 0
        max: 100
        unit: fps
        reload_tier: system_restart
      USE_GSTREAMER:
        type: boolean
        default: false
        description: true = Use GStreamer pipelines (better for embedded)
        unit: better for embedded
        reload_tier: system_restart
      VIDEO_FILE_PATH:
        type: string
        default: resources/test4.mp4
        description: Video file source
        reload_tier: system_restart
      CAMERA_INDEX:
        type: integer
        default: 0
        description: 0=first camera, 1=second, etc.
        min: 0
        max: 100
        reload_tier: system_restart
      RTSP_URL:
        type: string
        default: rtsp://192.168.0.108:554/stream=0
        description: Network streams
        reload_tier: system_restart
      RTSP_PROTOCOL:
        type: string
        default: tcp
        description: '- tcp: More reliable, prevents packet loss (RECOMMENDED for most setups) - udp: Lower latency but may have packet loss on congested networks - auto: Let GStreamer choose (may fall back to UDP which can cause issues) If you see "Invalid input packet" or "no frame!" errors, switch to tcp'
        reload_tier: system_restart
      RTSP_LATENCY:
        type: integer
        default: 200
        description: '- 200: Recommended for stable streaming (good balance) - 100: Lower latency, may drop frames on slow networks - 500: More stable for unreliable connections - 0: Minimum latency (may cause decoding errors, not recommended)'
        min: 0
        max: 10000
        unit: may cause decoding errors, not recommended
        reload_tier: system_restart
      UDP_URL:
        type: string
        default: udp://172.21.144.1:5000
        description: UDP_URL parameter
        reload_tier: system_restart
      HTTP_URL:
        type: string
        default: http://172.21.144.1:8100
        description: HTTP_URL parameter
        reload_tier: system_restart
      CSI_SENSOR_ID:
        type: integer
        default: 0
        description: Usually 0 for first CSI port
        min: 0
        max: 100
        reload_tier: system_restart
      CUSTOM_PIPELINE:
        type: string
        default: ''
        description: 'Custom GStreamer pipeline (advanced users) Example: "videotestsrc ! video/x-raw,width=640,height=480 ! videoconvert ! appsink"'
        reload_tier: system_restart
      STORE_LAST_FRAMES:
        type: integer
        default: 5
        description: Frame history buffer size
        min: 0
        max: 100
        reload_tier: system_restart
  USBCamera:
    display_name: USB Camera
    category: video
    icon: usb
    parameters:
      DEVICE_PATH:
        type: string
        default: /dev/video0
        description: Device path for Linux GStreamer (ignored on Windows)
        unit: ignored on windows
        reload_tier: system_restart
      PIXEL_FORMAT:
        type: string
        default: YUYV
        description: Pixel format selection (impacts CPU usage) YUYV = uncompressed (moderate CPU) MJPG = compressed (lowest CPU, recommended for RPi)
        unit: lowest cpu, recommended for rpi
        reload_tier: system_restart
      USE_V4L2_BACKEND:
        type: boolean
        default: false
        description: 'Linux only: direct V4L2 access'
        reload_tier: system_restart
      OPENCV_BUFFER_SIZE:
        type: integer
        default: 1
        description: 1=lowest latency, 3=smoother
        min: 0
        max: 100
        reload_tier: system_restart
      OPENCV_FOURCC:
        type: string
        default: ''
        description: 'Force codec: "MJPG", "H264", etc.'
        reload_tier: system_restart
  CSICamera:
    display_name: CSI Camera
    category: video
    icon: camera
    parameters:
      SENSOR_ID:
        type: integer
        default: 0
        description: Camera sensor ID
        min: 0
        max: 100
        reload_tier: system_restart
      FLIP_METHOD:
        type: integer
        default: 0
        description: Image rotation/flip
        min: 0
        max: 100
        reload_tier: system_restart
  PX4:
    display_name: PX4 Connection
    category: network
    icon: flight
    parameters:
      EXTERNAL_MAVSDK_SERVER:
        type: boolean
        default: true
        description: 🚁 PX4 Autopilot connection via MAVSDK
        reload_tier: system_restart
      SYSTEM_ADDRESS:
        type: string
        default: udp://127.0.0.1:14540
        description: 'For SITL: udp://127.0.0.1:14540 For companion computer: udp://127.0.0.1:14540 For WSL: udp://172.21.148.30:14540 (use your WSL IP)'
        unit: use your wsl ip
        reload_tier: system_restart
  MAVLink:
    display_name: MAVLink Telemetry
    category: network
    icon: router
    parameters:
      MAVLINK_ENABLED:
        type: boolean
        default: true
        description: 📡 MAVLink telemetry data source (MAVLink2REST)
        unit: mavlink2rest
        reload_tier: system_restart
      MAVLINK_HOST:
        type: string
        default: 127.0.0.1
        description: 'For local: 127.0.0.1 For WSL: 172.21.148.30 (your WSL IP)'
        unit: your wsl ip
        reload_tier: system_restart
      MAVLINK_PORT:
        type: integer
        default: 8088
        description: MAVLINK_PORT parameter
        min: 0
        max: 1000000
        reload_tier: system_restart
      MAVLINK_POLLING_INTERVAL:
        type: float
        default: 0.5
        description: Data refresh rate in seconds
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: system_restart
      MAVLINK_DATA_POINTS:
        type: object
        default:
          latitude: /vehicles/1/components/1/messages/GLOBAL_POSITION_INT/message/lat
          longitude: /vehicles/1/components/1/messages/GLOBAL_POSITION_INT/message/lon
          altitude_msl: /vehicles/1/components/1/messages/ALTITUDE/message/altitude_amsl
          altitude_agl: /vehicles/1/components/1/messages/ALTITUDE/message/altitude_relative
          voltage: /vehicles/1/components/1/messages/SYS_STATUS/message/voltage_battery
          airspeed: /vehicles/1/components/1/messages/VFR_HUD/message/airspeed
          throttle: /vehicles/1/components/1/messages/VFR_HUD/message/throttle
          groundspeed: /vehicles/1/components/1/messages/VFR_HUD/message/groundspeed
          climb: /vehicles/1/components/1/messages/VFR_HUD/message/climb
          vn: /vehicles/1/components/1/messages/LOCAL_POSITION_NED/message/vx
          ve: /vehicles/1/components/1/messages/LOCAL_POSITION_NED/message/vy
          vd: /vehicles/1/components/1/messages/LOCAL_POSITION_NED/message/vz
          flight_path_angle: null
          roll: /vehicles/1/components/1/messages/ATTITUDE/message/roll
          pitch: /vehicles/1/components/1/messages/ATTITUDE/message/pitch
          heading: /vehicles/1/components/1/messages/VFR_HUD/message/heading
          vdop: /vehicles/1/components/1/messages/GPS_RAW_INT/message/vdop
          hdop: /vehicles/1/components/1/messages/GPS_RAW_INT/message/hdop
          satellites_visible: /vehicles/1/components/1/messages/GPS_RAW_INT/message/satellites_visible
          flight_mode: /vehicles/1/components/1/messages/HEARTBEAT/message/custom_mode
          arm_status: /vehicles/1/components/1/messages/HEARTBEAT/message/base_mode
        description: Data points to fetch from MAVLink2REST API
        reload_tier: system_restart
  Streaming:
    display_name: Video Streaming
    category: network
    icon: stream
    parameters:
      ENABLE_STREAMING:
        type: boolean
        default: true
        description: 🖥️ Enable HTTP/WebSocket video streaming to web dashboard
        reload_tier: system_restart
      HTTP_STREAM_HOST:
        type: string
        default: 0.0.0.0
        description: 0.0.0.0 = all interfaces
        reload_tier: system_restart
      HTTP_STREAM_PORT:
        type: integer
        default: 5077
        description: Web dashboard port
        min: 0
        max: 1000000
        reload_tier: system_restart
      STREAM_WIDTH:
        type: integer
        default: 640
        description: Output resolution
        min: 0
        max: 10000
        reload_tier: system_restart
      STREAM_HEIGHT:
        type: integer
        default: 480
        description: STREAM_HEIGHT parameter
        min: 0
        max: 10000
        reload_tier: system_restart
      STREAM_FPS:
        type: integer
        default: 10
        description: Target FPS (actual may vary)
        min: 0
        max: 100
        unit: actual may vary
        reload_tier: system_restart
      STREAM_QUALITY:
        type: integer
        default: 50
        description: Default JPEG quality (0-100)
        min: 0
        max: 100
        unit: 0-100
        reload_tier: system_restart
      STREAM_PROCESSED_OSD:
        type: boolean
        default: true
        description: Include overlays
        reload_tier: system_restart
      ENABLE_ADAPTIVE_QUALITY:
        type: boolean
        default: true
        description: Set true for auto quality adjustment
        reload_tier: system_restart
      MIN_QUALITY:
        type: integer
        default: 30
        description: Minimum JPEG quality (1-100)
        min: 0
        max: 100
        unit: 1-100
        reload_tier: system_restart
      MAX_QUALITY:
        type: integer
        default: 85
        description: Maximum JPEG quality (1-100)
        min: 0
        max: 100
        unit: 1-100
        reload_tier: system_restart
      QUALITY_STEP:
        type: integer
        default: 10
        description: Quality adjustment step size
        min: 0
        max: 100
        reload_tier: system_restart
      ENABLE_FRAME_CACHE:
        type: boolean
        default: true
        description: Set true to cache encoded frames
        reload_tier: system_restart
      CACHE_TTL_MS:
        type: integer
        default: 100
        description: Cache time-to-live in milliseconds
        min: 0
        max: 100
        reload_tier: system_restart
      SKIP_IDENTICAL_FRAMES:
        type: boolean
        default: true
        description: Skip encoding if frame unchanged
        reload_tier: system_restart
      HTTP_MAX_CONNECTIONS:
        type: integer
        default: 20
        description: Max concurrent HTTP streams
        min: 0
        max: 100
        reload_tier: system_restart
      WS_MAX_CONNECTIONS:
        type: integer
        default: 10
        description: Max concurrent WebSocket clients
        min: 0
        max: 100
        reload_tier: system_restart
      MAX_FRAME_QUEUE:
        type: integer
        default: 3
        description: Max queued frames per WebSocket client
        min: 0
        max: 100
        reload_tier: system_restart
      HTTP_CHUNK_SIZE:
        type: integer
        default: 65536
        description: Chunk size for streaming
        min: 0
        max: 1000000
        reload_tier: system_restart
      ENCODING_THREADS:
        type: integer
        default: 2
        description: Parallel encoding threads
        min: 0
        max: 100
        reload_tier: system_restart
      ENABLE_HARDWARE_ENCODING:
        type: boolean
        default: false
        description: Use hardware encoder if available
        reload_tier: system_restart
      FRAME_DIFFERENCE_THRESHOLD:
        type: integer
        default: 5
        description: Pixel difference threshold
        min: 0
        max: 100
        reload_tier: system_restart
      ENABLE_H264_STREAM:
        type: boolean
        default: false
        description: Enable H.264/WebRTC streaming
        reload_tier: system_restart
      H264_BITRATE:
        type: integer
        default: 1000000
        description: H.264 bitrate (bps)
        min: 0
        max: 1000000
        unit: bps
        reload_tier: system_restart
      H264_PROFILE:
        type: string
        default: baseline
        description: H.264 profile (baseline/main/high)
        unit: baseline/main/high
        reload_tier: system_restart
      H264_PRESET:
        type: string
        default: ultrafast
        description: Encoding preset
        reload_tier: system_restart
  Tracking:
    display_name: Tracking Settings
    category: tracking
    icon: gps_fixed
    parameters:
      DEFAULT_TRACKING_ALGORITHM:
        type: string
        default: CSRT
        description: 'CSRT: Best for drone circling, rotation, perspective changes (15-20 FPS, 85% accuracy) KCF: Fast alternative for high-speed scenarios (25-30 FPS, 75% accuracy) dlib: Very fast correlation filter with PSR confidence (25-30 FPS, 85% accuracy)'
        options:
        - value: CSRT
          label: CSRT
        - value: KCF
          label: KCF
        - value: dlib
          label: dlib
        - value: Gimbal
          label: Gimbal
        unit: 25-30 fps, 85% accuracy
        reload_tier: immediate
      TRACKING_RECTANGLE_COLOR:
        type: array
        default:
        - 255
        - 0
        - 0
        description: 'BGR: Blue'
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: immediate
      CENTER_CIRCLE_COLOR:
        type: array
        default:
        - 0
        - 255
        - 0
        description: 'BGR: Green'
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: immediate
      CENTER_HISTORY_LENGTH:
        type: integer
        default: 10
        description: Trail length
        min: 0
        max: 100
        reload_tier: immediate
      MAX_DISPLACEMENT_THRESHOLD:
        type: float
        default: 0.25
        description: Max jump as fraction of frame
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      TRACKING_FAILURE_TIMEOUT:
        type: float
        default: 5.0
        description: Seconds before re-detection
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: immediate
      REDETECTION_ATTEMPTS:
        type: integer
        default: 5
        description: Max re-detection attempts
        min: 0
        max: 100
        reload_tier: immediate
      CONFIDENCE_THRESHOLD:
        type: float
        default: 0.6
        description: '⚠️ Deprecated: Use tracker-specific'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      APPEARANCE_THRESHOLD:
        type: float
        default: 0.3
        description: APPEARANCE_THRESHOLD parameter
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      APPEARANCE_CONFIDENCE_THRESHOLD:
        type: float
        default: 0.5
        description: Re-detection validation
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      MOTION_CONFIDENCE_WEIGHT:
        type: float
        default: 0.5
        description: MOTION_CONFIDENCE_WEIGHT parameter
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      APPEARANCE_CONFIDENCE_WEIGHT:
        type: float
        default: 0.5
        description: APPEARANCE_CONFIDENCE_WEIGHT parameter
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      CSRT_APPEARANCE_LEARNING_RATE:
        type: float
        default: 0.05
        description: 'Deprecated: See CSRT_Tracker section'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      MOTION_CONFIDENCE_THRESHOLD:
        type: float
        default: 0.7
        description: Minimum confidence from motion consistency check
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      PF_NUM_PARTICLES:
        type: integer
        default: 500
        description: More = accurate but slower
        min: 0
        max: 10000
        reload_tier: immediate
      PF_INIT_POS_STD:
        type: float
        default: 20.0
        description: Initial position uncertainty
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: immediate
      PF_INIT_VEL_STD:
        type: float
        default: 5.0
        description: Initial velocity uncertainty
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: immediate
      PF_INIT_ACC_STD:
        type: float
        default: 1.0
        description: Initial acceleration uncertainty
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      PF_POS_STD:
        type: float
        default: 15.0
        description: Process noise for position
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: immediate
      PF_VEL_STD:
        type: float
        default: 5.0
        description: Process noise for velocity
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: immediate
      PF_ACC_STD:
        type: float
        default: 1.0
        description: Process noise for acceleration
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      PF_APPEARANCE_LIKELIHOOD_SCALE:
        type: float
        default: 25.0
        description: PF_APPEARANCE_LIKELIHOOD_SCALE parameter
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: immediate
      PF_APPEARANCE_LEARNING_RATE:
        type: float
        default: 0.05
        description: PF_APPEARANCE_LEARNING_RATE parameter
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      PF_COLOR_WEIGHT:
        type: float
        default: 0.7
        description: Color vs edge importance
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      PF_EDGE_WEIGHT:
        type: float
        default: 0.3
        description: PF_EDGE_WEIGHT parameter
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      PF_CANNY_THRESHOLD1:
        type: integer
        default: 50
        description: Edge detection params
        min: 0
        max: 100
        reload_tier: immediate
      PF_CANNY_THRESHOLD2:
        type: integer
        default: 150
        description: PF_CANNY_THRESHOLD2 parameter
        min: 0
        max: 10000
        reload_tier: immediate
      PF_EFFECTIVE_PARTICLE_NUM_THRESHOLD:
        type: float
        default: 0.5
        description: PF_EFFECTIVE_PARTICLE_NUM_THRESHOLD parameter
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      PF_RANDOM_PARTICLE_RATIO:
        type: float
        default: 0.1
        description: Random resampling ratio
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      DESIRE_AIM:
        type: array
        default:
        - 0
        - 0
        description: Desired aim point in frame [x, y] (normalized)
        item_type: number
        min_items: 0
        max_items: 20
        unit: normalized
        reload_tier: immediate
  TrackerSafety:
    display_name: Tracker Safety
    category: tracking
    icon: security
    parameters:
      BOUNDARY_MARGIN_PIXELS:
        type: integer
        default: 15
        description: Global access for base_tracker.py
        min: 0
        max: 100
        reload_tier: tracker_restart
      ENABLE_BOUNDARY_PENALTY:
        type: boolean
        default: true
        description: Apply confidence penalty near frame edges
        reload_tier: tracker_restart
      BOUNDARY_PENALTY_MIN:
        type: float
        default: 0.5
        description: Minimum confidence multiplier at edge (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
  BOUNDARY_MARGIN_PIXELS:
    type: integer
    default: 15
    description: Global access for base_tracker.py
    reload_tier: immediate
    min: 0
    max: 100
  KCF_Tracker:
    display_name: KCF Tracker
    category: tracking
    icon: track_changes
    parameters:
      confidence_threshold:
        type: float
        default: 0.15
        description: Min confidence to accept (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      failure_threshold:
        type: integer
        default: 7
        description: Consecutive failures before declaring lost
        min: 0
        max: 100
        reload_tier: tracker_restart
      confidence_smoothing:
        type: float
        default: 0.6
        description: EMA alpha (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      max_scale_change_per_frame:
        type: float
        default: 0.6
        description: Max size change per frame (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      max_motion_per_frame:
        type: float
        default: 0.7
        description: Max motion as fraction of frame diagonal
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      appearance_learning_rate:
        type: float
        default: 0.18
        description: Template update learning rate (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      kalman_process_noise:
        type: float
        default: 0.1
        description: Process noise (Q matrix multiplier)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: q matrix multiplier
        reload_tier: tracker_restart
      kalman_velocity_noise_factor:
        type: float
        default: 0.5
        description: Velocity noise relative to position noise
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      kalman_measurement_noise:
        type: float
        default: 5.0
        description: Measurement noise (R matrix, ~pixels)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: r matrix, ~pixels
        reload_tier: tracker_restart
      kalman_initial_covariance:
        type: float
        default: 10.0
        description: Initial state covariance (P matrix)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: p matrix
        reload_tier: tracker_restart
      motion_consistency_threshold:
        type: float
        default: 0.15
        description: Max motion as fraction of frame diagonal
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      use_velocity_during_occlusion:
        type: boolean
        default: true
        description: Use Kalman velocity for position extrapolation
        reload_tier: tracker_restart
      occlusion_velocity_factor:
        type: float
        default: 0.5
        description: Conservative extrapolation (50% of velocity)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 50% of velocity
        reload_tier: tracker_restart
      kalman_initial_position_covariance:
        type: float
        default: 10.0
        description: 'Position: high confidence (known)'
        min: 0.0
        max: 100.0
        step: 0.1
        unit: known
        reload_tier: tracker_restart
      kalman_initial_velocity_covariance:
        type: float
        default: 100.0
        description: 'Velocity: low confidence (unknown at start)'
        min: 0.0
        max: 100.0
        step: 0.1
        unit: unknown at start
        reload_tier: tracker_restart
  CSRT_Tracker:
    display_name: CSRT Tracker
    category: tracking
    icon: track_changes
    parameters:
      performance_mode:
        type: string
        default: robust
        description: 'Choose: "fast", "balanced", or "robust"'
        reload_tier: tracker_restart
      confidence_threshold:
        type: float
        default: 0.45
        description: Min confidence to accept (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      failure_threshold:
        type: integer
        default: 5
        description: Consecutive failures before declaring lost
        min: 0
        max: 100
        reload_tier: tracker_restart
      confidence_smoothing:
        type: float
        default: 0.7
        description: EMA alpha (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      validation_start_frame:
        type: integer
        default: 10
        description: Grace period before validation starts
        min: 0
        max: 100
        reload_tier: tracker_restart
      max_scale_change_per_frame:
        type: float
        default: 0.5
        description: Max size change per frame (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      max_motion_per_frame:
        type: float
        default: 0.6
        description: Max motion as fraction of frame diagonal
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      appearance_learning_rate:
        type: float
        default: 0.1
        description: Template update learning rate (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      use_color_names:
        type: boolean
        default: true
        description: ColorNames features (+5-8% accuracy for colored targets)
        unit: +5-8% accuracy for colored targets
        reload_tier: tracker_restart
      use_hog:
        type: boolean
        default: true
        description: HOG features (essential for shape-based tracking)
        unit: essential for shape-based tracking
        reload_tier: tracker_restart
      csrt_learning_rate:
        type: float
        default: 0.02
        description: Filter learning rate (0.02 faster than default 0.015)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.02 faster than default 0.015
        reload_tier: tracker_restart
      number_of_scales:
        type: integer
        default: 33
        description: Scale pyramid levels (33 for finer scale precision)
        min: 0
        max: 100
        unit: 33 for finer scale precision
        reload_tier: tracker_restart
      scale_step:
        type: float
        default: 1.02
        description: Scale factor between levels (1.02 for smooth adaptation)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: 1.02 for smooth adaptation
        reload_tier: tracker_restart
      use_segmentation:
        type: boolean
        default: true
        description: Foreground/background segmentation
        reload_tier: tracker_restart
      appearance_update_min_confidence:
        type: float
        default: 0.55
        description: Only update appearance when conf > this
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      enable_multiframe_validation:
        type: boolean
        default: true
        description: Require N consecutive valid frames
        reload_tier: tracker_restart
      validation_consensus_frames:
        type: integer
        default: 3
        description: Frames required for consensus
        min: 0
        max: 100
        reload_tier: tracker_restart
  DLIB_Tracker:
    display_name: dlib Tracker
    category: tracking
    icon: track_changes
    parameters:
      performance_mode:
        type: string
        default: robust
        description: 'Choose: "fast", "balanced", or "robust"'
        reload_tier: tracker_restart
      psr_confidence_threshold:
        type: float
        default: 7.0
        description: Min PSR for reliable tracking
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: tracker_restart
      psr_high_confidence:
        type: float
        default: 20.0
        description: PSR above this = excellent tracking
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: tracker_restart
      psr_low_confidence:
        type: float
        default: 5.0
        description: PSR below this = poor tracking
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: tracker_restart
      failure_threshold:
        type: integer
        default: 5
        description: Consecutive failures before declaring lost
        min: 0
        max: 100
        reload_tier: tracker_restart
      confidence_smoothing_alpha:
        type: float
        default: 0.7
        description: EMA (Exponential Moving Average) alpha
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      validation_start_frame:
        type: integer
        default: 10
        description: Grace period before validation starts
        min: 0
        max: 100
        reload_tier: tracker_restart
      max_scale_change_per_frame:
        type: float
        default: 0.5
        description: Max size change per frame (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      max_motion_per_frame:
        type: float
        default: 0.6
        description: Max motion as fraction of frame diagonal
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      appearance_learning_rate:
        type: float
        default: 0.08
        description: Template update learning rate (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      enable_template_redetection:
        type: boolean
        default: true
        description: Use template matching for recovery
        reload_tier: tracker_restart
      redetection_psr_threshold:
        type: float
        default: 5.0
        description: Trigger re-detection below this PSR
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: tracker_restart
      adaptive:
        type: object
        default:
          enable: true
          psr_dynamic_scaling: true
          adapt_rate: 0.15
          psr_margin: 1.5
        description: 'Automatically adjusts PSR thresholds based on tracking history Research: Adaptive thresholding improves robustness in varying conditions Reference: Zhang et al. "Adaptive Correlation Filters with Long-Term and Short-Term Memory" IJCV 2018'
        reload_tier: tracker_restart
      appearance:
        type: object
        default:
          use_adaptive_learning: true
          adaptive_learning_bounds:
          - 0.05
          - 0.15
          freeze_on_low_confidence: true
          reference_update_interval: 30
        description: 'Advanced template update strategies for long-term tracking stability Research: Adaptive learning prevents template drift and corruption Reference: Danelljan et al. "Learning Spatially Regularized Correlation Filters" ICCV 2015'
        reload_tier: tracker_restart
      motion:
        type: object
        default:
          velocity_limit: 25.0
          velocity_normalize_by_size: true
          max_velocity_target_factor: 2.0
          stabilization_alpha: 0.3
        description: 'Advanced motion filtering and validation for smooth, reliable tracking Research: Temporal consistency improves tracking in noisy conditions'
        reload_tier: tracker_restart
      validation:
        type: object
        default:
          reinit_on_loss: true
          cooldown_after_reinit: 5
        description: Smart validation and recovery mechanisms for handling tracking loss
        reload_tier: tracker_restart
      debug:
        type: object
        default:
          enable_visual_feedback: true
          show_motion_vectors: false
        description: Visual feedback for tracking quality monitoring
        reload_tier: tracker_restart
  SmartTracker:
    display_name: Smart Tracker (YOLO)
    category: tracking
    icon: smart_toy
    parameters:
      SMART_TRACKER_ENABLED:
        type: boolean
        default: true
        description: 🤖 Enable AI-powered object tracking
        reload_tier: tracker_restart
      SMART_TRACKER_USE_GPU:
        type: boolean
        default: true
        description: Try GPU first (CUDA)
        unit: cuda
        reload_tier: tracker_restart
      SMART_TRACKER_FALLBACK_TO_CPU:
        type: boolean
        default: true
        description: Fall back if GPU fails
        reload_tier: tracker_restart
      SMART_TRACKER_GPU_MODEL_PATH:
        type: string
        default: yolo/yolo11n.pt
        description: PyTorch model
        reload_tier: tracker_restart
      SMART_TRACKER_CPU_MODEL_PATH:
        type: string
        default: yolo/yolo11n_ncnn_model
        description: Optimized for ARM
        reload_tier: tracker_restart
      SMART_TRACKER_CONFIDENCE_THRESHOLD:
        type: float
        default: 0.3
        description: Min detection confidence (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      SMART_TRACKER_IOU_THRESHOLD:
        type: float
        default: 0.3
        description: Overlap threshold for NMS (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: tracker_restart
      SMART_TRACKER_MAX_DETECTIONS:
        type: integer
        default: 20
        description: Max objects per frame
        min: 0
        max: 100
        reload_tier: tracker_restart
      SMART_TRACKER_COLOR:
        type: array
        default:
        - 0
        - 255
        - 255
        description: 'BGR: Yellow'
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: tracker_restart
      SMART_TRACKER_SHOW_FPS:
        type: boolean
        default: false
        description: SMART_TRACKER_SHOW_FPS parameter
        reload_tier: tracker_restart
      TRACKER_TYPE:
        type: string
        default: botsort_reid
        description: 'botsort         | -3-5%      | Low          | ✓       | No         | Any version botsort_reid    | -5-8%      | Excellent    | ✗       | Recommended| Required custom_reid     | -8-12%     | Good         | ✓       | No         | Any version Options:'
        reload_tier: tracker_restart
      BOTSORT_REID_MODEL:
        type: string
        default: auto
        description: 'ReID embedding model selection "auto": Uses native YOLO features (fastest, zero overhead, RECOMMENDED) "yolo11n-cls.pt": Classification model (better accuracy, +2-5ms latency) Custom path: Your own ReID embedding model'
        reload_tier: tracker_restart
      BOTSORT_APPEARANCE_THRESH:
        type: float
        default: 0.25
        description: 'Lower = stricter matching (fewer false positives, may miss correct matches) Higher = more lenient (more false positives, better recall) ⚠️ NOTE: Opposite direction from APPEARANCE_MATCH_THRESHOLD! Recommended: 0.20-0.30'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BOTSORT_PROXIMITY_THRESH:
        type: float
        default: 0.5
        description: 'IoU (geometric proximity) threshold for matching Works alongside appearance threshold Recommended: 0.4-0.6'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BOTSORT_TRACK_BUFFER:
        type: integer
        default: 60
        description: 'Number of frames to remember lost tracks (for re-identification) Higher = can recover after longer occlusions (uses more memory) Recommended: 30-120 frames 30 ≈ 1 second, 60 ≈ 2 seconds, 120 ≈ 4 seconds at 30 FPS'
        min: 0
        max: 100
        unit: s
        reload_tier: tracker_restart
      BOTSORT_MATCH_THRESH:
        type: float
        default: 0.8
        description: 'IoU threshold for matching detections to existing tracks Higher = stricter (fewer ID switches) Lower = more lenient (better recovery) Recommended: 0.7-0.9'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BOTSORT_TRACK_HIGH_THRESH:
        type: float
        default: 0.25
        description: 'High confidence detection threshold (first association pass) Recommended: 0.20-0.30'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BOTSORT_TRACK_LOW_THRESH:
        type: float
        default: 0.1
        description: 'Low confidence detection threshold (recovery pass) Recommended: 0.05-0.15'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BOTSORT_NEW_TRACK_THRESH:
        type: float
        default: 0.25
        description: 'Minimum confidence for creating new tracks Lower = easier to start tracking Higher = only track high-confidence objects Recommended: 0.20-0.30'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BOTSORT_FUSE_SCORE:
        type: boolean
        default: true
        description: Fuse detection confidence with IoU during matching true = better discrimination (recommended) false = pure geometric matching
        reload_tier: tracker_restart
      BOTSORT_CMC_METHOD:
        type: string
        default: sparseOptFlow
        description: 'Camera motion compensation method "sparseOptFlow": Optical flow-based (recommended) "none": Disable (use if camera is stationary)'
        unit: use if camera is stationary
        reload_tier: tracker_restart
      TRACKING_STRATEGY:
        type: string
        default: hybrid
        description: 'How to match tracked object across frames when ID changes - "id_only": Strict ID matching (fastest, but loses track on ID change) - "spatial_only": Position-based matching using IoU (works without IDs) - "hybrid": ID first, falls back to spatial matching (RECOMMENDED)'
        unit: recommended
        reload_tier: tracker_restart
      ID_LOSS_TOLERANCE_FRAMES:
        type: integer
        default: 5
        description: 'Number of frames to maintain tracking after losing track ID Useful when object is briefly occluded or detector misses 1-2 frames Higher = more forgiving, Lower = stricter (re-select needed sooner) Recommended: 5-10 frames for drone tracking'
        min: 0
        max: 100
        reload_tier: tracker_restart
      ENABLE_GRACEFUL_DEGRADATION:
        type: boolean
        default: true
        description: Enable multi-level fallback chain
        reload_tier: tracker_restart
      EXTENDED_TOLERANCE_FRAMES:
        type: integer
        default: 10
        description: Additional frames beyond ID_LOSS_TOLERANCE
        min: 0
        max: 100
        reload_tier: tracker_restart
      TRACK_CONFIDENCE_DECAY_RATE:
        type: float
        default: 0.05
        description: Decay rate per frame without detection (5%)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 5%
        reload_tier: tracker_restart
      MAX_LOST_OBJECTS_CACHED:
        type: integer
        default: 100
        description: Maximum objects in re-ID memory
        min: 0
        max: 100
        reload_tier: tracker_restart
      SPATIAL_IOU_THRESHOLD:
        type: float
        default: 0.35
        description: 'IoU threshold for matching by position when ID is lost Higher = stricter matching (less false positives) Lower = more lenient (might match wrong object) Range: 0.0-1.0, Recommended: 0.3-0.5'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      ENABLE_PREDICTION_BUFFER:
        type: boolean
        default: true
        description: Use velocity-based prediction to estimate object location during occlusion Helps find object even if it moves while temporarily invisible true = more robust to occlusions, false = simpler/faster
        reload_tier: tracker_restart
      CONFIDENCE_SMOOTHING_ALPHA:
        type: float
        default: 0.8
        description: 'Apply exponential moving average to detection confidence Reduces jitter from frame-to-frame confidence variations Higher = more smoothing, Lower = more responsive Range: 0.0-1.0, Recommended: 0.7-0.9'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BYTETRACK_TRACK_BUFFER:
        type: integer
        default: 50
        description: 'Number of frames to keep track alive after losing detection Higher = tracks survive longer occlusions Lower = faster cleanup of lost tracks Default: 30, Recommended for robust tracking: 50'
        min: 0
        max: 100
        reload_tier: tracker_restart
      BYTETRACK_MATCH_THRESH:
        type: float
        default: 0.8
        description: 'IoU threshold for matching detections to existing tracks Higher = stricter matching (less ID switches) Lower = more lenient (may cause ID switches) Range: 0.0-1.0, Recommended: 0.8'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BYTETRACK_NEW_TRACK_THRESH:
        type: float
        default: 0.2
        description: 'Confidence threshold for creating new tracks from detections Lower = easier to start tracking new objects (good for re-acquisition) Higher = only track high-confidence detections (fewer false positives) Range: 0.0-1.0, Recommended: 0.20-0.25'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BYTETRACK_TRACK_HIGH_THRESH:
        type: float
        default: 0.25
        description: 'ByteTrack uses two-stage association (high conf → low conf) High threshold: First association pass (confident detections) Low threshold: Second association pass (recover lost tracks)'
        min: 0.0
        max: 1.0
        step: 0.01
        unit: recover lost tracks
        reload_tier: tracker_restart
      BYTETRACK_TRACK_LOW_THRESH:
        type: float
        default: 0.1
        description: BYTETRACK_TRACK_LOW_THRESH parameter
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      BYTETRACK_FUSE_SCORE:
        type: boolean
        default: true
        description: Combine detection confidence with IoU distance during matching true = better handling of similar objects false = pure geometric matching
        reload_tier: tracker_restart
      ENABLE_APPEARANCE_MODEL:
        type: boolean
        default: true
        description: Set to false on low-performance boards (Raspberry Pi Zero, older ARM) Adds ~5-10ms per frame overhead on modern hardware
        reload_tier: tracker_restart
      APPEARANCE_MATCH_THRESHOLD:
        type: float
        default: 0.7
        description: 'Recommended values: - 0.8-0.9: Very strict (use when objects look very similar) - 0.6-0.7: Balanced (recommended for most scenarios) - 0.4-0.5: Lenient (use when lighting/angle changes dramatically)'
        min: 0.0
        max: 1.0
        step: 0.01
        unit: use when lighting/angle changes dramatically
        reload_tier: tracker_restart
      APPEARANCE_FEATURE_TYPE:
        type: string
        default: histogram
        description: '- Best for: Maximum accuracy in challenging scenarios - Speed: Slower (~8-10ms per object) - Accuracy: Highest overall - Recommended for: Powerful hardware, critical applications'
        reload_tier: tracker_restart
      MAX_REIDENTIFICATION_FRAMES:
        type: integer
        default: 30
        description: 'Recommended values: - 30 frames: ~1 second at 30 FPS (good for brief occlusions) - 60 frames: ~2 seconds (balanced) - 150 frames: ~5 seconds (long occlusions, uses more memory)'
        min: 0
        max: 100
        unit: long occlusions, uses more memory
        reload_tier: tracker_restart
      APPEARANCE_ADAPTIVE_LEARNING:
        type: boolean
        default: true
        description: Update appearance model during successful tracking Helps adapt to gradual changes in lighting, viewing angle, or object appearance true = model adapts over time (better for changing conditions) false = uses initial appearance only (more consistent but rigid)
        unit: more consistent but rigid
        reload_tier: tracker_restart
      APPEARANCE_LEARNING_RATE:
        type: float
        default: 0.1
        description: 'Learning rate for adaptive updates (0.0-1.0) Higher = faster adaptation to changes (may drift from original appearance) Lower = slower adaptation (more stable, less responsive to changes) Recommended: 0.05-0.15'
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      HOG_WIN_SIZE:
        type: array
        default:
        - 64
        - 64
        description: ROI resize dimensions (width, height)
        item_type: number
        min_items: 0
        max_items: 20
        unit: width, height
        reload_tier: tracker_restart
      HOG_BLOCK_SIZE:
        type: array
        default:
        - 16
        - 16
        description: Block size for normalization
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: tracker_restart
      HOG_BLOCK_STRIDE:
        type: array
        default:
        - 8
        - 8
        description: Step size for block overlap
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: tracker_restart
      HOG_CELL_SIZE:
        type: array
        default:
        - 8
        - 8
        description: Cell size within blocks
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: tracker_restart
      HOG_NBINS:
        type: integer
        default: 9
        description: Number of orientation bins (1-180 degrees)
        min: 0
        max: 100
        unit: 1-180 degrees
        reload_tier: tracker_restart
      HIST_H_BINS:
        type: integer
        default: 30
        description: Hue channel bins (H in HSV)
        min: 0
        max: 100
        unit: h in hsv
        reload_tier: tracker_restart
      HIST_S_BINS:
        type: integer
        default: 32
        description: Saturation channel bins (S in HSV)
        min: 0
        max: 100
        unit: s in hsv
        reload_tier: tracker_restart
      ENABLE_APPEARANCE_PROFILING:
        type: boolean
        default: false
        description: true = log timing metrics to console
        reload_tier: tracker_restart
  GimbalTracker:
    display_name: Gimbal Tracker
    category: tracking
    icon: control_camera
    parameters:
      ENABLED:
        type: boolean
        default: true
        description: Enable/disable gimbal tracker
        reload_tier: tracker_restart
      UDP_HOST:
        type: string
        default: 192.168.0.108
        description: Gimbal IP address
        reload_tier: tracker_restart
      UDP_PORT:
        type: integer
        default: 9003
        description: Gimbal UDP port
        min: 0
        max: 1000000
        reload_tier: tracker_restart
      CONNECTION_TIMEOUT:
        type: float
        default: 5.0
        description: Connection timeout in seconds
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: tracker_restart
      data_timeout_seconds:
        type: float
        default: 5.0
        description: Show cached data for up to N seconds when connection lost
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: tracker_restart
      max_consecutive_failures:
        type: integer
        default: 10
        description: Warning threshold for consecutive data reception failures
        min: 0
        max: 100
        reload_tier: tracker_restart
  GIMBAL_UDP_HOST:
    type: string
    default: 192.168.0.108
    description: Gimbal IP address
    reload_tier: immediate
  GIMBAL_LISTEN_PORT:
    type: integer
    default: 9004
    description: Port for listening to gimbal data
    reload_tier: immediate
    min: 0
    max: 1000000
  GIMBAL_COORDINATE_SYSTEM:
    type: string
    default: GIMBAL_BODY
    description: Coordinate system preference
    reload_tier: immediate
  GIMBAL_DISABLE_ESTIMATOR:
    type: boolean
    default: true
    description: Disable Kalman estimator for gimbal
    reload_tier: immediate
  GimbalTrackerSettings:
    display_name: Gimbal Tracker Settings
    category: tracking
    icon: settings
    parameters:
      COORDINATE_SYSTEM:
        type: string
        default: GIMBAL_BODY
        description: COORDINATE_SYSTEM parameter
        options:
        - value: GIMBAL_BODY
          label: GIMBAL_BODY
        - value: SPATIAL_FIXED
          label: SPATIAL_FIXED
        reload_tier: tracker_restart
      CAMERA_MOUNT_OFFSET_ROLL:
        type: float
        default: 0.0
        description: Roll offset
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      CAMERA_MOUNT_OFFSET_PITCH:
        type: float
        default: 0.0
        description: Pitch offset (+ = nose down from aircraft)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: + = nose down from aircraft
        reload_tier: tracker_restart
      CAMERA_MOUNT_OFFSET_YAW:
        type: float
        default: 0.0
        description: Yaw offset (+ = right from aircraft nose)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: + = right from aircraft nose
        reload_tier: tracker_restart
      YAW_LIMIT:
        type: float
        default: 180.0
        description: Maximum yaw angle (degrees)
        min: -10000.0
        max: 10000.0
        step: 0.1
        unit: deg
        reload_tier: tracker_restart
      PITCH_LIMIT:
        type: float
        default: 90.0
        description: Maximum pitch angle (degrees)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: tracker_restart
      ROLL_LIMIT:
        type: float
        default: 180.0
        description: Maximum roll angle (degrees)
        min: -10000.0
        max: 10000.0
        step: 0.1
        unit: deg
        reload_tier: tracker_restart
      SUPPRESS_DETECTOR:
        type: boolean
        default: true
        description: Disable image detector (not needed for gimbal)
        unit: not needed for gimbal
        reload_tier: tracker_restart
      SUPPRESS_PREDICTOR:
        type: boolean
        default: true
        description: Disable predictor (not needed for gimbal)
        unit: not needed for gimbal
        reload_tier: tracker_restart
      DISABLE_ESTIMATOR:
        type: boolean
        default: true
        description: Disable Kalman estimator (gimbal provides direct data)
        unit: gimbal provides direct data
        reload_tier: tracker_restart
      MAX_DATA_AGE:
        type: float
        default: 5.0
        description: Maximum age of data to consider valid (seconds)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: tracker_restart
  Follower:
    display_name: Follower Settings
    category: follower
    icon: navigation
    parameters:
      USE_MAVLINK2REST:
        type: boolean
        default: true
        description: Use REST API for MAVLink
        reload_tier: follower_restart
      FOLLOWER_DATA_REFRESH_RATE:
        type: integer
        default: 5
        description: Hz
        min: 0
        max: 100
        unit: hz
        reload_tier: follower_restart
      ROI_SELECTION_MODE:
        type: string
        default: MANUAL
        description: MANUAL or AUTO
        reload_tier: follower_restart
      TARGET_POSITION_MODE:
        type: string
        default: center
        description: 'Target positioning: "center", "initial"'
        reload_tier: follower_restart
      FOLLOWER_MODE:
        type: string
        default: mc_velocity_position
        description: FOLLOWER_MODE parameter
        options:
        - value: mc_velocity_ground
          label: mc_velocity_ground
        - value: mc_velocity_distance
          label: mc_velocity_distance
        - value: mc_velocity_position
          label: mc_velocity_position
        - value: mc_velocity_chase
          label: mc_velocity_chase
        - value: mc_velocity
          label: mc_velocity
        - value: mc_attitude_rate
          label: mc_attitude_rate
        - value: fw_attitude_rate
          label: fw_attitude_rate
        - value: gm_pid_pursuit
          label: gm_pid_pursuit
        - value: gm_velocity_vector
          label: gm_velocity_vector
        reload_tier: follower_restart
      SHOW_TRACKING_WINDOW:
        type: boolean
        default: false
        description: Debug window
        reload_tier: follower_restart
      DISPLAY_DEVIATIONS:
        type: boolean
        default: false
        description: Show error vectors
        reload_tier: follower_restart
      TRACKED_BBOX_STYLE:
        type: string
        default: fancy
        description: simple or fancy
        reload_tier: follower_restart
      FOLLOWER_ACTIVE_COLOR:
        type: array
        default:
        - 0
        - 0
        - 255
        description: 'BGR: Red when active'
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: follower_restart
      FOLLOWER_INACTIVE_COLOR:
        type: array
        default:
        - 0
        - 255
        - 255
        description: 'BGR: Yellow when inactive'
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: follower_restart
      BBOX_LINE_THICKNESS:
        type: integer
        default: 2
        description: BBOX_LINE_THICKNESS parameter
        min: 0
        max: 100
        reload_tier: follower_restart
      EXTENDED_LINE_THICKNESS:
        type: integer
        default: 2
        description: EXTENDED_LINE_THICKNESS parameter
        min: 0
        max: 100
        reload_tier: follower_restart
      CORNER_DOT_RADIUS:
        type: integer
        default: 4
        description: CORNER_DOT_RADIUS parameter
        min: 0
        max: 100
        reload_tier: follower_restart
      CROSSHAIR_ARM_LENGTH:
        type: integer
        default: 20
        description: CROSSHAIR_ARM_LENGTH parameter
        min: 0
        max: 100
        reload_tier: follower_restart
      BBOX_CORNER_ARM_LENGTH:
        type: integer
        default: 20
        description: BBOX_CORNER_ARM_LENGTH parameter
        min: 0
        max: 100
        reload_tier: follower_restart
      OVERLAY_OPACITY:
        type: integer
        default: 128
        description: OVERLAY_OPACITY parameter
        min: 0
        max: 10000
        reload_tier: follower_restart
  FOLLOWER_CIRCUIT_BREAKER:
    type: boolean
    default: true
    description: true = SAFE (log only), false = LIVE (send commands)
    reload_tier: immediate
    unit: send commands
  CIRCUIT_BREAKER_DISABLE_SAFETY:
    type: boolean
    default: false
    description: Skip ALL safety checks in CB test mode (set true for ground testing only)
    reload_tier: immediate
    unit: set true for ground testing only
  GM_PID_PURSUIT:
    display_name: Gimbal PID Pursuit
    category: follower
    icon: control_camera
    parameters:
      MOUNT_TYPE:
        type: string
        default: HORIZONTAL
        description: MOUNT_TYPE parameter
        options:
        - value: VERTICAL
          label: VERTICAL
        - value: HORIZONTAL
          label: HORIZONTAL
        - value: TILTED_45
          label: TILTED_45
        reload_tier: follower_restart
      CONTROL_MODE:
        type: string
        default: BODY
        description: CONTROL_MODE parameter
        options:
        - value: NED
          label: NED
        - value: BODY
          label: BODY
        reload_tier: follower_restart
      REQUIRED_TRACKER_FIELDS:
        type: array
        default:
        - angular
        description: Must have angular data from gimbal
        item_type: string
        reload_tier: follower_restart
      BASE_VELOCITY:
        type: float
        default: 2.0
        description: Base following velocity (m/s)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      VELOCITY_RAMP_RATE:
        type: float
        default: 0.5
        description: Acceleration rate (m/s²) - smooth ramp
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m
        reload_tier: follower_restart
      VELOCITY_FILTER_ALPHA:
        type: float
        default: 0.7
        description: Velocity smoothing filter coefficient (0-1)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1
        reload_tier: follower_restart
      FORWARD_VELOCITY_MODE:
        type: string
        default: CONSTANT
        description: constant | pn | los
        reload_tier: follower_restart
      BASE_FORWARD_SPEED:
        type: float
        default: 2.0
        description: Constant forward speed (m/s)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      FORWARD_ACCELERATION:
        type: float
        default: 2.0
        description: Ramp rate from current to target speed (m/s²)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s²
        reload_tier: follower_restart
      LATERAL_GUIDANCE_MODE:
        type: string
        default: coordinated_turn
        description: Lateral guidance strategy (sideslip = direct lateral velocity, coordinated_turn = yaw to track)
        options:
        - value: coordinated_turn
          label: Coordinated Turn (Yaw to Track)
        - value: sideslip
          label: Sideslip (Direct Lateral)
        reload_tier: follower_restart
      ENABLE_AUTO_MODE_SWITCHING:
        type: boolean
        default: false
        description: Auto-switch based on LOS rate/distance
        reload_tier: follower_restart
      GUIDANCE_MODE_SWITCH_VELOCITY:
        type: float
        default: 3.0
        description: m/s - auto-switch threshold
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      MODE_SWITCH_HYSTERESIS:
        type: float
        default: 0.5
        description: Hysteresis band to prevent mode oscillation (m/s)
        min: 0.0
        max: 5.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      MIN_MODE_SWITCH_INTERVAL:
        type: float
        default: 2.0
        description: Minimum time between mode switches (seconds)
        min: 0.0
        max: 10.0
        step: 0.1
        unit: seconds
        reload_tier: follower_restart
      YAW_SMOOTHING:
        type: object
        default:
          ENABLED: true
          DEADZONE_DEG_S: 0.5
          MAX_RATE_CHANGE_DEG_S2: 90.0
          SMOOTHING_ALPHA: 0.7
          ENABLE_SPEED_SCALING: true
          MIN_SPEED_THRESHOLD: 0.5
          MAX_SPEED_THRESHOLD: 5.0
          LOW_SPEED_YAW_FACTOR: 0.5
        description: Enterprise-grade yaw rate smoothing with deadzone, rate limiting, and speed-adaptive scaling
        reload_tier: follower_restart
      MAX_YAW_RATE:
        type: float
        default: 45.0
        description: Rate Limits (deg/s - converted to rad/s internally)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg/s - converted to rad/s internally
        reload_tier: follower_restart
      TARGET_LOSS_HANDLING:
        type: object
        default:
          CONTINUE_VELOCITY_TIMEOUT: 3.0
          ENABLE_RTL_ON_TIMEOUT: true
          RTL_ALTITUDE: 50.0
          ENABLE_ADAPTIVE_TIMEOUT: true
          ADAPTIVE_TIMEOUT_FACTOR: 1.5
          ENABLE_PREDICTIVE_VELOCITY: true
          PREDICTION_HISTORY_SIZE: 5
          CONFIDENCE_TIMEOUT_ADJUSTMENT: true
          HIGH_CONFIDENCE_THRESHOLD: 0.8
          LOW_CONFIDENCE_THRESHOLD: 0.3
        description: TARGET_LOSS_HANDLING parameter
        reload_tier: follower_restart
      MAX_SAFETY_VIOLATIONS:
        type: integer
        default: 5
        description: Max consecutive safety violations before halt
        min: 0
        max: 100
        reload_tier: follower_restart
      SAFETY_RETURN_SPEED:
        type: float
        default: 3.0
        description: Velocity when returning to safe altitude (m/s)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      EMERGENCY_STOP_ENABLED:
        type: boolean
        default: true
        description: Safety Behavior
        reload_tier: follower_restart
      ANGLE_DEADZONE:
        type: float
        default: 2.0
        description: Ignore gimbal movements below this (degrees)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      TRANSFORMATION_VALIDATION:
        type: boolean
        default: true
        description: Validate coordinate transformations
        reload_tier: follower_restart
      NEUTRAL_PITCH_ANGLE:
        type: float
        default: 0.0
        description: Neutral pitch for HORIZONTAL mount only (degrees)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: deg
        reload_tier: follower_restart
      PITCH_VELOCITY_SCALING:
        type: float
        default: 0.15
        description: Pitch error to velocity scaling factor
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      PITCH_DEADZONE_DEGREES:
        type: float
        default: 2.0
        description: Pitch deadzone to prevent jitter (degrees)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      MAX_ROLL_ANGLE:
        type: float
        default: 90.0
        description: degrees - max roll (bank)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: bank
        reload_tier: follower_restart
      MAX_PITCH_ANGLE:
        type: float
        default: 90.0
        description: degrees - max pitch (nose down)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: nose down
        reload_tier: follower_restart
      INVERT_LATERAL_CONTROL:
        type: boolean
        default: false
        description: Invert lateral control for different gimbal orientations
        reload_tier: follower_restart
      INVERT_VERTICAL_CONTROL:
        type: boolean
        default: true
        description: Invert vertical control for different gimbal orientations
        reload_tier: follower_restart
      ROLL_RIGHT_SIGN:
        type: string
        default: POSITIVE
        description: '"POSITIVE" if look_right=positive_roll, "NEGATIVE" if look_right=negative_roll'
        reload_tier: follower_restart
      DEFAULT_THRUST:
        type: float
        default: 0.5
        description: Default thrust for attitude rate control mode
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      UPDATE_RATE:
        type: float
        default: 20.0
        description: Hz - control loop frequency
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
      COMMAND_SMOOTHING_ENABLED:
        type: boolean
        default: true
        description: Enable velocity smoothing
        reload_tier: follower_restart
      SMOOTHING_FACTOR:
        type: float
        default: 0.8
        description: EMA coefficient (0-1, higher=smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher=smoother
        reload_tier: follower_restart
  GM_VELOCITY_VECTOR:
    display_name: Gimbal Velocity Vector
    category: follower
    icon: control_camera
    parameters:
      MOUNT_TYPE:
        type: string
        default: HORIZONTAL
        description: MOUNT_TYPE parameter
        options:
        - value: VERTICAL
          label: VERTICAL
        - value: HORIZONTAL
          label: HORIZONTAL
        - value: TILTED_45
          label: TILTED_45
        reload_tier: follower_restart
      RAMP_ACCELERATION:
        type: float
        default: 0.25
        description: m/s² - velocity ramp rate (smooth acceleration)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: smooth acceleration
        reload_tier: follower_restart
      INITIAL_VELOCITY:
        type: float
        default: 0.0
        description: m/s - starting velocity (0 = smooth start)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0 = smooth start
        reload_tier: follower_restart
      ENABLE_ALTITUDE_CONTROL:
        type: boolean
        default: false
        description: Enable vertical velocity control
        reload_tier: follower_restart
      ENABLE_YAW_CONTROL:
        type: boolean
        default: false
        description: 🔄 Drone yaw (heading) control
        reload_tier: follower_restart
      YAW_RATE_GAIN:
        type: float
        default: 0.5
        description: Yaw rate proportional gain (if enabled)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: if enabled
        reload_tier: follower_restart
      LATERAL_GUIDANCE_MODE:
        type: string
        default: sideslip
        description: Lateral guidance strategy (sideslip = direct lateral velocity, coordinated_turn = yaw to track)
        options:
        - value: sideslip
          label: Sideslip (Direct Lateral)
        - value: coordinated_turn
          label: Coordinated Turn (Yaw to Track)
        reload_tier: follower_restart
      ENABLE_AUTO_MODE_SWITCHING:
        type: boolean
        default: false
        description: Auto-switch lateral mode based on forward velocity
        reload_tier: follower_restart
      GUIDANCE_MODE_SWITCH_VELOCITY:
        type: float
        default: 3.0
        description: Forward velocity threshold for auto mode switching (m/s)
        min: 0.5
        max: 20.0
        step: 0.5
        unit: m/s
        reload_tier: follower_restart
      MODE_SWITCH_HYSTERESIS:
        type: float
        default: 0.5
        description: Hysteresis band to prevent mode oscillation (m/s)
        min: 0.1
        max: 2.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      MIN_MODE_SWITCH_INTERVAL:
        type: float
        default: 2.0
        description: Minimum time between lateral mode switches (seconds)
        min: 0.5
        max: 10.0
        step: 0.5
        unit: seconds
        reload_tier: follower_restart
      YAW_SMOOTHING:
        type: object
        default:
          ENABLED: true
          DEADZONE_DEG_S: 0.5
          MAX_RATE_CHANGE_DEG_S2: 90.0
          SMOOTHING_ALPHA: 0.7
          ENABLE_SPEED_SCALING: true
          MIN_SPEED_THRESHOLD: 0.5
          MAX_SPEED_THRESHOLD: 5.0
          LOW_SPEED_YAW_FACTOR: 0.5
        description: Enterprise-grade yaw rate smoothing for coordinated_turn mode
        reload_tier: follower_restart
      ANGLE_DEADZONE_DEG:
        type: float
        default: 2.0
        description: Ignore gimbal movements below this threshold (degrees)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      ANGLE_SMOOTHING_ALPHA:
        type: float
        default: 0.7
        description: EMA filter coefficient (0-1, higher = smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher = smoother
        reload_tier: follower_restart
      ALTITUDE_SAFETY_ENABLED:
        type: boolean
        default: true
        description: Enable altitude enforcement
        reload_tier: follower_restart
      ALTITUDE_CHECK_INTERVAL:
        type: float
        default: 1.0
        description: seconds - 100ms safety checks
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: follower_restart
      RTL_ON_ALTITUDE_VIOLATION:
        type: boolean
        default: false
        description: Trigger RTL on violation
        reload_tier: follower_restart
      EMERGENCY_STOP_ENABLED:
        type: boolean
        default: true
        description: Safety Behavior
        reload_tier: follower_restart
      MAX_SAFETY_VIOLATIONS:
        type: integer
        default: 5
        description: Max consecutive safety violations before halt
        min: 0
        max: 100
        reload_tier: follower_restart
      TARGET_LOSS_TIMEOUT:
        type: float
        default: 3.0
        description: seconds - before executing action
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      ENABLE_VELOCITY_DECAY:
        type: boolean
        default: true
        description: Gradually reduce velocity during loss
        reload_tier: follower_restart
      VELOCITY_DECAY_RATE:
        type: float
        default: 0.5
        description: m/s² - deceleration rate during target loss
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m
        reload_tier: follower_restart
      UPDATE_RATE:
        type: float
        default: 20.0
        description: Hz - control loop frequency
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
      COMMAND_SMOOTHING_ENABLED:
        type: boolean
        default: true
        description: Enable velocity smoothing
        reload_tier: follower_restart
      SMOOTHING_FACTOR:
        type: float
        default: 0.8
        description: EMA coefficient (0-1, higher=smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher=smoother
        reload_tier: follower_restart
      MOUNT_ROLL_OFFSET_DEG:
        type: float
        default: 0.0
        description: Manual roll offset (degrees)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: deg
        reload_tier: follower_restart
      MOUNT_PITCH_OFFSET_DEG:
        type: float
        default: 0.0
        description: Manual pitch offset (degrees)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: deg
        reload_tier: follower_restart
      MOUNT_YAW_OFFSET_DEG:
        type: float
        default: 0.0
        description: Manual yaw offset (degrees)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: deg
        reload_tier: follower_restart
      INVERT_GIMBAL_ROLL:
        type: boolean
        default: false
        description: Invert roll axis
        reload_tier: follower_restart
      INVERT_GIMBAL_PITCH:
        type: boolean
        default: false
        description: Invert pitch axis
        reload_tier: follower_restart
      INVERT_GIMBAL_YAW:
        type: boolean
        default: false
        description: Invert yaw axis
        reload_tier: follower_restart
  MC_VELOCITY_CHASE:
    display_name: MC Velocity Chase
    category: follower
    icon: sports_motorsports
    parameters:
      INITIAL_FORWARD_VELOCITY:
        type: float
        default: 0.0
        description: m/s - starting velocity
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m/s
        reload_tier: follower_restart
      FORWARD_RAMP_RATE:
        type: float
        default: 0.5
        description: m/s^2 - acceleration rate (smooth 4s ramp)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: smooth 4s ramp
        reload_tier: follower_restart
      RAMP_DOWN_ON_TARGET_LOSS:
        type: boolean
        default: true
        description: Gradually reduce rates on loss
        reload_tier: follower_restart
      TARGET_LOSS_TIMEOUT:
        type: float
        default: 2.0
        description: seconds - before executing action
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      TARGET_LOSS_STOP_VELOCITY:
        type: float
        default: 0.0
        description: m/s - velocity on target loss (0.0 = stop)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0 = stop
        reload_tier: follower_restart
      TARGET_LOSS_COORDINATE_THRESHOLD:
        type: float
        default: 1.5
        description: Normalized coord threshold for target loss (> this = lost)
        min: 0.5
        max: 5.0
        step: 0.1
        unit: normalized
        reload_tier: follower_restart
      ENABLE_ALTITUDE_CONTROL:
        type: boolean
        default: false
        description: Enable vertical velocity control
        reload_tier: follower_restart
      LATERAL_GUIDANCE_MODE:
        type: string
        default: coordinated_turn
        description: Lateral guidance strategy for fixed-camera chase (coordinated_turn recommended; sideslip is advanced and may lose target)
        options:
        - value: coordinated_turn
          label: Coordinated Turn (Yaw to Track)
          description: Recommended for fixed camera tracking reliability
        - value: sideslip
          label: Sideslip (Direct Lateral)
          description: Advanced mode; may increase target-loss risk with fixed camera
        reload_tier: follower_restart
      GUIDANCE_MODE_SWITCH_VELOCITY:
        type: float
        default: 3.0
        description: m/s - auto-switch threshold
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      ENABLE_AUTO_MODE_SWITCHING:
        type: boolean
        default: false
        description: Auto-switch based on LOS rate/distance
        reload_tier: follower_restart
      ALTITUDE_SAFETY_ENABLED:
        type: boolean
        default: true
        description: Enable altitude enforcement
        reload_tier: follower_restart
      ALTITUDE_CHECK_INTERVAL:
        type: float
        default: 0.1
        description: seconds - 100ms safety checks
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: follower_restart
      RTL_ON_ALTITUDE_VIOLATION:
        type: boolean
        default: false
        description: Trigger RTL on violation
        reload_tier: follower_restart
      EMERGENCY_STOP_ENABLED:
        type: boolean
        default: false
        description: Safety Behavior
        reload_tier: follower_restart
      MAX_TRACKING_ERROR:
        type: float
        default: 1.5
        description: Max normalized error before limiting
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: follower_restart
      VELOCITY_SMOOTHING_ENABLED:
        type: boolean
        default: true
        description: Enable velocity command smoothing
        reload_tier: follower_restart
      SMOOTHING_FACTOR:
        type: float
        default: 0.8
        description: EMA coefficient (0-1, higher=smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher=smoother
        reload_tier: follower_restart
      MIN_FORWARD_VELOCITY_THRESHOLD:
        type: float
        default: 0.2
        description: m/s - minimum velocity to maintain (important if using a fixedwing or VTOL)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: important if using a fixedwing or vtol
        reload_tier: follower_restart
      RAMP_UPDATE_RATE:
        type: float
        default: 10.0
        description: Hz - forward velocity update frequency
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
      PID_UPDATE_RATE:
        type: float
        default: 20.0
        description: Hz - tracking PID update frequency
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
      ENABLE_ADAPTIVE_DIVE_CLIMB:
        type: boolean
        default: false
        description: Master enable/disable (off by default for stability)
        unit: off by default for stability
        reload_tier: follower_restart
      ADAPTIVE_SMOOTHING_ALPHA:
        type: float
        default: 0.2
        description: EMA smoothing factor (0.1-0.5)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.1-0.5
        reload_tier: follower_restart
      ADAPTIVE_WARMUP_FRAMES:
        type: integer
        default: 10
        description: Frames to ignore on startup (5-15)
        min: 0
        max: 100
        unit: 5-15
        reload_tier: follower_restart
      ADAPTIVE_RATE_THRESHOLD:
        type: float
        default: 5.0
        description: Pixels/sec error threshold to trigger correction
        min: 0.0
        max: 100.0
        step: 0.1
        unit: px
        reload_tier: follower_restart
      ADAPTIVE_MAX_CORRECTION:
        type: float
        default: 1.0
        description: Maximum velocity correction (m/s)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m/s
        reload_tier: follower_restart
      ADAPTIVE_CORRECTION_GAIN:
        type: float
        default: 0.3
        description: Proportional gain on rate error (0.1-0.8)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.1-0.8
        reload_tier: follower_restart
      ADAPTIVE_MIN_CONFIDENCE:
        type: float
        default: 0.6
        description: Minimum tracker confidence to activate (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: follower_restart
      ADAPTIVE_FWD_COUPLING_ENABLED:
        type: boolean
        default: false
        description: Allow v_fwd adjustments (in addition to v_down)
        unit: in addition to v_down
        reload_tier: follower_restart
      ADAPTIVE_FWD_COUPLING_GAIN:
        type: float
        default: 0.1
        description: v_fwd adjustment gain (0.05-0.2)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.05-0.2
        reload_tier: follower_restart
      PIXEL_TO_RATE_CALIBRATION:
        type: float
        default: 0.05
        description: 'Calibration factor: meters/sec per pixels/sec'
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: follower_restart
      ADAPTIVE_OSCILLATION_DETECTION:
        type: boolean
        default: true
        description: Enable oscillation detection (recommended)
        unit: recommended
        reload_tier: follower_restart
      ADAPTIVE_MAX_SIGN_CHANGES:
        type: integer
        default: 3
        description: Max sign changes in 2 seconds before disable
        min: 0
        max: 100
        unit: s
        reload_tier: follower_restart
      ADAPTIVE_DIVERGENCE_TIMEOUT:
        type: float
        default: 5.0
        description: Seconds - disable if error grows despite corrections
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      ENABLE_PITCH_COMPENSATION:
        type: boolean
        default: false
        description: Master enable/disable (off by default for safety)
        unit: off by default for safety
        reload_tier: follower_restart
      PITCH_COMPENSATION_MODEL:
        type: string
        default: linear_velocity
        description: linear_velocity | linear_angle | quadratic
        reload_tier: follower_restart
      PITCH_COMPENSATION_GAIN:
        type: float
        default: 0.05
        description: Compensation aggressiveness (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: follower_restart
      PITCH_DATA_SMOOTHING_ALPHA:
        type: float
        default: 0.7
        description: EMA filter (0.6-0.8 recommended)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.6-0.8 recommended
        reload_tier: follower_restart
      PITCH_DATA_MAX_AGE:
        type: float
        default: 0.5
        description: Max data age in seconds
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: follower_restart
      PITCH_COMPENSATION_MIN_VELOCITY:
        type: float
        default: 1.0
        description: Min v_fwd to activate (m/s)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m/s
        reload_tier: follower_restart
      PITCH_COMPENSATION_DEADBAND:
        type: float
        default: 2.0
        description: Ignore pitch below this (degrees)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      PITCH_COMPENSATION_MAX_ANGLE:
        type: float
        default: 45.0
        description: Safety limit (degrees)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      PITCH_COMPENSATION_MAX_CORRECTION:
        type: float
        default: 0.3
        description: Max compensation magnitude (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: follower_restart
      PITCH_COMPENSATION_ADAPTIVE_GAIN:
        type: boolean
        default: false
        description: 'Future: adaptive gain tuning'
        reload_tier: follower_restart
      # === v5.7.0: Enterprise Hardening Parameters ===
      VIDEO_HEIGHT_PIXELS:
        type: float
        default: 480.0
        description: Camera resolution height for rate calculations (px)
        min: 120.0
        max: 2160.0
        step: 1.0
        unit: px
        reload_tier: follower_restart
      FORWARD_VELOCITY_DEADZONE:
        type: float
        default: 0.01
        description: Velocity deadzone for ramping (m/s)
        min: 0.001
        max: 0.5
        step: 0.001
        unit: m/s
        reload_tier: follower_restart
      # v5.7.1: Target validation thresholds
      TARGET_CONFIDENCE_THRESHOLD:
        type: float
        default: 0.5
        description: Minimum confidence to consider target valid (0-1)
        min: 0.0
        max: 1.0
        step: 0.05
        reload_tier: follower_restart
      MAX_REASONABLE_TARGET_VELOCITY:
        type: float
        default: 50.0
        description: Max target velocity before rejection (px/frame)
        min: 10.0
        max: 500.0
        step: 5.0
        unit: px/frame
        reload_tier: follower_restart
    # === YAW_SMOOTHING Nested Config (v5.7.0) ===
    YAW_SMOOTHING:
      display_name: Yaw Rate Smoothing
      category: follower
      icon: rotate_right
      parameters:
        ENABLED:
          type: boolean
          default: true
          description: Enable yaw rate smoothing
          reload_tier: follower_restart
        DEADZONE_DEG_S:
          type: float
          default: 0.5
          description: Ignore yaw rates below this (deg/s)
          min: 0.0
          max: 5.0
          step: 0.1
          unit: deg/s
          reload_tier: follower_restart
        MAX_RATE_CHANGE_DEG_S2:
          type: float
          default: 90.0
          description: Maximum yaw acceleration (deg/s²)
          min: 10.0
          max: 360.0
          step: 1.0
          unit: deg/s²
          reload_tier: follower_restart
        SMOOTHING_ALPHA:
          type: float
          default: 0.7
          description: EMA filter coefficient (0-1)
          min: 0.0
          max: 1.0
          step: 0.01
          reload_tier: follower_restart
        ENABLE_SPEED_SCALING:
          type: boolean
          default: true
          description: Scale yaw authority with forward speed
          reload_tier: follower_restart
        MIN_SPEED_THRESHOLD:
          type: float
          default: 0.5
          description: Below this speed, apply low-speed factor (m/s)
          min: 0.0
          max: 5.0
          step: 0.1
          unit: m/s
          reload_tier: follower_restart
        MAX_SPEED_THRESHOLD:
          type: float
          default: 5.0
          description: Above this speed, full yaw authority (m/s)
          min: 1.0
          max: 20.0
          step: 0.1
          unit: m/s
          reload_tier: follower_restart
        LOW_SPEED_YAW_FACTOR:
          type: float
          default: 0.5
          description: Yaw reduction at low speeds (0-1)
          min: 0.0
          max: 1.0
          step: 0.01
          reload_tier: follower_restart
  MC_VELOCITY_POSITION:
    display_name: MC Velocity Position
    category: follower
    icon: place
    parameters:
      ENABLE_ALTITUDE_CONTROL:
        type: boolean
        default: false
        description: Enable vertical velocity control
        reload_tier: follower_restart
      ENABLE_YAW_CONTROL:
        type: boolean
        default: true
        description: 🔄 Drone yaw (heading) control
        reload_tier: follower_restart
      MAX_YAW_RATE:
        type: float
        default: 10.0
        description: Rate Limits (deg/s - converted to rad/s internally)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg/s - converted to rad/s internally
        reload_tier: follower_restart
      YAW_CONTROL_THRESHOLD:
        type: float
        default: 0.02
        description: Activation threshold
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      TARGET_LOST_TIMEOUT:
        type: float
        default: 3.0
        description: Target loss timeout (seconds)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      CONTROL_UPDATE_RATE:
        type: float
        default: 20.0
        description: Control loop frequency (Hz)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
      COMMAND_SMOOTHING_ENABLED:
        type: boolean
        default: true
        description: Enable velocity smoothing
        reload_tier: follower_restart
      SMOOTHING_FACTOR:
        type: float
        default: 0.8
        description: EMA coefficient (0-1, higher=smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher=smoother
        reload_tier: follower_restart
  MC_VELOCITY_DISTANCE:
    display_name: MC Velocity Distance
    category: follower
    icon: straighten
    parameters:
      ENABLE_YAW_CONTROL:
        type: boolean
        default: false
        description: 🔄 Drone yaw (heading) control
        reload_tier: follower_restart
      ENABLE_ALTITUDE_CONTROL:
        type: boolean
        default: false
        description: Enable vertical velocity control
        reload_tier: follower_restart
      MAX_YAW_RATE:
        type: float
        default: 10.0
        description: Rate Limits (deg/s - converted to rad/s internally)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg/s - converted to rad/s internally
        reload_tier: follower_restart
      YAW_CONTROL_THRESHOLD:
        type: float
        default: 0.3
        description: Activation threshold
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      TARGET_LOST_TIMEOUT:
        type: float
        default: 3.0
        description: Target loss timeout (seconds)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      CONTROL_UPDATE_RATE:
        type: float
        default: 20.0
        description: Control loop frequency (Hz)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
      COMMAND_SMOOTHING_ENABLED:
        type: boolean
        default: true
        description: Enable velocity smoothing
        reload_tier: follower_restart
      SMOOTHING_FACTOR:
        type: float
        default: 0.8
        description: EMA coefficient (0-1, higher=smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher=smoother
        reload_tier: follower_restart
      DISTANCE_HOLD_ENABLED:
        type: boolean
        default: true
        description: Maintain constant forward distance
        reload_tier: follower_restart
      DISTANCE_HOLD_TOLERANCE:
        type: float
        default: 0.1
        description: Distance tolerance (normalized)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: normalized
        reload_tier: follower_restart
  FW_ATTITUDE_RATE:
    display_name: Fixed-Wing Attitude Rate
    category: follower
    icon: flight
    parameters:
      MIN_AIRSPEED:
        type: float
        default: 12.0
        description: m/s - stall speed + safety margin
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      CRUISE_AIRSPEED:
        type: float
        default: 18.0
        description: m/s - optimal efficiency airspeed
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      MAX_AIRSPEED:
        type: float
        default: 30.0
        description: m/s - structural/performance limit
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      STALL_MARGIN_BUFFER:
        type: float
        default: 3.0
        description: m/s - safety buffer above stall speed
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      MAX_BANK_ANGLE:
        type: float
        default: 35.0
        description: degrees
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      MAX_LOAD_FACTOR:
        type: float
        default: 2.5
        description: g - structural load factor limit
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: follower_restart
      MAX_PITCH_ANGLE:
        type: float
        default: 25.0
        description: degrees - max pitch (nose down)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: nose down
        reload_tier: follower_restart
      MIN_PITCH_ANGLE:
        type: float
        default: -20.0
        description: degrees - pitch down limit
        min: -10000.0
        max: 10000.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      MAX_ROLL_RATE:
        type: float
        default: 45.0
        description: deg/s
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      MAX_PITCH_RATE:
        type: float
        default: 20.0
        description: deg/s
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      MAX_YAW_RATE:
        type: float
        default: 25.0
        description: Rate Limits (deg/s - converted to rad/s internally)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg/s - converted to rad/s internally
        reload_tier: follower_restart
      L1_DISTANCE:
        type: float
        default: 50.0
        description: m - lookahead distance
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m
        reload_tier: follower_restart
      L1_DAMPING:
        type: float
        default: 0.75
        description: damping ratio (0.7-1.0 recommended)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.7-1.0 recommended
        reload_tier: follower_restart
      ENABLE_L1_ADAPTIVE:
        type: boolean
        default: false
        description: Adapt L1 distance to current airspeed
        reload_tier: follower_restart
      L1_MIN_DISTANCE:
        type: float
        default: 20.0
        description: m - minimum L1 for low speed
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m
        reload_tier: follower_restart
      L1_MAX_DISTANCE:
        type: float
        default: 100.0
        description: m - maximum L1 for high speed
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m
        reload_tier: follower_restart
      ENABLE_TECS:
        type: boolean
        default: true
        description: Use TECS energy management (recommended)
        unit: recommended
        reload_tier: follower_restart
      TECS_TIME_CONST:
        type: float
        default: 5.0
        description: seconds - energy response time constant
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      TECS_PITCH_DAMPING:
        type: float
        default: 1.0
        description: pitch axis damping factor
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      TECS_THROTTLE_DAMPING:
        type: float
        default: 0.5
        description: throttle axis damping factor
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      TECS_SPE_WEIGHT:
        type: float
        default: 1.0
        description: altitude vs speed priority (>1 = altitude priority)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: '>1 = altitude priority'
        reload_tier: follower_restart
      MIN_THRUST:
        type: float
        default: 0.2
        description: 0-1
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      MAX_THRUST:
        type: float
        default: 1.0
        description: 0-1
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      CRUISE_THRUST:
        type: float
        default: 0.6
        description: typical cruise throttle
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      THRUST_SLEW_RATE:
        type: float
        default: 0.5
        description: max throttle change per second (smooth transitions)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: smooth transitions
        reload_tier: follower_restart
      ENABLE_COORDINATED_TURN:
        type: boolean
        default: true
        description: Calculate bank from yaw rate (recommended)
        unit: recommended
        reload_tier: follower_restart
      TURN_COORDINATION_GAIN:
        type: float
        default: 1.0
        description: Coordination aggressiveness
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      SLIP_ANGLE_LIMIT:
        type: float
        default: 5.0
        description: degrees - max sideslip allowed
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      ENABLE_ALTITUDE_SAFETY:
        type: boolean
        default: true
        description: Enable altitude monitoring
        reload_tier: follower_restart
      ALTITUDE_CHECK_INTERVAL:
        type: float
        default: 0.1
        description: seconds - 100ms safety checks
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: follower_restart
      RTL_ON_ALTITUDE_VIOLATION:
        type: boolean
        default: true
        description: Trigger RTL on violation
        reload_tier: follower_restart
      ENABLE_STALL_PROTECTION:
        type: boolean
        default: true
        description: Monitor airspeed for stall conditions
        reload_tier: follower_restart
      STALL_RECOVERY_PITCH:
        type: float
        default: -5.0
        description: degrees - nose down for recovery
        min: -10000.0
        max: 10000.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      STALL_RECOVERY_THROTTLE:
        type: float
        default: 1.0
        description: full throttle for energy recovery
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      TARGET_LOSS_TIMEOUT:
        type: float
        default: 3.0
        description: seconds - before executing action
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      TARGET_LOSS_ACTION:
        type: string
        default: orbit
        description: hover, orbit, stop, rtl, continue
        reload_tier: follower_restart
      ORBIT_RADIUS:
        type: float
        default: 100.0
        description: m - loiter radius on target loss
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m
        reload_tier: follower_restart
      TARGET_LOSS_COORDINATE_THRESHOLD:
        type: integer
        default: 990
        description: Lost target detection threshold
        min: 0
        max: 10000
        reload_tier: follower_restart
      CONTROL_UPDATE_RATE:
        type: float
        default: 20.0
        description: Control loop frequency (Hz)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
      ENABLE_COMMAND_SMOOTHING:
        type: boolean
        default: true
        description: Apply EMA smoothing to commands
        reload_tier: follower_restart
      SMOOTHING_FACTOR:
        type: float
        default: 0.85
        description: EMA coefficient (0-1, higher=smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher=smoother
        reload_tier: follower_restart
  MC_VELOCITY:
    display_name: MC Velocity
    category: follower
    icon: speed
    parameters:
      LATERAL_GUIDANCE_MODE:
        type: string
        default: yaw_to_target
        description: Lateral guidance strategy (yaw_to_target = rotate to target, crab_strafe = lateral strafe)
        options:
        - value: yaw_to_target
          label: Yaw to Target
        - value: crab_strafe
          label: Crab Strafe
        reload_tier: follower_restart
      ENABLE_AUTO_MODE_SWITCHING:
        type: boolean
        default: false
        description: Auto-switch based on LOS rate/distance
        reload_tier: follower_restart
      AUTO_SWITCH_DISTANCE_THRESHOLD:
        type: float
        default: 20.0
        description: meters - close = crab_strafe
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: follower_restart
      FORWARD_VELOCITY_MODE:
        type: string
        default: constant
        description: constant | pn | los
        reload_tier: follower_restart
      INITIAL_FORWARD_VELOCITY:
        type: float
        default: 0.0
        description: m/s - starting velocity
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m/s
        reload_tier: follower_restart
      FORWARD_RAMP_RATE:
        type: float
        default: 0.5
        description: m/s^2 - acceleration rate (smooth 4s ramp)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: smooth 4s ramp
        reload_tier: follower_restart
      PN_NAVIGATION_CONSTANT:
        type: float
        default: 4.0
        description: N (3-5 typical for intercept)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: 3-5 typical for intercept
        reload_tier: follower_restart
      PN_BASE_VELOCITY:
        type: float
        default: 2.0
        description: m/s - base velocity for PN mode (conservative)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: conservative
        reload_tier: follower_restart
      PN_LOS_SMOOTHING_ALPHA:
        type: float
        default: 0.3
        description: EMA filter for LOS rate noise
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      PN_MAX_VELOCITY_SCALE:
        type: float
        default: 2.0
        description: Maximum velocity multiplier
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: follower_restart
      LOS_DISTANCE_GAIN:
        type: float
        default: 0.5
        description: Velocity gain for distance
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      TARGET_LOSS_TIMEOUT:
        type: float
        default: 2.0
        description: seconds - before executing action
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      TARGET_LOSS_ACTION:
        type: string
        default: hover
        description: hover, orbit, stop, rtl, continue
        reload_tier: follower_restart
      TARGET_LOSS_COORDINATE_THRESHOLD:
        type: integer
        default: 990
        description: Lost target detection threshold
        min: 0
        max: 10000
        reload_tier: follower_restart
      RAMP_DOWN_ON_TARGET_LOSS:
        type: boolean
        default: true
        description: Gradually reduce rates on loss
        reload_tier: follower_restart
      SLOW_FORWARD_VELOCITY:
        type: float
        default: 1.0
        description: m/s - velocity for slow_forward action
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m/s
        reload_tier: follower_restart
      ENABLE_ALTITUDE_SAFETY:
        type: boolean
        default: true
        description: Enable altitude monitoring
        reload_tier: follower_restart
      ALTITUDE_CHECK_INTERVAL:
        type: float
        default: 0.1
        description: seconds - 100ms safety checks
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: follower_restart
      RTL_ON_ALTITUDE_VIOLATION:
        type: boolean
        default: true
        description: Trigger RTL on violation
        reload_tier: follower_restart
      ENABLE_VELOCITY_MAGNITUDE_LIMIT:
        type: boolean
        default: true
        description: Enable magnitude limiting
        reload_tier: follower_restart
      VELOCITY_SMOOTHING_ENABLED:
        type: boolean
        default: true
        description: Enable velocity command smoothing
        reload_tier: follower_restart
      SMOOTHING_FACTOR:
        type: float
        default: 0.8
        description: EMA coefficient (0-1, higher=smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher=smoother
        reload_tier: follower_restart
      EMERGENCY_STOP_ENABLED:
        type: boolean
        default: true
        description: Safety Behavior
        reload_tier: follower_restart
      MAX_TRACKING_ERROR:
        type: float
        default: 1.5
        description: Max normalized error before limiting
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: follower_restart
      ENABLE_ALTITUDE_CONTROL:
        type: boolean
        default: true
        description: Enable vertical velocity control
        reload_tier: follower_restart
      CONTROL_UPDATE_RATE:
        type: float
        default: 20.0
        description: Control loop frequency (Hz)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
  MC_ATTITUDE_RATE:
    display_name: MC Attitude Rate
    category: follower
    icon: rotate_right
    parameters:
      GUIDANCE_MODE:
        type: string
        default: direct_rate
        description: direct_rate | png
        reload_tier: follower_restart
      MAX_PITCH_RATE:
        type: float
        default: 45.0
        description: deg/s
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      MAX_ROLL_RATE:
        type: float
        default: 45.0
        description: deg/s
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      MAX_YAW_RATE:
        type: float
        default: 60.0
        description: Rate Limits (deg/s - converted to rad/s internally)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg/s - converted to rad/s internally
        reload_tier: follower_restart
      MAX_PITCH_ANGLE:
        type: float
        default: 35.0
        description: degrees - max pitch (nose down)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: nose down
        reload_tier: follower_restart
      MAX_ROLL_ANGLE:
        type: float
        default: 35.0
        description: degrees - max roll (bank)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: bank
        reload_tier: follower_restart
      MAX_BANK_ANGLE:
        type: float
        default: 30.0
        description: degrees
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: follower_restart
      HOVER_THRUST:
        type: float
        default: 0.5
        description: Baseline hover thrust (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: follower_restart
      MIN_THRUST:
        type: float
        default: 0.1
        description: 0-1
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      MAX_THRUST:
        type: float
        default: 0.9
        description: 0-1
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      ENABLE_PITCH_THRUST_COMPENSATION:
        type: boolean
        default: true
        description: Compensate for thrust loss at pitch angles
        reload_tier: follower_restart
      PITCH_COMPENSATION_GAIN:
        type: float
        default: 0.5
        description: Compensation aggressiveness (0.0-1.0)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0.0-1.0
        reload_tier: follower_restart
      ENABLE_ALTITUDE_HOLD:
        type: boolean
        default: true
        description: Enable altitude PID for hover
        reload_tier: follower_restart
      TARGET_ALTITUDE:
        type: float
        default: 15.0
        description: meters - default target altitude
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: follower_restart
      ALTITUDE_TOLERANCE:
        type: float
        default: 1.0
        description: meters - acceptable altitude error
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      PN_NAVIGATION_CONSTANT:
        type: float
        default: 4.0
        description: N (3-5 typical for intercept)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: 3-5 typical for intercept
        reload_tier: follower_restart
      PN_LOS_SMOOTHING_ALPHA:
        type: float
        default: 0.3
        description: EMA filter for LOS rate noise
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      PN_CLOSING_VELOCITY:
        type: float
        default: 5.0
        description: m/s - assumed closing velocity
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m/s
        reload_tier: follower_restart
      PN_MIN_ACCELERATION:
        type: float
        default: 0.5
        description: m/s² - minimum command
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m
        reload_tier: follower_restart
      PN_MAX_ACCELERATION:
        type: float
        default: 15.0
        description: m/s² - maximum command
        min: 0.0
        max: 100.0
        step: 0.1
        unit: m
        reload_tier: follower_restart
      ENABLE_YAW_ERROR_GATING:
        type: boolean
        default: true
        description: Enable yaw alignment check
        reload_tier: follower_restart
      YAW_ERROR_THRESHOLD:
        type: float
        default: 0.3
        description: degrees
        min: 0.0
        max: 1.0
        step: 0.01
        unit: deg
        reload_tier: follower_restart
      YAW_GATING_PITCH_SCALE:
        type: float
        default: 0.3
        description: Pitch scaling when yaw misaligned
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      ENABLE_COORDINATED_TURNS:
        type: boolean
        default: true
        description: Enable roll-yaw coordination
        reload_tier: follower_restart
      TURN_COORDINATION_GAIN:
        type: float
        default: 1.0
        description: Coordination aggressiveness
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      TARGET_LOSS_TIMEOUT:
        type: float
        default: 2.0
        description: seconds - before executing action
        min: 0.0
        max: 100.0
        step: 0.1
        unit: s
        reload_tier: follower_restart
      TARGET_LOSS_ACTION:
        type: string
        default: hover
        description: hover, orbit, stop, rtl, continue
        reload_tier: follower_restart
      TARGET_LOSS_COORDINATE_THRESHOLD:
        type: integer
        default: 990
        description: Lost target detection threshold
        min: 0
        max: 10000
        reload_tier: follower_restart
      RAMP_DOWN_ON_TARGET_LOSS:
        type: boolean
        default: true
        description: Gradually reduce rates on loss
        reload_tier: follower_restart
      ENABLE_ALTITUDE_SAFETY:
        type: boolean
        default: true
        description: Enable altitude monitoring
        reload_tier: follower_restart
      RTL_ON_ALTITUDE_VIOLATION:
        type: boolean
        default: true
        description: Trigger RTL on violation
        reload_tier: follower_restart
      RATE_SMOOTHING_ENABLED:
        type: boolean
        default: true
        description: Enable rate command smoothing
        reload_tier: follower_restart
      SMOOTHING_FACTOR:
        type: float
        default: 0.85
        description: EMA coefficient (0-1, higher=smoother)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: 0-1, higher=smoother
        reload_tier: follower_restart
      EMERGENCY_STOP_ENABLED:
        type: boolean
        default: true
        description: Safety Behavior
        reload_tier: follower_restart
      MAX_TRACKING_ERROR:
        type: float
        default: 1.5
        description: Max normalized error before limiting
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: follower_restart
      CONTROL_UPDATE_RATE:
        type: float
        default: 50.0
        description: Control loop frequency (Hz)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
  MC_VELOCITY_GROUND:
    display_name: MC Velocity Ground
    category: follower
    icon: terrain
    parameters:
      TARGET_POSITION_MODE:
        type: string
        default: center
        description: 'Target positioning: "center", "initial"'
        reload_tier: follower_restart
      MAX_RATE_OF_DESCENT:
        type: float
        default: 1.0
        description: Max descent rate (m/s) - operational parameter
        min: 0.0
        max: 1.0
        step: 0.01
        unit: m/s
        reload_tier: follower_restart
      ENABLE_DESCEND_TO_TARGET:
        type: boolean
        default: false
        description: Enable automatic descent to target
        reload_tier: follower_restart
      IS_CAMERA_GIMBALED:
        type: boolean
        default: false
        description: Camera stabilization status
        reload_tier: follower_restart
      BASE_ADJUSTMENT_FACTOR_X:
        type: float
        default: 0.1
        description: Base correction factor for roll (X-axis)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: x-axis
        reload_tier: follower_restart
      BASE_ADJUSTMENT_FACTOR_Y:
        type: float
        default: 0.1
        description: Base correction factor for pitch (Y-axis)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: y-axis
        reload_tier: follower_restart
      ALTITUDE_FACTOR:
        type: float
        default: 0.005
        description: Altitude scaling factor for distance compensation
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: follower_restart
      ENABLE_GAIN_SCHEDULING:
        type: boolean
        default: false
        description: 📈 Altitude-based gain adjustment
        reload_tier: follower_restart
      GAIN_SCHEDULING_PARAMETER:
        type: string
        default: current_altitude
        description: Parameter for gain scheduling
        reload_tier: follower_restart
      CONTROL_UPDATE_RATE:
        type: float
        default: 20.0
        description: Control loop frequency (Hz)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: hz
        reload_tier: follower_restart
      COORDINATE_CORRECTIONS_ENABLED:
        type: boolean
        default: true
        description: Enable gimbal and altitude corrections
        reload_tier: follower_restart
      ERROR_LOGGING_ENABLED:
        type: boolean
        default: true
        description: Enable detailed error logging
        reload_tier: follower_restart
  Detector:
    display_name: Object Detector
    category: detection
    icon: search
    parameters:
      USE_DETECTOR:
        type: boolean
        default: true
        description: 🔍 Object detection for tracker initialization/recovery
        reload_tier: tracker_restart
      DETECTION_ALGORITHM:
        type: string
        default: TemplateMatching
        description: or FeatureMatching, YOLO
        reload_tier: tracker_restart
      TEMPLATE_MATCHING_METHOD:
        type: string
        default: TM_CCOEFF_NORMED
        description: TEMPLATE_MATCHING_METHOD parameter
        reload_tier: tracker_restart
      TEMPLATE_UPDATE_INTERVAL:
        type: integer
        default: 10
        description: Frames between template updates
        min: 0
        max: 100
        reload_tier: tracker_restart
      TEMPLATE_APPEARANCE_LEARNING_RATE:
        type: float
        default: 0.002
        description: Template adaptation speed
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      TEMPLATE_MATCHING_SCALES:
        type: array
        default:
        - 0.5
        - 0.7
        - 0.8
        - 0.9
        - 1.0
        - 1.1
        - 1.2
        - 1.5
        - 2.0
        description: Multi-scale search (0.5x to 2x target size for drone scenarios)
        item_type: number
        min_items: 0
        max_items: 20
        unit: 0.5x to 2x target size for drone scenarios
        reload_tier: tracker_restart
      TEMPLATE_MATCHING_THRESHOLD:
        type: float
        default: 0.7
        description: Match confidence threshold
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      APPEARANCE_CONFIDENCE_THRESHOLD:
        type: float
        default: 0.7
        description: Re-detection validation
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      TRACKER_CONFIDENCE_THRESHOLD_FOR_TEMPLATE_UPDATE:
        type: float
        default: 0.8
        description: TRACKER_CONFIDENCE_THRESHOLD_FOR_TEMPLATE_UPDATE parameter
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: tracker_restart
      DEFAULT_FEATURE_EXTRACTION_ALGORITHM:
        type: string
        default: ORB
        description: or SIFT, SURF
        reload_tier: tracker_restart
      ORB_NFEATURES:
        type: integer
        default: 500
        description: ORB_NFEATURES parameter
        min: 0
        max: 10000
        reload_tier: tracker_restart
      ORB_MIN_MATCH_COUNT:
        type: integer
        default: 30
        description: ORB_MIN_MATCH_COUNT parameter
        min: 0
        max: 100
        reload_tier: tracker_restart
      RANSAC_REPROJECTION_THRESHOLD:
        type: float
        default: 10.0
        description: RANSAC_REPROJECTION_THRESHOLD parameter
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: tracker_restart
      AUTO_REDETECT:
        type: boolean
        default: true
        description: AUTO_REDETECT parameter
        reload_tier: tracker_restart
      REDETECTION_SEARCH_RADIUS:
        type: integer
        default: 300
        description: Pixels
        min: 0
        max: 10000
        unit: px
        reload_tier: tracker_restart
      MIN_SEARCH_RADIUS:
        type: integer
        default: 50
        description: MIN_SEARCH_RADIUS parameter
        min: 0
        max: 100
        reload_tier: tracker_restart
      UNCERTAINTY_SCALE_FACTOR:
        type: float
        default: 2.0
        description: UNCERTAINTY_SCALE_FACTOR parameter
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: tracker_restart
      ESTIMATOR_UNCERTAINTY_THRESHOLD:
        type: float
        default: 1000.0
        description: ESTIMATOR_UNCERTAINTY_THRESHOLD parameter
        min: -10000.0
        max: 10000.0
        step: 0.1
        reload_tier: tracker_restart
  GStreamerPipelines:
    display_name: GStreamer Pipelines
    category: network
    icon: code
    parameters:
      USB_YUYV:
        type: string
        default: v4l2src device=/dev/video{device_id} ! video/x-raw,format=YUY2,width={width},height={height},framerate={fps}/1 ! videoconvert ! video/x-raw,format=BGR ! appsink drop=true max-buffers=1 sync=false
        description: USB camera pipelines
        reload_tier: system_restart
      USB_MJPEG:
        type: string
        default: v4l2src device=/dev/video{device_id} ! image/jpeg,width={width},height={height},framerate={fps}/1 ! jpegdec ! videoconvert ! video/x-raw,format=BGR ! appsink drop=true max-buffers=1 sync=false
        description: USB_MJPEG parameter
        reload_tier: system_restart
      CSI_NVIDIA:
        type: string
        default: nvarguscamerasrc sensor-id={sensor_id} ! video/x-raw(memory:NVMM),width={width},height={height},format=NV12,framerate={fps}/1 ! nvvidconv flip-method={flip_method} ! video/x-raw,format=BGRx ! videoconvert ! video/x-raw,format=BGR ! appsink drop=true sync=false
        description: CSI camera pipelines
        reload_tier: system_restart
      CSI_RPI:
        type: string
        default: libcamerasrc ! video/x-raw,width={width},height={height},framerate={fps}/1 ! videoconvert ! video/x-raw,format=BGR ! appsink drop=true sync=false
        description: CSI_RPI parameter
        reload_tier: system_restart
      RTSP:
        type: string
        default: rtspsrc location={url} latency={latency} ! decodebin ! videoconvert ! video/x-raw,format=BGR ! videoscale ! video/x-raw,width={width},height={height} ! appsink drop=true sync=false
        description: Network stream pipelines
        reload_tier: system_restart
      UDP:
        type: string
        default: udpsrc uri={url} ! application/x-rtp ! rtph264depay ! avdec_h264 ! videoconvert ! video/x-raw,format=BGR ! videoscale ! video/x-raw,width={width},height={height} ! appsink drop=true sync=false
        description: UDP parameter
        reload_tier: system_restart
      HTTP:
        type: string
        default: souphttpsrc location={url} ! decodebin ! videoconvert ! video/x-raw,format=BGR ! videoscale ! video/x-raw,width={width},height={height} ! appsink drop=true sync=false
        description: HTTP parameter
        reload_tier: system_restart
  FrameEstimation:
    display_name: Frame Display
    category: processing
    icon: picture_in_picture
    parameters:
      FRAME_TITLE:
        type: string
        default: Video
        description: Window title
        reload_tier: immediate
      SHOW_VIDEO_WINDOW:
        type: boolean
        default: false
        description: true for debugging with display
        reload_tier: immediate
  Estimator:
    display_name: State Estimator
    category: processing
    icon: insights
    parameters:
      USE_ESTIMATOR:
        type: boolean
        default: true
        description: Enable/disable state estimation
        reload_tier: immediate
      ESTIMATOR_TYPE:
        type: string
        default: Kalman
        description: ESTIMATOR_TYPE parameter
        options:
        - value: Kalman
          label: Kalman
        - value: None
          label: None
        reload_tier: immediate
      ESTIMATOR_HISTORY_LENGTH:
        type: integer
        default: 5
        description: State history buffer
        min: 0
        max: 100
        reload_tier: immediate
      ESTIMATOR_PROCESS_NOISE_VARIANCE:
        type: float
        default: 10.0
        description: Motion uncertainty
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: immediate
      ESTIMATOR_MEASUREMENT_NOISE_VARIANCE:
        type: float
        default: 1.0
        description: Sensor noise
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      ESTIMATOR_INITIAL_STATE_COVARIANCE:
        type: array
        default:
        - 5
        - 5
        - 50
        - 50
        - 100
        - 100
        description: 'Initial covariance: [x, y, dx, dy, ddx, ddy]'
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: immediate
      USE_ESTIMATOR_FOR_FOLLOWING:
        type: boolean
        default: true
        description: Use predictions for control
        reload_tier: immediate
      ESTIMATED_POSITION_COLOR:
        type: array
        default:
        - 255
        - 0
        - 0
        description: 'Blue: tracking active'
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: immediate
      ESTIMATION_ONLY_COLOR:
        type: array
        default:
        - 0
        - 165
        - 255
        description: 'Orange: prediction only'
        item_type: number
        min_items: 0
        max_items: 20
        reload_tier: immediate
  FramePreprocessor:
    display_name: Frame Preprocessor
    category: processing
    icon: auto_fix_high
    parameters:
      ENABLE_PREPROCESSING:
        type: boolean
        default: true
        description: 🎨 Image enhancement for better tracking
        reload_tier: tracker_restart
      PREPROCESSING_USE_BLUR:
        type: boolean
        default: true
        description: PREPROCESSING_USE_BLUR parameter
        reload_tier: tracker_restart
      PREPROCESSING_BLUR_KERNEL_SIZE:
        type: integer
        default: 5
        description: Must be odd (3, 5, 7...)
        min: 0
        max: 100
        unit: 3, 5, 7...
        reload_tier: tracker_restart
      PREPROCESSING_USE_MEDIAN_BLUR:
        type: boolean
        default: false
        description: PREPROCESSING_USE_MEDIAN_BLUR parameter
        reload_tier: tracker_restart
      PREPROCESSING_MEDIAN_BLUR_KERNEL_SIZE:
        type: integer
        default: 5
        description: PREPROCESSING_MEDIAN_BLUR_KERNEL_SIZE parameter
        min: 0
        max: 100
        reload_tier: tracker_restart
      PREPROCESSING_USE_CLAHE:
        type: boolean
        default: true
        description: Adaptive histogram equalization
        reload_tier: tracker_restart
      PREPROCESSING_CLAHE_CLIP_LIMIT:
        type: float
        default: 2.0
        description: Contrast limit
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: tracker_restart
      PREPROCESSING_CLAHE_TILE_GRID_SIZE:
        type: integer
        default: 8
        description: Grid size (1-10)
        min: 0
        max: 100
        unit: 1-10
        reload_tier: tracker_restart
      PREPROCESSING_COLOR_SPACE:
        type: string
        default: BGR
        description: PREPROCESSING_COLOR_SPACE parameter
        options:
        - value: BGR
          label: BGR
        - value: GRAY
          label: GRAY
        - value: HSV
          label: HSV
        - value: LAB
          label: LAB
        reload_tier: tracker_restart
  Segmentation:
    display_name: Segmentation
    category: processing
    icon: crop
    parameters:
      SEGMENTATION_ALGORITHMS:
        type: array
        default:
        - GrabCut
        - Watershed
        - yolov8s-seg
        - yolov8n-oiv7
        - yolov8s-obb
        - yolov11n
        description: 🎭 Object segmentation for precise tracking
        item_type: string
        reload_tier: tracker_restart
      DEFAULT_SEGMENTATION_ALGORITHM:
        type: string
        default: yolov11n.pt
        description: DEFAULT_SEGMENTATION_ALGORITHM parameter
        reload_tier: tracker_restart
      USE_SEGMENTATION_FOR_TRACKING:
        type: boolean
        default: true
        description: USE_SEGMENTATION_FOR_TRACKING parameter
        reload_tier: tracker_restart
      MORPHOLOGICAL_KERNEL_SIZE:
        type: integer
        default: 5
        description: MORPHOLOGICAL_KERNEL_SIZE parameter
        min: 0
        max: 100
        reload_tier: tracker_restart
      DILATION_ITERATIONS:
        type: integer
        default: 2
        description: DILATION_ITERATIONS parameter
        min: 0
        max: 100
        reload_tier: tracker_restart
      EROSION_ITERATIONS:
        type: integer
        default: 1
        description: EROSION_ITERATIONS parameter
        min: 0
        max: 100
        reload_tier: tracker_restart
      SEGMENTATION_GRABCUT_ITER:
        type: integer
        default: 5
        description: SEGMENTATION_GRABCUT_ITER parameter
        min: 0
        max: 100
        reload_tier: tracker_restart
      SEGMENTATION_MIN_AREA:
        type: integer
        default: 500
        description: Min segment area in pixels
        min: 0
        max: 10000
        unit: px
        reload_tier: tracker_restart
  YawControl:
    display_name: Yaw Control
    category: control
    icon: rotate_right
    parameters:
      ENABLE_YAW_CONTROL:
        type: boolean
        default: false
        description: 🔄 Drone yaw (heading) control
        reload_tier: immediate
      YAW_CONTROL_THRESHOLD:
        type: float
        default: 0.3
        description: Activation threshold
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      YAW_LATERAL_BLEND_FACTOR:
        type: float
        default: 0.5
        description: Blend yaw with lateral motion
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
      YAW_DEAD_ZONE:
        type: float
        default: 0.2
        description: Ignore small errors
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
  VerticalErrorRecalculation:
    display_name: Vertical Error
    category: processing
    icon: height
    parameters:
      VERTICAL_RECALC_DELAY:
        type: float
        default: 0.1
        description: Seconds
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: immediate
      YAW_PITCH_SYNC_FACTOR:
        type: float
        default: 0.5
        description: Coupling factor
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: immediate
  Gimbal:
    display_name: Gimbal Control
    category: control
    icon: control_camera
    parameters:
      ENABLE_GIMBAL_PRIORITY:
        type: boolean
        default: true
        description: Gimbal overrides body motion
        reload_tier: immediate
  AdaptiveControl:
    display_name: Adaptive Control
    category: control
    icon: auto_fix_high
    parameters:
      ADAPTIVE_YAW_CONTROL:
        type: boolean
        default: false
        description: Auto-tune yaw gains
        reload_tier: system_restart
  ChaseFollower:
    display_name: Chase Follower Limits
    category: control
    icon: speed
    parameters:
      MAX_ROLL_RATE:
        type: float
        default: 20.0
        description: deg/s
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: system_restart
      MAX_PITCH_RATE:
        type: float
        default: 10.0
        description: deg/s
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg
        reload_tier: system_restart
      MAX_YAW_RATE:
        type: float
        default: 10.0
        description: Rate Limits (deg/s - converted to rad/s internally)
        min: 0.0
        max: 100.0
        step: 0.1
        unit: deg/s - converted to rad/s internally
        reload_tier: system_restart
      MAX_THRUST:
        type: float
        default: 1.0
        description: 0-1
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: system_restart
      MIN_THRUST:
        type: float
        default: 0.3
        description: 0-1
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: system_restart
      MIN_GROUND_SPEED:
        type: integer
        default: 0
        description: m/s
        min: 0
        max: 100
        unit: m/s
        reload_tier: system_restart
      MAX_GROUND_SPEED:
        type: integer
        default: 100
        description: m/s
        min: 0
        max: 100
        unit: m/s
        reload_tier: system_restart
      TARGET_SPEED:
        type: integer
        default: 60
        description: m/s
        min: 0
        max: 100
        unit: m/s
        reload_tier: system_restart
      MAX_BANK_ANGLE:
        type: integer
        default: 20
        description: degrees
        min: 0
        max: 100
        unit: deg
        reload_tier: system_restart
      YAW_ERROR_CHECK_ENABLED:
        type: boolean
        default: true
        description: YAW_ERROR_CHECK_ENABLED parameter
        reload_tier: system_restart
      YAW_ERROR_THRESHOLD:
        type: integer
        default: 20
        description: degrees
        min: 0
        max: 100
        unit: deg
        reload_tier: system_restart
      ALTITUDE_FAILSAFE_ENABLED:
        type: boolean
        default: false
        description: ALTITUDE_FAILSAFE_ENABLED parameter
        reload_tier: system_restart
  PID:
    display_name: PID Controller
    category: control
    icon: tune
    parameters:
      PID_GAINS:
        type: object
        description: ⚙️ PID gains for each control axis All angular rates in deg/s (MAVSDK standard) One PID gain set per physical axis - no duplicates
        properties:
          x:
            type: object
            default:
              p: 4.5
              i: 0.1
              d: 1.5
            description: Lateral position
            reload_tier: immediate
          y:
            type: object
            default:
              p: 4.5
              i: 0.1
              d: 1.5
            description: Longitudinal position
            reload_tier: immediate
          z:
            type: object
            default:
              p: 2
              i: 0.03
              d: 0.05
            description: Vertical position
            reload_tier: immediate
          rollspeed_deg_s:
            type: object
            default:
              p: 1
              i: 0.05
              d: 0.2
            description: Roll rate (deg/s)
            unit: deg/s
            reload_tier: immediate
          pitchspeed_deg_s:
            type: object
            default:
              p: 20
              i: 0.1
              d: 0.4
            description: Pitch rate (deg/s)
            unit: deg/s
            reload_tier: immediate
          yawspeed_deg_s:
            type: object
            default:
              p: 12.0
              i: 0.5
              d: 1.5
            description: Yaw rate (deg/s) - conservative for safe demo
            unit: deg/s
            reload_tier: immediate
          thrust:
            type: object
            default:
              p: 0.1
              i: 3
              d: 1
            description: thrust parameter
            reload_tier: immediate
          vel_body_right:
            type: object
            default:
              p: 1.5
              i: 0.08
              d: 0.2
            description: Lateral velocity - conservative for safe demo
            reload_tier: immediate
          vel_body_down:
            type: object
            default:
              p: 1.0
              i: 0.08
              d: 0.15
            description: Vertical velocity - conservative for safe demo
            reload_tier: immediate
          fw_roll_rate:
            type: object
            default:
              p: 0.8
              i: 0.1
              d: 0.05
            description: Roll rate inner loop
            reload_tier: immediate
          fw_pitch_rate:
            type: object
            default:
              p: 0.6
              i: 0.15
              d: 0.03
            description: Pitch rate (TECS output)
            unit: tecs output
            reload_tier: immediate
          fw_yaw_rate:
            type: object
            default:
              p: 0.5
              i: 0.05
              d: 0.02
            description: Yaw rate (L1 output)
            unit: l1 output
            reload_tier: immediate
          fw_throttle:
            type: object
            default:
              p: 0.4
              i: 0.2
              d: 0.1
            description: Throttle (TECS energy)
            unit: tecs energy
            reload_tier: immediate
          fw_bank_angle:
            type: object
            default:
              p: 2.0
              i: 0.3
              d: 0.2
            description: Bank angle outer loop
            reload_tier: immediate
          mc_yawspeed_deg_s:
            type: object
            default:
              p: 15.0
              i: 0.5
              d: 2.0
            description: Yaw rate for YAW_TO_TARGET mode
            reload_tier: immediate
          mc_vel_body_right:
            type: object
            default:
              p: 8.0
              i: 0.2
              d: 1.0
            description: Lateral velocity for CRAB_STRAFE mode
            reload_tier: immediate
          mc_vel_body_down:
            type: object
            default:
              p: 5.0
              i: 0.3
              d: 0.8
            description: Vertical velocity control
            reload_tier: immediate
          mcar_pitchspeed_deg_s:
            type: object
            default:
              p: 18.0
              i: 0.15
              d: 0.4
            description: Pitch rate (aggressive response)
            unit: aggressive response
            reload_tier: immediate
          mcar_yawspeed_deg_s:
            type: object
            default:
              p: 12.0
              i: 0.1
              d: 0.3
            description: Yaw rate (turn to target)
            unit: turn to target
            reload_tier: immediate
          mcar_rollspeed_deg_s:
            type: object
            default:
              p: 1.5
              i: 0.08
              d: 0.25
            description: Roll rate (coordinated turns)
            unit: coordinated turns
            reload_tier: immediate
          mcar_thrust:
            type: object
            default:
              p: 0.15
              i: 0.5
              d: 0.2
            description: Thrust (altitude hold)
            unit: altitude hold
            reload_tier: immediate
          mcar_altitude:
            type: object
            default:
              p: 2.0
              i: 0.3
              d: 0.5
            description: Altitude (explicit control)
            unit: explicit control
            reload_tier: immediate
        reload_tier: system_restart
      PROPORTIONAL_ON_MEASUREMENT:
        type: boolean
        default: false
        description: Avoid derivative kick
        reload_tier: system_restart
      ENABLE_ANTI_WINDUP:
        type: boolean
        default: true
        description: Prevent integral windup
        reload_tier: system_restart
      ANTI_WINDUP_BACK_CALC_COEFF:
        type: float
        default: 0.1
        description: Back-calculation coefficient
        min: 0.0
        max: 1.0
        step: 0.01
        reload_tier: system_restart
  Safety:
    display_name: Safety Limits
    category: safety
    icon: shield
    parameters:
      GlobalLimits:
        type: object
        default:
          MIN_ALTITUDE: 3.0
          MAX_ALTITUDE: 120.0
          ALTITUDE_WARNING_BUFFER: 2.0
          ALTITUDE_SAFETY_ENABLED: true
          MAX_VELOCITY: 1.0
          MAX_VELOCITY_FORWARD: 0.5
          MAX_VELOCITY_LATERAL: 0.5
          MAX_VELOCITY_VERTICAL: 0.5
          MAX_YAW_RATE: 45.0
          MAX_PITCH_RATE: 45.0
          MAX_ROLL_RATE: 45.0
          EMERGENCY_STOP_ENABLED: true
          RTL_ON_VIOLATION: true
          TARGET_LOSS_ACTION: hover
          MAX_SAFETY_VIOLATIONS: 5
        description: GlobalLimits parameter
        reload_tier: immediate
      FollowerOverrides:
        type: object
        default: {}
        description: Empty - all followers use GlobalLimits
        reload_tier: immediate
  GainScheduling:
    display_name: Gain Scheduling
    category: control
    icon: timeline
    parameters:
      ENABLE_GAIN_SCHEDULING:
        type: boolean
        default: false
        description: 📈 Altitude-based gain adjustment
        reload_tier: immediate
      GAIN_SCHEDULING_PARAMETER:
        type: string
        default: current_altitude
        description: Parameter for gain scheduling
        reload_tier: immediate
      ALTITUDE_GAIN_SCHEDULE:
        type: object
        description: Altitude ranges and corresponding gains
        properties:
          (0, 20):
            type: object
            description: (0, 20) settings
            properties:
              x:
                type: object
                default:
                  p: 5.5
                  i: 0.2
                  d: 0.4
                description: Lateral position
                reload_tier: immediate
              y:
                type: object
                default:
                  p: 5.5
                  i: 0.2
                  d: 0.4
                description: Longitudinal position
                reload_tier: immediate
              z:
                type: object
                default:
                  p: 1
                  i: 0.01
                  d: 0.1
                description: Vertical position
                reload_tier: immediate
            reload_tier: immediate
          (20, 50):
            type: object
            description: (20, 50) settings
            properties:
              x:
                type: object
                default:
                  p: 6
                  i: 0.25
                  d: 0.5
                description: Lateral position
                reload_tier: immediate
              y:
                type: object
                default:
                  p: 6
                  i: 0.25
                  d: 0.5
                description: Longitudinal position
                reload_tier: immediate
              z:
                type: object
                default:
                  p: 1
                  i: 0.015
                  d: 0.15
                description: Vertical position
                reload_tier: immediate
            reload_tier: immediate
          (50, 100):
            type: object
            description: (50, 100) settings
            properties:
              x:
                type: object
                default:
                  p: 6.5
                  i: 0.3
                  d: 0.6
                description: Lateral position
                reload_tier: immediate
              y:
                type: object
                default:
                  p: 6.5
                  i: 0.3
                  d: 0.6
                description: Longitudinal position
                reload_tier: immediate
              z:
                type: object
                default:
                  p: 1
                  i: 0.02
                  d: 0.2
                description: Vertical position
                reload_tier: immediate
            reload_tier: immediate
          (100, 150):
            type: object
            description: (100, 150) settings
            properties:
              x:
                type: object
                default:
                  p: 6
                  i: 0.3
                  d: 1.0
                description: Lateral position
                reload_tier: immediate
              y:
                type: object
                default:
                  p: 6
                  i: 0.3
                  d: 1.0
                description: Longitudinal position
                reload_tier: immediate
              z:
                type: object
                default:
                  p: 1
                  i: 0.01
                  d: 0.01
                description: Vertical position
                reload_tier: immediate
            reload_tier: immediate
          (150, 200):
            type: object
            description: (150, 200) settings
            properties:
              x:
                type: object
                default:
                  p: 5.5
                  i: 0.25
                  d: 0.8
                description: Lateral position
                reload_tier: immediate
              y:
                type: object
                default:
                  p: 5.5
                  i: 0.25
                  d: 0.8
                description: Longitudinal position
                reload_tier: immediate
              z:
                type: object
                default:
                  p: 1
                  i: 0.01
                  d: 0.05
                description: Vertical position
                reload_tier: immediate
            reload_tier: immediate
        reload_tier: immediate
  Setpoint:
    display_name: Setpoint Config
    category: processing
    icon: my_location
    parameters:
      CAMERA_YAW_OFFSET:
        type: integer
        default: 0
        description: Camera mounting offset (degrees)
        min: 0
        max: 100
        unit: deg
        reload_tier: immediate
      SETPOINT_PUBLISH_RATE_S:
        type: float
        default: 0.1
        description: Setpoint update period (seconds)
        min: 0.0
        max: 1.0
        step: 0.01
        unit: s
        reload_tier: immediate
      ENABLE_SETPOINT_DEBUGGING:
        type: boolean
        default: false
        description: Debug output
        reload_tier: immediate
  Telemetry:
    display_name: Telemetry
    category: network
    icon: analytics
    parameters:
      ENABLE_TELEMETRY:
        type: boolean
        default: true
        description: 📊 Telemetry data distribution
        reload_tier: system_restart
      TELEMETRY_SEND_RATE:
        type: integer
        default: 2
        description: Hz
        min: 0
        max: 100
        unit: hz
        reload_tier: system_restart
      TELEMETRY_FLASK_ENDPOINT:
        type: string
        default: /telemetry
        description: REST endpoint
        reload_tier: system_restart
      ENABLE_UDP_STREAM:
        type: boolean
        default: false
        description: ENABLE_UDP_STREAM parameter
        reload_tier: system_restart
      UDP_HOST:
        type: string
        default: 127.0.0.1
        description: Gimbal IP address
        reload_tier: system_restart
      UDP_PORT:
        type: integer
        default: 5550
        description: Gimbal UDP port
        min: 0
        max: 1000000
        reload_tier: system_restart
      WEBSOCK_HOST:
        type: string
        default: 127.0.0.1
        description: WEBSOCK_HOST parameter
        reload_tier: system_restart
      WEBSOCK_PORT:
        type: integer
        default: 5551
        description: WEBSOCK_PORT parameter
        min: 0
        max: 1000000
        reload_tier: system_restart
      ENABLE_FOLLOWER_TELEMETRY:
        type: boolean
        default: true
        description: Include follower status
        reload_tier: system_restart
  GStreamer:
    display_name: GStreamer Output
    category: network
    icon: play_circle
    parameters:
      ENABLE_GSTREAMER_STREAM:
        type: boolean
        default: false
        description: 📹 Optional GStreamer pipeline for external streaming
        reload_tier: system_restart
      GSTREAMER_HOST:
        type: string
        default: 127.0.0.1
        description: GSTREAMER_HOST parameter
        reload_tier: system_restart
      GSTREAMER_PORT:
        type: integer
        default: 2000
        description: GSTREAMER_PORT parameter
        min: 0
        max: 1000000
        reload_tier: system_restart
      GSTREAMER_BITRATE:
        type: integer
        default: 2000
        description: kbps
        min: 0
        max: 1000000
        reload_tier: system_restart
      GSTREAMER_WIDTH:
        type: integer
        default: 1280
        description: GSTREAMER_WIDTH parameter
        min: 0
        max: 1000000
        reload_tier: system_restart
      GSTREAMER_HEIGHT:
        type: integer
        default: 720
        description: GSTREAMER_HEIGHT parameter
        min: 0
        max: 10000
        reload_tier: system_restart
      GSTREAMER_FRAMERATE:
        type: integer
        default: 15
        description: GSTREAMER_FRAMERATE parameter
        min: 0
        max: 100
        reload_tier: system_restart
      GSTREAMER_BUFFER_SIZE:
        type: integer
        default: 50000000
        description: bytes
        min: 0
        max: 1000000
        reload_tier: system_restart
      GSTREAMER_SPEED_PRESET:
        type: string
        default: ultrafast
        description: x264 preset
        reload_tier: system_restart
      GSTREAMER_KEY_INT_MAX:
        type: integer
        default: 30
        description: Keyframe interval
        min: 0
        max: 100
        reload_tier: system_restart
      GSTREAMER_TUNE:
        type: string
        default: zerolatency
        description: x264 tuning
        reload_tier: system_restart
      GSTREAMER_CONTRAST:
        type: float
        default: 5.0
        description: GSTREAMER_CONTRAST parameter
        min: 0.0
        max: 100.0
        step: 0.1
        reload_tier: system_restart
      GSTREAMER_BRIGHTNESS:
        type: integer
        default: 10
        description: GSTREAMER_BRIGHTNESS parameter
        min: 0
        max: 100
        reload_tier: system_restart
      GSTREAMER_SATURATION:
        type: integer
        default: 5
        description: GSTREAMER_SATURATION parameter
        min: 0
        max: 100
        reload_tier: system_restart
  OSD:
    display_name: On-Screen Display
    category: display
    icon: tv
    parameters:
      OSD_ENABLED:
        type: boolean
        default: true
        description: Master enable/disable for OSD
        reload_tier: immediate
      OSD_PRESET:
        type: string
        default: professional
        description: Active preset filename (without .yaml)
        options:
        - value: minimal
          label: minimal
        - value: professional
          label: professional
        - value: full_telemetry
          label: full_telemetry
        unit: without .yaml
        reload_tier: immediate
      OSD_PERFORMANCE_MODE:
        type: string
        default: balanced
        description: 'Choose rendering mode based on your hardware: "fast"     - OpenCV rendering (2-3ms/frame) - For Raspberry Pi, Jetson Nano "balanced" - PIL with native stroke (5-8ms/frame) - RECOMMENDED for most systems "quality"  - PIL with manual outline (50-80ms/frame) - For high-end desktops/recording'
        options:
        - value: fast
          label: fast
        - value: balanced
          label: balanced
        - value: quality
          label: quality
        reload_tier: immediate
  Debugging:
    display_name: Debugging
    category: display
    icon: bug_report
    parameters:
      ENABLE_DEBUGGING:
        type: boolean
        default: true
        description: 🐛 Debug and logging configuration
        reload_tier: immediate
      LOG_FILE_PATH:
        type: string
        default: logs/tracking_log.txt
        description: LOG_FILE_PATH parameter
        reload_tier: immediate
      LOG_LEVEL:
        type: string
        default: INFO
        description: LOG_LEVEL parameter
        options:
        - value: DEBUG
          label: DEBUG
        - value: INFO
          label: INFO
        - value: WARNING
          label: WARNING
        - value: ERROR
          label: ERROR
        reload_tier: immediate
      SAVE_DEBUG_FRAMES:
        type: boolean
        default: false
        description: Save frames for analysis
        reload_tier: immediate
      DEBUG_FRAME_PATH:
        type: string
        default: debug_frames/
        description: Debug frame storage
        reload_tier: immediate

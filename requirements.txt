# ============================================================================
# PixEagle Full Python Dependency Set
# ============================================================================
# Recommended setup:
#   make init
#
# Manual role-based setup:
#   pip install -r requirements-core.txt
#   bash scripts/setup/setup-pytorch.sh --mode auto   # optional accelerator setup
#   bash scripts/setup/install-ai-deps.sh             # optional AI/YOLO setup
#   pip install -r requirements-dev.txt               # optional dev/test tools
#
# This aggregate file is Core + development tooling only. AI must use the
# installer above because a normal `pip install -r` would let Ultralytics add
# opencv-python beside PixEagle's opencv-contrib or source-built OpenCV.
# ============================================================================

-r requirements-core.txt
-r requirements-dev.txt

# PixEagle Modernization Journal - 2026-06

## 2026-06-01 - Phase 2 Offboard Commander Boundary

- Resumed after interruption by checking branch/worktree state and rereading
  `AGENTS.md`, the phase map, issue register, modernization blueprint, API
  blueprint, latest journal entry, and Phase 2 command-intent checkpoint.
- Closed PXE-0007/PXE-0013 at code/mock-test/docs level by introducing
  `OffboardCommander` as the fixed-rate MAVSDK publication owner:
  - `AppController.connect_px4()` starts the commander after Offboard startup
    and no longer starts `SetpointSender` as a command publisher;
  - follower/frame-loop code only submits `CommandIntent` snapshots and refuses
    direct PX4 send fallback when the commander is absent;
  - disconnect, shutdown, emergency cleanup, and follower health paths stop or
    report the commander explicitly;
  - `SetpointSender` is monitor-only and reports
    `command_publication_source: offboard_commander`.
- Fixed independent review blockers before checkpointing:
  - `OffboardCommander.submit_intent()` now atomically applies accepted
    `CommandIntent.fields` into the active `SetpointHandler` before heartbeat
    publication, so the intent is the data source rather than an unused
    validation token;
  - `/commands/start_offboard_mode` now returns `failure` if controller startup
    reports errors or final state remains inactive;
  - `/api/follower/setpoints-status` unwraps the concrete follower handler and
    exposes commander publication status instead of treating circuit-breaker
    state as PX4-send truth;
  - keyboard/API smart-mode paths stop following through the async operator-safe
    path before synchronous tracker cleanup.
- Updated active drone/follower docs to teach
  `CommandIntent -> OffboardCommander -> PX4InterfaceManager`, removed stale
  `PX4Controller`/future-commander/frame-loop publication examples from the
  active docs touched by this slice, and strengthened docs hygiene tests.
- Refreshed companion references after the long pause:
  - MavlinkAnywhere `origin/main` latest tag: `v3.0.14`;
  - Smart Wi-Fi Manager `origin/main` latest tag: `v2.1.14`;
  - `mavsdk_drone_show`/MDS `origin/main` latest tag:
    `v5.5.23-simurgh-readonly-operator-coverage`.
  PXE-0022 remains open because local companion checkouts are behind and those
  newer API/MCP/devops standards must be re-reviewed before the PixEagle
  API/MCP phase.
- Independent review gate outcome:
  - PX4/MAVSDK reviewer initially blocked closure on unused intents, API
    fail-open success, and stale docs. Those blockers were fixed.
  - Docs/API reviewer found stale `/commands/start_offboard_mode`,
    `/api/follower/setpoints-status`, `mavsdk-offboard.md`, follower
    integration docs, and `SetpointSender` comments. Those blockers were fixed.
  - Companion-reference reviewer found drift in all three sidecar repos; this
    does not block the OffboardCommander slice but updates PXE-0022.
- Validation completed:
  - focused OffboardCommander/API/docs regression: 38 passed;
  - affected Phase 2 suite: 197 passed;
  - Phase 0 API/reload gate: 13 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - touched-module `py_compile`: passed;
  - stale-doc pattern scan: no matches in active drone/follower docs checked;
  - `git diff --check`: passed;
  - full backend suite: 1743 passed, 40 skipped.
- No dashboard gates were required because this slice did not touch frontend
  code. No SITL, HIL, real-aircraft, deployment, or field validation was run.
- Added PXE-0035 to track the remaining non-blocking commander risk: repeated
  publish failures are observable but do not yet trigger a local commander
  failure policy independent of PX4 Offboard-exit detection.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-01-phase-2-offboard-commander.md`.
- Next active slice is Phase 2 MAVLink telemetry freshness, PXE-0014.

## 2026-06-01 - Phase 2 MAVLink Telemetry Freshness

- Continued from the OffboardCommander checkpoint into PXE-0014 after rereading
  the updated phase map and issue register.
- Closed PXE-0014 by making MAVLink2REST freshness behavior explicit:
  - added typed config/schema knobs for `MAVLINK_REQUEST_TIMEOUT_S`,
    `MAVLINK_REQUEST_RETRIES`, and `MAVLINK_STALE_TIMEOUT_S`;
  - routed aggregate polling and per-message fetches through a shared
    timeout/retry request helper;
  - moved per-message async fetches through `asyncio.to_thread(...)` so they no
    longer block the event loop directly;
  - added `MavlinkDataManager.get_connection_status()` with fresh/stale/error
    status, age, timeout, retry, endpoint, error count, and last error fields;
  - exposed that status as `mavlink_telemetry` in `/status`.
- Updated MAVLink config/troubleshooting docs to teach the new typed knobs and
  removed the stale claim that timeout/retry exposure was still only a future
  modernization task.
- Added regression coverage for config validation, HTTP timeout use, retry
  behavior, stale status, successful freshness updates, `/status` exposure, and
  docs staleness guards.
- Independent telemetry review agents returned after the first checkpoint and
  correctly blocked closure on two issues. `MAVLINK_REQUEST_RETRIES` was
  generated as schema type `int`, which bypassed ConfigService/dashboard
  integer validation, and successful per-message MAVLink2REST fetches did not
  update the freshness status exposed through `/status`. Both blockers were
  fixed before re-closing PXE-0014.
- Added/updated docs for the API status shape and MavlinkDataManager component
  behavior so `mavlink_telemetry` is documented as MAVLink2REST request
  freshness, not PX4-in-loop follower evidence.
- No external runtime claim is made from this slice.
- Post-repair independent reviewers found no blockers to moving into PXE-0035.
  Residual telemetry-health semantics debt was recorded as PXE-0036.
- Validation completed:
  - focused MAVLink/API/docs/schema suite after reviewer fixes: 158 passed;
  - broader affected telemetry/drone-interface/schema suite after reviewer
    fixes: 289 passed;
  - Phase 0 API/reload gate: 13 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - touched-module `py_compile`: passed;
  - `git diff --check`: passed;
  - full backend suite: 1751 passed, 40 skipped.
- No dashboard gates were required because this slice did not touch frontend
  code. No SITL, HIL, real-aircraft, deployment, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-01-phase-2-mavlink-telemetry-freshness.md`.
- Next active slice is PXE-0035: OffboardCommander publish-failure policy.

## 2026-06-01 - Phase 2 Offboard Commander Failure Policy

- Continued from the PXE-0014 repair gate into PXE-0035 after refreshing the
  phase map, issue register, commander docs, and OffboardCommander tests.
- Closed PXE-0035 at unit/mock/docs level by adding a local commander failure
  policy:
  - `Setpoint.OFFBOARD_COMMAND_FAILURE_THRESHOLD` is typed as an integer in
    generated schema with bounds `1..100` and `follower_restart` reload tier;
  - `OffboardCommander` records `health_state`, consecutive failures, threshold,
    failure-policy reason, trigger count, and failure action;
  - transient failures below threshold recover on the next successful publish;
  - repeated publish failures and dependency validation failures trip the local
    policy once, stop the commander loop, and ask AppController to stop
    following through the normal Offboard disconnect path;
  - normal operator stop still sends a best-effort final default setpoint but
    does not trigger the sustained-failure policy.
- Fixed independent review blockers before closure:
  - serialized commander publishes with an async publish lock and stopped the
    heartbeat task before the final operator-stop publish, preventing concurrent
    MAVSDK publishes during stop;
  - corrected legacy `/status` docs to match the actual response shape;
  - made validation failures participate in the threshold path;
  - cleared stale `last_error` after successful recovery;
  - made follower health report a running commander with transient failures as
    degraded;
  - documented the `command_publication.offboard_commander` block returned by
    the follower setpoints API.
- Validation completed:
  - focused commander/controller/schema/docs suite after reviewer fixes:
    77 passed;
  - affected Offboard/drone-interface/API/schema suite after reviewer fixes:
    253 passed;
  - Phase 0 API/reload gate included in the affected suite: 13 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - touched-module `py_compile`: passed;
  - `git diff --check`: passed;
  - full backend suite: 1759 passed, 40 skipped.
- No dashboard gates were required because this slice did not touch frontend
  code. No SITL, HIL, real-aircraft, deployment, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-01-phase-2-offboard-commander-failure-policy.md`.
- Next active slice is PXE-0018: executable PX4-in-loop validation harness and
  evidence contract.

## 2026-06-01 - Phase 2 PX4-In-Loop Validation Harness

- Resumed after interruption by checking `git status`, rereading `README.md`,
  `docs/README.md`, the proposed improvement plan, modernization blueprint, API
  blueprint, phase map, issue register, and latest journal/checkpoint entries.
- Refreshed companion references because the pause was long enough for sidecar
  drift:
  - MavlinkAnywhere `origin/main`: latest tag `v3.0.14`, commit `7643d4d`;
  - Smart Wi-Fi Manager `origin/main`: latest tag `v2.1.14`, commit `a5414fc`;
  - `mavsdk_drone_show`/MDS `origin/main`: latest tag
    `v5.5.36-simurgh-swarm-readiness-routing`, commit `e0f565d4`.
  PXE-0022 remains open for the API/MCP/devops slice because local checkouts
  are still behind those refs.
- Closed PXE-0018 as a validation-harness foundation, not as a runtime SITL
  pass:
  - added `tools/sitl_plans/phase2_follower_validation.json` with scenarios for
    Offboard entry, Offboard heartbeat, follower setpoints, target loss, video
    stall, MAVSDK disconnect, MAVLink2REST timeout, operator abort, and
    commander publish failure;
  - added `tools/run_sitl_validation_suite.py` with side-effect-free dry-run,
    probe-only collection from an already running stack, and guarded
    `--execute --allow-process-start` PX4-container startup using
    `--pull=never`;
  - added `scripts/sitl/start_px4_sitl.sh` and
    `scripts/sitl/stop_px4_sitl.sh` helpers that use named Docker containers
    and do not stop arbitrary processes;
  - added pytest markers for `sitl`, `px4`, `e2e`, `hardware`, and `manual`,
    with normal CI/Make filters excluding all external/operator-gated markers;
  - added static contract tests proving plan coverage, marker gating,
    side-effect-free dry-run behavior, stale artifact-directory refusal, and
    incomplete status for a missing runtime stack.
- Fixed independent review blockers before closure:
  - required artifacts that are missing or placeholders now make the run
    `incomplete` and return nonzero;
  - stale evidence directories are refused instead of being reused;
  - execute mode now labels managed PX4 containers, checks for pre-existing
    names, verifies the harness-owned container ID, and runs `docker stop` only
    for a verified owned container;
  - the manual PX4 helper now creates/refuses new artifact directories instead
    of reusing fixed evidence paths;
  - the opt-in SITL pytest entry point now treats current placeholder
    PX4-log evidence as expected PXE-0037 debt instead of demanding an
    impossible pass;
  - the hard required evidence set now matches the full plan contract,
    including PixEagle/PX4 logs, PX4 params, ULog, tlog, route-map probes, and
    follower setpoint status;
  - MavlinkAnywhere required outputs and PixEagle runtime config are checked
    semantically from collected probes;
  - HTTP probe errors preserve status codes;
  - `manual` marker is excluded from normal CI/Make filters;
  - the manifest claim boundary is mode-specific and never claims HIL, field,
    or real-aircraft success.
- Recorded PXE-0037 for remaining SITL debt: active scenario stimuli/fault
  injection, structured route/profile parsing, image/container metadata, and
  automatic PX4 ULog/tlog/parameter collection are not yet implemented. Until
  PXE-0037 is done, accepted runtime evidence requires a real running stack
  plus complete artifacts, and the harness will correctly mark incomplete
  evidence as incomplete.
- Validation completed:
  - harness dry run JSON: passed;
  - `py_compile` for `tools/run_sitl_validation_suite.py`: passed;
  - `bash -n` for `scripts/sitl/start_px4_sitl.sh` and
    `scripts/sitl/stop_px4_sitl.sh`: passed;
  - focused SITL/docs/contract suite: 17 passed, 1 skipped;
  - `make phase0-check`: 28 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed;
  - full non-SITL backend suite: 1766 passed, 40 skipped, 1 deselected.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run in this slice. The only
  runtime-probe behavior exercised by tests was an intentionally missing
  localhost stack that returned `incomplete`.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-01-phase-2-px4-in-loop-validation-harness.md`.
- Next active slice is Phase 3 PXE-0019: tracker-in-loop validation.

## 2026-06-01 - Phase 3 Tracker-In-Loop Validation

- Continued from the PX4/SITL harness checkpoint into PXE-0019 after
  rereading the phase map, issue register, tracker testing docs, and the active
  fixture/test files.
- Closed PXE-0019 at deterministic L3 tracker/follower contract level:
  - added `tests/fixtures/synthetic_tracker_scene.py` with generated BGR
    target frames, normalized-center helpers, a test-only `BaseTracker`
    `ColorBlobTrackerProbe`, deterministic `GimbalReplaySample`, and a small
    synthetic video handler stub;
  - added `tests/fixtures/tracker_clips/linear_green_target.json` as a
    text-based simulated clip manifest rather than binary media;
  - added `tests/unit/trackers/test_tracker_in_loop_validation.py` covering
    deterministic visual target traces, public `MCVelocityPositionFollower`
    command intent generation, visual occlusion freshness metadata, active
    gimbal vector pursuit, and stale gimbal fail-closed behavior;
  - updated tracker development docs to define the fixture layer and explicitly
    separate `has_output`, `tracking_active`, `data_is_stale` /
    `freshness_reason`, and `usable_for_following`.
- Fixed review-gate blockers before closure:
  - stale visual tracker output now goes through
    `AppController._follow_tracker_output(...)`, is converted to inactive by
    the command-freshness contract, and submits a
    `mc_velocity_position_inactive_hold` command intent to the commander stub;
  - stale gimbal replay now goes through the same AppController public dispatch
    path and submits `gm_velocity_vector_unusable_external_input` instead of
    calling private fail-closed helpers directly;
  - tracker docs already avoided SITL/HIL/field overclaiming and were kept
    scoped to deterministic L3 contracts.
- Hidden fixture debt found during this slice was repaired:
  `TEST_SCHEMA_CACHE` had drifted from `configs/follower_commands.yaml` for
  command fields, profile required/optional fields, and the `dynamic` control
  type. The cache was aligned and
  `test_test_schema_cache_matches_command_profile_fields` now guards against
  future drift.
- Recorded PXE-0038 as follow-up debt because PXE-0019 intentionally proves a
  deterministic probe/replay contract, not a production tracker or full visual
  SITL contract. PXE-0038 covers production tracker/SmartTracker smoke and
  normalized trace artifact export.
- Independent review outcome:
  - one CV/tracker review agent failed to run because of an external usage
    limit and did not contribute findings;
  - the PX4/MAVSDK/GNC review agent blocked closure on stale visual and stale
    gimbal paths not using public follower/AppController routes; both blockers
    were fixed and validated before checkpointing.
- Validation completed:
  - tracker-in-loop focused suite: 5 passed;
  - affected follower/controller/schema suite: 103 passed;
  - docs plus tracker focused suite: 15 passed;
  - `py_compile` for the synthetic fixture and tracker-in-loop test: passed;
  - `git diff --check`: passed;
  - full normal backend suite:
    1772 passed, 40 skipped, 1 deselected.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run in this slice.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-01-phase-3-tracker-in-loop-validation.md`.
- Next active slice is PXE-0037: PX4/SITL scenario executor and artifact
  automation.

## 2026-06-01 - Phase 3 SITL Scenario Action Contract

- Continued PXE-0037 after closing PXE-0019, with companion references
  refreshed and unchanged from the Phase 2 harness checkpoint:
  - MavlinkAnywhere `origin/main`: latest tag `v3.0.14`, commit `7643d4d`;
  - `mavsdk_drone_show`/MDS `origin/main`: latest tag
    `v5.5.36-simurgh-swarm-readiness-routing`, commit `e0f565d4`;
  - Smart Wi-Fi Manager `origin/main`: latest tag `v2.1.14`, commit
    `a5414fc`.
- Completed the first PXE-0037 sub-slice by turning the SITL plan scenarios
  from prose into a checked-in action/result contract:
  - added per-scenario `actions` to
    `tools/sitl_plans/phase2_follower_validation.json`;
  - added `scenarios/scenario_results.json` as required evidence;
  - added `--run-scenarios` to execute checked-in action schedules against a
    running stack;
  - kept non-GET/control actions blocked unless the operator explicitly passes
    `--allow-control-actions`;
  - recorded current fault-injection gaps as `manual_fault` actions, which keep
    runtime evidence incomplete until replaced by automated injectors or
    explicit evidence-import steps.
  - added explicit PX4 evidence import flags for params, ULog, and tlog files,
    with copied artifacts and SHA-256 checksums in the run directory;
  - added `px4/container_metadata.json` to the required evidence contract and
    collect it through Docker image/container inspection when available.
- Fixed independent review blockers before checkpointing:
  - Offboard entry now requires `status=success`, `details.final_state=active`,
    `following_active=true`, and `offboard_commander.running=true`;
  - operator abort now requires API success and `following_active=false`;
  - the plan validator rejects non-manual scenarios that only check HTTP status
    or JSON field existence;
  - SITL setup docs no longer allow accepted reports with missing PX4 params,
    ULog, or tlog evidence.
- PXE-0037 remains active because this sub-slice does not yet automate
  synthetic target/fault injection, structured route-profile parsing beyond the
  existing semantic checks, PX4 params, ULog/tlog manifests with checksums, or
  image/container metadata capture.
- Validation completed:
  - `tools/run_sitl_validation_suite.py --plan-name phase2_follower_validation
    --dry-run --run-scenarios --json`: passed and reported 9 scenarios,
    26 actions, 2 gated control actions, and 5 manual fault blockers;
  - `py_compile` for the harness: passed;
  - focused SITL/docs suite: 22 passed, 1 skipped;
  - `make phase0-check`: schema up to date and 28 passed.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run in this sub-slice.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-01-phase-3-sitl-scenario-action-contract.md`.
- Next active work remains PXE-0037: replace `manual_fault` placeholders with
  automated fault injectors, then replace explicit PX4 evidence imports with
  automatic params/ULog/tlog collection where supported and improve
  operator-managed stack metadata capture.

## 2026-06-02 - PX4 SIH CI Validation Research

- Resumed from the Phase 3 plan reconciliation into a research/design slice for
  PX4 SIH as a lightweight CI/local-dev validation backend.
- Refreshed companion references:
  - MavlinkAnywhere `origin/main` remains
    `7643d4d9bc75a78fdc6b0f68358c466310ee2c4d`, latest tag `v3.0.14`;
  - Smart Wi-Fi Manager `origin/main` remains
    `a5414fc7d7df1fde47db11aeed1681f5515ea350`, latest tag `v2.1.14`;
  - MAVSDK Drone Show advanced to
    `f8a4016496fe0189b16c3fe5d060980991900ddf`, latest tag
    `v5.5.42-simurgh-quickscout-readiness`.
- Consulted independent read-only reviewers for PX4/SIH/MAVSDK, Docker/CI,
  tracker/follower validation, and MAVLink/API/MCP routing. Consensus:
  - use official PX4 SIH as the L2 PX4 control-plane gate;
  - keep normal PR CI side-effect-free until the gate is proven;
  - route accepted evidence through MavlinkAnywhere or an equivalent structured
    route profile, not direct PX4 ports;
  - keep synthetic video/replay/full visual SITL separate from SIH.
- Created
  `docs/reporting/agent-ops/codex-modernization/audits/2026-06-02-px4-sih-ci-validation-research.md`.
- Added PXE-0039 to track the optional PX4 SIH CI profile and updated the
  phase-slice map so future resumes do not lose this decision.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run.

## 2026-06-02 - Final Implementation Roadmap

- Converted maintainer feedback into an approval-ready implementation roadmap:
  `docs/reporting/agent-ops/codex-modernization/audits/2026-06-02-final-implementation-roadmap.md`.
- Updated the simulator strategy:
  - official `px4io/px4-sitl` remains the SIH quick control-plane backend;
  - official `px4io/px4-sitl-gazebo` becomes the preferred full visual SITL
    backend;
  - unofficial `jonasvautherin/px4-gazebo-headless` is kept only as a fallback
    or benchmark if official video export is insufficient;
  - X-Plane remains PXE-0020 manual/historical disposition unless headless
    mission automation is proven later.
- Added PXE-0040 for official Gazebo headless visual SITL and video-source
  integration.
- Added PXE-0041 for final no-legacy cleanup and release-readiness evidence.
- Recorded that PixEagle already has a `UDP_STREAM` plus GStreamer RTP/H.264
  path, so Gazebo's default UDP video on port `5600` should first be tried as a
  config/preset before adding a new video source type.
- Integrated independent read-only reviewer feedback:
  - run no-regression video/tracker/freshness/offboard tests before simulator
    changes;
  - prove generated H.264 RTP/UDP receiver behavior before Gazebo;
  - normalize `/api/v1` and dashboard clients before broad UI/toolchain cleanup;
  - remove legacy only after parity and inventory-tested deprecation gates.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run.

## 2026-06-02 - Phase 3 SITL Target-Loss Injector

- Resumed PXE-0037 after the approved final roadmap, reread the active
  operating docs, phase map, issue register, previous PXE-0037 checkpoint, SITL
  plan library, AppController/FastAPI docs, and tracker/follower integration
  docs.
- Completed the first owned PXE-0037 fault injector:
  - added `AppController.inject_tracker_output_for_validation()` so
    validation input reuses the normal command-freshness, inactive-output
    follower opt-in, `CommandIntent`, and `OffboardCommander` path;
  - added disabled-by-default
    `POST /api/v1/sitl/injections/tracker-output` with typed request/response
    models, operation ID, route metadata tests, structured `/api/v1` error
    envelopes, and route-specific request validation errors;
  - updated the Phase 2 plan so target loss posts an unusable synthetic
    `TrackerOutput`, remains gated as a control action, requires
    `PIXEAGLE_ENABLE_SITL_INJECTIONS=1` and
    `Follower.FOLLOWER_MODE=mc_velocity_position`, and asserts inactive
    conversion, `mc_velocity_position_inactive_hold`, zero hold fields, and
    active `OffboardCommander` publication metadata;
  - reduced dry-run `manual_fault` blockers from 5 to 4.
- Fixed independent review blockers before checkpointing:
  - API reviewer blocked initial closure on missing structured error envelope,
    missing plan env declaration, weak nested response contract, OpenAPI
    default `200` success status, and body-validation errors bypassing the
    envelope. Fixed with `APIErrorResponse`, `SITL_ERROR_RESPONSES`,
    `status_code=202`, route-specific `RequestValidationError` handling, typed
    nested response models, plan env/config declarations, and route metadata
    tests.
  - PX4/SITL and tracker/follower reviewers requested stronger runtime
    assertions. The plan now checks command intent reason/fields,
    commander presence/running/publication source, and post-loss zero setpoints.
  - Tracker/follower reviewer found a conflicting doc snippet suggesting
    last-known-coordinate pursuit after target loss. It now says to publish
    explicit hold/stop/orbit intent or request Offboard exit.
- Final independent reviewer outcome:
  - PX4/SITL safety/evidence: no remaining blockers;
  - tracker/follower freshness/command-intent: no remaining blockers;
  - API/MCP contract: previously reported blockers cleared; non-blocking note
    remains that `dry_run=true` returns `200` and could use explicit OpenAPI
    metadata in later `/api/v1` polish.
- Validation completed:
  - touched Python syntax/import compile: passed;
  - focused API/SITL/tracker injector suite: 28 passed;
  - focused API envelope/SITL contract suite after API repair: 23 passed;
  - Phase 0 route/reload/docs gate: 24 passed;
  - nearby safety/freshness suite: 42 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed;
  - SITL dry-run with scenario schedule: passed and reported 9 scenarios,
    26 actions, 3 gated control actions, and 4 remaining manual blockers.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-02-phase-3-sitl-target-loss-injector.md`.
- PXE-0037 remains active. Next work: automate video stall, MAVSDK disconnect,
  MAVLink2REST timeout, and commander publish failure injectors; then automate
  PX4 params/ULog/tlog collection and improve operator-managed stack metadata.

## 2026-06-02 - Phase 3 SITL Video-Stall Injector

- Continued PXE-0037 from the target-loss injector checkpoint into the next
  smallest manual-fault blocker: video stall.
- Completed the second owned PXE-0037 fault injector:
  - added `AppController.inject_video_stall_for_validation()` so validation
    input reuses `handle_video_frame_unavailable()`, preserves explicit
    non-video external tracker behavior, rejects inactive follow mode, and
    returns command-intent plus stable `OffboardCommander` evidence;
  - added disabled-by-default
    `POST /api/v1/sitl/injections/video-stall` with typed request/response
    models, typed `SITLFrameStatusSummary`, operation ID, `202` OpenAPI status,
    route metadata tests, structured `/api/v1` error envelopes, and
    route-specific request validation errors;
  - updated the Phase 2 plan so video stall posts frame-status metadata,
    remains gated as a control action, asserts frame unusability,
    `mc_velocity_position_inactive_hold`, zero hold fields, active
    `OffboardCommander` publication metadata, and post-stall
    `/api/follower/setpoints-status` zero/commander state;
  - reduced dry-run `manual_fault` blockers from 4 to 3.
- Independent reviewers initially found no blockers and noted two hardening
  opportunities: type the video-stall `frame_status` response and add a
  post-stall setpoints-status assertion. Both were implemented before
  checkpointing.
- Final recheck after hardening found no blockers by inspection. The reviewer
  shell lacked `pytest`, so the authoritative validation evidence is the
  main-shell test/schema/dry-run output below. A stale reporting read during the
  patch window was resolved by rechecking the on-disk phase map, issue register,
  journal, and checkpoint before closure.
- Validation completed:
  - touched Python syntax compile: passed;
  - focused video-stall/API/SITL/controller suite: 58 passed;
  - Phase 0 route/reload/docs gate: 24 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed;
  - SITL dry-run with scenario schedule: passed and reported 9 scenarios,
    27 actions, 4 gated control actions, and 3 remaining manual blockers.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-02-phase-3-sitl-video-stall-injector.md`.
- PXE-0037 remains active. Next work: automate MAVSDK disconnect,
  MAVLink2REST timeout, and commander publish failure injectors; then automate
  PX4 params/ULog/tlog collection and improve operator-managed stack metadata.

## 2026-06-02 - Phase 3 SITL Commander Publish-Failure Injector

- Continued PXE-0037 from the video-stall checkpoint into the next owned
  manual-fault blocker: commander publish failure.
- Completed the third owned PXE-0037 fault injector:
  - added `OffboardCommander.inject_publish_failures_for_validation()` so
    operator-gated validation can record bounded synthetic failed publishes
    without synthesizing MAVSDK setpoint publishes, replacing PX4 interfaces,
    stopping services, or mutating MAVLink routing;
  - added `AppController.inject_commander_publish_failure_for_validation()` so
    the validation path crosses the configured commander failure threshold and
    awaits the same fail-closed cleanup handler used by real repeated commander
    publish failures;
  - added disabled-by-default
    `POST /api/v1/sitl/injections/commander-publish-failure` with typed
    request/response models, operation ID, `202` OpenAPI status, route metadata
    tests, structured `/api/v1` error envelopes, route-specific request
    validation errors, and dry-run support;
  - updated the Phase 2 plan so commander publish failure is a gated control
    action that asserts before/after commander status, threshold crossing,
    retained `/status` failure evidence, `disconnect_result.errors == []`, and
    inactive follow mode after cleanup;
  - reduced dry-run `manual_fault` blockers from 3 to 2.
- Design decision: reviewers recommended an owned route and warned that
  scheduled cleanup could race scenario assertions. The implementation avoids
  monkey-patching a live MAVSDK send method in this slice; it records failures
  inside the commander under its publish lock, then directly awaits
  AppController cleanup. A true transport-break test remains the MAVSDK
  disconnect slice.
- Final code/API recheck found two closure concerns, both addressed before
  closure: wording now states that cleanup still calls the normal Offboard stop
  path, and the commander failure recorder now marks `running=False` under the
  publish lock while heartbeat publishes that wake after stop return without
  sending.
- Validation completed after final independent recheck fixes:
  - touched Python syntax compile: passed;
  - JSON plan validation: passed;
  - focused commander/API/SITL/controller suite: 79 passed;
  - SITL dry-run with scenario schedule: passed and reported 9 scenarios,
    28 actions, 5 gated control actions, and 2 remaining manual blockers.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-02-phase-3-sitl-commander-publish-failure-injector.md`.
- PXE-0037 remains active. Next work: automate MAVSDK disconnect and
  MAVLink2REST timeout injectors; then automate PX4 params/ULog/tlog collection
  and improve operator-managed stack metadata.

## 2026-06-02 - Phase 3 SITL MAVLink2REST Timeout Injector

- Continued PXE-0037 from the commander publish-failure checkpoint into the
  next owned manual-fault blocker: MAVLink2REST timeout.
- Completed the fourth owned PXE-0037 fault injector:
  - added `MavlinkDataManager.inject_timeout_for_validation()` so
    operator-gated validation can record a bounded PixEagle-local
    MAVLink2REST client timeout without stopping MAVLink2REST, PX4, Docker,
    MavlinkAnywhere, routing, or network interfaces;
  - hardened MAVLink telemetry connection/status fields with a reentrant lock
    so validation timeout windows, polling updates, success/error counters,
    and `/status` snapshots do not race each other;
  - added `AppController.inject_mavlink2rest_timeout_for_validation()` to
    delegate into the telemetry manager and return typed injection metadata
    plus current `mavlink_telemetry`;
  - added disabled-by-default
    `POST /api/v1/sitl/injections/mavlink2rest-timeout` with typed
    request/response models, operation ID, `202` OpenAPI status, route
    metadata tests, structured `/api/v1` error envelopes, route-specific
    request validation errors, and dry-run support;
  - updated the Phase 2 plan so MAVLink2REST timeout is a gated control action
    that first asserts fresh PixEagle `mavlink_telemetry` with a real
    last-success age, then asserts stale/error telemetry, exact timeout reason,
    preserved endpoint, `validation_timeout_active = true`, and a direct
    MAVLink2REST service probe after injection;
  - reduced dry-run `manual_fault` blockers from 2 to 1.
- Design decision: reviewers recommended validating PixEagle's local telemetry
  freshness behavior rather than killing the external service or mutating
  routes. The implementation follows that boundary and keeps true transport
  break evidence separate from this local-client timeout path.
- First broad validation found one stale unit-test assumption after the lock
  hardening: the old test required exactly `threading.Lock()`. It now asserts
  lock protocol and acquire/release behavior so `threading.RLock()` is covered
  without weakening thread-safety intent.
- Independent code/safety recheck found one blocker before closure: the first
  implementation could fabricate stale history by setting
  `last_successful_fetch_monotonic_s` when no PixEagle MAVLink2REST fetch had
  ever succeeded. Fixed before closure: `force_stale` now only ages an existing
  successful timestamp; fresh managers report timeout as `error` with
  `last_success_age_s = null`; the Phase 2 plan asserts fresh PixEagle
  telemetry before injection; and a regression test covers the no-prior-success
  case.
- Targeted recheck after the fix found no blockers. Residual runtime risk
  remains documented: the post-injection assertion can still miss a 5-second
  local timeout window if the scenario runner stalls, and the direct
  MAVLink2REST probe proves HTTP reachability rather than PX4/MAVLink
  freshness.
- Validation completed:
  - touched Python syntax compile: passed;
  - JSON plan validation: passed;
  - focused MAVLink/API/SITL/controller suite: 115 passed after the lock-test
    update and no-fabricated-history regression;
  - Phase 0 route/reload/docs gate: 24 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed;
  - SITL dry-run with scenario schedule: passed and reported 9 scenarios,
    29 actions, 6 gated control actions, and 1 remaining manual blocker.
- No Docker/PX4 runtime, SITL scenario, HIL, real-aircraft, deployment,
  service installation, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-02-phase-3-sitl-mavlink2rest-timeout-injector.md`.
- PXE-0037 remains active. Next work: automate MAVSDK disconnect; then
  automate PX4 params/ULog/tlog collection and improve operator-managed stack
  metadata.

## 2026-06-03 - Phase 3 SITL MAVSDK Disconnect Injector

- Continued PXE-0037 from the MAVLink2REST timeout checkpoint into the final
  checked-in `manual_fault` blocker: MAVSDK disconnect.
- Completed the fifth owned PXE-0037 fault injector:
  - used the existing `PX4InterfaceManager.inject_mavsdk_disconnect_for_validation()`
    hook as the owned local command-path disconnect boundary;
  - added `AppController.inject_mavsdk_disconnect_for_validation()` to require
    active follow mode plus a running `OffboardCommander`, mark the local
    PX4/MAVSDK command path validation-disconnected, record bounded commander
    publish failures, await the normal fail-closed cleanup path, and return
    before/after PX4 command-path plus commander evidence;
  - added disabled-by-default
    `POST /api/v1/sitl/injections/mavsdk-disconnect` with typed
    request/response models, operation ID, route inventory coverage,
    structured `/api/v1` error envelopes, route-specific request-validation
    errors, dry-run support, and `/status.px4_connection` visibility;
  - updated the Phase 2 plan so MAVSDK disconnect is a gated HTTP control
    action, not a `manual_fault`, and asserts local validation-disconnect
    state, failed commander state, inactive follow mode, retained `/status`
    failure evidence, and the expected failed Offboard stop error;
  - added explicit re-entry actions before destructive scenarios that require
    active follow mode so previous stop/failure scenarios do not silently break
    later evidence.
- Design boundary: the route proves PixEagle-local fail-closed behavior only.
  It does not stop PX4, Docker, MavlinkAnywhere, MAVLink2REST, a MAVSDK server,
  network interfaces, or MAVLink routes, and it is not evidence of a real
  transport outage or PX4 failsafe event by itself.
- Final independent review found two blockers before closure: under-threshold
  `failure_count` requests could be accepted without forcing cleanup, and a
  running commander heartbeat could race exact failure counters if PX4 was
  marked validation-disconnected before synthetic commander failures were
  recorded. Fixed before closure: the hook now applies at least the failures
  needed to cross the active commander threshold and records commander failures
  before marking the PX4 command path validation-disconnected.
- Validation completed:
  - touched Python syntax compile: passed;
  - JSON plan validation: passed;
  - focused PX4/controller/API/SITL route-contract suite: 142 passed;
  - SITL dry-run with scenario schedule: passed and reported 9 scenarios,
    32 actions, 10 gated control actions, and 0 remaining manual blockers.
- No Docker/PX4 runtime, SITL scenario execution, HIL, real-aircraft,
  deployment, service installation, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-03-phase-3-sitl-mavsdk-disconnect-injector.md`.
- PXE-0037 remains active. Next work: automate PX4 params/ULog/tlog collection
  where supported and improve operator-managed stack metadata capture.

## 2026-06-03 - Phase 3 PX4 Artifact Auto-Collection

- Continued PXE-0037 from the MAVSDK disconnect checkpoint into automatic PX4
  params/ULog/tlog evidence collection.
- Completed the PX4 artifact automation slice:
  - added plan-level `stack.px4.artifact_collection` search roots, file
    patterns, and per-kind file limit for params, ULog, and tlog discovery;
  - added read-only container discovery to the SITL harness using
    `docker exec <container> sh -lc "find ..."` and `docker cp` only;
  - kept explicit `--px4-params-file`, `--px4-ulog`, and `--px4-tlog` imports
    as first-class evidence APIs and preserved incomplete status when explicit
    inputs are missing;
  - enabled automatic collection for harness-owned `--execute` runs only after
    the PX4 container labels and run ID are verified;
  - added `--px4-container-id` and `--auto-px4-container-artifacts` so
    probe-only operator-managed containers require both a selector and an
    explicit auto-copy opt-in;
  - added `--px4-log` and `--pixeagle-log` for explicit required log imports;
  - allowed `--px4-container-name`/`--px4-container-id` in probe-only metadata
    collection without enabling artifact copy;
  - recorded `px4_artifact_collection` in the manifest so evidence reports show
    explicit imports, auto-copy state, container ref, and collection mode.
- Safety boundary: the auto path does not stop containers, restart services,
  edit files inside the container, rotate logs, change MavlinkAnywhere routes,
  command PX4, or start PixEagle/MAVLink2REST. Operator-managed containers are
  never stopped by the harness.
- Independent pre-review flagged the important boundaries before patching:
  auto-copy must be separate from explicit imports, probe-only must require a
  selector plus explicit flag, execute mode must use only verified
  harness-owned containers, and docs must preserve incomplete status for
  missing PX4 evidence. The implementation follows those constraints.
- Final independent reviewers found closure blockers. Fixed before closure:
  scenario failures now take precedence over incomplete artifacts in the
  manifest, plain `--probe-only` docs now describe a probe package rather than
  accepted evidence, PX4/PixEagle log import flags are documented and tested,
  and heartbeat/follower-setpoint scenarios now assert local commander counters,
  successful publish metadata, finite command rate, finite setpoint fields, and
  active publication source. Remaining legacy `/commands/*` start/abort actions
  and PX4-level ULog/tlog cadence parsing are tracked as PXE-0042.
- Validation completed:
  - touched Python syntax compile: passed;
  - focused SITL contract/operator-gated harness suite: 27 passed, 1 skipped;
  - SITL dry-run with scenario schedule: passed and reported 9 scenarios,
    32 actions, 10 gated control actions, and 0 manual blockers;
  - plan JSON parse with `json.tool`: passed;
  - Phase 0 route/reload/docs gate: 24 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed.
- No Docker/PX4 runtime, SITL scenario execution, HIL, real-aircraft,
  deployment, service installation, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-03-phase-3-px4-artifact-auto-collection.md`.
- PXE-0037 remains active for structured MavlinkAnywhere route/profile parsing;
  string-containment endpoint checks must still be replaced before the SITL
  evidence contract is fully closed. PXE-0042 tracks typed `/api/v1` start/abort
  actions and PX4-level cadence evidence.

## 2026-06-03 - Phase 3 Structured MavlinkAnywhere Validation

- Continued PXE-0037 from the PX4 artifact auto-collection checkpoint into the
  final harness-side route/profile evidence debt.
- Refreshed companion references without pulling or modifying their local
  checkouts:
  - MavlinkAnywhere `origin/main` at `7643d4d9bc75`
    (`v3.0.14-2-g7643d4d`), local checkout behind 10 commits;
  - MAVSDK Drone Show `origin/main` at `2c60d8e403c4`
    (`v5.5.45-simurgh-active-tools-panel`), local checkout behind 114 commits
    after fetch;
  - Smart Wi-Fi Manager `origin/main` at `a5414fc7d7df`
    (`v2.1.14-2-ga5414fc`), local checkout behind 5 commits.
- Completed the structured MavlinkAnywhere route/profile validation slice:
  - added required artifact `route_map/mavlink_anywhere_endpoints.json` from
    `/api/v1/endpoints`;
  - added `/api/v1/endpoints` to probe collection and core probe gating;
  - replaced string-containment route checks with structured JSON parsing from
    `/api/v1/endpoints`, `/api/v1/config`, and `/api/v1/profiles/summary`;
  - required full MavlinkAnywhere endpoint-shaped objects with `name`, `type`,
    `mode`, `address`, `port`, `category`, and `enabled`;
  - required each output to be enabled `UdpEndpoint` in `normal` mode in all
    three route/profile sources;
  - rejected incidental text, address/port-only JSON, disabled endpoints, and
    server-mode endpoints;
  - added profile-summary metadata gating for `backend=mavlink-anywhere` and
    `present=true`;
  - updated operator docs to include `/api/v1/endpoints` and `/api/v1/config`
    in MavlinkAnywhere route preflight checks.
- Review gate:
  - first fresh review found a blocker: the initial structured parser could
    still accept incidental address/port-only JSON. Fixed before closure by
    requiring full MavlinkAnywhere endpoint fields and adding a regression test;
  - final read-only reviewer found no blockers after the fix and recommended
    profile-summary metadata gating. Implemented before closure.
- Validation completed before broader final gates:
  - touched Python syntax compile: passed;
  - focused SITL contract/operator-gated harness suite: 32 passed, 1 skipped;
  - SITL dry-run with scenario schedule: passed and reported 9 scenarios,
    32 actions, 10 gated control actions, and 0 manual blockers; plan hash
    `1e6bae1acc67421176119eb295e4778d0dee69d4bd4987e000a84d6f1d10be7d`;
  - plan JSON parse with `json.tool`: passed;
  - Phase 0 route/reload/docs gate: 24 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed.
- No Docker/PX4 runtime, SITL scenario execution, HIL, real-aircraft,
  deployment, service installation, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-03-phase-3-structured-mavlinkanywhere-validation.md`.
- PXE-0037 is now done for the harness-side implementation contract. PXE-0042
  remains open for typed `/api/v1` start/abort actions and parsed PX4
  ULog/tlog or telemetry assertions for Offboard mode and setpoint cadence.
- Next active slice: PXE-0039 lightweight official PX4 SIH CI profile, scoped
  to opt-in L2 control-plane evidence only.

## 2026-06-03 - Phase 3 PX4 SIH CI Profile

- Continued from the structured MavlinkAnywhere checkpoint into PXE-0039, the
  lightweight official PX4 SIH CI/local profile.
- Added `scripts/sitl/run_px4_sih_profile.sh` as a thin operational wrapper
  over the existing evidence harness:
  - defaults to side-effect-free dry-run;
  - supports explicit probe-only collection from an already running stack;
  - supports guarded execute-px4 mode that starts only a harness-owned official
    PX4 SIH container through `--execute --allow-process-start`;
  - forwards scenario execution, control-action gating, timeout/startup wait,
    image/model, artifact imports, container selectors, and JSON output;
  - does not configure MavlinkAnywhere, start PixEagle, start MAVLink2REST,
    install services, mutate host routing, or touch real hardware.
- Added `.github/workflows/px4-sih-validation.yml`:
  - `workflow_dispatch` inputs select dry-run/probe-only/execute-px4, image,
    model, optional image pull, scenario execution, control-action allowance,
    container selectors, and artifact root;
  - weekly schedule runs dry-run only;
  - no `push` or `pull_request` trigger is present, so normal PR CI remains
    free of Docker/PX4 runtime requirements;
  - runtime artifacts under `reports/sitl/**` upload with `if: always()`.
- Added Make targets for the profile:
  - `make sitl-sih-dry-run`;
  - `make sitl-sih-probe`;
  - `make sitl-sih-execute-px4`.
- Updated SITL setup and plan docs to describe the SIH wrapper, Actions
  workflow, optional image pull versus harness `--pull=never`, artifact upload,
  prepared-stack requirement, and no-overclaim boundaries.
- Added tests for wrapper dry-run behavior, explicit runtime modes, no sudo or
  host route configuration in the wrapper, opt-in workflow triggers, artifact
  upload, optional image pull, and Make target exposure.
- Local review caught and fixed one implementation mistake before closure:
  a `pixeagle.log` assertion was accidentally placed in the new Makefile target
  test where `run_dir` was not defined.
- Independent subagent review attempts for PX4/SIH/CI blockers were started
  twice, but both errored because usage limits were hit before findings were
  returned. These attempts are recorded as not completed and should be rerun
  before promoting the SIH workflow to a required runtime gate.
- Validation completed:
  - SIH/profile shell syntax checks: passed;
  - touched Python syntax compile: passed;
  - wrapper dry-run with scenario schedule: passed and produced valid JSON with
    9 scenarios, 32 actions, 10 gated control actions, 0 manual faults, and
    plan hash `1e6bae1acc67421176119eb295e4778d0dee69d4bd4987e000a84d6f1d10be7d`;
  - `make sitl-sih-dry-run`: passed and produced valid dry-run JSON;
  - focused SITL contract/operator-gated harness suite: 36 passed, 1 skipped;
  - Phase 0 route/reload/docs gate: 24 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed.
- No Docker/PX4 runtime, PX4 SIH container execution, SITL scenario execution,
  GitHub Actions hosted workflow execution, HIL, real-aircraft, deployment,
  service installation, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-03-phase-3-px4-sih-ci-profile.md`.
- PXE-0039 is now done for the opt-in profile contract. Next active slice:
  PXE-0040 generated RTP/UDP video receiver proof, which should prove the
  deterministic UDP/GStreamer ingest contract before Gazebo camera evidence.

## 2026-06-03 - Phase 3 Generated RTP/UDP Video Receiver Proof

- Continued from the PXE-0039 SIH profile into the PXE-0040 generated video
  prerequisite after closing a stale hung one-off Python/GStreamer proof process
  from the interrupted session.
- Scouting found a real UDP video-path hazard:
  - OpenCV with GStreamer can receive RTP/H.264 frames successfully;
  - `cv2.VideoCapture(..., cv2.CAP_GSTREAMER).read()` blocks indefinitely after
    the UDP sender stops;
  - constructing the UDP receiver can also block when no sender exists;
  - OpenCV `CAP_PROP_READ_TIMEOUT_MSEC` is not supported for this backend.
- Fixed the hazard for PixEagle's main frame path by moving
  `UDP_STREAM` + `USE_GSTREAMER=true` open/read ownership into an asynchronous
  daemon reader. `VideoHandler.get_frame()` now consumes the latest reader frame
  without blocking and returns cached/stale frames as
  `usable_for_following=false` when no fresh frame arrives.
- Added `tools/run_udp_video_receiver_proof.py`:
  - dry-run is default and validates the checked-in UDP receiver pipeline
    without starting processes or writing artifacts;
  - guarded execute requires `--execute --allow-process-start`;
  - execute starts only a local `gst-launch-1.0 videotestsrc` H.264 RTP sender;
  - artifacts include manifest, source config, sender/receiver pipelines,
    frame status sequences, post-stop status sequence, frame hashes, sender log,
    and runtime versions.
- Updated the checked-in UDP GStreamer template to use explicit H.264 RTP caps,
  `h264parse`, BGR conversion, `videoscale`, configured dimensions, and
  `appsink drop=true max-buffers=1 sync=false`.
- Added Make targets:
  - `make video-udp-proof-dry-run`;
  - `make video-udp-proof-execute`.
- Updated video/SITL docs to teach the proven UDP contract, async loss behavior,
  proof commands, and no-overclaim boundaries.
- Runtime proof command run after reviewer fixes:
  - `python3 tools/run_udp_video_receiver_proof.py --execute --allow-process-start --artifact-root reports/video --run-id 20260603T-pxe0040-generated-rtp-udp-proof-v2 --port 5636 --json`
- Runtime proof result:
  - status `passed`;
  - runtime artifacts at
    `reports/video/20260603T-pxe0040-generated-rtp-udp-proof-v2/`;
  - portable evidence copied to
    `docs/reporting/agent-ops/codex-modernization/evidence/2026-06-03-pxe0040-generated-rtp-udp-video-receiver-proof/`;
  - OpenCV `4.6.0` with GStreamer enabled;
  - GStreamer `1.24.2`;
  - 8/8 fresh frames captured at `[240, 320, 3]`;
  - first frame hash
    `7ea59dfc68dcea8b17deea9d980a1d7fa84ff0facae3a59862b888f52b899952`;
  - last frame hash
    `e258a7fcef6add72dd36d542a00db0ace9f87773d6d2c7888db09ac725c6d0e1`;
  - after sender stop, cached/stale statuses were
    `usable_for_following=false`, including strict accepted statuses with
    `reason=udp_async_frame_stale` and `frame_age_seconds >= 0.6`;
  - manifest records `sender_stop_expected=true` for the intentional sender
    termination.
- Independent review gate found blockers and they were fixed before closing the
  slice:
  - async UDP reconnect/release could strand a stopping reader and claim
    success without a fresh reader; fixed with per-reader stop events,
    generation ownership, and a reconnect regression test;
  - proof acceptance could pass on generic cached/no-new-frame statuses; fixed
    by requiring an actual `udp_async_frame_stale` status beyond the configured
    stale timeout;
  - the pipeline validator did not enforce caps before depay; fixed with
    ordering tests;
  - execute attempts without runtime prerequisites did not write a manifest;
    fixed with an incomplete-manifest contract test;
  - nearby video docs and helper receive scripts still showed weaker RTP/H.264
    examples; updated to the same caps/depay/parse guidance.
- Validation completed:
  - touched Python syntax compile: passed;
  - focused video proof/VideoHandler/frame-freshness suite after reviewer
    fixes: 112 passed;
  - `make video-udp-proof-dry-run`: passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed with only a CRLF normalization warning for the
    edited Windows GStreamer helper;
  - broader focused route/reload/docs/SITL/video suite after reviewer fixes:
    172 passed, 1 skipped.
  - weak H.264 RTP receive-pipeline scan across active config/docs/source/tools:
    no stale matches outside intentional proof tests and archived report text.
  - leftover process scan: no proof or GStreamer sender processes running.
- No Docker/PX4 runtime, PX4 SIH or Gazebo container execution, SITL scenario
  execution against a full stack, GitHub Actions hosted workflow, tracker,
  follower, HIL, field, deployment, service installation, or real-aircraft
  validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-03-phase-3-generated-rtp-udp-video-receiver-proof.md`.
- PXE-0040 remains open for the official Gazebo visual SITL profile. The
  generated RTP/UDP receiver prerequisite is now done and should remain a
  required gate before accepting Gazebo camera evidence.

## 2026-06-04 - Phase 3 Official Gazebo Visual Profile Contract

- Resumed after usage-limit interruption and reviewed the active PXE-0040
  state before making more edits.
- Continued the official PX4 Gazebo visual slice without starting Docker, PX4,
  Gazebo, PixEagle, MavlinkAnywhere, MAVLink2REST, HIL, field hardware,
  services, or deployment actions.
- Added `tools/sitl_plans/gazebo_visual_validation.json`, an L4 official Gazebo
  visual profile using `px4io/px4-sitl-gazebo:v1.17.0`, `HEADLESS=1`,
  `gz_x500_mono_cam`, host networking, MAVSDK/offboard UDP `14540`, GCS UDP
  `14550`, and Gazebo RTP/H.264 video UDP `5600`.
  - Later runtime probing found this plain `v1.17.0` tag is not published; the
    active profile now uses
    `px4io/px4-sitl-gazebo:v1.17.0-alpha1-1551-g381149fb01` plus exact repo
    digest.
- Added `scripts/sitl/run_px4_gazebo_visual_profile.sh` with side-effect-free
  dry-run default, explicit `probe-only` and `execute-gazebo` modes, and no
  host route/service mutation.
- Added `.github/workflows/px4-gazebo-visual-validation.yml` as an opt-in
  `workflow_dispatch` plus scheduled dry-run workflow with read-only
  permissions, optional image pull, artifact upload, and no `push` or
  `pull_request` trigger.
- Added Make targets:
  - `make sitl-gazebo-dry-run`;
  - `make sitl-gazebo-probe`;
  - `make sitl-gazebo-execute-px4`.
- Independent review gate found blockers:
  - visual artifacts were declared but not imported or content-validated;
  - L4 scenario objectives overclaimed what GET-only config probes prove;
  - PX4 image/container repo digest policy was not enforced;
  - SITL docs still framed the guide as Phase 2 only;
  - tests were too string-oriented for evidence quality.
- Fixed the blockers before checkpoint:
  - added visual artifact import options to the harness, wrapper, and workflow;
  - added fixed visual evidence paths and plan validation for Gazebo visual
    artifact requirements;
  - added artifact-content checks for generated receiver proof status,
    strict H.264 RTP pipeline ordering, decoded frame hashes, JSONL traces with
    timing evidence, PX4 container inspection, repo digests, and optional exact
    digest matching;
  - changed manifest result logic so weak content keeps runtime evidence
    incomplete even if files exist;
  - reworded Gazebo scenario objectives/acceptance so they match artifact
    checks rather than claiming GET-only actions prove video/tracker behavior;
  - updated SITL docs and plan-library docs with visual import flags, content
    checks, source references, and claim boundaries.
- Validation completed:
  - Gazebo/SIH wrapper shell syntax: passed;
  - touched Python syntax compile: passed;
  - Gazebo plan JSON formatting: passed;
  - Gazebo profile dry-run: passed with `would_start_processes=false`,
    3 scenarios, 3 GET actions, 0 control actions, 0 manual faults, and plan
    hash `51965e5de76e23cd8ff35d9d3c3f80803b6b310b4ac8416fd7e321e9338cb483`;
  - SITL contract suite: 45 passed;
  - docs/SITL/harness focused suite: 55 passed, 1 skipped;
  - `make sitl-gazebo-dry-run`: passed;
  - broader route/reload/docs/SITL/video suite: 181 passed, 1 skipped;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed with only the existing CRLF normalization warning
    for the edited Windows GStreamer helper, no whitespace errors.
- Docker runtime boundary rechecked on 2026-06-04:
  - `docker --version`: Docker `29.1.3`;
  - `docker ps --format '{{.ID}} {{.Image}} {{.Names}}'`: failed with
    `permission denied while trying to connect to the docker API at
    unix:///var/run/docker.sock`.
- No runtime official Gazebo/PX4 evidence is claimed. PXE-0040 remains open for
  the actual artifacted official Gazebo visual run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-04-phase-3-official-gazebo-visual-profile-contract.md`.

## 2026-06-04 - Phase 3 Tracker Trace Artifacts

- Continued to PXE-0038 because official Gazebo runtime proof is blocked in
  this session by Docker API permissions.
  - Later the same day, `sg docker` allowed Docker access; the blocker changed
    to official PX4/Gazebo world-readiness timeout on this VPS/headless path.
- Added `src/classes/tracker_trace.py`, a normalized JSONL trace helper layer
  for tracker/follower evidence:
  - `build_tracker_command_trace_record()`;
  - `build_offboard_publish_trace_record()`;
  - `write_trace_jsonl()`.
- Added trace fields for schema version, record type, timestamp, frame
  index/sequence, source, tracker ID, data type, bbox/geometry/angles/position,
  freshness fields, command intent reason/fields, frame status,
  OffboardCommander/publish status, and no-overclaim claim boundaries.
- Added guarded AppController runtime trace capture:
  - `configure_tracker_trace_artifacts(...)`;
  - `disable_tracker_trace_artifacts()`;
  - append-only recording at `_dispatch_tracker_output_to_follower()`;
  - inert unless explicitly configured and no PX4/start/routing side effects.
- Added deterministic smoke in
  `tests/unit/trackers/test_tracker_in_loop_validation.py` that runs generated
  green-target frames through `ColorBlobTrackerProbe`,
  `AppController._follow_tracker_output()`, `MCVelocityPositionFollower`,
  `CommandIntent`, and a capturing Offboard commander with the configured
  runtime trace hook, then parses both normalized trace artifacts.
- Strengthened `tools/run_sitl_validation_suite.py` so Gazebo visual trace
  artifacts must use normalized schemas; arbitrary JSONL with only timestamps
  is no longer accepted.
- Independent review found blockers and high-risk gaps:
  - loose Gazebo trace validation;
  - Python JSON NaN/Infinity output risk;
  - omitted future geometry/motion fields;
  - runtime trace generation still not wired into live AppController/Gazebo.
- Fixed the first three before checkpoint:
  - strict tracker/offboard trace schema validation;
  - `allow_nan=false` in JSONL writer;
  - geometry type, oriented bbox, polygon, normalized polygon, velocity,
    acceleration, and targets included in trace summaries.
- Recorded real Gazebo/visual runtime proof as remaining PXE-0040 work rather
  than overclaiming it.
- Validation completed:
  - tracker trace/test syntax compile: passed;
  - tracker-in-loop focused suite: 9 passed;
  - tracker/SITL/docs focused suite: 65 passed;
  - broader route/reload/docs/SITL/video/tracker/AppController suite after
    runtime trace hook: 250 passed, 1 skipped;
  - `git diff --check`: passed with only the existing CRLF normalization warning
    for the edited Windows GStreamer helper, no whitespace errors.
- No live SmartTracker/YOLO runtime, camera runtime, Docker/PX4/Gazebo, SITL
  runtime scenario execution, HIL, field, deployment, service installation, or
  real-aircraft validation was run.
- PXE-0038 is now done for normalized trace export and guarded runtime trace
  capture. Live proof in an official Gazebo visual run remains PXE-0040.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-04-phase-3-tracker-trace-artifacts.md`.

## 2026-06-04 - Phase 3 Official Gazebo Runtime Probe

- Resumed after usage-limit interruption and rechecked worktree, Docker, and
  runtime processes.
- The interrupted reviewer agent produced no usable review output because it
  hit usage limits.
- Verified the current shell still lacks the `docker` group in `id`, but
  `sg docker -c 'docker ps ...'` works because `alireza` is now listed in the
  `docker` group.
- Confirmed no PX4/Gazebo/GStreamer/PixEagle probe processes were left running;
  only an unrelated existing `freqtrade` container was active.
- Found a real PXE-0040 profile issue: Docker Hub does not provide
  `px4io/px4-sitl-gazebo:v1.17.0`.
- Updated the active Gazebo profile, wrapper, workflow, docs, and tests to the
  pullable v1.17-family tag
  `px4io/px4-sitl-gazebo:v1.17.0-alpha1-1551-g381149fb01` and pinned repo
  digest
  `px4io/px4-sitl-gazebo@sha256:fe3608d282e214db19763d63e857b603781c6471fe0bc3276373927bb01f51db`.
- Pulled and inspected the official image successfully under `sg docker`.
- Ran guarded `execute-gazebo` once with 45s startup wait:
  - artifact directory:
    `reports/sitl/20260604T-pxe0040-official-gazebo-container-probe-gazebo_visual_validation/`;
  - result: incomplete;
  - image/container metadata and exact repo digest passed;
  - missing PixEagle/MAVLink2REST/video/trace artifacts correctly kept the run
    incomplete.
- Ran guarded `execute-gazebo` again with 120s startup wait:
  - artifact directory:
    `reports/sitl/20260604T-pxe0040-official-gazebo-container-probe-120s-gazebo_visual_validation/`;
  - result: incomplete;
  - PX4/Gazebo container exited with return code `255`;
  - `logs/px4_sitl.log` shows `Timed out waiting for Gazebo world`.
- Ran direct diagnostics inside the same official image:
  - Gazebo Sim Server `8.11.0` can load `default.sdf`;
  - `GstCameraSystem` is present and configured for UDP host `127.0.0.1`, UDP
    port `5600`;
  - the scene service appears late/fragile relative to the official PX4 init
    script's fixed readiness loop on this VPS/headless path.
- Fixed a harness fail-closed issue: execute-mode PX4 container artifact
  discovery now requires a verified harness-owned container ID. If the
  container exits before label verification, the harness records preflight
  failure and does not `docker exec`/`docker cp` against a stale container name.
- Validation completed:
  - `python3 -m py_compile tools/run_sitl_validation_suite.py tests/test_sitl_validation_contract.py`: passed;
  - `pytest tests/test_sitl_validation_contract.py -q`: 47 passed;
  - Gazebo profile dry-run: passed with plan hash
    `d86fe1369e83ea0b8a38fc61bc0fe55cad890c3c87a3b3ec9610a56222dc3579`.
- No PixEagle runtime, MAVLink2REST runtime, route mutation, service
  installation, Gazebo frame ingest through PixEagle, tracker/follower runtime
  scenario, GitHub Actions hosted run, HIL, field, deployment, or real-aircraft
  validation was run.
- Decision: do not over-force full official Gazebo L4 on this VPS. Keep
  PXE-0040 open for native Ubuntu GUI/GPU, a stronger headless runner, or a
  separately proven official-image startup workaround. Continue local progress
  through SIH control-plane evidence plus deterministic synthetic
  video/tracker-in-loop traces and PXE-0042 typed action contracts.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-04-phase-3-official-gazebo-runtime-probe.md`.

## 2026-06-04 - Phase 3 Official SIH Runtime Probe

- Continued from the official Gazebo runtime probe into the local VPS-friendly
  SIH L2 evidence path.
- Found the same official-image tag issue in SIH:
  `px4io/px4-sitl:v1.17.0` is not a pullable Docker Hub tag.
- Pulled and inspected the official SIH image
  `px4io/px4-sitl:v1.17.0-alpha1-1551-g381149fb01` with repo digest
  `px4io/px4-sitl@sha256:fd6d93dc2705482aeb64ea26fdf16185d8a511010fdc53e26305f10d91855865`.
- Updated the active SIH plan, wrapper, workflow, helper script, tests, and
  active docs to the pullable tag and exact digest.
- Added plan-level `require_container_metadata`,
  `require_image_repo_digest`, and `expected_repo_digest` for SIH runtime
  evidence.
- Ran guarded `execute-px4` probes through `sg docker`.
  - The official SIH container started with `PX4_SIM_MODEL=sihsim_quadx`,
    stayed alive through the 20s startup window, and was stopped by verified
    container ID.
  - Image/container metadata matched the expected repo digest.
  - PX4 params and one ULog were collected from the container.
  - The run correctly remained incomplete because PixEagle, MAVLink2REST,
    complete MavlinkAnywhere route/profile evidence, scenario results,
    PixEagle log, and tlog evidence were absent.
- Fixed a harness evidence-quality issue exposed by SIH: PX4 shell prompt
  redraws produced a 159 MB stdout log in about 20 seconds. Managed PX4 stdout
  is now captured through a bounded scrubber that filters prompt redraws,
  caps output at 4 MiB by default, flushes during runtime, and refreshes
  `artifact_status` after close.
- Final reviewed SIH evidence directory:
  `reports/sitl/20260604T-pxe0039-official-sih-container-probe-reviewed-phase2_follower_validation/`.
- Final SIH manifest details:
  - result: incomplete;
  - plan hash:
    `12504909390f49ac8b4102e96a24ab4065881485ce094b11fc9b7f4213d77051`;
  - PX4 stdout: 162,054,071 raw bytes read, 16,205,148 prompt redraw lines
    filtered, 2,574 bytes written, not truncated, reader finished cleanly;
  - PX4 params were collected from `parameters.bson`; manifest metadata marks
    `parameter_format=bson` and `readable_text_export=false`;
  - ULog artifact: `px4/ulog/000-container-07_15_37.ulg`, 4,133,504 bytes.
- Sidecar repo recheck:
  - MavlinkAnywhere local main remains behind `origin/main`; remote latest tag
    remains `v3.0.14` at `7643d4d`;
  - Smart Wi-Fi Manager local main remains behind `origin/main`; remote latest
    tag remains `v2.1.14` at `a5414fc`;
  - `mavsdk_drone_show` local main remains behind `origin/main`; remote latest
    tag is now `v5.5.52-simurgh-evidence-foundation` at `6e6aec35`.
- The SIH probe saw a local service on `127.0.0.1:9070` answer status and
  diagnostics, while endpoints/config/profile-summary probes failed. This was
  recorded as a PXE-0022 follow-up because the routing stack was not prepared
  for this PX4-only run and local MavlinkAnywhere is behind origin.
- Validation completed:
  - `python3 -m py_compile tools/run_sitl_validation_suite.py tests/test_sitl_validation_contract.py`: passed;
  - `pytest tests/test_sitl_validation_contract.py tests/test_docs_infrastructure_consistency.py -q`: 61 passed;
  - `pytest tests/test_api_route_inventory.py tests/unit/core_app/test_parameters_reload.py -q`: 14 passed;
  - `PYTHON=/tmp/pixeagle-audit-venv/bin/python bash scripts/check_schema.sh`: schema up-to-date;
  - `bash -n scripts/sitl/run_px4_sih_profile.sh scripts/sitl/start_px4_sitl.sh`: passed;
  - `git diff --check`: passed;
  - SIH profile dry-run: passed with plan hash
    `12504909390f49ac8b4102e96a24ab4065881485ce094b11fc9b7f4213d77051`;
  - guarded SIH `execute-px4`: returned exit code 3 with manifest result
    `incomplete`, as expected for missing full-stack evidence.
- No PixEagle runtime, MAVLink2REST runtime, route mutation, service
  installation, SITL scenario action execution, tracker/video runtime,
  Gazebo visual run, HIL, field, deployment, or real-aircraft validation was
  run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-04-phase-3-official-sih-runtime-probe.md`.

## 2026-06-04 - Phase 4 SITL Typed Actions And PX4 Observation Gate

- Closed PXE-0042 implementation debt without claiming a runtime PX4/SITL pass.
- Added typed `/api/v1/actions/offboard-start`,
  `/api/v1/actions/operator-abort`, and `/api/v1/actions/{action_id}` action
  resources with request/response models, structured error envelopes,
  operation IDs, route inventory coverage, claim boundaries, dry-run support,
  required idempotency keys for confirmed mutations, per-key concurrency
  serialization, and bounded process-local action records.
- Legacy `/commands/start_offboard_mode` and `/commands/cancel_activities`
  remain compatibility aliases only; they now attach `action_audit` pointers
  and are marked deprecated in route metadata.
- Replaced remaining Phase 2 SITL start/abort scenario actions with typed
  `/api/v1/actions/*` calls and scenario-scoped idempotency keys.
- Added a scenario-local Offboard-start action to `offboard_heartbeat` so tlog
  cadence can be bound to a deliberate observation window.
- Added `px4/offboard_observation.json` generation and required evidence
  contract coverage for Phase 2 and Gazebo plans.
- Hardened PX4 observation acceptance:
  - heartbeat must identify PX4 autopilot component `1`;
  - `custom_mode` must equal PX4 Offboard `393216`;
  - `base_mode` must include the MAVLink custom-mode flag, including wrapped
    `base_mode.bits` payloads;
  - tlog setpoints must target the same accepted PX4 system ID and component
    `1`;
  - tlog cadence must occur inside a scenario-local Offboard-start window;
  - absent tlogs, absent `pymavlink`, parse errors, mixed system IDs, malformed
    heartbeat identity, or missing scenario timing keep evidence incomplete.
- Independent API/MCP review found and drove fixes for optional idempotency
  keys, missing `200` OpenAPI response metadata, missing legacy deprecation
  metadata, and missing audit records for denied attempts. Second pass had no
  blockers.
- Independent PX4/SITL review found and drove fixes for heartbeat-system/tlog
  mismatch, missing base-mode semantics, unbounded cadence windowing, and
  wrapped `base_mode.bits`. A second pass flagged the first window as too
  broad; the window now ends at the containing scenario finish, with action
  finish as fallback.
- Validation completed:
  - touched Python syntax compile: passed;
  - focused route/action/SITL/docs suite: 118 passed;
  - minimum route/reload gate: 16 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema up to date;
  - Phase 2 plan dry-run: passed, no processes started, plan hash
    `ab69b6939a5a009f8366c7f8f917505d835cea2d1b0f263ef45e40ebcf030835`;
  - Gazebo visual plan dry-run: passed, no processes started, plan hash
    `b80f52540b1fa92a4f6bb43eef3954b7d1b243cc9eac3d22cacbff82cd4da73d`;
  - `git diff --check`: passed.
- No live PixEagle backend, MAVLink2REST runtime, prepared MavlinkAnywhere
  route/profile runtime, PX4 scenario execution, accepted SIH/Gazebo runtime
  pass, tracker/video runtime, dashboard test/build, HIL, field, deployment,
  service installation, or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-04-phase-4-sitl-typed-actions-px4-observation.md`.

## 2026-06-04 - Phase 4 Typed MAVLink Telemetry Health

- Closed PXE-0036 backend/API implementation debt without claiming a runtime
  PX4/SITL pass.
- Added typed `GET /api/v1/telemetry/health` with route inventory coverage,
  operation ID, response models, telemetry tag, and structured `/api/v1` error
  envelope.
- Added `MavlinkDataManager.get_telemetry_health()` to separate latest
  MAVLink2REST request result, last-success freshness, cached payload
  availability, validation-timeout state, consumer guidance, and claim
  boundary.
- Preserved legacy `/status.mavlink_telemetry` compatibility while forcing
  disabled telemetry freshness false for fail-closed behavior.
- Covered healthy, stale, latest-request-failed-but-cache-fresh degraded,
  disabled cached payload, validation timeout with and without prior success,
  unavailable manager fallback, and structured telemetry-health error paths in
  tests.
- Refreshed companion refs before the API slice:
  - MavlinkAnywhere `origin/main` latest tag `v3.0.14` at `7643d4d`;
  - Smart Wi-Fi Manager `origin/main` latest tag `v2.1.14` at `a5414fc`;
  - `mavsdk_drone_show` `origin/main` latest tag
    `v5.5.55-simurgh-evidence-followups` at `56d5b24b`.
- Independent API/MCP first-pass review found structured-error and disabled
  cached-freshness issues. Independent telemetry/safety review independently
  found disabled cached-freshness, validation-timeout typed-test, and timestamp
  locking concerns. Fixes were made, and both second-pass reviews reported no
  blockers.
- Validation completed:
  - focused telemetry/API suite: 57 passed;
  - touched Python syntax compile: passed;
  - required route/reload/telemetry/docs suite: 77 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema up to date;
  - `git diff --check`: passed.
- No live PixEagle backend, MAVLink2REST runtime, prepared MavlinkAnywhere
  route/profile runtime, PX4 scenario execution, accepted SIH/Gazebo runtime
  pass, tracker/video runtime, dashboard test/build, HIL, field, deployment,
  service installation, or real-aircraft validation was run.
- Dashboard/client adoption of typed telemetry health is tracked as PXE-0043;
  broader `/api/v1`/MCP modernization remains PXE-0008 and companion-runtime
  reconciliation remains PXE-0022.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-04-phase-4-typed-telemetry-health.md`.

## 2026-06-04 - Phase 4 Dashboard Typed Telemetry Health

- Closed PXE-0043 dashboard/client adoption of typed MAVLink telemetry health
  without claiming a runtime PX4/SITL pass.
- Added `endpoints.telemetryHealth` for `GET /api/v1/telemetry/health` and
  named legacy tracker/follower telemetry endpoint constants.
- Added `normalizeTelemetryHealth()` and `useTelemetryHealth()` in
  `dashboard/src/hooks/useStatuses.js`.
- Dashboard normalization converts typed backend telemetry health into stable
  fields for `guidance`, `chipLabel`, `color`, `detail`,
  `usableForFollowing`, transport state, last-success freshness, and payload
  raw/display values for `flight_mode` and `arm_status`.
- `DashboardPage` now feeds typed telemetry status into `OperationalStatusBar`.
- `OperationalStatusBar` now shows a compact telemetry chip for
  `Telemetry: Usable`, `Telemetry: Degraded`, `Telemetry: Stale`,
  `Telemetry: Unavailable`, `Telemetry: Disabled`, and
  `Telemetry: Connecting`.
- Frontend tests cover:
  - degraded latest-request-failed while cached payload remains fresh;
  - disabled telemetry with cached payload;
  - all known consumer guidance chip labels;
  - typed endpoint polling with no-cache metadata;
  - connecting initial telemetry state;
  - stale out-of-order response rejection;
  - stale raw telemetry replacement on failed refresh;
  - reverse-proxy-safe smart-mode status polling via the endpoint registry;
  - visible `Telemetry: Degraded` and `Telemetry: Disabled` chip states.
- Refreshed companion refs before the dashboard/API slice:
  - MavlinkAnywhere `origin/main` latest tag `v3.0.14` at `7643d4d`;
  - Smart Wi-Fi Manager `origin/main` latest tag `v2.1.14` at `a5414fc`;
  - `mavsdk_drone_show` `origin/main` latest tag
    `v5.5.56-simurgh-source-followups` at `7e0eddd7`.
- Validation completed:
  - focused dashboard telemetry tests: 2 suites, 10 tests passed;
  - `npm run lint`: passed;
  - full dashboard tests: 3 suites, 11 tests passed;
  - `npm run build`: compiled successfully.
- Independent frontend/UI review found and drove fixes for disabled mixed-state
  frontend fail-closed normalization, stale/out-of-order response handling,
  reverse-proxy-safe smart-mode status polling, and the initial connecting
  state. Independent API/MCP and safety review found and drove fixes for stale
  raw telemetry on failed refresh and a stale phase-map sentence.
- No live PixEagle backend, MAVLink2REST runtime, prepared MavlinkAnywhere
  route/profile runtime, PX4 scenario execution, accepted SIH/Gazebo runtime
  pass, tracker/video runtime, HIL, field, deployment, service installation, or
  real-aircraft validation was run.
- Broader `/api/v1` migration remains PXE-0008, dashboard toolchain debt
  remains PXE-0021, tracker stale/unusable state UI remains PXE-0024, and
  companion-runtime reconciliation remains PXE-0022.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-04-phase-4-dashboard-telemetry-health.md`.

## 2026-06-04 - Phase 4 Dashboard Tracker-State Clarity

- Closed PXE-0024 dashboard tracker-state clarity without claiming runtime
  PixEagle, PX4/SITL, tracker/video, HIL, field, deployment, service, or
  real-aircraft validation.
- Added a shared dashboard tracker runtime normalizer for output-visible,
  active, stale, not-usable, no-output, checking, and unavailable states.
- Updated `TrackerStatusCard` and `TrackerDataDisplay` so inactive but visible
  external/gimbal output remains visible for diagnosis while clearly marked not
  follower-usable.
- Updated `useTrackerStatus()` to poll `/api/tracker/current-status` through
  the endpoint registry instead of collapsing legacy `/telemetry/tracker_data`
  into `tracker_started`.
- Updated the operational status bar, side navigation chip, and follow controls
  to consume normalized tracker runtime status. `Start Following` is disabled
  unless tracker output is fresh and `usable_for_following=true`; stop/cancel
  paths remain available.
- Added the same fail-closed tracker readiness guard to legacy
  `/commands/start_offboard_mode`; the typed `/api/v1/actions/offboard-start`
  path inherits it before `connect_px4()` can run.
- Fixed targets-only SmartTracker `MULTI_TARGET` detections so
  `/api/tracker/current-status` and legacy telemetry report visible output even
  when no selected target is follower-usable.
- Enriched legacy tracker telemetry with `has_output`, `usable_for_following`,
  and `data_is_stale` at the top level and inside `tracker_data`.
- Updated API/core docs to state that visible output is not the same thing as a
  follower-usable target.
- Independent first-pass review found active-only pockets in legacy tracker
  polling, follow controls, selector/current API, and target-loss/direct helper
  paths. Final review then found UI-only start-follow gating and targets-only
  `MULTI_TARGET` visibility gaps. This slice fixed the dashboard, legacy
  telemetry, direct Offboard-start guard, and multi-target visibility items,
  and opened PXE-0044 for remaining selector/runtime-contract and deeper
  direct-follower cleanup; PXE-0044 was closed on 2026-06-05.
- Validation completed:
  - focused tracker UI tests: 3 suites, 7 tests passed;
  - expanded dashboard tracker/status tests: 6 suites, 23 tests passed;
  - focused telemetry handler tests: 33 passed;
  - focused direct Offboard-start/current-status reviewer regressions: 4 passed;
  - full AppController Offboard safety suite: 47 passed;
  - touched Python syntax compile: passed;
  - `npm run lint`: passed;
  - full dashboard tests: 7 suites, 24 tests passed;
  - required route/reload/Offboard safety/telemetry handler suite: 97 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema up to date;
  - `npm run build`: compiled successfully.
- Broader `/api/v1` migration remains PXE-0008, dashboard toolchain debt
  remains PXE-0021, companion-runtime reconciliation remains PXE-0022, typed
  tracker runtime API/internal cleanup was later closed under PXE-0044 on
  2026-06-05, and official Gazebo
  visual runtime evidence remains PXE-0040.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-04-phase-4-dashboard-tracker-state-clarity.md`.

## 2026-06-05 - Phase 4 Typed Tracker Runtime Status

- Closed PXE-0044 typed tracker runtime API/internal cleanup without claiming a
  live PixEagle backend, PX4/SITL/HIL, tracker/video runtime, field,
  deployment, service, or real-aircraft validation.
- Added shared `src/classes/tracker_runtime_status.py` evaluator for
  `has_output`, `active_tracking`, fail-closed `usable_for_following`,
  `data_is_stale`, runtime state, consumer guidance, target summary,
  source/provider metadata, and claim boundary.
- Added typed `GET /api/v1/tracking/runtime-status` with route inventory,
  response model, typed error envelope, operation ID, and tracking tag.
- Replaced duplicated Offboard-start readiness logic with the shared runtime
  snapshot and enriched legacy `/api/tracker/current` plus
  `/api/tracker/current-status` with compatibility runtime fields.
- Updated `TargetLossHandler` so `tracking_active=True` with
  `usable_for_following=false` or `data_is_stale=true` enters target-loss
  behavior instead of being treated as a live target.
- Migrated dashboard `useTrackerStatus()` to the typed tracker runtime route.
- Updated tracker hooks to use the endpoint registry rather than direct
  `protocol://host:port` URL reconstruction, preserving reverse-proxy behavior.
- Updated `TrackerSelector` to display normalized runtime status and follower
  usability chips for visible, stale, not-usable, active, unavailable, and
  pending states.
- Fixed independent review findings before commit:
  - active output without explicit `usable_for_following=true` remains
    not usable;
  - provider string tokens such as `stale`, `not_usable`, and `unusable` are
    parsed field-specifically in the frontend runtime normalizer;
  - tracker runtime polling ignores stale out-of-order responses;
  - selector pending-selection state clears after backend convergence.
- Added backend/API/frontend tests for:
  - route metadata and GET inventory count;
  - inactive visible `MULTI_TARGET` output;
  - active stale output;
  - active not-usable output;
  - TargetLossHandler active/stale and active/not-usable behavior;
  - typed `active_tracking` and top-level stale frontend normalization;
  - selector visible/stale runtime chips;
  - typed endpoint polling.
- Refreshed companion refs before reporting:
  - MavlinkAnywhere `origin/main` `7643d4d` /
    `v3.0.14-2-g7643d4d`;
  - Smart Wi-Fi Manager `origin/main` `a5414fc` /
    `v2.1.14-2-ga5414fc`;
  - `mavsdk_drone_show` `origin/main` `9cfeb367` /
    `v5.5.61-simurgh-web-search-trace-truth`.
- Validation completed:
  - touched Python syntax compile: passed;
  - focused frontend runtime/selector suite after review fixes:
    3 suites, 23 tests passed;
  - required route/reload/Offboard/target-loss safe-publication suite after
    review fixes: 83 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema up to date;
  - `npm run lint`: passed;
  - full dashboard tests: 8 suites, 33 tests passed;
  - `npm run build`: compiled successfully;
  - `git diff --check`: passed.
- Remaining open modernization work:
  - broader `/api/v1`/MCP migration remains PXE-0008;
  - dashboard CRA/toolchain debt remains PXE-0021;
  - companion-runtime standards reconciliation remains PXE-0022;
  - official Gazebo visual runtime evidence remains PXE-0040;
  - final no-legacy cleanup gate remains PXE-0041.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-05-phase-4-typed-tracker-runtime-status.md`.

## 2026-06-05 - Phase 4 Typed Runtime Status

- Closed PXE-0045 typed PixEagle process-local runtime status without claiming a
  live PixEagle backend, PX4/SITL/HIL, tracker/video runtime, field,
  deployment, service, or real-aircraft validation.
- Added typed `GET /api/v1/runtime/status` with route inventory, response
  model, operation ID, runtime tag, structured error envelope, and explicit
  claim boundary.
- Added shared runtime snapshot assembly so legacy `/status` and the typed
  runtime endpoint use one source for:
  - `smart_mode_active`;
  - `tracking_started`;
  - `segmentation_active`;
  - `following_active`;
  - video status;
  - Offboard commander status/failure;
  - PX4 connection snapshot;
  - MAVLink telemetry snapshot;
  - Smart Tracker runtime snapshot.
- Added local runtime classification:
  - commander failure/degraded state becomes `degraded/operator_attention`;
  - local following with stopped/non-running Offboard commander publication,
    inactive commander task, stale command intent, active failsafe defaults, or
    missing commander status/unknown publication fields becomes
    `degraded/operator_attention`;
  - active following becomes `active/following_active`;
  - active smart/tracking/segmentation mode becomes `active/vision_active`;
  - otherwise the route reports `idle/idle`.
- Migrated dashboard `useSmartModeStatus()` from legacy `/status` to
  `endpoints.runtimeStatus` and `modes.smart_mode_active`, with legacy
  `/status` fallback only when the typed runtime route is missing during rolling
  updates and stale out-of-order response protection.
- Updated core API docs to document `/api/v1/runtime/status`, its response
  shape, its allowed local states, and the distinction from MDS
  `/api/v1/system/runtime-status`.
- Fast-forwarded companion reference repositories before reporting:
  - MavlinkAnywhere `origin/main` `7643d4d` /
    `v3.0.14-2-g7643d4d`;
  - Smart Wi-Fi Manager `origin/main` `a5414fc` /
    `v2.1.14-2-ga5414fc`;
  - `mavsdk_drone_show` `origin/main` `04e53b1f` /
    `v5.5.64-simurgh-mcp-smoke-heuristic`.
- Companion observations recorded under PXE-0022:
  - current MDS adds agent-context/MCP documentation, generated non-callable
    OpenAPI tool-candidate review, and explicit MCP auth defaults;
  - MavlinkAnywhere and Smart Wi-Fi Manager reinforce token/Basic Auth guarded
    remote mutations, dry-run/apply profile APIs, and local sidecar scope.
- Fixed independent review findings before final validation:
  - backend/API reviewer found following could remain `active` when local
    Offboard commander publication was stopped; fixed with fail-closed
    `degraded/operator_attention` classification and regression tests;
  - frontend/dashboard reviewer found the rolling-update fallback did not call
    legacy `/status` and smart-mode polling accepted stale out-of-order
    responses; fixed with route fallback, mounted/request-sequence guards, and
    tests.
  - docs/API/MCP reviewer found non-emitted `healthy` values in the runtime
    example and an incomplete smart-mode consumer note; fixed the example to use
    emitted `running`/`fresh` states and documented route fallback plus
    stale-response guards.
- Focused validation completed before final gates:
  - touched Python syntax compile: passed;
  - route inventory plus AppController Offboard safety suite after reviewer
    fixes and unknown-field hardening: 66 passed;
  - focused dashboard status-hook suite after reviewer fixes:
    1 suite, 14 tests passed.
- Final validation completed:
  - route inventory, parameters reload, and AppController Offboard safety:
    76 passed;
  - schema check: up to date;
  - dashboard lint: passed;
  - full dashboard tests: 8 suites, 35 tests passed;
  - dashboard build: compiled successfully;
  - `git diff --check`: passed;
  - post-doc-fix route inventory recheck: 9 passed;
  - docs infrastructure consistency: 10 passed.
- Broader `/api/v1` migration remains PXE-0008, dashboard toolchain debt
  remains PXE-0021, companion-runtime reconciliation remains PXE-0022,
  official Gazebo visual runtime evidence remains PXE-0040, and final
  no-legacy cleanup remains PXE-0041.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-05-phase-4-typed-runtime-status.md`.

## 2026-06-06 Phase 4 Typed Following Status

- Closed PXE-0046 typed process-local following status without claiming a live
  PixEagle backend, PX4/SITL/HIL, tracker/video runtime, field, deployment,
  service, or real-aircraft validation.
- Added typed `GET /api/v1/following/status` with route inventory, response
  model, operation ID, following tag, structured error envelope, and explicit
  claim boundary.
- Added following response models for:
  - follower profile identity and schema validity;
  - OffboardCommander command-publication status;
  - health issues and fail-closed reason;
  - process-local following active/inactive/degraded state.
- Reused a shared active-following OffboardCommander degradation classifier from
  both runtime and following status, while keeping the inactive-commander
  cleanup warning scoped to the following status route.
- Following status now reports `degraded/operator_attention` for:
  - active local following without a follower instance;
  - invalid configured follower profile;
  - active local following without an available OffboardCommander status;
  - commander failure/degraded/error state;
  - commander stopped/non-running/unknown running state;
  - commander task inactive/unknown;
  - stale or unknown latest command intent freshness;
  - active or unknown failsafe-default publication;
  - inactive local following while command publication appears to remain active.
- Migrated dashboard `useFollowerStatus()` from legacy
  `/telemetry/follower_data` to `endpoints.followingStatus`, with legacy
  telemetry fallback only when the typed route is missing during rolling
  updates and stale out-of-order response protection.
- Left detailed follower cards/pages on legacy `/telemetry/follower_data`
  intentionally; a richer typed follower telemetry contract remains a later
  PXE-0008 slice.
- Updated core API docs and follower integration docs; corrected stale
  follower integration diagram text from direct `SetpointHandler -> MAVSDK` to
  `CommandIntent -> OffboardCommander -> MAVSDK -> PX4`.
- Local independent review after the usage-limit pause found one API wording
  risk: the draft typed route exposed `commands_sent_to_px4`, but the signal
  was only a PixEagle-local successful MAVSDK publication counter. Renamed it
  to `local_successful_publish_observed` and documented that it is not
  PX4-observed Offboard or vehicle-response proof.
- Focused validation completed:
  - touched Python syntax compile: passed;
  - route inventory plus AppController Offboard safety: 72 passed;
  - focused dashboard status-hook suite: 1 suite, 17 tests passed.
- Final validation completed:
  - route inventory, parameters reload, and AppController Offboard safety:
    82 passed;
  - schema check: up to date;
  - docs infrastructure consistency: 10 passed;
  - `git diff --check`: passed;
  - dashboard lint: passed;
  - full dashboard tests: 8 suites, 38 tests passed;
  - dashboard build: compiled successfully.
- Broader `/api/v1` migration remains PXE-0008, companion-runtime
  reconciliation remains PXE-0022, dashboard toolchain debt remains PXE-0021,
  official Gazebo visual runtime evidence remains PXE-0040, richer typed
  follower telemetry remains open under PXE-0008, and final no-legacy cleanup
  remains PXE-0041.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-06-phase-4-typed-following-status.md`.

## 2026-06-06 Phase 4 Typed Following Telemetry

- Closed PXE-0047 typed process-local following telemetry without claiming a
  live PixEagle backend, PX4/SITL/HIL, tracker/video runtime, field,
  deployment, service, or real-aircraft validation.
- Added typed `GET /api/v1/following/telemetry` with route inventory, response
  model, operation ID, following tag, structured error envelope, and explicit
  claim boundary.
- The typed telemetry response includes:
  - typed following status classification;
  - follower profile identity;
  - current `fields` with `field_source`;
  - optional command intent, target-loss, safety, performance, circuit-breaker,
    and flight-mode diagnostics;
  - OffboardCommander command-publication summary;
  - legacy payload key inventory for compatibility visibility.
- The route prefers live active setpoint-handler fields, falls back to legacy
  follower telemetry fields only when needed, and uses
  `local_successful_publish_observed` so local MAVSDK publication counters are
  not confused with PX4-observed Offboard proof.
- Added dashboard `useFollowingTelemetry()` and `normalizeFollowingTelemetry()`.
  The hook polls `/api/v1/following/telemetry`, falls back to legacy
  `/telemetry/follower_data` only when the typed route is missing during rolling
  updates, and ignores stale out-of-order responses.
- Migrated `DashboardPage` detailed follower status card data from direct
  legacy follower telemetry polling to `useFollowingTelemetry()`.
- At this checkpoint, the Follower visualization page follower-history work was
  intentionally left as a separate follow-up; that follower/setpoint history
  follow-up was later closed under PXE-0048. Tracker center/bounding-box
  history remains separate.
- Updated core API docs and follower integration docs for
  `/api/v1/following/telemetry` and the local-only claim boundary.
- Focused validation completed:
  - touched Python syntax compile: passed;
  - route inventory plus AppController Offboard safety: 76 passed;
  - focused dashboard status-hook suite: 1 suite, 21 tests passed;
  - dashboard lint: passed.
- Final validation completed:
  - route inventory, parameters reload, and AppController Offboard safety:
    86 passed;
  - schema check: up to date;
  - docs infrastructure consistency: 10 passed;
  - `git diff --check`: passed;
  - dashboard lint: passed;
  - full dashboard tests: 8 suites, 42 tests passed;
  - dashboard build: compiled successfully.
- Broader `/api/v1` migration remains PXE-0008, companion-runtime
  reconciliation remains PXE-0022, dashboard toolchain debt remains PXE-0021,
  official Gazebo visual runtime evidence remains PXE-0040, and final no-legacy
  cleanup remains PXE-0041. At that checkpoint, typed tracker history for the
  Follower visualization tracker plots remained open; it was later closed under
  PXE-0049.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-06-phase-4-typed-following-telemetry.md`.

## 2026-06-06 Phase 4 Follower Visualization Typed Telemetry History

- Closed PXE-0048 for the Follower visualization page's follower/setpoint
  history migration without claiming a live PixEagle backend, PX4/SITL/HIL,
  tracker/video runtime, field, deployment, service, or real-aircraft
  validation.
- Migrated `dashboard/src/pages/FollowerPage.js` away from direct
  `API_URL + /telemetry/follower_data` construction:
  - at that checkpoint, tracker center/bounding-box history still used
    endpoint-registry legacy `/telemetry/tracker_data`; the companion tracker
    plot migration was later closed under PXE-0049;
  - follower/setpoint history now polls typed
    `/api/v1/following/telemetry`;
  - legacy `/telemetry/follower_data` is used only when the typed route is
    missing during rolling updates;
  - polling now performs an initial refresh, bounds history/raw-log growth, and
    ignores stale out-of-order responses.
- Extended `normalizeFollowingTelemetry()` so typed `fields` and legacy
  `setpoints` both preserve nested `fields` and expose top-level aliases needed
  by existing chart components, including `vel_body_fwd`, `vel_x`, `vel_y`,
  `vel_z`, and `yaw_rate`.
- Added focused frontend coverage:
  - normalizer aliases for typed and legacy follower telemetry;
  - Follower page typed telemetry polling;
  - legacy fallback only for missing typed route;
  - stale out-of-order history response suppression.
- Updated core API docs, FastAPIHandler docs, follower integration docs, issue
  register, and phase map to remove the stale statement that the Follower
  visualization page still uses legacy follower telemetry arrays.
- Focused validation completed:
  - `CI=true npm test -- --watchAll=false --runTestsByPath src/hooks/useStatuses.test.js src/pages/FollowerPage.test.js`:
    2 suites, 25 tests passed;
  - `npm run lint -- --format unix`: passed.
- Independent review found two frontend/reporting blockers before closure:
  typed telemetry timestamps were epoch seconds while `StaticPlot` expects
  JavaScript-date-compatible timestamps, and the stale-response test assumed a
  hardcoded 1000 ms polling interval. Both were fixed by normalizing numeric
  telemetry timestamps to ISO strings and advancing one scheduled timer in the
  test. Resume-map review also found stale validation-pending and next-slice
  wording; those reporting issues were fixed before commit.
- Final validation completed:
  - route inventory, parameters reload, and AppController Offboard safety:
    86 passed;
  - schema check: up to date;
  - docs infrastructure consistency: 10 passed;
  - `git diff --check`: passed;
  - dashboard lint: passed;
  - full dashboard tests: 9 suites, 46 tests passed;
  - dashboard build: compiled successfully.

## 2026-06-06 Phase 4 Typed Tracker Telemetry History

- Closed PXE-0049 for typed tracker telemetry and the Follower visualization
  page's tracker center/bounding-box history migration without claiming a live
  PixEagle backend, PX4/SITL/HIL, tracker/video runtime, field, deployment,
  service, follower-response, vehicle-response, or real-aircraft validation.
- Added typed `GET /api/v1/tracking/telemetry` with route inventory, response
  model, operation ID, tracking tag, structured error envelope, and explicit
  claim boundary.
- The typed tracker telemetry response includes:
  - current tracker runtime/readiness classification;
  - chart-compatible `center` and `bounding_box`;
  - `fields`, `tracker_data`, and `field_source`;
  - embedded typed `runtime_status`;
  - `tracking_active` and `tracker_started` compatibility aliases;
  - legacy payload key inventory.
- The route prefers live `TrackerOutput` geometry/fields and falls back to
  legacy tracker telemetry only as a compatibility source. Top-level
  `bounding_box` is normalized-only; pixel boxes remain in explicit fields, and
  malformed/non-finite top-level geometry is rejected.
- Added dashboard `endpoints.trackingTelemetry`,
  `normalizeTrackingTelemetry()`, and shared numeric timestamp normalization.
  The Follower visualization page now polls `/api/v1/tracking/telemetry` for
  tracker history samples and falls back to `/telemetry/tracker_data` only when
  the typed route is missing during rolling updates.
- Updated core API docs, FastAPIHandler docs, follower integration docs,
  developer API docs, issue register, and phase map so active docs no longer
  say the Follower visualization page still depends on legacy tracker telemetry
  for tracker plots.
- Focused validation completed:
  - touched Python syntax compile: passed;
  - route inventory plus AppController Offboard safety: 83 passed;
  - focused dashboard status/page suites: 2 suites, 31 tests passed.
- Independent review findings fixed before final gates:
  - live `TrackerOutput` geometry now wins over legacy cached geometry;
  - top-level tracker `bounding_box` no longer falls back to pixel boxes;
  - malformed/non-finite top-level geometry is rejected by strict helpers;
  - no-output tracker telemetry normalizes to `null` geometry instead of
    `undefined`;
  - raw logs now keep both wire payload and normalized chart payload;
  - live `TrackerOutput` telemetry no longer depends on legacy telemetry
    retrieval succeeding;
  - frontend fallback no longer promotes legacy pixel `bbox`/`bbox_pixel` into
    normalized `bounding_box`.
- Final validation completed:
  - route inventory, parameters reload, and AppController Offboard safety:
    93 passed;
  - schema check: up to date;
  - docs infrastructure consistency: 10 passed;
  - `git diff --check`: passed;
  - dashboard lint: passed;
  - full dashboard tests: 9 suites, 52 tests passed;
  - dashboard build: compiled successfully.
- Broader `/api/v1` migration remains PXE-0008, companion-runtime
  reconciliation remains PXE-0022, dashboard toolchain debt remains PXE-0021,
  official Gazebo visual runtime evidence remains PXE-0040, and final no-legacy
  cleanup remains PXE-0041.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-06-phase-4-typed-tracker-telemetry-history.md`.

## 2026-06-06 Phase 4 API/MCP Candidate Inventory

- Closed PXE-0050 for generated API/MCP candidate inventory without exposing
  any MCP endpoint, registry, `tools/list`, `tools/call`, callable automation
  surface, live PixEagle backend, PX4/SITL/HIL, field, deployment, service, or
  real-aircraft validation.
- Refreshed companion standards before implementation:
  - `mavsdk_drone_show` origin/main at `623bb3fa` /
    `v5.5.71-simurgh-readonly-closure`;
  - MavlinkAnywhere origin/main at `7643d4d` /
    `v3.0.14-2-g7643d4d`;
  - Smart Wi-Fi Manager origin/main at `a5414fc` /
    `v2.1.14-2-ga5414fc`.
  The imported standard is Simurgh-style generated candidate inventory as
  review coverage only, not execution.
- Added `tools/generate_api_tool_candidates.py`, a static AST generator that
  parses `src/classes/fastapi_handler.py` without instantiating FastAPI,
  Uvicorn, video, MAVLink, PX4, or runtime subsystems.
- Added
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml` with
  source file hash, route metadata, request/response models, candidate IDs,
  typed-contract state, read-only status, risk/sensitivity/side-effect
  classification, required review items, explicit claim boundaries,
  `callable: false`, `mcp_exposure: none`,
  `default_registry_exposure: exclude`, and registry coverage status showing no
  curated registry yet.
- The generated inventory represents all 14 current `/api/v1` HTTP routes
  exactly once. Only these six typed process-local GETs are unpromoted
  read-only candidates:
  - `/api/v1/runtime/status`;
  - `/api/v1/following/status`;
  - `/api/v1/following/telemetry`;
  - `/api/v1/telemetry/health`;
  - `/api/v1/tracking/runtime-status`;
  - `/api/v1/tracking/telemetry`.
- Action POST routes, `GET /api/v1/actions/{action_id}`, and all SITL
  injection POST routes are blocked from read-only promotion. They require
  curated policy, docs, tests, confirmation/idempotency/dry-run review, and
  future action/SITL-specific guard design before any callable consideration.
- Added `tests/test_api_tool_candidates.py` and wired the generator check into
  `.github/workflows/tests.yml` and `make phase0-check`.
- Added `docs/agent-context/README.md`, updated the API modernization
  blueprint, clarified the REST API docs, and added explicit notes in
  FastAPI/MAVLink/follower docs that "API/MCP" means schema-stable typed route
  readiness, while the generated inventory is candidate inventory only, not
  MCP execution.
- Independent reviewers found no blockers after implementation. A non-blocking
  note about existing "API/MCP consumers" wording was fixed in active docs
  before closure.
- Validation completed:
  - `python3 -m py_compile tools/generate_api_tool_candidates.py`: passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - focused candidate suite: 6 passed;
  - candidate plus docs consistency suite: 16 passed;
  - `PYTHON=.venv/bin/python make phase0-check`: 43 passed, schema up to
    date, candidate inventory current;
  - independent reviewer read-only validation: generator check, diff check,
    and route/candidate tests passed;
  - `git diff --check`: passed.
- Broader `/api/v1` migration and router extraction remain PXE-0008,
  companion-runtime reconciliation remains PXE-0022, dashboard toolchain debt
  remains PXE-0021, official Gazebo visual runtime evidence remains PXE-0040,
  and final no-legacy cleanup remains PXE-0041.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-06-phase-4-api-tool-candidate-inventory.md`.

## 2026-06-06 Phase 4 Docs-Stage Agent Registry And Policy

- Closed PXE-0051 for docs-stage agent registry and policy governance without
  exposing any MCP endpoint, runtime tool executor, `tools/list`, `tools/call`,
  callable automation surface, live PixEagle backend proof, PX4/SITL/HIL,
  field, deployment, service, or real-aircraft validation.
- Added `docs/agent-context/agent_tools.yaml` as a review-only registry for
  exactly six typed process-local status/telemetry GET candidates:
  - `/api/v1/runtime/status`;
  - `/api/v1/following/status`;
  - `/api/v1/following/telemetry`;
  - `/api/v1/telemetry/health`;
  - `/api/v1/tracking/runtime-status`;
  - `/api/v1/tracking/telemetry`.
- Added `docs/agent-context/agent_policy.yaml` with default-deny posture:
  `agent_enabled=false`, `mcp_enabled=false`,
  `registry_runtime_loaded=false`, `unknown_tool_policy=deny`,
  `allow_openapi_autopromotion=false`, `allow_action_tools=false`,
  `allow_sitl_injection_tools=false`, and
  `auto_promote_generated_candidates=false`.
- Kept every registry tool `callable: false`, `mcp_exposure: none`,
  `default_registry_exposure: exclude`, and `promotion_status: unpromoted`.
- Extended `tools/generate_api_tool_candidates.py` so registry coverage now
  distinguishes docs registration from runtime promotion and reports unsafe
  registry metadata, unsafe registry tool fields, extra registry routes,
  callable/MCP-exposed matches, missing policy deny defaults, and unregistered
  eligible candidates.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml`:
  all 14 current `/api/v1` routes remain represented once, the six reviewed
  candidates are `registry_reviewed_unexposed`, and action POSTs,
  `GET /api/v1/actions/{action_id}`, and SITL injection POSTs remain blocked
  and unregistered.
- Expanded `tests/test_api_tool_candidates.py` so Phase 0 proves registry tools
  map only to eligible generated candidates and policy denies execution,
  actions, SITL injection, direct drone/PX4 exposure, and auto-promotion.
- Updated `docs/agent-context/README.md` and
  `docs/apis/api-modernization-blueprint.md` to state that the registry and
  policy files are docs-stage governance artifacts only.
- Validation completed:
  - `python3 -m py_compile tools/generate_api_tool_candidates.py`: passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - route inventory plus candidate tests: 20 passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date, candidate
    inventory current, 45 passed;
  - `git diff --check`: passed.
- Independent review requirements were satisfied after the generator was
  tightened to fail the coverage status on unsafe registry/policy drift.
- Broader `/api/v1` migration and router extraction remain PXE-0008,
  companion-runtime reconciliation remains PXE-0022, dashboard toolchain debt
  remains PXE-0021, official Gazebo visual runtime evidence remains PXE-0040,
  and final no-legacy cleanup remains PXE-0041.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-06-phase-4-docs-stage-agent-registry-policy.md`.

## 2026-06-06 Phase 4 API v1 Route Registry Extraction

- Closed PXE-0052 as the first PXE-0008 route extraction slice without
  changing public API behavior, response models, handler methods, action
  semantics, SITL injection gating, or agent/MCP runtime exposure.
- Added `src/classes/fastapi_api_v1_routes.py` with static `ApiV1RouteSpec`
  entries for all 14 current `/api/v1` HTTP routes:
  process-local status/telemetry GETs, guarded action resources, and
  validation-only SITL injection routes.
- Updated `FastAPIHandler.define_routes()` to delegate typed `/api/v1`
  registration through the registry helper while the existing handler methods
  and Pydantic models remain in `fastapi_handler.py`.
- Updated `tests/test_api_route_inventory.py` so the static inventory parses
  both legacy inline handler declarations and the typed `/api/v1` route
  registry without instantiating FastAPIHandler or runtime subsystems.
- Updated `tools/generate_api_tool_candidates.py` so the non-callable
  candidate inventory reads both route sources, keeps request-model inference
  from handler signatures, records both source hashes, and preserves the same
  129 declared HTTP routes, 14 `/api/v1` candidates, six reviewed read-only
  candidates, and eight blocked/guarded action/SITL candidates.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml` with
  multi-source provenance.
- Updated `docs/core-app/02-components/fastapi-handler.md`,
  `docs/apis/api-modernization-blueprint.md`, and
  `docs/agent-context/README.md` to document the new route-source boundary.
- Validation completed:
  - `python3 -m py_compile src/classes/fastapi_api_v1_routes.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py -q`: 14 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_tool_candidates.py -q`: 8 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 22 passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date,
    candidate inventory current, 47 passed;
  - `git diff --check`: passed.
- Independent API/MCP, safety/runtime-boundary, and docs/maintainability
  reviewers found no blockers. Two residual maintainability concerns were
  fixed before closure: a static guard now proves `FastAPIHandler.define_routes()`
  delegates to the route registry exactly once, and a fake-app test proves
  registry helper order, handler binding, metadata resolution, and status-code
  resolution without constructing the runtime app.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-06-phase-4-api-v1-route-registry-extraction.md`.

## 2026-06-06 Phase 4 API v1 Contract Extraction

- Closed PXE-0053 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler methods, response model names,
  request model names, action semantics, SITL injection gating, agent/MCP
  runtime exposure, or callable automation.
- Added `src/classes/api_v1_contracts.py` for typed `/api/v1` request/response
  models, error-response metadata, and process-local claim-boundary strings.
- Updated `fastapi_handler.py` to import and re-export the API/SITL contract
  names during migration, preserving existing imports such as
  `from classes.fastapi_handler import APIActionRequest`.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml` so the
  candidate inventory now records source hashes for:
  - `src/classes/fastapi_handler.py`;
  - `src/classes/fastapi_api_v1_routes.py`;
  - `src/classes/api_v1_contracts.py`.
- Extended route/candidate tests so:
  - `API*` and `SITL*` contracts are not redefined in `fastapi_handler.py`;
  - the expected contract classes exist in `api_v1_contracts.py`;
  - generated candidate provenance includes the contracts source file.
- Updated active FastAPI/API/agent-context docs to show that route metadata,
  API contracts, and handler methods now have separate source files.
- Validation completed:
  - `.venv/bin/python -m py_compile src/classes/api_v1_contracts.py src/classes/fastapi_handler.py src/classes/fastapi_api_v1_routes.py tools/generate_api_tool_candidates.py tests/test_api_tool_candidates.py tests/test_api_route_inventory.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -c "from classes.fastapi_handler import APIActionRequest, APIRuntimeStatusResponse, SITLTrackerOutputInjection, FastAPIHandler; from classes.api_v1_contracts import APIActionRequest as DirectAPIActionRequest; assert APIActionRequest is DirectAPIActionRequest"`: passed with expected optional dependency warnings only;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 23 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_sitl_injection_api.py tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or sitl" -q`: 49 passed, 50 deselected;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date,
    candidate inventory current, 48 passed.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-06-phase-4-api-v1-contract-extraction.md`.

## 2026-06-07 Phase 4 API v1 Path/Error Boundary

- Closed PXE-0054 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler methods, response model names,
  request model names, action semantics, SITL injection gating, agent/MCP
  runtime exposure, or callable automation.
- Added `src/classes/api_v1_paths.py` for canonical typed `/api/v1` path
  constants, SITL validation route-family membership, process-local read-only
  candidate membership, typed error-envelope predicates, and request-id prefix
  selection.
- Aligned typed validation-error predicates so all six reviewed process-local
  read-only `/api/v1` routes, including `/api/v1/tracking/telemetry`, share the
  structured envelope if FastAPI request validation is ever triggered. The
  current tracking telemetry GET route has no request body, path parameter, or
  query parameter, so this closes predicate drift without changing action,
  SITL-gate, PX4/SITL/HIL, deployment, or field behavior.
- Added `src/classes/api_v1_errors.py` for typed `APIErrorResponse` envelope
  construction and SITL validation error response construction.
- Updated `src/classes/fastapi_api_v1_routes.py` so static
  `ApiV1RouteSpec.path` entries consume the shared path constants instead of
  duplicating typed route strings.
- Updated `fastapi_handler.py` to keep compatibility wrappers while delegating
  typed error-envelope construction and path-family selection to the new
  modules.
- Updated `tools/generate_api_tool_candidates.py` and
  `tests/test_api_route_inventory.py` so static parsers resolve path constants
  from `api_v1_paths.py` without instantiating FastAPIHandler or runtime
  subsystems.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml` so the
  candidate inventory now records source hashes for:
  - `src/classes/fastapi_handler.py`;
  - `src/classes/fastapi_api_v1_routes.py`;
  - `src/classes/api_v1_contracts.py`;
  - `src/classes/api_v1_paths.py`.
- Extended route/candidate tests so:
  - typed `/api/v1` route specs use canonical path constants;
  - path constants and direct `APIErrorResponse` construction are not redefined
    in `fastapi_handler.py`;
  - typed validation-envelope predicates match the current reviewed `/api/v1`
    route families;
  - generated candidate provenance includes the path source file.
- Validation completed:
  - `.venv/bin/python -m py_compile src/classes/api_v1_paths.py src/classes/api_v1_errors.py src/classes/api_v1_contracts.py src/classes/fastapi_api_v1_routes.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 26 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_sitl_injection_api.py tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or sitl" -q`: 49 passed, 50 deselected.
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date,
    candidate inventory current, 51 passed.
- Independent API/MCP, safety/runtime-boundary, and docs/maintainability
  reviewers found no blockers. Residual follow-up risks are limited to
  compatibility wrappers still imported by `fastapi_handler.py`,
  `api_v1_errors.py` being guarded by behavior tests rather than candidate
  provenance, and the static path parser intentionally supporting only
  top-level literal path constants.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-07-phase-4-api-v1-path-error-boundary.md`.

## 2026-06-07 Phase 4 API v1 Action Boundary

- Closed PXE-0055 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler methods, response model names,
  request model names, action confirmation/idempotency/dry-run behavior, SITL
  injection gating, agent/MCP runtime exposure, or callable automation.
- Added `src/classes/api_v1_actions.py` for:
  - `ApiActionStore` process-local action records;
  - per-idempotency-key async locks;
  - idempotent replay lookup;
  - bounded in-memory action history;
  - typed action record construction and action claim boundary;
  - legacy command action-audit attachment;
  - confirmation/idempotency precondition failure responses.
- Updated `fastapi_handler.py` so it owns a single `ApiActionStore` instance
  and keeps migration wrappers while delegating action storage, replay lookup,
  record creation, legacy action audit attachment, and precondition failure
  response construction to `api_v1_actions.py`.
- Updated `get_action_resource()` to read through the store instead of direct
  handler dictionaries.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml` so the
  candidate inventory now records source hashes for:
  - `src/classes/fastapi_handler.py`;
  - `src/classes/fastapi_api_v1_routes.py`;
  - `src/classes/api_v1_contracts.py`;
  - `src/classes/api_v1_paths.py`;
  - `src/classes/api_v1_actions.py`.
- Extended route/candidate tests so:
  - direct action-store dictionaries, idempotency indexes, history queues, and
    action locks are not redefined in `fastapi_handler.py`;
  - direct UUID-based action record construction is not reintroduced in
    `fastapi_handler.py`;
  - generated candidate provenance includes the action-helper source file.
- Validation completed:
  - `.venv/bin/python -m py_compile src/classes/api_v1_actions.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or action or legacy_cancel" -q`: 35 passed, 63 deselected.
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 27 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_sitl_injection_api.py tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or sitl" -q`: 49 passed, 50 deselected;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date,
    candidate inventory current, 52 passed.
- Independent API/MCP and safety/runtime reviewers found no blockers. The
  accepted residuals are process-local non-durable action storage, unchanged
  conservative idempotency-key consumption for executed failed mutations,
  temporary handler migration wrappers, and candidate provenance that hashes
  `api_v1_actions.py` while behavior semantics remain covered by tests. A local
  docs/maintainability pass found no blocking stale guidance after the dedicated
  docs reviewer was unavailable.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-07-phase-4-api-v1-action-boundary.md`.

## 2026-06-07 Phase 4 API v1 Snapshot Boundary

- Closed PXE-0056 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler method names, response model
  names, request model names, action confirmation/idempotency/dry-run behavior,
  SITL injection gating, agent/MCP runtime exposure, or callable automation.
- Added `src/classes/api_v1_snapshots.py` for:
  - legacy `/status` snapshot construction;
  - process-local runtime status classification;
  - following commander degradation classification;
  - following profile and command-publication snapshots;
  - following telemetry/setpoint snapshot fallback behavior;
  - circuit-breaker snapshot fallback for typed following telemetry;
  - tracker runtime status snapshot lookup;
  - tracker geometry/field normalization for typed telemetry;
  - tracker-following readiness snapshot assembly.
- Updated `fastapi_handler.py` so existing private snapshot helper names remain
  as migration wrappers while delegating the implementation to
  `api_v1_snapshots.py`.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml` so the
  candidate inventory now records source hashes for:
  - `src/classes/fastapi_handler.py`;
  - `src/classes/fastapi_api_v1_routes.py`;
  - `src/classes/api_v1_contracts.py`;
  - `src/classes/api_v1_paths.py`;
  - `src/classes/api_v1_actions.py`;
  - `src/classes/api_v1_snapshots.py`.
- Extended route/candidate tests so:
  - process-local read-state snapshot semantics are not reimplemented inside
    `fastapi_handler.py`;
  - snapshot claim-boundary constants are not re-imported into the handler;
  - generated candidate provenance includes the snapshot source file.
- Validation completed:
  - `.venv/bin/python -m py_compile src/classes/api_v1_snapshots.py src/classes/fastapi_handler.py`: passed;
  - `.venv/bin/python -m py_compile src/classes/api_v1_snapshots.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 28 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or legacy_cancel or tracking_telemetry or runtime_status or following" -q`: 40 passed, 31 deselected.
  - `git diff --check`: passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_sitl_injection_api.py tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or sitl" -q`: 49 passed, 50 deselected;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date,
    candidate inventory current, 53 passed.
- Independent API/MCP and safety/runtime subagent reviews were attempted, but
  both reviewers returned usage-limit errors before producing findings. No
  independent approval is claimed for PXE-0056. Local recovery review found no
  blockers: handler snapshot wrappers delegate to `api_v1_snapshots.py`,
  generated candidates remain non-callable/unexposed, tracker readiness still
  comes from `evaluate_tracker_runtime_status()`, `usable_for_following` remains
  the strict fail-closed signal, following command-publication status remains
  local-only, and no PX4/SITL/HIL/field success is claimed.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-07-phase-4-api-v1-snapshot-boundary.md`.

## 2026-06-07 Phase 4 API v1 Telemetry Health Boundary

- Closed PXE-0057 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler method names, response model
  names, request model names, action confirmation/idempotency/dry-run behavior,
  SITL injection gating, agent/MCP runtime exposure, or callable automation.
- Added `src/classes/api_v1_telemetry.py` for:
  - `MavlinkDataManager.get_telemetry_health()` delegation;
  - fail-closed unavailable telemetry-health fallback payload construction;
  - MAVLink telemetry-health claim-boundary usage.
- Updated `fastapi_handler.py` so `get_telemetry_health()` remains the route
  error-boundary wrapper while delegating helper behavior to
  `api_v1_telemetry.py`.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml` so the
  candidate inventory now records source hashes for:
  - `src/classes/fastapi_handler.py`;
  - `src/classes/fastapi_api_v1_routes.py`;
  - `src/classes/api_v1_contracts.py`;
  - `src/classes/api_v1_paths.py`;
  - `src/classes/api_v1_actions.py`;
  - `src/classes/api_v1_snapshots.py`;
  - `src/classes/api_v1_telemetry.py`.
- Extended route/candidate tests so:
  - telemetry-health fallback strings and claim-boundary imports are not
    reintroduced in `fastapi_handler.py`;
  - generated candidate provenance includes the telemetry-helper source file.
- Validation completed so far:
  - `.venv/bin/python -m py_compile src/classes/api_v1_telemetry.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 29 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "telemetry_health or api_v1" -q`: 20 passed, 51 deselected.
  - `git diff --check`: passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py tests/unit/core_app/test_sitl_injection_api.py -k "telemetry_health or api_v1 or sitl" -q`: 52 passed, 47 deselected;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date,
    candidate inventory current, 54 passed.
- Local review found no blockers: generated candidate provenance includes
  `src/classes/api_v1_telemetry.py`; generated candidates remain
  non-callable/unexposed; the telemetry claim boundary and fail-closed fallback
  string live only in `api_v1_telemetry.py`; `FastAPIHandler.get_telemetry_health()`
  delegates once to `get_telemetry_health_snapshot()`; and manager exceptions
  still flow through the route method's typed `/api/v1` error envelope. Rerun
  independent reviewer checks when subagent quota is available again.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-07-phase-4-api-v1-telemetry-health-boundary.md`.

## 2026-06-07 Phase 4 API v1 SITL Injection Boundary

- Closed PXE-0058 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler method names, response model
  names, request model names, action confirmation/idempotency/dry-run behavior,
  SITL enablement default, agent/MCP runtime exposure, or callable automation.
- Added `src/classes/api_v1_sitl.py` for:
  - validation-only SITL injection gate evaluation;
  - typed `/api/v1` SITL error envelope delegation;
  - `TrackerOutput` construction from `SITLTrackerOutputInjection`;
  - frame-status construction from `SITLVideoStallInjection`;
  - dry-run summaries for all five SITL injection routes;
  - AppController validation-hook dispatch for tracker output, video stall,
    commander publish failure, local MAVSDK disconnect, and local
    MAVLink2REST timeout injections.
- Updated `fastapi_handler.py` so `_sitl_*` helpers and `inject_sitl_*` route
  methods remain compatibility wrappers while delegating behavior to
  `api_v1_sitl.py`.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml` so the
  candidate inventory now records source hashes for:
  - `src/classes/fastapi_handler.py`;
  - `src/classes/fastapi_api_v1_routes.py`;
  - `src/classes/api_v1_contracts.py`;
  - `src/classes/api_v1_paths.py`;
  - `src/classes/api_v1_actions.py`;
  - `src/classes/api_v1_snapshots.py`;
  - `src/classes/api_v1_telemetry.py`;
  - `src/classes/api_v1_sitl.py`.
- Extended route/candidate tests so:
  - SITL gate strings, response codes, AppController validation-hook names,
    local transport-scope metadata, and `TrackerOutput` construction are not
    reintroduced in `fastapi_handler.py`;
  - handler SITL wrappers stay one-call delegation points;
  - generated candidate provenance includes the SITL-helper source file.
- Validation completed so far:
  - `.venv/bin/python -m py_compile src/classes/api_v1_sitl.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_sitl_injection_api.py -q`: 28 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 30 passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_sitl_injection_api.py tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or sitl" -q`: 49 passed, 50 deselected;
  - `git diff --check`: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date,
    candidate inventory current, 55 passed.
- Local review found no blockers: the SITL injection routes remain disabled
  unless `PIXEAGLE_ENABLE_SITL_INJECTIONS=1`; generated candidates remain
  non-callable/unexposed; SITL POST routes remain blocked validation-stimulus
  candidates; route inventory remains unchanged at 129 declared HTTP routes and
  14 `/api/v1` candidates; and no PX4/SITL/HIL/field success is claimed.
- Independent API/MCP and safety/SITL reviewers also found no blockers. The
  API/MCP reviewer reran candidate inventory and route/candidate tests: 30
  passed. The safety/SITL reviewer reran diff hygiene, syntax checks, focused
  safety/SITL tests: 49 passed, and route/candidate tests: 30 passed.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-07-phase-4-api-v1-sitl-injection-boundary.md`.

## 2026-06-07 Phase 4 API v1 Action Route Boundary

- Closed PXE-0059 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler method names, response model
  names, request model names, action confirmation/idempotency/dry-run policy,
  legacy action-audit behavior, SITL enablement default, agent/MCP runtime
  exposure, or callable automation.
- Moved guarded typed action route execution into
  `src/classes/api_v1_actions.py`:
  - `start_offboard_action()`;
  - `start_offboard_action_unlocked()`;
  - `operator_abort_action()`;
  - `operator_abort_action_unlocked()`;
  - `get_action_resource()`.
- Updated `fastapi_handler.py` so typed action route methods are one-call
  migration wrappers.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml`; route
  inventory remains 129 declared HTTP routes, 14 `/api/v1` candidates, six
  eligible read-only candidates, eight blocked/guarded candidates, zero
  callable tools, and zero MCP-exposed tools.
- Extended static tests so action route helpers stay in `api_v1_actions.py`,
  handler action route methods stay delegated wrappers, and action-store
  internals/direct UUID record construction remain out of `fastapi_handler.py`.
- Added a focused regression test for concurrent idempotent operator-abort
  requests so duplicate abort calls serialize through the same per-key lock and
  only execute the legacy cancel path once.
- Validation completed:
  - `.venv/bin/python -m py_compile src/classes/api_v1_actions.py src/classes/fastapi_handler.py tests/test_api_route_inventory.py tools/generate_api_tool_candidates.py tests/test_api_tool_candidates.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1_offboard_action or api_v1_operator_abort_action or legacy_cancel_activities_route_records_action_audit" -q`: 11 passed, 60 deselected;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 30 passed;
  - `.venv/bin/python -m py_compile src/classes/api_v1_actions.py src/classes/fastapi_handler.py tests/test_api_route_inventory.py tests/unit/core_app/test_app_controller_offboard_safety.py tools/generate_api_tool_candidates.py tests/test_api_tool_candidates.py`: passed after the extra replay test;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or action or legacy_cancel" -q`: 19 passed, 53 deselected;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `git diff --check`: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date, candidate inventory current, 55 passed.
- Independent review:
  - API/MCP reviewer found no blockers, confirmed generated candidates remain
    non-callable/unexposed with 129 HTTP routes, 14 `/api/v1` candidates, six
    eligible read-only candidates, eight blocked/guarded candidates, zero
    callable tools, and zero MCP-exposed tools, and flagged low pending-report
    wording that was fixed before closure.
  - Safety/control-action reviewer found no blockers, confirmed confirmation,
    idempotency, per-key lock, replay, dry-run, typed lookup error, and legacy
    action-audit behavior remained intact, and flagged the missing
    operator-abort duplicate/concurrent replay test that was added before
    closure.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-07-phase-4-api-v1-action-route-boundary.md`.

## 2026-06-09 Phase 4 API v1 Read Route Boundary

- Closed PXE-0060 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler method names, response model
  names, request model names, read-only candidate classification, action
  confirmation/idempotency/dry-run policy, SITL enablement default,
  agent/MCP runtime exposure, or callable automation.
- Added `src/classes/api_v1_read_routes.py` for the six typed read-route
  error boundaries:
  - `get_runtime_status()`;
  - `get_following_status()`;
  - `get_following_telemetry()`;
  - `get_telemetry_health()`;
  - `get_tracking_runtime_status()`;
  - `get_tracking_telemetry()`.
- Updated `fastapi_handler.py` so those six route methods are one-call
  migration wrappers.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml`; route
  inventory remains 129 declared HTTP routes, 14 `/api/v1` candidates, six
  eligible read-only candidates, eight blocked/guarded candidates, zero
  callable tools, and zero MCP-exposed tools.
- Extended static tests so read-route helpers stay in
  `api_v1_read_routes.py`, handler read-route methods stay delegated wrappers,
  typed read-route error strings stay out of `fastapi_handler.py`, and
  generated candidate provenance includes the read-route-helper source file.
- Validation completed:
  - `.venv/bin/python -m py_compile src/classes/api_v1_read_routes.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 31 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "runtime_status or following_status or following_telemetry or telemetry_health or tracking_runtime_status or tracking_telemetry" -q`: 27 passed, 45 deselected;
  - `.venv/bin/python -m py_compile src/classes/api_v1_read_routes.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py tests/unit/core_app/test_app_controller_offboard_safety.py`: passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `git diff --check`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "api_v1 or runtime_status or following_status or following_telemetry or telemetry_health or tracking_runtime_status or tracking_telemetry" -q`: 38 passed, 34 deselected;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date, candidate inventory current, 56 passed.
- Independent reviewer checks were attempted, but both reviewer agents returned
  account usage-limit errors before producing findings; no independent approval
  is claimed for PXE-0060. Local recovery review found no blockers: handler
  read-route methods are one-call wrappers, typed read-route error strings and
  path constants live in `api_v1_read_routes.py`, generated candidates remain
  non-callable/unexposed, action/SITL routes remain blocked or guarded, and no
  PX4/SITL/HIL/field success is claimed.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-09-phase-4-api-v1-read-route-boundary.md`.

## 2026-06-10 Phase 4 Legacy Control Route Boundary

- Closed PXE-0061 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler method names, response model
  names, request model names, typed action confirmation/idempotency/dry-run
  policy, legacy action-audit behavior, read-only candidate classification,
  SITL enablement default, agent/MCP runtime exposure, or callable automation.
- Added `src/classes/api_legacy_control_routes.py` for the remaining dangerous
  legacy compatibility control bodies:
  - `/commands/start_offboard_mode`;
  - `/commands/cancel_activities`.
- Updated `FastAPIHandler.start_offboard_mode()` and
  `FastAPIHandler.cancel_activities()` so those route methods are one-call
  migration wrappers.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml`; route
  inventory remains 129 declared HTTP routes, 14 `/api/v1` candidates, six
  eligible read-only candidates, eight blocked/guarded candidates, zero
  callable tools, and zero MCP-exposed tools.
- Candidate provenance now records
  `src/classes/api_legacy_control_routes.py` because guarded typed action
  candidates still delegate through legacy compatibility execution until those
  aliases are retired.
- Extended static tests so legacy control helpers stay in
  `api_legacy_control_routes.py`, handler legacy control methods stay delegated
  wrappers, dangerous compatibility-route validation/error strings stay out of
  `fastapi_handler.py`, and generated candidate provenance includes the helper
  source file.
- Validation completed:
  - `.venv/bin/python -m py_compile src/classes/api_legacy_control_routes.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py`: regenerated the
    candidate inventory;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 32 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "start_offboard_mode or legacy_cancel_activities_route_records_action_audit or api_v1_offboard_action or api_v1_operator_abort_action" -q`: 14 passed, 58 deselected;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `git diff --check`: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date, candidate
    inventory current, 57 passed.
- Independent reviewer checks found no blockers:
  - API/MCP reviewer confirmed route inventory stability, source-hash
    provenance for `api_legacy_control_routes.py`, unchanged blocked/guarded
    action candidate classification, zero callable tools, zero MCP-exposed
    tools, and no runtime MCP/tool exposure.
  - Safety/control-action reviewer confirmed typed action delegation still
    reaches the legacy wrappers, route registration still binds deprecated
    legacy metadata, cancel preserves action audit and HTTP 500 compatibility
    behavior, Offboard start still fails closed before `connect_px4()` on
    missing PX4/tracker/video/tracker-readiness, and typed action idempotency
    remains in `api_v1_actions.py`.
  - A minor extraction parity note about `except Exception` versus the previous
    catch-all final-state fallback was fixed with `except BaseException` before
    final validation, preserving legacy catch-all semantics without adding a
    bare `except` to the new helper.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-10-phase-4-legacy-control-route-boundary.md`.

## 2026-06-10 Phase 4 Legacy Offboard Stop Boundary

- Closed PXE-0062 as the next PXE-0008 monolith-reduction slice without
  changing public routes, route order, handler method names, response model
  names, request model names, legacy stop payload shape, typed action
  confirmation/idempotency/dry-run policy, legacy action-audit behavior,
  read-only candidate classification, SITL enablement default, agent/MCP
  runtime exposure, or callable automation.
- Moved legacy `/commands/stop_offboard_mode` execution into
  `src/classes/api_legacy_control_routes.py`, beside legacy Offboard start and
  operator cancel.
- Updated `FastAPIHandler.stop_offboard_mode()` so the route method is a
  one-call migration wrapper.
- Preserved the existing stop semantics:
  - inactive stop is idempotent and does not call `disconnect_px4()`;
  - active stop delegates to `AppController.disconnect_px4()`;
  - disconnect exceptions trigger emergency cleanup for
    `offboard_commander`, `setpoint_sender`, and `follower`, then force local
    `following_active=False`;
  - final-state fallback keeps the old catch-all behavior with
    `except BaseException` rather than a bare `except`.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml`; route
  inventory remains 129 declared HTTP routes, 14 `/api/v1` candidates, six
  eligible read-only candidates, eight blocked/guarded candidates, zero
  callable tools, and zero MCP-exposed tools.
- Extended static tests so legacy Offboard stop stays in
  `api_legacy_control_routes.py`, handler stop remains a delegated wrapper,
  legacy control wrappers pass `self`, and Offboard-stop emergency-cleanup/
  idempotency strings stay out of `fastapi_handler.py`.
- Added direct route tests for inactive idempotency, active disconnect
  delegation, emergency cleanup after disconnect failure, cleanup-failure
  reporting, and unreadable final-state fallback.
- Validation completed:
  - `.venv/bin/python tools/generate_api_tool_candidates.py`: regenerated the
    candidate inventory;
  - `.venv/bin/python -m py_compile src/classes/api_legacy_control_routes.py src/classes/fastapi_handler.py tests/test_api_route_inventory.py tests/unit/core_app/test_app_controller_offboard_safety.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 32 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "stop_offboard_mode_api or start_offboard_mode or legacy_cancel_activities_route_records_action_audit or api_v1_offboard_action or api_v1_operator_abort_action" -q`: 19 passed, 58 deselected;
  - `.venv/bin/python -m py_compile src/classes/api_legacy_control_routes.py src/classes/fastapi_handler.py tools/generate_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py tests/unit/core_app/test_app_controller_offboard_safety.py`: passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `git diff --check`: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date, candidate
    inventory current, 57 passed.
- Independent reviewer checks found no blockers:
  - API/MCP reviewer confirmed route inventory stability, source-hash
    provenance for `api_legacy_control_routes.py`, unchanged candidate
    non-callable/unexposed status, zero callable tools, zero MCP-exposed tools,
    and no runtime MCP/tool exposure or action-tool promotion.
  - Safety/control-action reviewer confirmed inactive idempotency, active
    `disconnect_px4()` delegation, emergency cleanup of commander/sender/
    follower, forced local inactive state on cleanup success, catch-all-
    equivalent final-state fallback, unchanged route registration, and
    preserved surrounding start/cancel behavior.
  - Reviewer-noted hardening gaps were fixed before final validation:
    wrapper guardrails now verify `self` delegation, and direct tests now cover
    cleanup-failure reporting plus unreadable final-state fallback.
  - PXE-0063 was opened to track the remaining typed Offboard-stop
    action/deprecation decision because legacy stop still has no
    `/api/v1/actions/offboard-stop`, no action audit, and no explicit
    deprecation metadata.
- No dashboard code, schema, PX4/SITL runtime, HIL, field, deployment, service,
  or real-aircraft validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-10-phase-4-legacy-offboard-stop-boundary.md`.

## 2026-06-11 Phase 4 Typed Offboard Stop Action

- Recovered from the usage-limit interruption by rechecking worktree state,
  closing/replacing interrupted reviewer attempts, and rerunning the affected
  validation gates with the repo venv.
- Closed PXE-0063 as the typed Offboard-stop action/deprecation slice.
- Added typed `POST /api/v1/actions/offboard-stop` with:
  - `APIActionRequest` / `APIActionResponse`;
  - operation ID `stop_offboard_action`;
  - structured action-route responses;
  - dry-run validation;
  - confirmation required before mutation;
  - idempotency key required for confirmed mutations;
  - per-key concurrency serialization and replay;
  - process-local action records;
  - local fail-closed success semantics for cleanup warnings/errors and
    still-active following.
- Marked legacy `/commands/stop_offboard_mode` as deprecated with operation ID
  `legacy_stop_offboard_mode` and `action_audit` attachment.
- Tightened legacy stop:
  - delegated cleanup warnings/errors now return failure;
  - local following still active after stop now returns failure;
  - emergency fallback cleanup failures are returned in `details.errors`,
    `details.cleanup_errors`, top-level `error`, and action audit error.
- Migrated the dashboard operator control panel to typed action routes:
  - Start Following uses `/api/v1/actions/offboard-start`;
  - Stop Following uses `/api/v1/actions/offboard-stop`;
  - Cancel Tracker uses `/api/v1/actions/operator-abort`;
  - all three send confirmed idempotent action requests with dashboard source
    metadata.
- Regenerated
  `docs/agent-context/generated/pixeagle-openapi-tool-candidates.yaml`; route
  inventory now reports 130 declared HTTP routes, 15 `/api/v1` candidates, six
  eligible read-only candidates, nine blocked/guarded candidates, zero callable
  tools, and zero MCP-exposed tools.
- Updated active API, SITL, dashboard/control, agent-context, issue-register,
  and phase-map docs.
- Validation completed:
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema up to date;
  - `.venv/bin/python tools/generate_api_tool_candidates.py`: regenerated the
    candidate inventory;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`: passed;
  - `.venv/bin/python -m py_compile src/classes/api_v1_actions.py src/classes/api_legacy_control_routes.py src/classes/api_v1_contracts.py src/classes/api_v1_paths.py src/classes/fastapi_api_v1_routes.py src/classes/fastapi_handler.py tests/unit/core_app/test_app_controller_offboard_safety.py tests/test_api_route_inventory.py tests/test_api_tool_candidates.py`: passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/test_api_tool_candidates.py -q`: 32 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_app_controller_offboard_safety.py -k "offboard_stop_action or stop_offboard_mode_api or start_offboard_mode or legacy_cancel_activities_route_records_action_audit or api_v1_offboard_action or api_v1_operator_abort_action" -q`: 29 passed, 58 deselected;
  - `CI=true npm test -- --runTestsByPath src/components/ActionButtons.test.js --watchAll=false`: 4 passed;
  - `CI=true npm test -- --watchAll=false`: 9 suites passed, 54 tests passed;
  - `npm run build`: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema up to date, candidate
    inventory current, 57 passed;
  - `git diff --check`: passed.
- Replacement reviewers found no blockers:
  - API/MCP reviewer confirmed typed route metadata/contracts, guarded
    non-callable candidate classification, zero callable/MCP exposure, and
    legacy stop deprecation/audit metadata.
  - Safety/control reviewer confirmed no safety blockers and noted emergency
    cleanup diagnostics plus dashboard typed-action consistency; both were
    hardened before final validation.
- Residual transition boundaries remain explicit:
  - action records are process-local and not durable flight logs;
  - typed stop delegates through the legacy compatibility body until final
    alias removal, so a typed action record can contain nested legacy audit
    metadata;
  - different idempotency keys can create separate action records, while actual
    follower state transitions remain protected by controller locking.
- No PX4/SITL/HIL/field, deployment, service installation, real-aircraft
  control, runtime MCP endpoint, `tools/list`, `tools/call`, or callable action
  tool was added or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-11-phase-4-typed-offboard-stop-action.md`.

## 2026-06-11 Phase 4 Companion Runtime Reconciliation

- Recovered from the pause with a clean/synchronized PixEagle worktree at
  `eafbc63c`.
- Fetched and reviewed current clean companion refs:
  - MDS `623bb3fa`, `v5.5.71-simurgh-readonly-closure`;
  - MavlinkAnywhere `7643d4d`, `v3.0.14-2-g7643d4d`;
  - Smart Wi-Fi Manager `a5414fc`, `v2.1.14-2-ga5414fc`.
- Added canonical `docs/architecture/companion-runtime-contract.md` for:
  ownership, local-first exposure, auth/token/CSRF boundaries, process-local
  dry-run/confirmed apply, policy/runtime modes, secrets, exact evidence pins,
  sidecar preparation evidence, and API/agent/MCP separation.
- Reconciled active routing, SITL, API, architecture, installation,
  troubleshooting, port, and agent-context docs.
- Replaced active MavlinkAnywhere pull-latest update guidance with explicit
  validated tag/commit selection.
- Removed active unqualified PixEagle LAN/firewall exposure guidance and added
  a docs regression guard because backend `5077` currently has no production
  authentication boundary.
- Replaced the stale active Windows/X-Plane procedure with an explicit
  unmaintained-path disposition; PXE-0020 remains open for a future maintained
  evidence workflow decision.
- Corrected remote configuration/troubleshooting guidance and required
  validated revision checkout before initial MavlinkAnywhere root install.
- Read-only local probe confirmed the installed MavlinkAnywhere dashboard is
  old/unprepared `v3.0.8`: status/health alive, missing router config,
  endpoints/config HTTP 500, profile summary HTTP 404. Smart Wi-Fi Manager was
  not running. No sidecar mutation was performed.
- Closed PXE-0022 as the contract/reconciliation decision and opened:
  - PXE-0064 for PixEagle API authentication/exposure;
  - PXE-0065 for SITL sidecar version/compatibility/preparation classification
    and evidence secret scanning.
  - PXE-0066 for approved/blocked/deferred candidate-disposition governance.
- Validation:
  - docs consistency: 13 passed;
  - `PYTHON=.venv/bin/python make phase0-check`: 60 passed;
  - required route/reload batch: 34 passed;
  - venv schema check: passed;
  - candidate inventory check: passed;
  - active stale/unsafe guidance scan: clean;
  - `git diff --check`: passed.
- Bare system-Python schema check failed only because system Python lacks
  `ruamel`; the repository venv schema gate passed.
- Final finished-diff reviews:
  - security/devops review found remaining troubleshooting/configuration,
    stale Windows/X-Plane, unpinned initial sidecar install, and guard-coverage
    gaps; all were corrected and the exposure acceptance gate was strengthened;
  - API/MCP review found a direct-HTTP wording contradiction, prose-only
    candidate dispositions, and overbroad MDS MCP-default wording; wording was
    corrected and PXE-0066 was opened for the implementation debt;
  - no final untracked closure blocker remained.
- No sidecar mutation/update/restart, PX4/SITL/HIL/field operation, deployment,
  service installation, runtime MCP, callable tool, or real-aircraft control
  was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-11-phase-4-companion-runtime-reconciliation.md`.

## 2026-06-12 Phase 4 API Exposure Containment Foundation

- Recovered from the usage-limit interruption by rechecking branch/worktree
  state, rereading the root operating docs and modernization/API blueprints,
  and resuming PXE-0064 from the last known reviewer findings.
- Completed the first PXE-0064 containment foundation:
  - added `src/classes/api_exposure_policy.py` with `local_only` and
    `trusted_lan_legacy` modes;
  - changed checked-in backend defaults to `127.0.0.1:5077` plus explicit
    loopback CORS origins;
  - rejected wildcard CORS, remote local-only origins, non-loopback local-only
    binds, unsupported modes, and empty bind hosts;
  - coerced old missing-mode remote binds to loopback instead of preserving
    accidental broad exposure;
  - added HTTP Host/DNS-rebinding, browser Origin, and `Sec-Fetch-Site`
    checks before route execution;
  - added same-site resource and anti-framing response headers;
  - added video and WebRTC-signaling WebSocket Host/Origin checks before
    `accept()`;
  - changed dashboard and MAVLink2REST launchers to loopback defaults with
    explicit `trusted_lan_legacy` gates for non-loopback binds;
  - removed local launcher/service URL output that advertised default LAN
    access;
  - corrected active README/config/install/troubleshooting/Windows/API/video
    and port docs around the local-first boundary;
  - added `docs/apis/api-exposure-boundary.md`;
  - regenerated config schema and API candidate inventory.
- Reviewer-found blockers from the interrupted run were fixed before final
  validation:
  - HTTP Host checks close the DNS-rebinding gap;
  - service helpers, run output, port docs, and MAVLink2REST docs no longer
    teach stale default LAN exposure;
  - CORS preflight headers include dashboard cache headers;
  - Host/authority enforcement now wraps CORS preflight;
  - WebSocket paths now validate Host as well as Origin before accept;
  - `trusted_lan_legacy` rejects empty bind hosts;
  - legacy MAVLink2REST helper defaults/examples are loopback-first and
    reject non-loopback HTTP binds without explicit `trusted_lan_legacy`;
  - dashboard env/config docs and schema descriptions no longer mislabel
    backend `5077` as the dashboard port.
- New guardrails:
  - `tests/unit/core_app/test_api_exposure_policy.py` covers policy, middleware,
    WebSocket, legacy migration, DNS-rebinding, CORS headers, and fail-closed
    cases;
  - `tests/test_network_exposure_defaults.py` statically checks backend,
    dashboard, service, MAVLink2REST, helper, and docs defaults;
  - `tests/test_docs_infrastructure_consistency.py` includes the exposure
    boundary doc and rejects unqualified backend firewall guidance.
- Validation completed:
  - focused PXE-0064 tests/docs: 79 passed, with one Starlette/httpx
    deprecation warning from `TestClient`;
  - shell syntax for touched shell launchers/helpers: passed;
  - Python compile for touched runtime modules: passed;
  - stale exposure search: clean except intentional negative guardrail asserts;
  - API candidate inventory regenerated;
  - venv schema check: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: 60 passed;
  - dashboard `CI=true npm test -- --watchAll=false`: 9 suites, 54 tests passed;
  - dashboard `npm run build`: passed;
  - `git diff --check`: passed with only expected Windows batch line-ending
    warnings.
- The generated API candidate inventory remains non-callable: 130 declared HTTP
  routes, 15 `/api/v1` candidates, 6 reviewed read-only candidates, 9 guarded
  or blocked candidates, 0 callable tools, and 0 MCP-exposed tools.
- PXE-0064 remains `in_progress`, not done. Remaining work is authenticated
  browser/operator sessions, machine tokens, CSRF, role/scope authorization,
  authenticated media/WebSockets, security audit events, typed-action-only
  enforcement, and legacy mutation retirement.
- No PX4/SITL/HIL/field run, service install, sidecar mutation/update,
  deployment, runtime MCP endpoint, callable tool, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-12-phase-4-api-exposure-containment.md`.

## 2026-06-13 Phase 4 API Security Policy Foundation

- Recovered from the usage-limit pause and resumed PXE-0064 with a clean
  branch and two untracked policy modules already present.
- Added `src/classes/api_security_types.py` and
  `src/classes/api_security_policy.py` as a declarative default-deny security
  classification layer for all 132 declared custom routes plus FastAPI's
  implicit docs/OpenAPI routes.
- Split the policy surface so it matches the actual route surface exactly:
  no accidental cross-product overclassification across method/path families.
- Added `tests/test_api_security_policy.py` to prove exact route coverage,
  default-deny behavior, least-privilege viewer/operator/admin scopes, exact
  bearer scopes, session CSRF semantics, local-only route handling, and legacy
  and SITL boundaries.
- Narrowed the scope model so config reads start at operator, model selection is
  separate from model installation/deletion, and machine tokens do not collapse
  into broader roles.
- Updated API/MCP candidate provenance, route-inventory docs, exposure docs,
  core API docs, agent-context docs, and the modernization blueprint to reflect
  the policy foundation without implying runtime authentication is active.
- Validation completed:
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_security_policy.py -q`:
    17 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_security_policy.py tests/test_api_tool_candidates.py tests/test_api_route_inventory.py tests/test_docs_infrastructure_consistency.py -q`:
    62 passed;
  - `PYTHON=.venv/bin/python make phase0-check`:
    schema current, candidate inventory current, 77 tests passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py`:
    regenerated the inventory after policy-hash changes.
- Independent subagent review could not be completed because the subagent
  service refresh token was revoked. Local finished-diff review was used
  instead, and the issue is recorded for later reattempt when the service is
  available.
- At this policy-foundation checkpoint, runtime auth was not yet active; that
  status is superseded by the runtime-auth foundation entry below. No sessions,
  bearer issuance UI, dashboard migration, PX4/SITL/HIL run, deployment, or
  real-aircraft control was added or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-13-phase-4-api-security-policy-foundation.md`.

## 2026-06-13 Phase 4 API Auth Runtime Foundation

- Recovered from the usage-limit interruption by rechecking the branch and
  worktree, confirming the pending runtime-auth diff, rerunning focused gates,
  and waiting for the independent reviewer agents that were already running.
- Completed the next PXE-0064 foundation:
  - added `src/classes/api_auth_runtime.py` for runtime auth configuration,
    hashed bearer token loading, exact-scope principal construction, query-token
    rejection, and transport-level route authorization;
  - wired HTTP/MJPEG route authorization into `FastAPIHandler` before route
    execution/stream creation, after Host/Origin/fetch-site exposure checks;
  - wired video and WebRTC-signaling WebSocket authorization before `accept()`;
  - added `Streaming.API_AUTH_MODE` and `Streaming.API_BEARER_TOKEN_FILE` with
    generated schema metadata and docs;
  - kept checked-in `local_compat` ergonomics for same-host loopback socket
    clients while requiring scoped bearer tokens for non-loopback API clients;
  - added `machine_bearer` mode for machine/API clients and documented that the
    current browser dashboard/native media transports cannot use it until
    browser sessions land;
  - rejected bearer tokens in query strings for HTTP, MJPEG, video WebSocket,
    and WebRTC-signaling routes.
- Reviewer-found blockers were fixed before final validation:
  - `local_compat` no longer trusts HTTP `Host` as transport proof;
  - proxy-forwarded client identity headers disable local compatibility and
    local-only route elevation, even when the immediate socket peer is loopback;
  - docs now warn not to place `local_compat` behind an externally reachable
    reverse proxy;
  - generated schema wording no longer calls backend `trusted_lan_legacy`
    unauthenticated;
  - WebRTC and WebSocket docs now use `/ws/webrtc_signaling` and show
    pre-accept exposure/auth checks;
  - API/MCP candidate provenance now includes `api_exposure_policy.py` in
    addition to auth and route-policy sources.
- Independent reviewer results:
  - API/security reviewer flagged Host fallback and stale schema wording; both
    were corrected and regression tests were added.
  - Browser/media reviewer flagged reverse-proxy local-compat risk,
    `machine_bearer` dashboard/media limitations, and stale streaming docs;
    all were documented or corrected in this slice.
  - API/MCP reviewer flagged stale reporting, missing exposure-policy
    provenance, and stale schema wording; reporting/provenance/schema were
    updated.
- New guardrails:
  - `tests/unit/core_app/test_api_auth_runtime.py` covers token files, unknown
    scopes, machine-bearer startup safety, same-host local compatibility,
    non-loopback denial, Host non-trust, proxy-forwarded denial, exact bearer
    scopes, local-only rejection for remote/proxy clients, invalid bearer
    non-fallback, query-token rejection, and unknown-route default deny;
  - `tests/unit/core_app/test_api_exposure_policy.py` covers FastAPI
    middleware authorization and pre-accept video/WebRTC WebSocket denial;
  - Phase 0 now includes both runtime-auth and exposure-policy tests.
- Validation completed:
  - focused auth/policy/candidate/docs batch: 115 passed, with one unrelated
    Starlette/httpx `TestClient` deprecation warning;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`:
    inventory current;
  - Python compile for touched runtime/test/generator modules: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 154 tests passed, with the same `TestClient` warning;
  - `git diff --check`: passed;
  - shell syntax for touched launchers/helpers: passed;
  - active stale-route/auth wording scan is clean except intentional
    dashboard/MAVLink2REST sidecar unauthenticated warnings and the historical
    2026-06-12 checkpoint.
- PXE-0064 remains `in_progress`, not done. Remaining work is
  browser/operator users and sessions, real session CSRF, dashboard
  credential-aware API/media migration, durable security audit events,
  typed-action-only enforcement, and final legacy mutation retirement.
- No PX4/SITL/HIL/field run, service install, sidecar mutation/update,
  deployment, runtime MCP endpoint, callable tool, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-13-phase-4-api-auth-runtime-foundation.md`.

## 2026-06-14 Phase 4 Browser-Session Auth Foundation

- Recovered from the usage-limit interruption by rechecking the in-progress
  PXE-0064 diff, rerunning focused gates, and resuming reviewer coverage.
- Completed the backend browser-session foundation:
  - added `API_AUTH_MODE=browser_session`;
  - added external hashed browser/operator user loading through
    `API_SESSION_USER_FILE`;
  - added typed `/api/v1/auth/session`, `/api/v1/auth/login`, and
    `/api/v1/auth/logout`;
  - added HttpOnly session cookies and session-bound CSRF for browser
    mutations;
  - enabled credentialed exact-origin CORS only in browser-session mode;
  - kept auth route bodies out of `FastAPIHandler` behind thin wrappers;
  - updated generated config schema and API/MCP candidate provenance without
    exposing callable MCP tools.
- Reviewer-found blockers were fixed before reporting:
  - typed `/api/v1` pre-route auth failures now return the typed API error
    envelope;
  - runtime session TTL bounds now match generated schema bounds;
  - missing checkpoint and stale phase/docs wording were corrected;
  - PBKDF2 hashes now enforce min/max iterations plus salt/digest length;
  - login throttling now has per-key, per-client, and global key caps with
    expiry pruning;
  - missing/disabled user login attempts run dummy password verification;
  - user and bearer token `enabled` fields require strict JSON booleans.
- Independent reviewer status:
  - API/MCP reviewer initially blocked on typed-error and TTL mismatches; both
    were fixed.
  - Docs/operator reviewer initially blocked on missing checkpoint and stale
    wording; both were fixed.
  - First security reviewer hit a usage-limit error; replacement security
    reviewer found four blockers, all fixed, then re-reviewed and approved.
- Validation completed:
  - focused API/auth suite: 149 passed, with one Starlette/httpx `TestClient`
    deprecation warning;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    544 parameters;
  - generated API candidate inventory regenerated and `--check` current;
  - `PYTHON=.venv/bin/python make phase0-check`: 177 passed, with the same
    `TestClient` warning;
  - `git diff --check`: passed;
  - active stale browser-session/CSRF wording scan: clean outside historical
    checkpoint/journal text.
- PXE-0064 remains `in_progress`, not done. Remaining work is dashboard
  credential-aware API/media migration, durable security audit events, operator
  credential/TLS hardening, typed-action-only enforcement, final legacy
  mutation retirement, and broader adversarial auth/session/browser tests.
- No PX4/SITL/HIL/field run, service install, sidecar mutation/update,
  deployment, runtime MCP endpoint, callable tool, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-14-phase-4-browser-session-auth-foundation.md`.

## 2026-06-14 Phase 4 Dashboard Auth Client/Media Foundation

- Recovered from the usage-limit interruption by rechecking branch/worktree
  status, rereading the required modernization anchors, inspecting the
  interrupted checkpoint, and rerunning static/source scans before committing
  any further edits.
- Completed the dashboard/browser side of the PXE-0064 auth foundation:
  - added `dashboard/src/services/apiClient.js` as the single production
    boundary for `fetch`, axios interceptors, credentialed requests, dynamic
    CSRF header injection, auth-failure session refresh events, media helper
    props, WebSocket construction, and protected blob downloads;
  - added `AuthSessionProvider`, `AuthGate`, and `AuthStatusMenu` for typed
    `/api/v1/auth/session`, login, logout, session state, browser-session
    sign-in, local-mode status, and machine-bearer dashboard blocking;
  - migrated production dashboard raw `fetch` calls to `apiFetch`, direct axios
    imports to the wrapped client, direct media WebSockets to
    `createDashboardWebSocket`, and protected recording/model file links to
    authenticated blob fetches;
  - kept native browser media cookie-session only, with no bearer header or
    query-token browser workaround;
  - made the backend auth session/login response expose the configured CSRF
    header name so the dashboard does not rely on a hard-coded mutation header;
  - added scope-aware operator-control disabling for `control:write` and
    `actions:execute`;
  - added source guardrails that reject new production raw `fetch`, direct
    axios package imports, direct `new WebSocket`, and protected endpoint
    `href` bypasses outside the approved client.
- Reviewer-found issues addressed:
  - frontend/API review flagged fragmented raw calls, direct media sockets,
    direct protected downloads, and missing test coverage; the shared client,
    call-site migration, blob downloads, media helpers, and source hygiene test
    cover those paths;
  - security/API review flagged native browser WebSocket auth constraints,
    query-token rejection, missing dynamic CSRF discovery, and scope-aware UI
    gaps; browser media remains cookie-session only, the backend exposes
    `csrf_header_name`, and sensitive action buttons now honor session scopes.
- Generated API/MCP candidate provenance was intentionally regenerated because
  `api_v1_contracts.py` and `api_v1_auth_routes.py` changed.
- Validation completed:
  - focused auth/source guard:
    `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_api_auth_runtime.py::test_auth_session_response_reports_configured_csrf_header_name tests/test_test_hygiene.py -q`:
    4 passed;
  - dashboard focused tests:
    `npm test -- --runTestsByPath src/services/apiClient.test.js src/components/AuthGate.test.js src/components/ActionButtons.test.js --watchAll=false`:
    3 suites passed, 12 tests passed;
  - dashboard full tests: `npm test -- --watchAll=false`: 11 suites passed,
    62 tests passed;
  - dashboard production build: `npm run build`: compiled successfully;
  - `.venv/bin/python tools/generate_api_tool_candidates.py`: regenerated
    candidate provenance;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 179 tests passed, with the existing Starlette/httpx
    `TestClient` deprecation warning;
  - `git diff --check`: passed;
  - active stale dashboard-migration wording scan and production dashboard
    source-boundary scan: clean after the phase-map wording cleanup.
- PXE-0064 remains `in_progress`, not done. Remaining work is durable
  security/audit events using authenticated principals, operator credential
  lifecycle and TLS deployment guidance, typed-action-only enforcement with
  final legacy mutation retirement, and broader adversarial browser/session/
  media tests.
- No PX4/SITL/HIL/field run, service install, sidecar mutation/update,
  deployment, runtime MCP endpoint, callable tool, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-14-phase-4-dashboard-auth-client-media.md`.

## 2026-06-16 Phase 4 API Security-Audit Foundation

- Recovered from the usage-limit interruption by checking branch/worktree
  status, rereading the required modernization anchors, inspecting the
  interrupted PXE-0064 diff, and rerunning focused validation before closing
  the slice.
- Completed the durable API security-audit foundation:
  - added `src/classes/api_security_audit.py` for sanitized append-only JSONL
    events with bounded local rotation and `fsync`;
  - added typed config/schema knobs under `Streaming.API_SECURITY_AUDIT_*`;
  - HTTP route auth, browser-origin denials, video WebSocket auth, WebRTC
    signaling auth, login, and logout now emit security audit events without
    raw cookies, bearer token values, passwords, or session credential IDs;
  - allowed `mutation` and `security_critical` requests fail closed with
    `503 security_audit_unavailable` if their required audit event cannot be
    recorded;
  - successful login rolls back the newly created session before the cookie is
    returned when audit recording fails;
  - API/MCP candidate provenance now includes `api_security_audit.py`, with no
    callable MCP/tool exposure.
- Local review fixed ambiguous active wording that still implied the previous
  dashboard/media slice closed the audit-event work; the active phase map now
  records that this security-audit slice closed that portion.
- Generated API/MCP candidate inventory was regenerated after source-hash drift
  from `api_security_policy.py`.
- Independent reviewer agents found blockers before closure:
  - audit-disabled config skipped required allowed mutation/security-critical
    events instead of failing closed;
  - logout audit failure could leave the server session active;
  - login audit failure could return a stale `Set-Cookie` header despite
    revoking the session;
  - active phase-map prose had tab indentation;
  - `docs/CONFIGURATION.md` did not document the new audit config knobs.
  All were fixed before commit, and focused regressions now cover the runtime
  blockers.
- Validation completed:
  - focused security-audit/auth tests after reviewer fixes: 11 passed, with the
    existing Starlette/httpx `TestClient` deprecation warning;
  - touched-module `py_compile`: passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    548 parameters;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`:
    candidate inventory current after regeneration;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 185 tests passed, with the same `TestClient` warning;
  - `git diff --check`: passed;
  - active stale audit/dashboard wording scan: clean in active guidance after
    excluding historical checkpoint, journal, and audit records.
- PXE-0064 remains `in_progress`, not done. Remaining work is operator
  credential/TLS hardening, typed-action-only enforcement, final legacy mutation
  retirement, and broader adversarial browser/session/media/audit tests.
- No PX4/SITL/HIL/field run, service install, sidecar mutation/update,
  deployment, runtime MCP endpoint, callable tool, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-16-phase-4-api-security-audit-foundation.md`.

## 2026-06-16 Phase 4 Offboard/Operator Action-Only Alias Retirement

- Recovered from the usage-limit interruption by checking branch/worktree
  status, reading the required operating/modernization anchors, inspecting the
  interrupted diff, and waiting for both reviewer agents to return.
- Completed a narrow PXE-0064 typed-action-only slice for the dangerous
  Offboard/operator aliases:
  - removed public FastAPI registration for
    `/commands/start_offboard_mode`, `/commands/stop_offboard_mode`, and
    `/commands/cancel_activities`;
  - kept the typed public surfaces as
    `/api/v1/actions/offboard-start`, `/api/v1/actions/offboard-stop`, and
    `/api/v1/actions/operator-abort`;
  - renamed handler wrappers to private internal executors and updated typed
    action handlers to call those executors directly;
  - replaced route-like action metadata with internal-handler metadata and
    changed compatibility audit source from `legacy_compatibility` to
    `internal_compatibility`;
  - removed the three retired paths from the API security legacy-command rule;
  - removed the three legacy dashboard endpoint constants and kept dashboard
    action classification on typed endpoints only;
  - updated route inventory, API/MCP candidate inventory, active docs, phase
    map, issue register, and SITL validation contract guards.
- Reviewer agents found no current first-party dashboard or checked-in SITL
  caller for the retired aliases, but flagged stale generated inventory,
  stale route counts, and active docs that still implied public aliases were
  pending retirement. Those issues were fixed before closure.
- Validation completed:
  - focused backend route/security/candidate/SITL/action tests: 61 passed;
  - touched-module `py_compile`: passed;
  - dashboard action-button test suite: 1 suite passed, 5 tests passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    548 parameters;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`:
    candidate inventory current;
  - `npm run build`: compiled successfully;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 185 tests passed with the existing Starlette/httpx
    `TestClient` deprecation warning;
  - `git diff --check`: passed;
  - active stale-text scan for old typed-action-only/route-count wording:
    clean outside historical checkpoint, journal, and audit records.
- PXE-0064 remains `in_progress`, not done. Remaining work is operator
  credential/TLS hardening, typed replacements/retirement for remaining legacy
  tracking/control mutations, and broader adversarial browser/session/media
  tests. PXE-0065 and PXE-0066 remain open for SITL sidecar evidence hardening
  and API/MCP candidate disposition governance.
- No PX4/SITL/HIL/field run, service install, sidecar mutation/update,
  deployment, runtime MCP endpoint, callable tool, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-16-phase-4-offboard-operator-action-only-retirement.md`.

## 2026-06-16 Phase 4 Typed Tracking Actions

- Resumed after usage-limit interruption by checking branch/worktree status,
  rereading the required operating/modernization anchors, and inspecting the
  interrupted PXE-0064 typed tracking-action diff before continuing.
- Completed a narrow typed tracking action foundation:
  - added `POST /api/v1/actions/tracking-start` and
    `POST /api/v1/actions/tracking-stop`;
  - added typed tracking bbox/start request contracts;
  - implemented dry-run, confirmation, idempotency-key requirement,
    idempotent replay, process-local action records, and local
    tracking/following state capture for tracking actions;
  - split the legacy tracking route bodies into internal executors and kept
    `/commands/start_tracking` and `/commands/stop_tracking` as local-only
    compatibility aliases for this slice;
  - migrated dashboard ROI start/stop and toolbar stop calls to the typed
    action routes through a shared action-request helper;
  - updated route inventory, security policy, generated API/MCP candidate
    inventory, active docs, phase map, and issue register.
- Read-only reviewer findings were reconciled:
  - dashboard ROI/toolbar paths were the only first-party production callers
    of the legacy tracking command routes;
  - no checked-in SITL helper or production script used those exact legacy
    routes;
  - immediate alias retirement was deferred to avoid breaking unknown external
    operator scripts before release-note/operator compatibility review;
  - missing handler wrappers flagged during review were added before
    validation.
- Validation completed:
  - focused backend route/security/action tests: 136 passed;
  - API/MCP candidate tests: 9 passed;
  - touched-module `py_compile`: passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    548 parameters;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`:
    candidate inventory current;
  - `CI=true npm test -- --watchAll=false`: 12 suites passed, 63 tests passed;
  - `npm run build`: compiled successfully with the existing Node `fs.F_OK`
    deprecation warning;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 185 tests passed with the existing Starlette/httpx
    `TestClient` deprecation warning;
  - `git diff --check`: passed.
- PXE-0064 remains `in_progress`, not done. This entry's alias-retirement and
  typed tracking utility work was later superseded by the 2026-06-17 typed
  tracking utility action slice and the 2026-06-19 tracking/control
  alias-retirement slice. Remaining PXE-0064 work after those slices is
  operator credential/TLS hardening and broader adversarial browser-session/media
  tests.
- No PX4/SITL/HIL/field run, service install, sidecar mutation/update,
  deployment, runtime MCP endpoint, callable tool, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-16-phase-4-typed-tracking-actions.md`.

## 2026-06-17 Phase 4 Typed Tracking Utility Actions

- Completed the remaining typed tracking utility action migration under
  PXE-0064:
  - added `POST /api/v1/actions/tracking-redetect`;
  - added `POST /api/v1/actions/segmentation-toggle`;
  - added `POST /api/v1/actions/smart-mode-toggle`;
  - added `POST /api/v1/actions/smart-click`;
  - kept `/commands/redetect`, `/commands/toggle_segmentation`,
    `/commands/toggle_smart_mode`, and `/commands/smart_click` as local-only
    compatibility aliases pending the alias-retirement gate.
- Supersession note: the compatibility-alias statement above was true at this
  2026-06-17 checkpoint. Those aliases were later retired by the 2026-06-19
  tracking/control alias-retirement slice.
- Added typed contracts, route specs, path constants, route inventory entries,
  typed error-envelope coverage, policy classifications, and generated
  non-callable API/MCP candidate inventory entries for the new action routes.
- Migrated first-party dashboard controls for redetect, segmentation toggle,
  smart-mode toggle, and smart-click to typed `/api/v1/actions/*` calls.
- Fixed reviewer-found safety/UX issues before closure:
  - smart-click action records now report success only when a target override is
    actually applied;
  - smart-click coordinates must be finite and inside the active frame;
  - idempotent replay now happens after dry-run and confirmation validation;
  - dashboard controls use `actions:execute`, matching backend action policy;
  - smart-click failures are visible in the video overlay instead of only in
    the browser console.
- Updated active docs and reporting:
  - route inventory now records 138 total declared route pairs, 136 declared
    HTTP route pairs, and 59 POST routes;
  - API/security/exposure/core docs now describe the typed replacements and the
    remaining local-only alias-retirement work;
  - phase map and issue register now point to this checkpoint.
- Independent reviewer disposition:
  - backend/API reviewer approved the smart-click truthfulness, coordinate
    validation, action classification, and replay-order fixes;
  - dashboard/API-client reviewer approved the `actions:execute` gating and
    smart-click failure feedback fixes;
  - docs/devops reviewer findings were closed by the checkpoint, journal, home
    report, and stale phase-map fix.
- Validation completed:
  - focused backend/API/security/candidate suite: 162 passed;
  - touched dashboard component suites: 13 passed;
  - `PYTHONPATH=src .venv/bin/python -m py_compile ...`: passed for touched
    backend/API modules and generator;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    548 parameters;
  - `.venv/bin/python tools/generate_api_tool_candidates.py`: inventory
    regenerated and current;
  - `CI=true npm --prefix dashboard test -- --watchAll=false`: 14 suites
    passed, 71 tests passed;
  - `npm --prefix dashboard run build`: compiled successfully with the existing
    Node `fs.F_OK` deprecation warning;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 185 tests passed with the existing Starlette/httpx
    `TestClient` deprecation warning;
  - `git diff --check`: passed.
- PXE-0064 remains `in_progress`, not done. Remaining alias-retirement work
  from this checkpoint was later closed by the 2026-06-19 tracking/control
  alias-retirement slice. Remaining PXE-0064 work after that slice is operator
  credential/TLS hardening and broader adversarial browser-session/media tests.
- No PX4/SITL/HIL/field run, service install, sidecar mutation/update,
  deployment, runtime MCP endpoint, callable tool, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-17-phase-4-typed-tracking-utility-actions.md`.

## 2026-06-17 Phase 4 QGC Video Compatibility Reconciliation

- Closed PXE-0067 as a PixEagle-side QGC video compatibility and docs slice.
- Reviewed the open QGroundControl PR #13594 at head
  `alireza787b/qgroundcontrol@f0a4feba97f2f563b3edd6cda0d17cfb15269550`;
  GitHub reported `mergeable=false`, `mergeable_state=dirty`, and
  `rebaseable=false`.
- Restored the local native-client WebSocket path without weakening remote or
  browser media security:
  - browser WebSocket clients still need an explicit allowlisted `Origin`;
  - remote/native WebSocket clients still fail closed without an allowed Origin
    and normal media authorization;
  - same-host native clients such as QGC may omit `Origin` only when both the
    socket peer and `Host` authority are loopback.
- Documented the compatibility matrix:
  - same-host QGC HTTP MJPEG and WebSocket are the direct-development paths;
  - remote unauthenticated QGC HTTP/WS media remains rejected by design;
  - field QGC video should use PixEagle GStreamer H.264/RTP/UDP output;
  - future remote direct HTTP/WS requires QGC Origin/header/auth support and a
    reviewed PixEagle exposure profile;
  - query-string tokens remain rejected.
- Cleaned active video docs to use current `Streaming` and `GStreamer` keys
  instead of stale `FastAPI.*`, optimizer, and old GStreamer output names.
- Added docs regression coverage preventing those stale video streaming config
  keys from returning.
- Independent review disposition:
  - QGC/Qt/GStreamer reviewer confirmed PR #13594 lacks configured
    Origin/Authorization/header support and still needs branch rebase/build
    repair;
  - PixEagle API/security reviewer confirmed the current policy matrix and the
    same-host native missing-Origin exception;
  - installer/bootstrap reviewer findings were split into PXE-0068 instead of
    hidden inside this media slice.
- Validation completed:
  - docs/API/media/security suite: 136 passed;
  - route inventory and parameter reload gate: 36 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    548 parameters;
  - `.venv/bin/python tools/generate_api_tool_candidates.py`: inventory
    regenerated for changed provenance hashes;
  - Python `py_compile` for touched backend/test modules: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 187 passed with the existing Starlette/httpx
    `TestClient` deprecation warning;
  - `git diff --check`: passed.
- Posted a factual QGC PR status comment at
  <https://github.com/mavlink/qgroundcontrol/pull/13594#issuecomment-4727942860>
  clarifying the same-host direct-development path, the rejected unauthenticated
  remote path, and the need for future authenticated Origin/header support.
- No QGC branch mutation, QGC build, PX4/SITL/HIL/field run, service install,
  sidecar mutation/update, deployment, runtime MCP endpoint, callable tool, or
  real-aircraft control was performed or claimed.
- Opened PXE-0068 for bootstrap/setup UX cleanup covering setup profiles,
  config drift, stale script refs, service install/start guidance, binary
  version policy, port-label confusion, companion/MavlinkAnywhere/SITL docs,
  and docs guardrails.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-17-phase-4-qgc-video-compatibility.md`.

## 2026-06-17 Phase 4 Remote Media Security Policy

- Closed PXE-0069 as a follow-up to the QGC video compatibility slice.
- Settled the normal onboard companion-to-GCS media policy:
  - remote PixEagle operation is normal, but anonymous backend HTTP/WebSocket
    media on the LAN is not supported;
  - local same-host development remains loopback `local_compat`;
  - field QGroundControl video should use GStreamer H.264/RTP/UDP while the
    PixEagle backend remains loopback;
  - remote browser operation uses SSH tunnel now and hardened
    `browser_session` over TLS only after the remaining PXE-0064 gates;
  - future remote direct QGC HTTP/WS requires a native machine-client profile
    with bearer `media:read`, Origin, TLS/CA handling, no query tokens, and
    credential redaction.
- Added `Streaming.API_ALLOWED_HOSTS` so backend Host authority allowlisting is
  explicit and no longer confused with browser CORS origins:
  - local-only rejects non-loopback allowed hosts;
  - trusted-LAN profiles reject wildcard, URL, credential, and wildcard-bind
    Host entries;
  - older trusted-LAN configs retain a compatibility fallback, but new remote
    profiles should set `API_ALLOWED_HOSTS` directly.
- Added `docs/video/04-streaming/remote-media-security.md` and updated active
  API/video/config/port docs with the local-dev, field-QGC, remote-browser,
  remote-native, and rejected anonymous-LAN profiles.
- Removed stale `/video_feed?quality`, `/video_feed?resize`, `/video_feed?osd`,
  and `OSD.ENABLE` examples from active video docs; added docs guards so they
  do not return.
- Opened PXE-0070 for the external QGroundControl PR work: rebase/repair
  PR #13594, add Authorization/Origin/TLS/redaction support, and test negative
  auth paths before advertising remote PixEagle HTTP/WS compatibility from QGC.
- Validation completed:
  - focused exposure/docs suite: 92 passed with the existing Starlette/httpx
    `TestClient` warning;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    549 parameters;
  - `.venv/bin/python tools/generate_api_tool_candidates.py`: candidate
    inventory regenerated; `api_exposure_policy.py` provenance hash changed;
  - Python `py_compile` for touched policy/test modules: passed;
  - route inventory and parameter reload gate: 36 passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 195 passed with the existing Starlette/httpx
    `TestClient` warning;
  - `git diff --check`: passed;
  - manual stale-doc scans for old video query/config keys: no matches.
- No QGC branch mutation, QGC build, PX4/SITL/HIL/field run, service install,
  sidecar mutation/update, deployment, runtime MCP endpoint, callable tool, or
  real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-17-phase-4-remote-media-security-policy.md`.

## 2026-06-17 Phase 4 QGC Source Profile And Demo Policy

- Closed PXE-0071 as a follow-up to the QGC remote-media/security discussion.
- Clarified that QGroundControl PR #13594 should stay generic:
  - ordinary non-PixEagle HTTP/HTTPS MJPEG and WebSocket sources may remain
    URL-only when that source does not require auth;
  - PixEagle same-host development remains loopback-only direct HTTP/WS;
  - PixEagle remote HTTP/WS is a stricter source profile requiring
    Authorization, WebSocket Origin, TLS/WSS handling, and credential
    redaction;
  - PixEagle anonymous LAN backend access remains rejected.
- Added `docs/video/04-streaming/qgc-http-websocket-source-plan.md` to keep
  future QGC PR comments, PixEagle docs, and bootstrap profile work aligned.
- Clarified the PixEagle-side config contract for future generic QGC interop:
  - field QGC video remains GStreamer H.264/RTP/UDP with backend loopback;
  - same-host direct HTTP/WS remains `local_only` + `local_compat`;
  - future remote QGC HTTP/WS requires exact `API_ALLOWED_HOSTS`, matching QGC
    URL host authority, `Authorization: Bearer <token>`, optional WebSocket
    Origin, TLS/WSS handling, and credential redaction;
  - a video-only QGC token should have `media:read` only.
- Updated remote media docs to make beginner demo behavior explicit:
  - full remote browser demos should use generated `browser_session`
    credentials with explicit Host/CORS allowlists;
  - no-password remote demos must be media-only, explicitly unsafe, never
    default, and not a dashboard/control profile.
- Updated `docs/CONFIGURATION.md` to state that the official quick-start
  default is same-host and credential-free, while remote phone/tablet/GCS access
  must select an explicit profile with generated credentials or media-only
  output.
- Expanded PXE-0068 acceptance gates for setup profiles:
  `local_dev`, `field_qgc_video`, `demo_lan_browser`, `production_remote`, and
  possible future `unsafe_demo_lan_media_only`.
- Expanded PXE-0070 acceptance gates so QGC authenticated PixEagle support does
  not break generic non-PixEagle HTTP/WS source support.
- Corrected the remaining stale active video OSD sample from `OSD.ENABLE` to
  `OSD.OSD_ENABLED`.
- Added docs guard coverage for the QGC source-plan terms and demo/control
  boundary.
- Posted a QGC PR clarification comment at
  <https://github.com/mavlink/qgroundcontrol/pull/13594#issuecomment-4731276373>
  stating that generic non-PixEagle HTTP/WS source support remains in scope and
  PixEagle remote HTTP/WS is a stricter future profile.
- Validation completed:
  - focused docs infrastructure suite: 16 passed;
  - manual stale-doc scans for old video query params and `OSD.ENABLE`: no
    matches;
  - schema check: current, 41 sections, 549 parameters;
  - route inventory and parameter reload gate: 36 passed;
  - Python `py_compile` for touched test module: passed;
  - `git diff --check`: passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, API tool
    candidate inventory current, 196 passed with the existing Starlette/httpx
    `TestClient` warning.
- No QGC branch mutation, QGC build, PX4/SITL/HIL/field run,
  service install, sidecar mutation/update, deployment, runtime MCP endpoint,
  callable tool, or real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-17-phase-4-qgc-source-profile-demo-policy.md`.

## 2026-06-17 Phase 4 Setup Profile Foundation

- Advanced PXE-0068 from `open` to `in_progress` by completing the setup-profile
  foundation while leaving binary pin/checksum policy and remote profile
  automation as tracked follow-up work.
- Added `scripts/setup/apply-setup-profile.py` with explicit profile support:
  - `local_dev` writes a safe loopback/local-compat override when requested;
  - `field_qgc_video` enables only GStreamer H.264/RTP/UDP output to a supplied
    GCS host while the backend remains loopback-only;
  - `demo_lan_browser`, `production_remote`, and
    `unsafe_demo_lan_media_only` are listed but fail closed without writing
    config until their credential/TLS/evidence gates are implemented.
- Added `make setup-profile` and `make qgc-video-profile GCS_HOST=<ip>`.
- Stopped Linux and Windows init from creating or replacing
  `configs/config.yaml` by default. Init now keeps existing local config,
  otherwise uses `configs/config_default.yaml` as the runtime source of truth.
- Moved Linux service onboarding behind explicit deployment opt-in:
  `PIXEAGLE_ENABLE_SERVICE_SETUP=1 make init`. Normal init prints that service
  setup was skipped and points to the explicit service installer.
- Aligned QGC GStreamer output defaults on UDP port `5600` across
  `configs/config_default.yaml`, generated schema, runtime API fallback hints,
  setup profile tooling, and docs.
- Updated onboarding/config/docs:
  - new `docs/setup/setup-profiles.md`;
  - linked setup profiles from README, docs index, installation, and
    configuration docs;
  - clarified QGC field video setup in remote-media and QGC source-plan docs;
  - corrected Troubleshooting and Windows docs so `5551` is legacy telemetry
    WebSocket, not backend video WebSocket;
  - replaced exposed-backend troubleshooting probes with loopback/SSH-tunnel
    checks;
  - reframed companion service/Wi-Fi guidance around explicit deployment and
    credentialed/SSH access boundaries;
  - narrowed `docs/KNOWN_ISSUES.md` to the remaining backend media WebSocket
    health-probe gap.
- Updated setup helper script summaries so OpenCV/dlib/PyTorch setup does not
  imply mandatory direct `configs/config.yaml` edits.
- Added guardrails:
  - `tests/test_setup_profiles.py` covers profile list, local-dev loopback
    config, field-QGC video config, required GCS host, port validation,
    deferred-profile fail-closed behavior, dry-run no-write behavior, and QGC
    port single-source consistency;
  - `tests/test_docs_infrastructure_consistency.py` now guards setup-profile
    docs links, default config-copy drift, service prompt default-yes drift,
    exposed backend troubleshooting patterns, and 5551/media-WebSocket label
    confusion;
  - `make phase0-check` now includes `tests/test_setup_profiles.py`.
- Independent read-only reviewers found:
  - QGC port split (`2000` versus `5600`), missing profile tests, and stale
    Troubleshooting wording;
  - service setup still wired into first-run init, companion exposure docs,
    Windows port fallback, and ambiguous 5551 labels.
  All actionable findings were fixed in this slice.
- Validation completed:
  - Python compile for setup profile tool and touched tests: passed;
  - focused setup/docs/clean-clone suite: 34 passed;
  - focused shell syntax for touched shell scripts: passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - `.venv/bin/python tools/generate_api_tool_candidates.py`: regenerated
    candidate inventory after `fastapi_handler.py` fallback changes;
  - route inventory and parameter reload gate: 36 passed;
  - final `PYTHON=.venv/bin/python make phase0-check`: schema current,
    candidate inventory current, 211 passed with the existing Starlette/httpx
    `TestClient` deprecation warning;
  - `git diff --check`: passed with expected CRLF normalization warnings for
    edited Windows batch/PowerShell files.
- No service install/start/enable, sidecar mutation/update, QGC branch
  mutation/build, PX4/SITL/HIL/field run, deployment, runtime MCP endpoint,
  callable tool exposure, or real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-17-phase-4-setup-profile-foundation.md`.

## 2026-06-17 Phase 4 Binary Download Provenance

- Continued PXE-0068 by closing the MAVSDK Server and MAVLink2REST binary
  provenance gate without closing the remaining remote-profile automation work.
- Added `scripts/setup/binary-manifest.env` as the setup-time source of truth
  for external helper binaries:
  - MAVSDK Server pinned to upstream `mavlink/MAVSDK` `v3.12.0`;
  - MAVLink2REST pinned to upstream `mavlink/mavlink2rest` `1.0.0`;
  - exact Linux, macOS, and Windows asset filenames;
  - SHA-256 digests captured from upstream GitHub release asset metadata.
- Reworked `scripts/setup/download-binaries.sh`:
  - reads the shared manifest;
  - supports `--dry-run` / `--print-plan`;
  - resolves Linux/macOS x86_64, ARM64, ARMv7, ARMv6, and macOS ARM64 assets
    from manifest keys;
  - verifies SHA-256 before moving temp files into `bin/`;
  - rejects unverified overrides unless `PIXEAGLE_ALLOW_UNVERIFIED_BINARY=1`;
  - appends verified provenance to `bin/binary-provenance.jsonl`;
  - exits nonzero when any requested component fails.
- Replaced Windows fallback-tag probing in `scripts/setup/download-binaries.bat`
  with the same manifest, dry-run, SHA-256, override, and provenance policy.
- Updated Linux and Windows init scripts so existing `bin/` binaries are routed
  through manifest verification instead of being silently accepted. Root-level
  legacy binaries no longer satisfy init summaries; Linux downloader can migrate
  a verified legacy root binary into `bin/`.
- Added `make binary-download-plan` for no-write operator preview.
- Added `docs/setup/binary-download-policy.md` and linked it from README,
  docs index, setup profiles, installation, and Windows setup docs.
- Updated GitHub Actions and `make phase0-check` to include
  `tests/test_binary_download_policy.py`.
- Independent read-only reviewers found:
  - Linux failure propagation and checksum/provenance gaps;
  - Windows/Linux version divergence due to fallback tags;
  - incomplete dry-run wiring;
  - stale ARM/macOS mapping;
  - init bypass of existing/legacy binaries;
  - missing docs for pins, checksums, dry-run, provenance, and offline
    placement.
  All actionable findings were fixed in this slice.
- Validation completed:
  - `bash -n scripts/setup/download-binaries.sh scripts/init.sh install.sh`:
    passed;
  - Python compile for touched tests: passed;
  - focused binary/setup/docs suite: 37 passed;
  - `make binary-download-plan`: dry-run printed pinned URLs/checksums and made
    no writes;
  - final `PYTHON=.venv/bin/python make phase0-check`: schema current,
    candidate inventory current, 217 passed with the existing Starlette/httpx
    `TestClient` warning;
  - `git diff --check`: exit code 0 with the expected CRLF normalization
    warning for the edited Windows batch file.
- No binary download/install, service install/start/enable, sidecar
  mutation/update, QGC branch mutation/build, PX4/SITL/HIL/field run,
  deployment, runtime MCP endpoint, callable tool exposure, or real-aircraft
  control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-17-phase-4-binary-download-provenance.md`.

## 2026-06-18 Phase 4 API/MCP Candidate Disposition Governance

- Closed PXE-0066 by adding explicit review dispositions to every generated
  `/api/v1` API/MCP candidate without creating any runtime MCP surface.
- Updated `tools/generate_api_tool_candidates.py`:
  - every candidate now receives a `review_disposition` with state, owner,
    review date, rationale, evidence pointers, next gate,
    `does_not_imply_mcp_exposure: true`, and
    `runtime_promotion: not_promoted`;
  - sensitive action/auth/SITL/audit paths are defensively excluded from the
    initial read-only eligible set even if a future constant is misconfigured;
  - generated summary now records disposition counts and fails coverage when a
    candidate has missing, invalid, or unsafe disposition metadata.
- Current generated candidate split:
  - 6 `approved_for_review_only` typed process-local GET candidates;
  - 13 `blocked` auth/action/action-audit candidates;
  - 5 `deferred` SITL validation-stimulus candidates.
- Updated docs-stage governance:
  - `agent_policy.yaml` owns the valid disposition states and fail-closed
    defaults;
  - `agent_tools.yaml` mirrors approved-for-review-only for the six curated
    read-only entries while keeping them `callable: false`,
    `mcp_exposure: none`, and `promotion_status: unpromoted`;
  - active API/agent-context docs now state that disposition completion is
    review coverage only, not MCP `tools/list`/`tools/call` exposure.
- Added test coverage in `tests/test_api_tool_candidates.py` for:
  - total disposition coverage and 6/13/5 state counts;
  - approved-for-review-only candidates remaining non-callable and unpromoted;
  - blocked/deferred action/auth/SITL candidates remaining unregistered and
    outside read-only promotion;
  - policy disposition vocabulary/defaults;
  - registry disposition mirroring without runtime exposure.
- Independent read-only reviewers confirmed the disposition mapping and flagged
  stale generated inventory, missing explicit disposition tests, and the
  sensitive-path eligibility edge case; all actionable findings were fixed.
- Validation completed:
  - `python3 tools/generate_api_tool_candidates.py --check`: passed;
  - `python3 -m py_compile tools/generate_api_tool_candidates.py
    tests/test_api_tool_candidates.py`: passed;
  - `.venv/bin/python -m pytest tests/test_api_tool_candidates.py -q`: 9
    passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest
    tests/test_api_tool_candidates.py tests/test_api_route_inventory.py
    tests/unit/core_app/test_parameters_reload.py -q`: 45 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - `PYTHONPATH=src .venv/bin/python -m pytest
    tests/test_docs_infrastructure_consistency.py -q`: 20 passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 217 passed with the existing Starlette/httpx
    `TestClient` deprecation warning;
  - `git diff --check`: passed.
- Note: plain `bash scripts/check_schema.sh` uses system `python3` on this host
  and failed because that interpreter lacks `ruamel.yaml`; the same gate passed
  with the repo virtualenv via `PYTHON=.venv/bin/python`.
- No runtime MCP endpoint, `tools/list`, `tools/call`, service/deployment,
  sidecar mutation, QGC branch mutation/build, PX4/SITL/HIL/field run, or
  real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-18-phase-4-api-mcp-candidate-disposition.md`.

## 2026-06-18 Phase 4 SITL Sidecar Evidence Hardening

- Closed PXE-0065 by hardening the checked-in SITL evidence contract for
  MavlinkAnywhere sidecar compatibility and secret handling.
- Updated both maintained SITL plans:
  - `phase2_follower_validation`;
  - `gazebo_visual_validation`.
- Each plan now declares a `stack.routing.compatibility_policy` for
  MavlinkAnywhere:
  - reviewed source ref
    `7643d4d9bc75a78fdc6b0f68358c466310ee2c4d`;
  - reviewed version `v3.0.14-2-g7643d4d`;
  - minimum dashboard semantic version `3.0.14`;
  - loopback read-without-credentials expectation;
  - required read paths: `/api/v1/status`, `/api/v1/diagnostics`,
    `/api/v1/endpoints`, `/api/v1/profiles/summary`, and `/api/v1/config`;
  - required structured endpoint fields and profile metadata.
- Updated the harness so dry-run summaries expose the sidecar policy and runtime
  evidence writes `versions/mavlink_anywhere_dashboard.json`.
- Added structured MavlinkAnywhere compatibility classification:
  - `unavailable`;
  - `unexpected_auth`;
  - `unsupported_contract_version`;
  - `unprepared_config`;
  - `prepared_routing`.
- Accepted SITL evidence now requires the compatibility semantic check to reach
  `prepared_routing`; status or endpoint probes alone are not enough.
- Added `security/secret_scan.json` to the required evidence contract. The
  scanner blocks high-confidence credential-bearing text artifacts:
  - tokens, passwords, private keys, URL credentials, Authorization headers,
    query-string secrets, and Wi-Fi PSKs;
  - findings report detector/path/key metadata only and intentionally omit
    matched values and context lines;
  - binary flight artifacts such as `.ulg`, `.tlog`, and NUL-containing BSON-like
    params files are skipped with metadata instead of decoded.
- Updated active SITL and companion-runtime docs to describe the sidecar
  policy, classifications, version evidence, and secret-scan gate.
- Independent read-only reviewers inspected the planned SITL/PX4 sidecar
  contract and evidence-security design. Findings fixed in this slice:
  - plan-level compatibility policy was missing;
  - version/ref evidence artifact was missing;
  - auth, unavailable, unsupported-version, and unprepared-config failures were
    collapsed into vague probe failures;
  - diagnostics/config/profile-summary probes needed to be core evidence;
  - secret scanning needed high-confidence detectors, placeholder/path
    suppressions, binary skips, and no raw secret echo in reports.
- Validation completed:
  - Python syntax compile for harness and tests: passed;
  - dry-run JSON validation for both maintained SITL plans: passed and exposed
    sidecar policy plus new evidence artifacts;
  - focused SITL contract suite: 69 passed;
  - docs infrastructure guard: 20 passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, candidate
    inventory current, 217 passed with the existing Starlette/httpx
    `TestClient` warning;
  - `git diff --check`: passed.
- No PX4/SITL/HIL/field runtime pass, Docker/PX4 start, service install/start,
  sidecar mutation/update, deployment, QGC branch mutation/build, runtime MCP
  endpoint, callable tool exposure, or real-aircraft control was performed or
  claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-18-phase-4-sitl-sidecar-evidence-hardening.md`.

## 2026-06-18 Phase 4 Demo LAN Browser Profile

- Continued PXE-0068 by automating the previously deferred
  `demo_lan_browser` setup profile without creating anonymous remote backend
  access.
- Implemented the profile in `scripts/setup/apply-setup-profile.py`:
  - requires `--lan-host` for the PixEagle companion LAN address/hostname;
  - rejects wildcard, loopback, URL, credential-bearing, port-bearing, public IP,
    and public DNS host values;
  - allows private/link-local IP literals and local-scope single-label,
    `.local`, or `.lan` hostnames;
  - sets `Streaming.API_EXPOSURE_MODE: trusted_lan_legacy`,
    `HTTP_STREAM_HOST: 0.0.0.0`, exact `API_ALLOWED_HOSTS`, exact
    `API_CORS_ALLOWED_ORIGINS`, and `API_AUTH_MODE: browser_session`;
  - writes a gitignored external PBKDF2-SHA256 hashed user file under
    `configs/secrets/` by default;
  - generates a random password, prints it once, and never writes plaintext;
  - refuses credential overwrite unless `--rotate-demo-credentials` is explicit.
- Added `make demo-lan-browser-profile LAN_HOST=<this-pixeagle-lan-ip-or-hostname>`
  and allowed the wrapper to pass through `SETUP_PROFILE_ARGS` for tests and
  local override paths.
- Updated `make run` so the dashboard static server binds on LAN only when
  runtime config is both `trusted_lan_legacy` and `browser_session`; backend
  APIs, MJPEG, video WebSocket, and WebRTC signaling still rely on session auth,
  CSRF for browser mutations, exact Host, and exact Origin checks.
- Updated operator docs:
  - README now tells beginners to apply the profile, run/restart `make run`, and
    open `http://<LAN_HOST>:3040`;
  - setup profile docs explain the generated credential file, rotation guard,
    local-scope hostname rule, and lab-only HTTP boundary;
  - remote media security docs now point full browser LAN demos at the automated
    `demo_lan_browser` profile while preserving the no-password remote-control
    rejection.
- Updated PXE-0068 issue-register and phase-slice-map entries: the remaining
  PXE-0068 work is now production remote-profile hardening, backend media
  WebSocket health reporting, and later stale setup/update doc cleanup.
- Independent read-only reviewers found and confirmed fixes for:
  - public DNS hostnames being allowed in the lab HTTP profile;
  - credential artifact write ordering;
  - missing `0600` credential-file assertion;
  - missing Makefile wrapper and launcher handoff tests;
  - README workflow clarity;
  - the missing checkpoint file referenced by the issue register.
- Validation completed:
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_setup_profiles.py`:
    27 passed;
  - Python compile for `scripts/setup/apply-setup-profile.py`: passed;
  - `bash -n scripts/run.sh scripts/components/dashboard.sh`: passed;
  - docs guard focused on setup/QGC source boundaries: 2 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py
    tests/unit/core_app/test_parameters_reload.py`: 36 passed;
  - `PYTHON=.venv/bin/python make phase0-check`: schema current, API tool
    inventory current, 233 passed with the existing Starlette/httpx
    `TestClient` deprecation warning;
  - `git diff --check`: passed.
- No service install/start, deployment, Docker/PX4/SITL/HIL, sidecar mutation,
  QGC branch mutation/build, runtime MCP promotion, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-18-phase-4-demo-lan-browser-profile.md`.

## 2026-06-18 Phase 4 Streaming Media-Health API

- Continued PXE-0068 by adding typed backend media-health reporting:
  `GET /api/v1/streams/media-health`.
- Added `src/classes/api_v1_streams.py` for process-local MJPEG, backend JPEG
  WebSocket, WebRTC signaling, GStreamer UDP/H.264, frame-publisher, and
  adaptive-quality state.
- Added typed contracts, route metadata, read-route error boundary, canonical
  path constant, `media:read` security policy classification, and route
  inventory coverage.
- Made `Streaming.ENABLE_STREAMING` an enforced runtime gate for `/video_feed`,
  `/ws/video_feed`, and `/ws/webrtc_signaling`; disabled streaming now fails
  closed instead of only being documented.
- Fixed reviewer-found media-health semantics:
  - stale/frozen published frames now degrade the route;
  - GStreamer active output without a frame now degrades the route;
  - zero connection limits report transports as disabled;
  - GStreamer UDP reports pipeline state without a fake client connection;
  - media-health generated candidate sensitivity is `media_transport_health`,
    not `runtime_status`;
  - unnecessary direct `FastAPIHandler` media-health helper coupling was removed.
- Updated API, core-app, video, streaming config, known-issues, agent-context,
  issue-register, and phase-slice-map docs. Live governance now records 25
  `/api/v1` candidates, 7 approved-for-review-only process-local
  status/telemetry/media-health GET candidates, 13 blocked candidates, and 5
  deferred SITL validation-stimulus candidates.
- Independent read-only reviewers covered API contracts, media runtime
  semantics, security/MCP governance, and docs. Remaining reviewer notes are
  tracked as follow-up: dashboard/service adoption of media-health, WebRTC peer
  shutdown, GStreamer shutdown release, and stale WebSocket lifecycle cleanup.
- Validation completed:
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_api_v1_streams.py -q`:
    7 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_security_policy.py
    tests/unit/core_app/test_api_exposure_policy.py -q`: 99 passed with the
    existing Starlette/httpx warning;
  - route/tool/security/media/exposure focused gate: 143 passed with the
    existing Starlette/httpx warning;
  - docs infrastructure guard: 20 passed;
  - `PYTHONPATH=src .venv/bin/python tools/generate_api_tool_candidates.py --check`:
    inventory current;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - `PYTHON=.venv/bin/python make PYTHON=.venv/bin/python phase0-check`:
    schema current, API tool candidate inventory current, 246 passed with the
    existing Starlette/httpx warning;
  - `git diff --check`: passed.
- No remote browser/QGC/GCS receipt proof, QGC branch mutation, Docker/PX4/SITL/HIL,
  service install/start, deployment, runtime MCP endpoint, callable tool
  exposure, or real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-18-phase-4-streaming-media-health.md`.

## 2026-06-18 Phase 4 Dashboard/Service Media-Health Adoption

- Continued PXE-0068 by wiring existing dashboard and service status consumers
  to typed process-local media health.
- Added `useStreamingMediaHealth()` plus `normalizeStreamingMediaHealth()` in
  the dashboard status hook layer:
  - polls `/api/v1/streams/media-health` with no-cache requests;
  - preserves typed transport/frame/security fields;
  - provides legacy-compatible fields for existing streaming widgets;
  - falls back to `/api/streaming/status` only for missing-route rolling-update
    errors;
  - does not fallback on `401/403`, network errors, or server errors;
  - ignores stale out-of-order responses.
- Migrated `StreamingStatusIndicator` and `StreamingStats` away from direct
  legacy streaming-status and `/stats` polling for media state. They now render
  typed status/guidance, frame freshness, transport counts, GStreamer UDP
  pipeline state without client-count claims, health issues, and typed frame
  counters.
- Added `pixeagle-service status` media-health output:
  - same-host loopback probe by default;
  - optional explicit bearer token file through
    `PIXEAGLE_MEDIA_HEALTH_BEARER_TOKEN_FILE`;
  - no browser login, cookie scraping, query-string token, or proxy-forwarded
    local proof;
  - `401/403` reported as auth-required, not media-down;
  - output repeats that remote receipt is not proven by the process-local check.
- Updated service, troubleshooting, video streaming/config, known-issues, issue
  register, and phase-slice map docs.
- Independent read-only reviewers covered dashboard migration and service
  auth/security behavior before implementation; the implemented slice follows
  their recommendations.
- Validation completed:
  - `bash -n scripts/service/utils.sh`: passed;
  - focused dashboard tests for the new hook/components: 39 passed;
  - full dashboard test suite: 84 passed;
  - dashboard production build: passed;
  - focused Python/docs/route/parameter gate: 63 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - `PYTHON=.venv/bin/python make PYTHON=.venv/bin/python phase0-check`:
    schema current, API tool candidate inventory current, 247 passed with the
    existing Starlette/httpx warning;
  - `git diff --check`: passed.
- No service install/start, deployment, Docker/PX4/SITL/HIL, sidecar mutation,
  QGC branch mutation/build, runtime MCP endpoint, callable tool exposure, or
  real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-18-phase-4-dashboard-service-media-health.md`.

## 2026-06-19 Phase 4 Streaming Lifecycle Cleanup

- Continued PXE-0068 by closing the media runtime lifecycle follow-ups left
  after typed media-health and dashboard/service adoption.
- Backend WebSocket video streaming now uses one idempotent cleanup path:
  - stale clients are evaluated from `last_frame_time` after frames are sent;
  - never-fed clients are evaluated from `connected_at`;
  - cleanup removes the tracked client, updates active connection counts,
    unregisters adaptive-quality and frame-publisher resources once, and closes
    the transport when requested.
- `FastAPIHandler.stop()` now cancels background tasks, closes tracked
  WebSocket streaming clients, clears HTTP MJPEG records, drains WebRTC peers,
  shuts down the streaming encoder pool, and stops the server.
- `WebRTCManager.shutdown()` now closes all active peers, and peer close waits
  are bounded so a slow `RTCPeerConnection.close()` cannot stall API shutdown
  indefinitely. Frame-publisher unregister happens in `finally`.
- `AppController.shutdown()` releases initialized GStreamer output.
  `GStreamerHandler.release()` clears the writer reference and drains queued
  frames, including release-error paths. `initialize_stream()` releases any
  existing writer before replacing it, and GStreamer status/toggle checks now
  require `out.isOpened()`.
- Updated active WebSocket, WebRTC, and GStreamer docs plus issue-register and
  phase-slice-map resume anchors. Regenerated API tool candidate provenance
  because `fastapi_handler.py` changed.
- Independent reviewers:
  - runtime reviewer found no blockers and requested bounded WebRTC close plus
    safer GStreamer release/reinitialize handling; those were implemented;
  - docs/reporting reviewer found the missing checkpoint/journal gap; this
    entry and the checkpoint close that blocker.
- Validation completed:
  - focused lifecycle suite: 10 passed;
  - focused Python/docs/media/security/route gate: 160 passed with the existing
    Starlette/httpx warning;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - Python compile for touched backend modules: passed;
  - `git diff --check`: passed;
  - `PYTHON=.venv/bin/python make PYTHON=.venv/bin/python phase0-check`:
    schema current, API tool candidate inventory current, 247 passed with the
    existing Starlette/httpx warning.
- No service install/start, deployment, Docker/PX4/SITL/HIL, sidecar mutation,
  QGC branch mutation/build, runtime MCP endpoint, callable tool exposure, or
  real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-19-phase-4-streaming-lifecycle-cleanup.md`.

## 2026-06-19 Phase 4 Tracking/Control Alias Retirement

- Continued PXE-0064 by retiring the remaining tracking/control command
  compatibility aliases after their typed `/api/v1/actions/*` replacements and
  dashboard migration were already in place.
- Removed public FastAPI route registration and alias methods for:
  - `/commands/start_tracking`;
  - `/commands/stop_tracking`;
  - `/commands/redetect`;
  - `/commands/toggle_segmentation`;
  - `/commands/toggle_smart_mode`;
  - `/commands/smart_click`.
- Kept the internal `_execute_*` methods used by typed action dispatchers, so
  typed tracking start/stop, redetect, segmentation toggle, smart-mode toggle,
  and smart-click actions remain the HTTP control surface.
- Removed the `legacy_commands` security-policy rule and unused
  `LOCAL_LEGACY_CONTROL` policy. Guardrails now assert retired tracking/control
  command paths resolve to `DENY_UNCLASSIFIED`.
- Updated route inventory from 59 POST routes to 53 POST routes and regenerated
  the generated API/MCP candidate inventory. `/api/v1` candidate count remains
  25, read-only candidate count remains 7, and total declared HTTP route count
  is now 131.
- Updated active README, setup, API/security/exposure/component/config, SITL,
  video, issue-register, phase-slice-map, and this journal so resume anchors no
  longer reopen tracking/control alias retirement as pending.
- Independent reviewers:
  - API/security reviewer confirmed route/security retirement and typed action
    continuity, then found stale active API/SITL/security docs; those were
    fixed;
  - docs/reporting reviewer found more stale active setup/exposure/video resume
    anchors, the untracked checkpoint reminder, a needed 2026-06-17 journal
    supersession note, and MCP/agent wording that could imply callable action
    control; those were fixed.
- Validation completed so far:
  - focused Python route/security/action/SITL API gate: 172 passed;
  - focused Python route/security/docs/candidate/action/SITL API gate after
    reviewer fixes: 205 passed;
  - candidate inventory check: current after regeneration;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - touched backend module `py_compile`: passed;
  - `git diff --check`: passed after reviewer fixes;
  - `PYTHON=.venv/bin/python make PYTHON=.venv/bin/python phase0-check`:
    schema current, API tool candidate inventory current, 247 passed with the
    existing Starlette/httpx `TestClient` deprecation warning.
- No service install/start, deployment, Docker/PX4/SITL/HIL, sidecar mutation,
  QGC branch mutation/build, runtime MCP endpoint, callable tool exposure, field
  test, or real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-19-phase-4-tracking-control-alias-retirement.md`.

## 2026-06-19 Phase 4 Browser Session / Media Adversarial Tests

- Continued PXE-0064 by adding focused backend adversarial regression tests for
  browser-session and media authorization behavior.
- Added coverage for expired browser-session cookies:
  - public `/api/v1/auth/session` treats the expired cookie as anonymous;
  - the expired session is purged from the in-memory store;
  - protected `/api/v1/streams/media-health` rejects the same cookie with
    `401 invalid_session`.
- Added coverage for logout invalidating sibling browser tabs that still hold
  the old cookie. Media-health is allowed before logout and rejected after
  logout.
- Added coverage that a `viewer` browser session can read typed media-health but
  cannot execute typed tracking actions even with a valid CSRF token; the action
  fails with `403 insufficient_scope` and missing `actions:execute`.
- No runtime code change was required; this codifies current fail-closed
  behavior.
- Validation completed so far:
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_api_auth_runtime.py -q`:
    37 passed.
  - focused auth/security/docs/route gate:
    `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/core_app/test_api_auth_runtime.py tests/test_api_security_policy.py tests/test_docs_infrastructure_consistency.py tests/test_api_route_inventory.py -q`:
    105 passed;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`:
    candidate inventory current;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - touched test `py_compile`: passed;
  - `PYTHON=.venv/bin/python make PYTHON=.venv/bin/python phase0-check`:
    schema current, API tool candidate inventory current, 250 passed with the
    existing Starlette/httpx `TestClient` deprecation warning.
- No browser automation, dashboard build, service install/start, deployment,
  Docker/PX4/SITL/HIL, sidecar mutation, QGC branch mutation/build, runtime MCP
  endpoint, callable tool exposure, field test, or real-aircraft control was
  performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-19-phase-4-browser-session-media-adversarial-tests.md`.

## 2026-06-19 Phase 4 LAN/Private-Overlay Browser Profile Hardening

- Continued PXE-0068/PXE-0064 cleanup by answering and codifying the TLS/LAN
  policy question:
  - TLS is not only for domain names; it is an application-layer trust boundary
    that can use public DNS certificates, internal PKI, or another reviewed
    trust anchor.
  - HTTP over isolated LAN or operator-approved private overlay/VPN remains
    allowed only through the explicit `demo_lan_browser` lab profile.
  - Production remote browser access remains gated on TLS or an equivalent
    reviewed trust boundary, durable credentials, adversarial auth/media tests,
    audit/evidence, and operator deployment hardening.
- Hardened `demo_lan_browser` host validation:
  - accepts RFC1918 private LAN, shared private-overlay/CGNAT `100.64.0.0/10`,
    IPv4 link-local, IPv6 ULA, plain IPv6 link-local, bracketed IPv6, and
    local-scope hostnames;
  - rejects public, documentation, multicast, loopback, unspecified, wildcard,
    scheme/path/credential, query/fragment, port-bearing, malformed bracketed
    IPv6, and IPv6 zone-identifier inputs.
- Clarified that the browser demo requires both dashboard port `3040` and
  authenticated backend/API media port `5077` reachable only from trusted demo
  devices/CIDRs. This avoids the false impression that a dashboard-only
  firewall rule is enough for the standalone dashboard.
- Aligned Windows `scripts/run.bat` with Linux `make run`: when the generated
  config is `trusted_lan_legacy` plus `browser_session`, the launcher exports
  `PIXEAGLE_DASHBOARD_HOST=0.0.0.0` and
  `PIXEAGLE_DASHBOARD_EXPOSURE_MODE=trusted_lan_legacy` before starting the
  dashboard component.
- Updated README, setup, configuration, installation, troubleshooting,
  Windows, API exposure, remote media, and QGC source-profile docs. Added docs
  guardrails for private-overlay/TLS wording, two-port browser-demo guidance,
  and IPv6 zone-identifier rejection.
- Independent reviewers:
  - security/setup reviewer found query/fragment suffixes were silently
    normalized, malformed bracketed IPv6 could produce raw tracebacks, IPv6
    scope zones created browser Host/CORS ambiguity, and the address test matrix
    was too thin; all were fixed with parser changes and expanded tests;
  - docs/reporting reviewer found that dashboard-only firewall guidance would
    not work with the standalone dashboard, Windows docs advertised a profile
    the launcher did not fully activate, one API doc said "Public
    LAN/private-overlay", and CLI output still said LAN-only; all were fixed.
- Validation completed:
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_setup_profiles.py -q`:
    58 passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_docs_infrastructure_consistency.py -q`:
    22 passed;
  - focused setup/docs/API baseline:
    `PYTHONPATH=src .venv/bin/python -m pytest tests/test_setup_profiles.py tests/test_docs_infrastructure_consistency.py tests/test_api_route_inventory.py tests/unit/core_app/test_parameters_reload.py -q`:
    118 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - `.venv/bin/python tools/generate_api_tool_candidates.py --check`:
    candidate inventory current;
  - touched Python files `py_compile`: passed;
  - `git diff --check`: passed, with the existing Git line-ending warning for
    `scripts/run.bat`.
  - `PYTHON=.venv/bin/python make PYTHON=.venv/bin/python phase0-check`:
    schema current, API tool candidate inventory current, 282 passed with the
    existing Starlette/httpx `TestClient` deprecation warning.
- No browser automation, service install/start, deployment, Docker/PX4/SITL/HIL,
  sidecar mutation, QGC branch mutation/build, runtime MCP endpoint, callable
  tool exposure, field test, or real-aircraft control was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-19-phase-4-lan-overlay-browser-profile-hardening.md`.

## 2026-06-19 Phase 4 Dashboard Browser Session / Media Adversarial Tests

- Continued PXE-0064 by closing the dashboard-side browser-session/media gaps
  that remained after the backend adversarial checkpoint.
- Added `AuthSessionContext` regression tests for auth-failure refresh,
  failed-silent-refresh cleanup, and logout CSRF/local-session cleanup after an
  expired-cookie response.
- Expanded `apiClient` tests so JSON and blob helpers dispatch the shared
  auth-failure event on `401`/`403`, preserve structured payloads, fall back to
  stable `HTTP <status>` errors for non-JSON rejected responses, and enforce
  `media:read` before browser-session WebSocket media creation.
- Hardened `VideoStream` so browser-session HTTP, WebSocket, and WebRTC media
  fail closed without authenticated `media:read`.
- Moved WebRTC authorization ahead of `RTCPeerConnection` construction so a
  denied browser session cannot allocate peer/media state before signaling is
  blocked.
- Added `VideoStream` regression tests for:
  - WebSocket block before socket creation when `media:read` is missing;
  - active WebSocket close after browser-session state loses `media:read`;
  - WebSocket `1008` auth close guidance without reconnect;
  - WebRTC block before peer/signaling creation when `media:read` is missing;
  - HTTP media block before image source render when `media:read` is missing;
  - credentialed HTTP media props for authorized browser-session media.
- Cleaned active docs that still described legacy tracking/control alias
  retirement as pending; those aliases are retired and production remote
  approval now remains gated on trust-boundary and evidence work.
- Independent reviewers:
  - frontend/security reviewer found the HTTP fail-open test gap, WebRTC
    pre-auth peer allocation, missing active-media auth-loss coverage,
    non-JSON `apiFetchJson` gap, and missing no-reconnect assertion; all were
    fixed in runtime/tests;
  - reporting reviewer found missing checkpoint/journal anchors, untracked test
    risk, and stale active docs; all were fixed before final validation.
- Validation completed so far:
  - focused dashboard auth/media/action gate: 29 passed;
  - full dashboard test suite: 96 passed;
  - dashboard production build: passed with the existing CRA/Node `fs.F_OK`
    deprecation warning;
  - focused Python docs/hygiene/route/parameter gate: 63 passed;
  - `PYTHON=.venv/bin/python bash scripts/check_schema.sh`: schema current,
    41 sections, 549 parameters;
  - `git diff --check`: passed;
  - `PYTHON=.venv/bin/python make PYTHON=.venv/bin/python phase0-check`:
    schema current, API tool candidate inventory current, 282 passed with the
    existing Starlette/httpx `TestClient` deprecation warning.
- No browser automation, real media playback success, TLS deployment, QGC
  integration success, service install/start, Docker/PX4/SITL/HIL, sidecar
  mutation, runtime MCP endpoint, callable tool exposure, field test, or
  real-aircraft behavior was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-19-phase-4-dashboard-browser-session-media-adversarial-tests.md`.

## 2026-06-20 Phase 4 Production Remote Profile

- Continued PXE-0064/PXE-0068 with a guarded production remote-browser profile
  that generates PixEagle-side config while keeping backend/dashboard launchers
  loopback behind an operator-managed HTTPS/WSS reverse proxy.
- Added exact TLS Host-authority and CORS generation, Secure browser-session
  cookies, audit enablement, external PBKDF2 user records, controlled one-time
  credential handoff, host/origin validation, dry-run behavior, and a Makefile
  wrapper.
- Made config/credential writes atomic and transactional:
  - rejects config/default/user/handoff collisions, symlinks, and hardlink
    aliases;
  - rolls credentials back when config commit fails;
  - reports incomplete rollback;
  - backs up config and hashed users during explicit rotation;
  - does not archive plaintext handoff files.
- Tightened Host policy so the production profile emits exact `:443` or custom
  TLS authority ports; omitted browser default ports match `:443`, while
  unrelated ports and backend `:5077` are rejected for the public host.
- Production credential generation now fails closed on Windows because Windows
  ACL automation is not evidence-backed; dry-run remains available.
- Added the production remote nginx/TLS/firewall/evidence/rollback runbook and
  removed stale guidance to expose raw dashboard port `3040`.
- Fixed the dashboard `/pixeagle` deployment contract:
  - CRA production assets are relative through `homepage: "."`;
  - follower/settings controls use basename-aware router links instead of
    root-absolute `window.location`.
- Independent security/runtime and docs/product reviewers found transactional,
  authority-port, plaintext backup, firewall/runbook, and dashboard subpath
  defects. The slice iterated until those findings were fixed.
- Validation:
  - focused setup/exposure/docs gate: 213 passed;
  - full dashboard suite: 96 passed;
  - dashboard production build: passed and emitted relative assets;
  - schema current: 41 sections, 549 parameters;
  - API tool candidate inventory current;
  - Python compile, shell syntax, and whitespace checks passed;
  - Phase 0 gate: 335 passed with the existing Starlette/httpx warning.
- No reverse-proxy/service installation, TLS deployment, firewall mutation,
  browser E2E claim, Windows ACL claim, QGC build, Docker/PX4/SITL/HIL, field
  test, runtime MCP endpoint, callable tool exposure, or real-aircraft behavior
  was performed or claimed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-20-phase-4-production-remote-profile.md`.

## 2026-06-20 Phase 4 Production Remote Browser E2E

- Continued PXE-0064/PXE-0068 under PXE-0073 with an opt-in, self-signed local
  HTTPS/Playwright evidence harness for the checked-in `production_remote`
  application boundary.
- Migrated the remaining follower/safety/tracker dashboard modules that rebuilt
  direct backend URLs to the canonical reverse-proxy-aware endpoint registry.
- Removed the unused client contract for the retired safety vehicle-profile
  route.
- Added active browser-session revocation for production MJPEG response
  delivery, video WebSockets, and WebRTC signaling/peers.
- Made WebRTC peer IDs server-owned and signaling capacity atomically reserved.
- Hardened browser evidence with exact host/port/path/query allowlists, expected
  status enforcement, named transition-only cancellation accounting,
  current-checkout dashboard builds, Playwright-managed Chromium provenance,
  bounded subprocess/server cleanup, raw/final upload secret scans, and a fixed
  sanitized upload allowlist.
- Added a manual-only GitHub Actions workflow and explicit local
  `production-remote-browser-install`, dry-run, and execute Make targets.
- Independent security, frontend, and closure reviewers found and drove fixes
  for stale evidence, accepted 404/abort traffic, broad path matching,
  subprocess leaks, active-MJPEG revocation, upload scan ordering, peer-ID
  overwrite, signaling capacity, route-specific queries, stale generated
  provenance, WebRTC evidence wording, and missing Chromium setup guidance.
- Current gates:
  - focused browser/runtime/docs: 57 passed;
  - Phase 0: 353 passed with the existing Starlette/httpx warning;
  - dashboard: 98 passed; lint/build passed; runtime npm audit has zero
    vulnerabilities;
  - full non-SITL: 2321 passed, 40 dlib-dependent skips, and 1 explicit
    deselection;
  - accepted development browser evidence:
    `reports/production-remote-browser/20260620T214336Z-d0664d`;
  - exact clean-revision browser evidence:
    `reports/production-remote-browser/20260620T215137Z-5ce38f`, commit
    `bf32df19ec7f1e8855c1ea934cfb50128a0cf4ea`,
    `git_worktree_clean: true`, matching source hashes, 210 requests, 111
    responses, 9 WebSocket observations, zero page errors, zero unexpected
    request failures, 104 expected route-navigation cancellations, 115
    sanitized audit events, and passing raw/upload secret scans.
- No trusted-certificate/proxy/firewall deployment, service installation,
  QGC build, camera/tracker/follower evidence, PX4/SITL/HIL, field test, or
  real-aircraft behavior was performed or claimed.
- Final checkpoint:
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-06-20-phase-4-production-remote-browser-e2e.md`.

## 2026-06-25 Resume: QGC Direct-Media Draft Gate

- Resumed after usage-limit interruption and re-read the required agent
  operating context: root `README.md`, `docs/README.md`, the proposed
  improvement plan, the modernization blueprint, and the API modernization
  blueprint.
- Recovered the active worktree on
  `codex/modernization-pxe0040-runtime-20260604`: the uncommitted slice is the
  PXE-0070/PXE-0068 QGC authenticated direct-media follow-up, not a stale
  subagent output.
- Verified the PixEagle-side direct-media/profile/auth changes in the repo
  venv:
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_setup_profiles.py tests/unit/core_app/test_api_auth_runtime.py`
    passed: 164 tests;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api_route_inventory.py tests/unit/core_app/test_parameters_reload.py`
    passed: 38 tests;
  - `bash scripts/check_schema.sh` passed with 41 sections and 549 parameters;
  - `git diff --check` passed.
- Checked QGroundControl PR #13594 with `gh pr view`: the PR was open and not
  draft, head `bdc4f85c0`, with mixed CI. Converted it to draft with
  `gh pr ready 13594 --repo mavlink/qgroundcontrol --undo` and verified
  `isDraft: true`.
- Recorded the current QGC evidence boundary:
  - passed checks include check-links, docs, Doxygen, pre-commit, CodeQL,
    custom plugin build, Linux release builds, macOS, iOS, Android, Docker, and
    Windows arm64/arm64-cross release builds;
  - failing checks are `Windows / Build win64_msvc2022_64 Release` during Build
    Setup and `Linux / Test + Coverage linux_gcc_64 Debug` during unit and
    integration test steps.
- Pulled failed QGC job logs:
  - the Windows x64 failure on head `bdc4f85c0` happened before build while
    `aqtinstall` unpacked Qt and reported
    `Bad7zFile: lib/Qt6PositioningQuick.lib`;
  - the Linux failure was in
    `GStreamerTest::_testWebSocketJpegDelivery()` at the appsink sample wait.
- Pushed QGC commit `717f083c5`, which added explicit `image/jpeg` caps to the
  test `appsrc` pipeline and replaced fixed 3000 ms waits with
  `TestTimeout::mediumMs()`.
- Rechecked PR #13594 on QGC head `717f083c5`: the PR stayed draft, Windows
  x64 Release passed, but Linux `Test + Coverage linux_gcc_64 Debug` still
  failed in the WebSocket JPEG delivery sample assertion.
- Pushed QGC commit `27e6f4a12`, which aligns the delivery-test `appsrc`
  settings with the production source, waits for a usable GStreamer pipeline
  state, explicitly flushes/waits for WebSocket bytes, and uses a bounded
  appsink pull timeout.
- Rechecked PR #13594 after the push: the PR stayed draft, head is
  `27e6f4a120cb59da313a1191c2babcf9fe6f465b`. Windows and the broader matrix
  were green except Linux `Test + Coverage linux_gcc_64 Debug`, which no longer
  reported `GStreamerTest::_testWebSocketJpegDelivery()` as the failing test
  but segfaulted in `VideoSettingsTest::_testAuthenticatedTransportPolicy()`
  during fixture teardown.
- Pushed QGC commit `b2f6405a4`, which keeps `VideoSettings` alive until after
  `SettingsFixture` restores saved `Fact` values in the VideoSettings tests.
- Rechecked PR #13594 after the push: the PR stayed draft, head is
  `b2f6405a4e190f01bd468d07aebc52a4947b6d9b`. The unit-test phase passed
  all 156 tests, including the VideoSettings fixture-lifetime path, but Linux
  `Test + Coverage linux_gcc_64 Debug` still failed in the integration
  `GStreamerTest::_testWebSocketJpegDelivery()` appsink sample assertion.
- Pushed QGC commit `d97e3f84e`, which runs the WebSocket JPEG delivery test on
  a dedicated source thread like production and adds a source-side
  `jpegFramePushed` signal emitted only after `gst_app_src_push_buffer`
  succeeds.
- Rechecked PR #13594 after the push: the PR stayed draft, head is
  `d97e3f84e`. All visible platform builds/checks outside Linux coverage were
  green or neutral, and Linux unit tests passed, but Linux integration still
  failed in `GStreamerTest::_testWebSocketJpegDelivery()` at the downstream
  appsink sample assertion.
- Pushed QGC commit `b98848b2c`, which removes the temporary production
  `jpegFramePushed` signal and asserts WebSocket JPEG delivery by observing the
  exact JPEG bytes on the `appsrc` source pad instead of relying on the GitHub
  runner's appsink sample queue.
- Rechecked PR #13594 after the push: the PR stayed draft, head is
  `b98848b2c`, and a fresh CI matrix was queued/in progress at the first
  report time. The 2026-06-26 resume poll later recorded the successful run
  below.
- Updated the checkpoint, phase map, issue register, and streaming/security docs
  so maintainers see PR #13594 as draft, not field-proven, and not
  merge-ready until target receiver/proxy evidence exists.
- Re-ran PixEagle gates after the report update:
  - `git diff --check` passed;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_docs_infrastructure_consistency.py tests/test_test_hygiene.py`
    passed: 27 tests;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 372 tests passing with the existing
    Starlette/httpx warning.
- Opened PXE-0074 as the final release/handoff gate: before any tag, release,
  or handoff, run a clean temporary-directory walkthrough using only the public
  README, bootstrap, setup, update, and configuration docs for beginner demo
  and senior-dev override paths; fix stale, noisy, confusing, or deprecated
  docs/scripts before tagging.
- Independent resume reviewers then found and closed two reporting gaps:
  PXE-0070 needed to be `in_progress` rather than generic `open`, and the phase
  map active note still said to finish a PixEagle-side report commit that had
  already landed.
- The setup/bootstrap preflight for PXE-0068/PXE-0074 found concrete release
  walkthrough probes: macOS advertising versus apt-only init, `venv`/`.venv`
  mismatch, missing `make`/`nc` prerequisites, manual dashboard dotenv
  generation, manual full-requirements installs, optimistic setup summaries,
  port-conflict process termination, and weak verification guidance. These are
  now tracked in
  `docs/reporting/agent-ops/codex-modernization/audits/2026-06-25-setup-bootstrap-clean-walkthrough-preflight.md`.
- On 2026-06-26, rechecked QGC PR #13594 after the interruption:
  - QGC branch `feature/http-websocket-video-streaming` remained clean and
    synced at head `b98848b2c5e9afb5109bd49200c1d9aaa0185e5c`;
  - PR #13594 remained draft/open as intended;
  - Linux run `28184014057` completed successfully, including
    `Release linux_gcc_64`, `Release linux_gcc_arm64`, and
    `Test + Coverage linux_gcc_64 Debug`;
  - the visible PR rollup was successful across Windows, macOS, iOS, Android,
    Docker, Linux, docs, Doxygen, pre-commit, and CodeQL checks, with docs
    deploy skipped and CodeQL/grype annotations neutral;
  - PXE-0070 therefore moves from CI repair to target receiver/proxy/operator
    evidence while the PR stays draft.
- No QGC target playback, TLS proxy/firewall deployment, service installation,
  PX4/SITL/HIL, field test, runtime MCP exposure, or real-aircraft behavior was
  performed or claimed.

## 2026-06-26 Phase 4 Setup Bootstrap Preflight Cleanup

- Continued PXE-0068/PXE-0074 after the clean-walkthrough preflight.
- Removed macOS from the guided one-command install path and added explicit
  `install.sh` fail-fast behavior for macOS because the maintained init path is
  apt-oriented.
- Made the Makefile use `.venv/bin/python`, then init-created
  `venv/bin/python`, then system `python3`.
- Added `make` to Linux public prerequisites and to the init package set.
- Rewrote the manual install docs so Python dependencies follow the same
  core-first split as `scripts/init.sh`, and dashboard `.env` creation uses a
  YAML-to-dotenv conversion rather than copying YAML into dotenv.
- Hardened `scripts/run.sh` so port cleanup kills only PixEagle-owned listener
  PIDs and blocks startup on foreign port occupants; readiness now falls back
  to a Python socket probe when `nc` is absent.
- Added static regressions for the Python fallback, port-owner guard, macOS
  bootstrap boundary, manual core/AI dependency split, and dashboard dotenv
  conversion.
- Validation before final reporting:
  - `bash -n scripts/run.sh scripts/init.sh install.sh` passed;
  - `git diff --check` passed;
  - focused setup/docs gate passed: 150 tests;
  - new focused static regressions passed: 4 tests;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 376 tests passing with the
    existing Starlette/httpx warning.
- No clean temp checkout, binary download/install, service installation,
  deployment, target proxy/firewall/TLS setup, QGC playback, PX4/SITL/HIL,
  field test, runtime MCP exposure, or real-aircraft behavior was performed or
  claimed.

## 2026-06-26 Phase 4 Init Summary Precision

- Continued PXE-0068 by closing the setup-summary precision item from the
  clean-walkthrough preflight.
- `scripts/init.sh` now carries explicit component state into the final summary
  for Node.js, dashboard dependencies, configuration defaults, dashboard `.env`,
  MAVSDK Server, and MAVLink2REST instead of inferring dashboard readiness from
  `node -v` or binary readiness from file presence.
- Dashboard dependency outcomes now distinguish missing dashboard directory
  (`skipped`), missing npm (`manual follow-up`), npm install failure
  (`degraded`), and successful install (`ready`).
- MAVSDK Server and MAVLink2REST outcomes now distinguish verified/downloaded
  (`ready`), operator-deferred download (`skipped`), checksum/download failure
  (`degraded`), and missing downloader script (`manual follow-up`).
- `install.sh` no longer prints a blanket "Installation Complete" message; it
  prints "Bootstrap Finished" and tells users to review non-ready init-summary
  items before running services.
- README, installation docs, and setup-profile docs now describe the component
  readiness summary and state vocabulary.
- A read-only reviewer independently flagged the same dashboard/binary summary
  overstatement plus the one-line installer wording; those findings were closed
  in this slice.
- Validation before final reporting:
  - `bash -n scripts/init.sh install.sh` passed;
  - focused setup-summary regressions passed: 4 tests;
  - focused setup/docs gate passed: 153 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 379 tests passing with the
    existing Starlette/httpx warning.
- No clean temp checkout, binary download/install, service installation,
  deployment, target proxy/firewall/TLS setup, QGC playback, PX4/SITL/HIL,
  field test, runtime MCP exposure, or real-aircraft behavior was performed or
  claimed.

## 2026-06-26 Phase 4 Legacy Config Defaults-Sync Boundary

- Continued PXE-0008 with a behavior-preserving legacy config defaults-sync
  boundary.
- Added `src/classes/api_legacy_config_sync.py` and moved the legacy
  `/api/config/defaults-sync*` request model plus report and dry-run plan
  helpers out of `FastAPIHandler`.
- Kept legacy route registration, auth/security policy, response payload shape,
  apply execution, backup/save/rollback, and reload behavior unchanged.
- Added direct helper tests for schema/default union reporting, operation
  normalization, invalid operations, and skipped operations.
- Added route-inventory guardrails so defaults-sync report/plan semantics do
  not drift back into the handler monolith.
- Independent end-of-slice review found one missed provenance guard: the
  generated API/MCP candidate inventory initially hashed `fastapi_handler.py`
  but omitted the new legacy config-sync helper that owns the moved request
  model and helper semantics. Added `api_legacy_config_sync.py` to
  `tools/generate_api_tool_candidates.py`, regenerated the inventory, and
  expanded `tests/test_api_tool_candidates.py` so future helper changes drift
  the inventory.
- Updated API modernization blueprint, issue register, phase map, and
  checkpoint reporting.
- Independent API review recommended the next PXE-0008 slice: extract legacy
  `/api/models/*` and `/api/yolo/*` route bodies into
  `api_legacy_model_routes.py` without changing routes, aliases, or
  auth/security policy.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests;
  - focused API/security/helper gate passed: 51 tests;
  - py_compile passed again after provenance hardening for the touched helper,
    handler, generator, route-inventory tests, candidate tests, and helper
    tests;
  - `.venv/bin/python tools/generate_api_tool_candidates.py` updated the
    non-callable API/MCP candidate inventory source hash for
    `src/classes/fastapi_handler.py` and added
    `src/classes/api_legacy_config_sync.py` to generated provenance;
  - focused API/security/helper/candidate gate passed after provenance
    hardening: 60 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 380 tests passing with the
    existing Starlette/httpx warning.
- No `/api/v1/config` route, legacy alias retirement, service/deployment
  action, target proxy/firewall/TLS setup, QGC playback, PX4/SITL/HIL, field
  test, runtime MCP exposure, or real-aircraft behavior was performed or
  claimed.

## 2026-06-26 Phase 4 Legacy Model Route Boundary

- Continued PXE-0008 with the queued legacy `/api/models/*` and deprecated
  `/api/yolo/*` route-body extraction.
- Added `src/classes/api_legacy_model_routes.py` and moved model-list,
  active-model, label pagination/search, model file download, switch, upload,
  download, delete, runtime model resolution, active summary, and standby
  SmartTracker persistence helper logic out of `FastAPIHandler`.
- Kept public route registrations, canonical `/api/models/*` paths,
  deprecated `/api/yolo/*` aliases, route security policy, model-manager calls,
  and response payload shapes unchanged.
- Added static route-inventory guardrails proving model/yolo route wrappers
  delegate to the helper and model route strings stay out of the handler
  monolith.
- Added direct helper tests for active-model summaries, runtime NCNN-to-PT name
  resolution, force-rescan propagation, label search/pagination, and NCNN
  standby path preference.
- Added `api_legacy_model_routes.py` to generated non-callable API/MCP
  candidate provenance and candidate inventory tests.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests/generator;
  - generated candidate inventory was regenerated;
  - focused model-route/API/security/candidate gate passed: 62 tests.
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 381 tests passing with the
    existing Starlette/httpx warning.
- No `/api/v1/models/*` route, `/api/yolo/*` alias retirement, model behavior
  change, service/deployment action, target proxy/firewall/TLS setup, QGC
  playback, PX4/SITL/HIL, field test, runtime MCP exposure, or real-aircraft
  behavior was performed or claimed.

## 2026-06-26 Phase 4 Legacy Config Mutation Boundary

- Continued PXE-0008 with the next legacy config API monolith-reduction slice.
- Added `src/classes/api_legacy_config_routes.py` and moved legacy
  parameter/section update, validation, defaults-sync apply, revert, backup
  restore, and import route bodies out of `FastAPIHandler`.
- Kept route registration, route inventory, auth/security policy, legacy paths,
  response payload shapes, rate-limit behavior, save/reload sequencing,
  defaults-sync rollback behavior, and import/revert/restore semantics
  unchanged.
- Kept `api_legacy_config_sync.py` focused on pure defaults-sync report/plan
  helpers; the new route helper owns HTTP compatibility behavior and runtime
  save/reload orchestration.
- Added helper tests for config-write 429 response shape, validation
  short-circuit, immediate versus restart-tier `applied` reporting, section
  highest-tier selection, validation request parsing, defaults-sync apply
  success/invalid/failure rollback, revert/restore/import reload contracts, and
  import without hot reload.
- Added static route-inventory guardrails proving the moved config mutation
  methods are one-call handler delegates and that the route helper owns the
  response/rollback strings.
- Added `api_legacy_config_routes.py` to generated non-callable API/MCP
  candidate provenance and candidate inventory tests.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests/generator;
  - generated candidate inventory was regenerated;
  - focused config-route/API/security/candidate gate passed: 72 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 382 tests passing with the
    existing Starlette/httpx warning.
- No `/api/v1/config/*` route, legacy config alias retirement, service/
  deployment action, target proxy/firewall/TLS setup, QGC playback, PX4/SITL/
  HIL, field test, runtime MCP exposure, or real-aircraft behavior was
  performed or claimed.

## 2026-06-27 Phase 4 Legacy Config Read Boundary

- Continued PXE-0008 by finishing the remaining legacy config route-body
  extraction after the mutation boundary.
- Moved legacy config schema/current/default reads, section/category listing,
  diff/compare, defaults-sync read/plan route bodies, backup history, export,
  search, and audit HTTP compatibility behavior into
  `src/classes/api_legacy_config_routes.py`.
- Kept route registration, route inventory, auth/security policy, legacy paths,
  response payload shapes, query parsing, broad legacy 500 error behavior, and
  defaults-sync baseline initialization side effect unchanged.
- Preserved odd legacy compatibility behavior explicitly in tests: missing
  section schema returns 404, missing current/default sections return success
  with `{}`, invalid integer query parsing becomes HTTP 500, and invalid
  defaults-sync dry-run plans still return HTTP 200 with `success: true`.
- Kept `api_legacy_config_sync.py` focused on defaults-sync report/plan
  computation, with `api_legacy_config_routes.py` owning HTTP response shaping
  and side effects.
- Expanded route-inventory guardrails so all legacy config read, mutation,
  defaults-sync, backup/export/search/audit wrappers stay as one-call handler
  delegates and the route helper owns the response/error strings.
- Regenerated non-callable API/MCP candidate provenance because
  `fastapi_handler.py` and `api_legacy_config_routes.py` hashes changed.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests;
  - generated candidate inventory was regenerated;
  - focused config-route/API/security/candidate gate passed: 77 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 382 tests passing with the
    existing Starlette/httpx warning.
- No `/api/v1/config/*` route, legacy config alias retirement, service/
  deployment action, target proxy/firewall/TLS setup, QGC playback, PX4/SITL/
  HIL, field test, runtime MCP exposure, or real-aircraft behavior was
  performed or claimed.

## 2026-06-27 Phase 4 Legacy Recording Route Boundary

- Continued PXE-0008 with the next legacy API monolith-reduction slice.
- Added `src/classes/api_legacy_recording_routes.py` and moved legacy
  recording start/pause/resume/stop/status/toggle, recordings list/download/
  delete, storage status, and include-OSD route bodies out of
  `FastAPIHandler`.
- Kept route registration, route inventory, auth/security policy, legacy paths,
  response payload shapes, source dimension probing, file download media types,
  Range response headers, path basename sanitization, delete error mapping, and
  include-OSD truthy parsing unchanged.
- Added direct helper tests for source dimension probing, manager-unavailable
  503 responses, status/storage/list shapes, full-file and Range download
  headers, missing/bad-range error mappings, delete 404/400 mappings, and
  include-OSD toggling.
- Added route-inventory guardrails proving all legacy recording methods are
  one-call handler delegates and that the helper owns recording response/error
  strings.
- Added `api_legacy_recording_routes.py` to generated non-callable API/MCP
  candidate provenance and candidate inventory tests.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests/generator;
  - generated candidate inventory was regenerated;
  - focused recording-route/API/security/candidate gate passed: 66 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 383 tests passing with the
    existing Starlette/httpx warning.
- No `/api/v1/recordings/*` route, legacy recording alias retirement, service/
  deployment action, target proxy/firewall/TLS setup, QGC playback, PX4/SITL/
  HIL, field test, runtime MCP exposure, or real-aircraft behavior was
  performed or claimed.

## 2026-06-27 Phase 4 Legacy OSD Route Boundary

- Continued PXE-0008 with the next legacy API monolith-reduction slice.
- Added `src/classes/api_legacy_osd_routes.py` and moved legacy OSD
  status/toggle, preset listing/loading, color-mode switching, and mode status
  route bodies out of `FastAPIHandler`.
- Kept route registration, route inventory, auth/security policy, legacy
  paths, response payload shapes, preset directory lookup, preset-name
  allowlist validation, cache invalidation reasons, renderer reinitialization,
  and existing legacy error mappings unchanged.
- Preserved the legacy unavailable-toggle behavior explicitly in tests: the
  route raises an inner 503 but the old broad exception wrapper maps it to HTTP
  500. Typed `/api/v1/osd/*` should correct that in a separate promotion slice.
- Added direct helper tests for status payloads, toggle/cache behavior,
  unavailable status/toggle semantics, preset listing/loading/missing/invalid
  cases, mode-manager responses, invalid color mode, and failed mode switching.
- Added route-inventory guardrails proving all legacy OSD methods are one-call
  handler delegates and that the helper owns OSD response/error strings.
- Added `api_legacy_osd_routes.py` to generated non-callable API/MCP candidate
  provenance and candidate inventory tests.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests/generator;
  - generated candidate inventory was regenerated;
  - focused OSD-route/API/security/candidate gate passed: 67 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 384 tests passing with the
    existing Starlette/httpx warning.
- No `/api/v1/osd/*` route, legacy OSD alias retirement, service/deployment
  action, target proxy/firewall/TLS setup, QGC playback, PX4/SITL/HIL, field
  test, runtime MCP exposure, or real-aircraft behavior was performed or
  claimed.

## 2026-06-27 Phase 4 Legacy GStreamer Route Boundary

- Continued PXE-0008 with the next legacy API monolith-reduction slice.
- Added `src/classes/api_legacy_gstreamer_routes.py` and moved legacy
  GStreamer status and runtime toggle route bodies out of `FastAPIHandler`.
- Kept route registration, route inventory, auth/security policy, legacy
  paths, response payload shapes, process-local writer/open detection, QGC
  UDP/RTP setup hints, existing handler reuse, new handler creation,
  `Parameters.ENABLE_GSTREAMER_STREAM` mutation, and failed-open HTTP 500
  response shape unchanged.
- Added direct helper tests for inactive and active status payloads, active
  stop behavior, handler creation and successful start, inactive failed-open
  500 response, and initialization exception HTTP 500 mapping.
- Added route-inventory guardrails proving both legacy GStreamer methods are
  one-call handler delegates and that the helper owns GStreamer response/error
  strings.
- Added `api_legacy_gstreamer_routes.py` to generated non-callable API/MCP
  candidate provenance and candidate inventory tests.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests/generator;
  - generated candidate inventory was regenerated;
  - focused GStreamer-route/API/security/candidate gate passed: 68 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 385 tests passing with the
    existing Starlette/httpx warning.
- No `/api/v1/streams/gstreamer*` route, legacy GStreamer alias retirement,
  service/deployment action, target proxy/firewall/TLS setup, QGC playback,
  PX4/SITL/HIL, field test, runtime MCP exposure, or real-aircraft behavior was
  performed or claimed.

## 2026-06-28 Phase 4 Legacy Follower Profile Route Boundary

- Continued PXE-0008 with the next legacy API monolith-reduction slice.
- Added `src/classes/api_legacy_follower_routes.py` and moved legacy follower
  schema, profile listing, current profile, switch-profile, configured-mode,
  setpoints-status, and current-mode route bodies out of `FastAPIHandler`.
- Kept route registration, route inventory, auth/security policy, legacy paths,
  response payload shapes, active-versus-configured profile behavior, profile
  validation quirks, `Parameters.FOLLOWER_MODE` mutation behavior,
  setpoint-status compatibility fields, OffboardCommander publication summary,
  and safety-limit summary lookup unchanged.
- Left follower health, restart, and config-manager route bodies in
  `FastAPIHandler` for a later follower sub-slice; this checkpoint does not
  claim that the entire follower legacy API boundary is complete.
- Added direct helper tests for schema/profile payloads, active and configured
  current-profile responses, unknown configured profile errors, configured and
  active switch behavior, invalid/missing profile errors, active switch failure,
  configured/current-mode payloads, inactive/active setpoint status publication
  fields, and missing setpoint-handler errors.
- Added route-inventory guardrails proving the moved follower methods are
  one-call handler delegates and that the helper owns follower profile/setpoint
  response/error strings.
- Added `api_legacy_follower_routes.py` to generated non-callable API/MCP
  candidate provenance and candidate inventory tests.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests/generator;
  - generated candidate inventory was regenerated;
  - focused follower-route/API/security/candidate gate passed: 72 tests;
  - a first resumed Phase 0 attempt caught a local test-hygiene issue in the
    new follower test fake (`return False`); the test helper was fixed without
    production behavior changes;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 386 tests passing with the existing
    Starlette/httpx warning.
- Independent reviewer `Bernoulli` reported no blockers and confirmed the
  tracked residual risks are limited to intentionally preserved legacy behavior:
  broad validation wrapping, direct `Parameters.FOLLOWER_MODE` mutation,
  process-local-only setpoint status, and no new ASGI path-level coverage for
  all seven compatibility routes.
- No `/api/v1/following/*` or `/api/v1/follower/*` route, complete follower
  boundary, legacy follower alias retirement, service/deployment action, target
  proxy/firewall/TLS setup, QGC playback, PX4/SITL/HIL, field test, runtime MCP
  exposure, or real-aircraft behavior was performed or claimed.

## 2026-06-28 Phase 4 Legacy Follower Route Boundary

- Continued PXE-0008 immediately after the follower profile/setpoint boundary.
- Moved the remaining legacy follower health, restart, general config, and
  effective config route bodies into `src/classes/api_legacy_follower_routes.py`,
  completing current `/api/follower/*` route-body extraction.
- Kept route registration, route inventory, auth/security policy, legacy paths,
  response payload shapes, follower health resource diagnostics,
  OffboardCommander degraded-health semantics, restart rate limiting,
  `Parameters.reload_config()` behavior, active follower stop/start behavior,
  and follower config manager response shapes unchanged.
- Preserved legacy compatibility rough edges explicitly: restart still executes
  reload/stop/start in the request path, follower health remains process-local
  evidence only, config-manager reads still expose `_general` and `_overrides`,
  and all responses remain ad hoc compatibility payloads rather than typed
  `/api/v1` contracts.
- Added direct helper tests for degraded commander health, inactive resource
  cleanup/error reporting, restart rate limiting, inactive config reload, active
  follower restart stop/start, config general/effective read payloads, and
  config-manager error mapping.
- Expanded route-inventory guardrails so all current `/api/follower/*` handler
  methods stay one-call delegates and the helper owns follower response/error
  strings.
- Regenerated non-callable API/MCP candidate provenance because
  `api_legacy_follower_routes.py` and `fastapi_handler.py` hashes changed.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests/generator;
  - generated candidate inventory was regenerated;
  - focused follower-route/API/security/candidate gate passed: 78 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 386 tests passing with the existing
    Starlette/httpx warning.
- No typed `/api/v1/following/*` or `/api/v1/follower/*` route, legacy follower
  alias retirement, service/deployment action, target proxy/firewall/TLS setup,
  QGC playback, PX4/SITL/HIL, field test, runtime MCP exposure, or real-aircraft
  behavior was performed or claimed.
- Remaining PXE-0008 legacy route-body cleanup now focuses on safety and
  video/media families before typed `/api/v1` promotion or compatibility
  retirement.

## 2026-06-29 Phase 4 Legacy Safety Read Route Boundary

- Continued PXE-0008 with the read-only safety and safety-adjacent route-body
  boundary.
- Added `src/classes/api_legacy_safety_routes.py` and moved legacy
  circuit-breaker status/statistics, safety config, follower safety limits,
  effective-limit summary, and relevant-section route bodies out of
  `FastAPIHandler`.
- Kept route registration, route inventory, auth/security policy, legacy paths,
  response payload shapes, SafetyManager fallback behavior, circuit-breaker read
  diagnostics, rate-limit radians-to-degrees conversion, section relevance
  mapping, and legacy broad exception wrapping unchanged.
- Left circuit-breaker toggle, safety-bypass toggle, and statistics reset in
  `FastAPIHandler` because they are mutations and need a separate guarded
  action/deprecation cleanup rather than a read-only helper boundary. The
  follow-up legacy circuit-breaker mutation boundary below closes the
  route-body move for those endpoints.
- Added direct helper tests for circuit-breaker status/statistics payloads,
  circuit-breaker unavailable legacy behavior, safety config available and
  unavailable payloads, follower safety limits through SafetyManager and
  fallback `Parameters.get_effective_limit()`, effective-limit/relevant-section
  payloads, default follower-mode handling, and 500 error mapping.
- Added route-inventory guardrails so the moved safety/circuit-breaker/config
  read methods stay one-call handler delegates and the helper owns the legacy
  response/error strings.
- Regenerated non-callable API/MCP candidate provenance because
  `api_legacy_safety_routes.py` was added and `fastapi_handler.py` changed.
- Updated API docs so `GET /api/safety/config`,
  `GET /api/safety/limits/{follower_name}`, and
  `GET /api/circuit-breaker/status` examples match current compatibility
  payloads.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests/generator;
  - generated candidate inventory was regenerated;
  - focused safety-route/API/security/candidate gate passed: 72 tests;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 387 tests passing with the existing
    Starlette/httpx warning.
- No typed `/api/v1/safety/*` route, circuit-breaker mutation redesign,
  compatibility alias retirement, service/deployment action, target
  proxy/firewall/TLS setup, QGC playback, PX4/SITL/HIL, field test, runtime MCP
  exposure, or real-aircraft behavior was performed or claimed.
- At that checkpoint, remaining PXE-0008 legacy route-body cleanup focused on
  video/media route families plus the separate circuit-breaker mutation
  route-body move closed by the follow-up boundary below.

## 2026-06-29 Phase 4 Legacy Media Status Route Boundary

- Continued PXE-0008 with the bounded media observability route-body boundary.
- Added `src/classes/api_legacy_media_routes.py` and moved legacy streaming
  status, streaming stats, and video health route bodies out of
  `FastAPIHandler`.
- Kept route registration, route inventory, auth/security policy, legacy paths,
  response payload shapes, active transport selection, quality-engine state,
  GStreamer encoder status, pipeline metrics, OSD pipeline stat fallback, video
  connection-health response, SmartTracker/OBB diagnostics, and legacy video
  health error mapping unchanged.
- At this checkpoint, left `POST /api/video/reconnect` in `FastAPIHandler`
  because it is a live media mutation that calls
  `video_handler.force_recovery()` and needed its own cleanup slice; the
  follow-up reconnect boundary below closes that route-body move.
- Left `GET /video_feed`, `WS /ws/video_feed`, and WebRTC signaling outside this
  helper because they own long-lived generator/task/session-revocation and peer
  cleanup behavior.
- Added direct helper tests for streaming status active-method selection and
  config payloads, streaming stats client/cache/uptime/OSD payloads, OSD stats
  fallback logging, video health/OBB payloads, unavailable video state, and
  video-health 500 error mapping.
- Added route-inventory guardrails so the moved media observability methods stay
  one-call handler delegates and the helper owns legacy response/error strings.
- Regenerated non-callable API/MCP candidate provenance because
  `api_legacy_media_routes.py` was added and `fastapi_handler.py` changed.
- Independent media/API review found no blockers. The reviewer noted possible
  legacy OpenAPI description drift if wrapper docstrings were dropped, so the
  wrapper docstrings were restored while keeping one-call delegate bodies.
- Validation:
  - py_compile passed for touched Python modules/tests/generator;
  - focused media-route/API/security/docs gate passed with 72 tests;
  - generated candidate inventory was regenerated;
  - `git diff --check` passed;
  - full Phase 0 passed with 388 tests and one existing Starlette/httpx
    deprecation warning.
- No typed `/api/v1/streams/*` route, media reconnect mutation redesign,
  long-lived transport route move, compatibility alias retirement, service or
  deployment action, QGC playback, PX4/SITL/HIL, field test, runtime MCP
  exposure, or real-aircraft behavior was performed or claimed.
- At that checkpoint, remaining PXE-0008 legacy route-body cleanup focused on
  media reconnect, long-lived media transport bodies, and the separate
  circuit-breaker mutation route-body move closed by the follow-up boundary
  below.

## 2026-06-29 Phase 4 Legacy Media Reconnect Route Boundary

- Continued PXE-0008 with the legacy media reconnect mutation boundary.
- Added `reconnect_video(handler)` to `src/classes/api_legacy_media_routes.py`
  and moved the `POST /api/video/reconnect` route body out of
  `FastAPIHandler`.
- Preserved route registration, auth/security policy, legacy path and method,
  wrapper docstring, `video_handler.force_recovery()` call, updated video health
  reporting, HTTP 200 success response, HTTP 503 failed recovery response, HTTP
  503 missing-video-handler response, and HTTP 500 unexpected-error mapping.
- At this checkpoint, kept `GET /video_feed`, `WS /ws/video_feed`, and WebRTC
  signaling in `FastAPIHandler` because they own long-lived
  generator/task/session-revocation and peer cleanup behavior; the follow-up
  HTTP boundary below closes the `GET /video_feed` route-body move.
- Added direct helper tests for reconnect success, failed recovery, missing
  video handler, and unexpected video health failure.
- Updated route-inventory guardrails so reconnect body strings stay out of
  `FastAPIHandler`, while long-lived transport handlers remain outside the
  media helper.
- Regenerated non-callable API/MCP candidate provenance because
  `api_legacy_media_routes.py` and `fastapi_handler.py` changed.
- Updated API/core/video docs and modernization reports so reconnect is no
  longer listed as remaining route-body debt.
- Independent media/API review found no code blockers and verified reconnect
  behavior, wrapper registration/docstring, unchanged security policy, and
  generated candidate inventory. The reviewer noted no ASGI-level reconnect
  route test and no exact logger-message assertion for unexpected reconnect
  exceptions as low-risk residual gaps.
- Validation:
  - generated candidate inventory was regenerated;
  - py_compile passed for touched Python modules/tests/generator;
  - focused media/API/security/docs gate passed with 75 tests;
  - `git diff --check` passed;
  - full Phase 0 passed with 388 tests and one existing Starlette/httpx
    deprecation warning.
- No typed `/api/v1/streams/*` action promotion, action-store/idempotency
  redesign, long-lived transport route move, compatibility alias retirement,
  service or deployment action, QGC playback, PX4/SITL/HIL, field test, runtime
  MCP exposure, or real-aircraft behavior was performed or claimed.
- At that checkpoint, remaining PXE-0008 legacy route-body cleanup focused on
  long-lived media transport bodies and the separate circuit-breaker mutation
  route-body move closed by the follow-up boundary below.

## 2026-06-29 Phase 4 Legacy Media HTTP Route Boundary

- Continued PXE-0008 with the legacy HTTP MJPEG route boundary.
- Added `video_feed(handler, request)` to
  `src/classes/api_legacy_media_routes.py` and moved the `GET /video_feed`
  route body out of `FastAPIHandler`.
- Moved `SessionBoundStreamingResponse` into `api_legacy_media_routes.py`
  because it is only used by the legacy HTTP MJPEG route.
- Preserved route registration, auth/security policy, legacy path and method,
  wrapper docstring, streaming-disabled HTTP 503 behavior, max-connection HTTP
  503 behavior, frame publisher and quality-engine registration, duplicate-frame
  skipping, frame-id cache encoding, adaptive quality, frame stats, frame-drop
  accounting, browser-session revocation termination, media session-revoked
  audit recording, and generator cleanup/unregister behavior.
- Kept `WS /ws/video_feed` and WebRTC signaling outside this slice because they
  own bidirectional task orchestration, peer state, and close-path behavior.
- Updated route-inventory guardrails so HTTP MJPEG body markers stay out of
  `FastAPIHandler`, while the optimized WebSocket media handler remains outside
  the helper.
- Regenerated non-callable API/MCP candidate provenance because
  `api_legacy_media_routes.py` and `fastapi_handler.py` changed.
- Updated API/core docs, issue register, previous media checkpoints, and phase
  map so `/video_feed` is no longer listed as remaining route-body debt.
- Validation:
  - generated candidate inventory was regenerated;
  - py_compile passed for touched Python modules/tests;
  - focused media/API/HTTP-lifecycle/docs gate passed with 60 tests and one
    existing Starlette/httpx deprecation warning;
  - `git diff --check` passed;
  - full Phase 0 passed with 388 tests and one existing Starlette/httpx
    deprecation warning;
- Independent media/API review found no code blockers and verified wrapper
  delegation, unchanged security policy/MCP exposure, preserved HTTP MJPEG
  generator/session-revocation behavior, and unchanged WebSocket/WebRTC
  ownership. The reviewer noted no direct helper test and no ASGI path-level
  `/video_feed` max-connection/adaptive-quality/stats/drop test as low-risk
  residual gaps for this extraction boundary.
- No typed `/api/v1/streams/*` promotion, compatibility alias retirement,
  service or deployment action, QGC playback, PX4/SITL/HIL, field test, runtime
  MCP exposure, or real-aircraft behavior was performed or claimed.
- At that checkpoint, remaining PXE-0008 legacy route-body cleanup focused on
  video WebSocket/WebRTC transport bodies and the separate circuit-breaker
  mutation route-body move closed by the follow-up boundary below. The follow-up
  WebSocket boundary below closes the `WS /ws/video_feed` route-body move.

## 2026-06-29 Phase 4 Legacy Media WebSocket Route Boundary

- Continued PXE-0008 with the legacy video WebSocket route boundary.
- Added `ClientConnection` and `video_feed_websocket_optimized(handler,
  websocket)` to `src/classes/api_legacy_media_routes.py` and moved the `WS
  /ws/video_feed` route body out of `FastAPIHandler`.
- Preserved route registration, auth/security policy, legacy path and method,
  wrapper docstring, streaming-disabled pre-accept close, Host/Origin pre-accept
  rejection and audit event, WebSocket authorization before accept, audit-failure
  close, current accept-then-capacity behavior, client principal/quality/frame
  queue registration, frame-publisher and quality-engine registration, send /
  receive / session-monitor task orchestration, browser-session revocation
  closure, and final cleanup through the existing idempotent cleanup helper.
- Kept `_ws_send_frames`, `_ws_receive_messages`, `_ws_monitor_session`,
  `_cleanup_websocket_client`, heartbeat stale-close, shutdown close-all, and
  WebRTC signaling outside this route-body slice because they are shared
  streaming lifecycle or peer-state code.
- Added direct production-helper tests for WebSocket JSON metadata plus binary
  JPEG delivery, quality-command and ping/pong handling, and three-error
  send-loop termination.
- Fixed WebSocket dropped-frame accounting so three consecutive send errors
  count as exactly three drops instead of five.
- Updated API/core/video docs to describe the actual WebSocket wire envelope:
  one JSON metadata message followed by one binary JPEG message per frame.
- Updated route-inventory and exposure-default guardrails so the WebSocket route
  body and pre-accept Host/Origin check are owned by `api_legacy_media_routes.py`.
- Regenerated non-callable API/MCP candidate provenance because
  `api_legacy_media_routes.py` and `fastapi_handler.py` changed.
- Read-only media/API review found no extraction blockers, identified the
  pre-accept security, accept-then-capacity, registration, task orchestration,
  cleanup, frame-envelope, quality/ping, and session-revocation invariants to
  preserve, and recommended the direct send/receive tests added in this slice.
- Final read-only media/API review found no blockers after implementation and
  verified unchanged route registration, one-call wrapper delegation,
  pre-accept security gates, safe `ClientConnection` ownership, unchanged
  security/MCP exposure, correct drop-count fix, accurate
  JSON-metadata-plus-binary-JPEG docs, and remaining debt alignment.
- Validation:
  - generated candidate inventory was regenerated;
  - py_compile passed for touched Python modules/tests;
  - focused WebSocket/media/security gate passed with 14 tests and one existing
    Starlette/httpx deprecation warning;
  - focused docs/API guardrails passed with 12 tests;
  - `git diff --check` passed;
  - full Phase 0 passed with 388 tests and one existing Starlette/httpx
    deprecation warning.
- No typed `/api/v1/streams/*` promotion, WebRTC signaling move, compatibility
  alias retirement, service or deployment action, QGC playback, PX4/SITL/HIL,
  field test, runtime MCP exposure, or real-aircraft behavior was performed or
  claimed.
- At that checkpoint, remaining PXE-0008 legacy route-body cleanup focused on
  WebRTC signaling route-body ownership; the follow-up WebRTC boundary below
  closes that record before typed `/api/v1` promotion or compatibility
  retirement.

## 2026-06-29 Phase 4 Legacy WebRTC Signaling Boundary

- Continued PXE-0008 with the legacy WebRTC signaling route-boundary record.
- Confirmed that `WS /ws/webrtc_signaling` is already registered directly to
  `WebRTCManager.signaling_handler`, so no runtime route-body move was needed.
- Added static route-inventory guardrails proving `FastAPIHandler` only
  registers `self.webrtc_manager.signaling_handler` for that path while
  `src/classes/webrtc_manager.py` owns signaling state-machine markers:
  pre-accept authorization and audit, capacity, server-owned peer IDs, SDP/ICE
  error handling, session revocation, and peer cleanup.
- Added `src/classes/webrtc_manager.py` to generated non-callable API/MCP
  candidate provenance so future signaling changes drift the reviewed
  inventory.
- Added a focused WebRTC signaling path test proving disabled audit for an
  allowed media-read request follows the existing policy and reaches the
  accept-then-capacity gate instead of silently changing route behavior.
- Updated WebRTC/API docs and route-inventory docs to describe the current
  manager ownership and current route counts.
- Pre-implementation read-only review found no hard blocker and identified the
  invariants preserved in this slice: exact route and security classification,
  pre-accept streaming/Host/Origin/auth/audit gates, accept-then-capacity
  behavior, server-owned peer IDs, browser-session revocation, bounded cleanup,
  SDP/ICE handling, provenance, and no typed/MCP/SITL/field claims.
- Validation:
  - py_compile passed for touched Python tools/tests;
  - generated candidate inventory was regenerated;
  - focused WebRTC/API/docs gate passed with 20 tests and one existing
    Starlette/httpx warning;
  - broader API/security/docs/WebRTC lifecycle gate passed with 192 tests and
    the same warning;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 390 tests passing with the existing
    Starlette/httpx warning.
- Final independent review found no blockers, verified the WebRTC route
  ownership/security/provenance invariants, and noted only that the new
  checkpoint file was still untracked before staging.
- No typed `/api/v1/streams/*` WebRTC route, signaling protocol redesign,
  compatibility alias retirement, service/deployment action, target
  proxy/firewall/TLS setup, QGC playback, WebRTC receipt claim, PX4/SITL/HIL,
  field test, runtime MCP exposure, or real-aircraft behavior was performed or
  claimed.
- Remaining PXE-0008 API work now focuses on typed `/api/v1` replacement and
  compatibility-retirement planning while keeping static route-boundary
  guardrails current.

## 2026-06-29 Phase 4 Legacy Circuit-Breaker Mutation Boundary

- Continued PXE-0008 with the legacy circuit-breaker mutation route-body
  boundary.
- Added circuit-breaker mutation helpers to
  `src/classes/api_legacy_safety_routes.py` and moved the route bodies for
  `POST /api/circuit-breaker/toggle`,
  `POST /api/circuit-breaker/toggle-safety`, and
  `POST /api/circuit-breaker/reset-statistics` out of `FastAPIHandler`.
- Preserved route registration, auth/security policy, legacy paths, wrapper
  docstrings, process-local `Parameters.FOLLOWER_CIRCUIT_BREAKER` mutation,
  reset-on-enable behavior, process-local
  `Parameters.CIRCUIT_BREAKER_DISABLE_SAFETY` mutation, safety-bypass
  effective-state reporting, statistics reset payload shape, logging side
  effects, and legacy broad 503-to-500 unavailable error wrapping.
- Added direct helper tests for enable/reset, disable/no-reset,
  safety-bypass active and inactive effectiveness, statistics reset payloads,
  and unavailable circuit-breaker legacy error shape.
- Expanded route-inventory guardrails so circuit-breaker mutation body strings
  stay out of `FastAPIHandler` while the wrappers remain one-call delegates.
- Regenerated non-callable API/MCP candidate provenance because
  `api_legacy_safety_routes.py` and `fastapi_handler.py` changed.
- Updated modernization docs, the safety-read checkpoint, issue register, and
  phase map so circuit-breaker mutations are no longer listed as remaining
  route-body cleanup debt.
- Validation before final reporting:
  - py_compile passed for touched Python modules/tests;
  - generated candidate inventory was regenerated;
  - focused safety/API/security/candidate/docs gate passed with 44 tests;
  - final independent review found no blockers and independently ran focused
    safety/API/candidate and security-policy gates with 60 and 19 tests passing;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    API/MCP candidate inventory current, and 388 tests passing with the existing
    Starlette/httpx warning.
- No typed `/api/v1/safety/*` action, idempotency/action-store redesign,
  compatibility alias retirement, service/deployment action, target
  proxy/firewall/TLS setup, QGC playback, PX4/SITL/HIL, field test, runtime MCP
  exposure, or real-aircraft behavior was performed or claimed.
- Remaining PXE-0008 API work now focuses on typed `/api/v1` replacement and
  compatibility-retirement planning while keeping static route-boundary
  guardrails current.

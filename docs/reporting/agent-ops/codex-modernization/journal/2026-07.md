# PixEagle Codex Modernization Journal - 2026-07

## 2026-07-04 Runtime Logging Foundation

- Resumed after usage-limit interruption and verified the active branch,
  worktree, PXE-0079 plan, and stale reviewer-agent state. The two interrupted
  reviewer agents were closed and restarted with narrow review prompts.
- Implemented PixEagle runtime log sessions inspired by `mavsdk_drone_show`
  session JSONL patterns but scoped to PixEagle's current single-runtime
  architecture: per-run manifests, `components/backend.jsonl`, path-safe run
  IDs/components, retention by session count and byte budget, credential
  redaction, and a strict claim boundary.
- `scripts/run.sh` now exports one `PIXEAGLE_RUN_ID` and
  `PIXEAGLE_RUNTIME_LOG_DIR` for the launched panes; startup output shows the
  active run ID and runtime-log directory.
- Added read-only typed routes:
  `/api/v1/logs/status`, `/api/v1/logs/sessions`, and
  `/api/v1/logs/sessions/{run_id}` with route inventory, structured-error,
  and security-policy coverage.
- Added a dashboard `Logs` page that shows the active run, retained sessions,
  component/level filters, bounded entries, and the process-local evidence
  boundary.
- Added focused backend and dashboard tests for runtime logging and the Logs
  page.
- This was a foundation slice. At this checkpoint PXE-0079 still needed
  dashboard and sidecar stdout/stderr capture, frontend error ingestion, live
  log streaming, export/evidence bundle, and fresh setup walkthrough evidence.
  Superseded: all listed follow-ups are closed as of the 2026-07-07 handoff
  walkthrough.
- No PX4/SITL/HIL, QGC playback, deployment hardening, field behavior, or
  real-aircraft behavior is claimed.

## 2026-07-04 WebRTC, Bootstrap, And Runtime Logging Planning

- Researched the user's follow-up questions after the public quick-demo retest:
  manual WebRTC policy, bootstrap/setup option sequence, and whether PixEagle
  should adopt unified logging from `mavsdk_drone_show`.
- Confirmed no Telegram bridge command or environment configuration is present
  on this VPS. Saved a Telegram-ready report at
  `/home/alireza/PIXEAGLE_TELEGRAM_WEBRTC_BOOTSTRAP_LOGGING_REPORT_2026-07-04.md`.
- Confirmed the public HTTP/IP demo should not default to WebRTC. Prior host-
  local WebRTC success proves signaling and same-host ICE only; public remote
  WebRTC needs HTTPS/WSS or equivalent trust boundary, STUN/TURN/firewall
  evidence, and media-state diagnostics. Auto/WebSocket remains the correct
  beginner demo default.
- Audited setup/bootstrap flow and recorded PXE-0078 for setup UX
  consolidation: split/gate dependency roles, fix stale dlib path, publish one
  setup matrix, clarify quick-demo side effects/cleanup, and align or document
  update semantics.
- Audited PixEagle logging against local `mavsdk_drone_show` and recorded
  PXE-0079 for a PixEagle-specific unified runtime logging surface:
  per-run sessions, component JSONL, retention, redaction, typed
  `/api/v1/logs/*`, dashboard Logs page, frontend error reports, and evidence
  bundle export. Security audit remains separate.
- Live demo observability note: current tester activity is visible through
  `logs/security_audit.jsonl` and tmux pane history, but there is no clean
  API/UI path to retrieve the last warnings/errors for the active run. Backend
  pane history is currently noisy with repeated tracker-loss warnings, so the
  logging work should include dedup/rate-limit/operator-severity design.
- No PX4/SITL/HIL, QGC playback, deployment hardening, field behavior, or
  real-aircraft behavior is claimed.

## 2026-07-04 Demo Feedback Fixes

- Resumed after usage-limit interruption and reconciled the active worktree,
  demo evidence, running `pixeagle` tmux session, phase map, journal, issue
  register, and two independent reviewer agents. The reviewer agents were
  closed after their findings were addressed.
- Fixed the user-reported `ACTION_TRACKER_SWITCH_INVALID` path by preserving
  canonical tracker catalog keys such as `CSRTTracker` in
  `ui_trackers[].name`, keeping friendly labels in `display_name`, and making
  the dashboard selector submit the canonical key.
- Added integration coverage proving catalog names validate through typed
  tracker-switch dry-run, plus dashboard coverage that `CSRTTracker` is
  submitted rather than a display label.
- Stopped Tracker/Follower polling-status flicker by keeping the prior visible
  success/error state while the next poll is in flight; added page-level tests
  for that behavior.
- Added generated schema options for `VideoSource.VIDEO_SOURCE_TYPE` and
  regenerated `configs/config_schema.yaml`, so Settings can show source choices
  including video file, USB camera, RTSP, UDP, HTTP, CSI, and custom GStreamer.
- Fixed a Settings safety blocker from frontend review: Manual save mode now
  applies to enum/custom enum, boolean, safety object, follower object, array,
  and object edits instead of persisting those changes immediately.
- Changed manual WebRTC on public non-local HTTP to fail closed with operator
  guidance instead of opening signaling and waiting indefinitely. Auto mode
  remains WebSocket JPEG for the public quick demo.
- Updated API docs to clarify that tracker catalog `name` is the action token
  and `display_name` is UI text.
- Validation passed:
  - backend/schema focused gate: 142 tests passed;
  - schema check passed;
  - broader backend/docs gate: 215 tests passed;
  - focused dashboard regression suite: 6 suites, 47 tests passed;
  - dashboard production build passed as `main.f93b865e.js`;
  - Python compile check and `git diff --check` passed;
  - live public browser/API smoke passed against `http://204.168.181.45:3040`
    and backend `http://204.168.181.45:5077` without printing the password.
- Evidence lives under
  `docs/reporting/agent-ops/codex-modernization/evidence/2026-07-04-demo-feedback-fixes/`.
- No PX4/SITL/HIL, MAVSDK Server, MAVLink2REST routing, MavlinkAnywhere routing,
  QGC playback, deployment hardening, field behavior, or real-aircraft behavior
  is claimed.
- Next step remains user retest on the public quick demo with the same current
  credential; after acceptance, stop/rotate/delete the temporary public HTTP
  credential and continue PXE-0074 clean setup/update walkthrough.

## 2026-07-04 Dashboard Operator UX Cleanup

- Resumed after public demo testing feedback and treated the WebRTC report
  separately from dashboard page UX. Public plain-HTTP/IP Auto streaming remains
  WebSocket JPEG by policy from PXE-0075; this slice focused on Settings,
  Tracker, and Follower operator UI defects.
- Reworked Settings mobile behavior: removed the floating menu button, added an
  inline `Sections` control, bounded drawer/sidebar scrolling, tightened mobile
  spacing, fixed stale `Fab` import, and kept the section editor content from
  forcing horizontal scroll.
- Rebuilt Tracker and Follower pages as operator telemetry pages:
  - summary tiles first;
  - compact labels and units;
  - unavailable data shown as `--` instead of false `0`;
  - raw JSON moved behind Diagnostics accordions;
  - bounded telemetry/diagnostic histories;
  - fixed chart heights and responsive chart containers;
  - removed per-tick chart console logging.
- Added shared `operatorFormat` utilities and tests for compact numbers,
  percentages, vectors, timestamps, and unavailable values.
- Fixed responsive shell issues found by authenticated Playwright evidence:
  Header/Auth chip shrink constraints, root layout `minWidth: 0`, footer
  wrapping, theme-toggle sizing, backend-status spacing, follower profile
  selector mobile stacking, and MUI Grid horizontal gutter overflow on `xs`.
- Validation passed:
  - `git diff --check`;
  - schema drift check passed;
  - route inventory plus parameter reload gate: 50 tests passed;
  - docs infrastructure consistency gate: 23 tests passed;
  - focused dashboard tests: 4 suites, 20 tests passed;
  - dashboard production build passed;
  - minimal public browser demo restarted with `bash scripts/run.sh
    --no-attach --rebuild -m -k`;
  - authenticated Playwright/Chrome public-demo check passed for `/settings`,
    `/tracker`, and `/follower` at `390x844`, `768x1024`, and `1366x768` with
    0 horizontal-overflow failures and 0 sign-in false positives.
- Evidence lives under
  `docs/reporting/agent-ops/codex-modernization/evidence/2026-07-04-dashboard-operator-ux/`.
- No PX4/SITL/HIL, MAVSDK/MAVLink2REST runtime routing, QGC playback,
  deployment, field, or real-aircraft behavior is claimed.
- Remaining recommended work: record user retest result and stop/rotate the
  temporary public HTTP credential after testing; continue PXE-0074 final clean
  setup/update walkthrough; continue production remote evidence under
  PXE-0064/PXE-0068 when target TLS/proxy/firewall choices are ready; keep
  PXE-0021 dashboard toolchain migration separate.

## 2026-07-01 Resume And Dashboard Typed Tracker Catalog Closure

- Resumed after usage-limit interruption and reconciled the open PXE-0008
  dashboard typed tracker catalog adoption slice against the phase map,
  checkpoint, issue register, journal, worktree, and independent reviewer
  output.
- Closed the independent review blocker: malformed typed catalog object
  payloads can no longer normalize into empty/default dashboard state. The
  dashboard validates the typed `/api/v1/tracking/catalog` payload before
  normalization, including top-level catalog source/status/guidance/timestamp,
  collection shapes, and required embedded `runtime_status` fields.
- Added regression coverage for `403` policy denial and malformed object
  payloads. Both cases surface errors and do not fall back to legacy
  `/api/tracker/*` reads.
- Validation after the fix:
  - focused dashboard tracker-catalog gate passed with 4 suites and 15 tests;
  - dashboard production build passed;
  - docs-link and endpoint-registry hygiene gate passed with 2 tests;
  - stale tracker-catalog wording scan returned no matches;
  - `git diff --check` passed;
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    generated API/MCP candidate inventory current, 393 tests passed, and one
    existing Starlette/httpx deprecation warning.
- Independent read-only reviewer recheck found no blockers and verified the
  malformed-payload fix, fallback boundaries, unchanged legacy mutation paths,
  production remote path allowlist, checkpoint consistency, and claim boundary.
- No backend route change, MCP promotion, alias retirement, QGC media
  validation, PX4/SITL/HIL, field test, service/deployment action, or
  real-aircraft behavior was performed or claimed.
- Remaining PXE-0008 tracker API work at that checkpoint: typed tracker
  mutation/restart/configuration design, fallback telemetry for legacy
  catalog/config compatibility reads, and tracked compatibility-retirement
  planning. Tracker switch was then closed by the typed tracker-switch action
  slice below.

## 2026-07-01 Typed Tracker Switch Action

- Added typed `POST /api/v1/actions/tracker-switch` for tracker selection.
  The action uses the existing action-resource contract: dry-run validation,
  explicit `confirm=true`, required idempotency keys for confirmed mutations,
  process-local action records, structured typed errors, and security-critical
  action-execute policy coverage.
- Extracted `switch_tracker_to_type()` from the legacy tracker route helper so
  the typed action can reuse current schema-manager validation and
  `AppController.switch_tracker_type()` behavior without fabricating a FastAPI
  request. Legacy `/api/tracker/switch` response shapes are preserved for
  rolling-update compatibility.
- Dashboard tracker selection now uses the typed action through the endpoint
  registry and existing action request envelope. It falls back to legacy
  `/api/tracker/switch` only when the typed action is missing or explicitly
  unsupported, and it no longer uses deprecated `/api/tracker/set-type`.
- Independent read-only review found and closed one dashboard blocker: typed
  action failures with object-shaped `detail` payloads could be rendered as raw
  React children. The hook now normalizes nested typed error envelopes to
  strings, and the policy-failure regression uses an object-shaped typed
  `detail`.
- Generated API/MCP candidate inventory now includes
  `pixeagle.actions.tracker_switch.submit_candidate` as blocked, unregistered,
  non-callable, and excluded from docs-stage `agent_tools.yaml`.
- Focused validation passed:
  - route inventory, security policy, and API/MCP candidate tests: 69 passed;
  - legacy tracker and typed tracker-switch backend tests: 37 passed, 97
    deselected;
  - dashboard tracker hook/endpoint/selector tests: 3 suites, 16 tests passed;
  - dashboard production build passed;
  - docs-link, endpoint-registry hygiene, production-remote harness, and
    security-critical audit gates passed.
  - `PYTHON=.venv/bin/python make phase0-check` passed with schema current,
    generated API/MCP candidate inventory current, 393 tests passed, and one
    existing Starlette/httpx deprecation warning.
- No tracker runtime success, PX4/SITL/HIL, field behavior, QGC media
  validation, service/deployment action, alias retirement, MCP promotion, or
  real-aircraft behavior was performed or claimed.
- Remaining PXE-0008 tracker API work at that checkpoint: tracker restart,
  broader tracker configuration mutation design, fallback telemetry/deprecation
  tracking for legacy tracker catalog/config reads, and compatibility-retirement
  planning. Tracker restart was then closed by the typed tracker-restart action
  slice below; broader tracker configuration mutation remains open.

## 2026-07-01 Typed Tracker Restart Action

- Added typed `POST /api/v1/actions/tracker-restart` for tracker config
  reload/restart. The action uses the existing action-resource contract:
  dry-run validation, explicit `confirm=true`, required idempotency keys for
  confirmed mutations, process-local action records, structured typed errors,
  and security-critical action-execute policy coverage.
- The typed action validates the currently configured tracker through the
  schema manager before executing the existing legacy restart helper. Legacy
  `/api/tracker/restart` response shapes and rate-limit behavior remain
  compatibility implementation details.
- Dashboard endpoint registry and production-remote route allowlist now include
  the typed restart action. No config UI restart-button migration is claimed in
  this slice.
- Generated API/MCP candidate inventory now includes
  `pixeagle.actions.tracker_restart.submit_candidate` as blocked,
  unregistered, non-callable, and excluded from docs-stage `agent_tools.yaml`.
- Focused validation passed:
  - syntax/import compile check for touched backend API modules passed;
  - route inventory, security policy, and API/MCP candidate tests: 69 passed;
  - tracker restart/switch backend action tests: 10 passed, 100 deselected;
  - dashboard endpoint-registry test: 1 suite, 2 tests passed.
- Broader validation passed:
  - schema check current;
  - minimum phase route inventory plus parameter reload gate: 50 passed;
  - docs-link plus production-remote harness gate: 18 passed;
  - dashboard production build passed;
  - generated API/MCP candidate inventory check passed;
  - stale tracker restart/configuration wording scan returned no matches for
    current false-open patterns;
  - `git diff --check` passed;
  - final `PYTHON=.venv/bin/python make phase0-check` passed with schema
    current, generated API/MCP candidate inventory current, 393 tests passed,
    and one existing Starlette/httpx deprecation warning.
- Independent read-only blocker review found no blockers. It found one
  non-blocking `FastAPIHandler` `Literal[...]` annotation omission for
  `tracker_restart`; fixed before commit, regenerated the candidate inventory,
  and reran the affected gates plus final phase0.
- No tracker runtime success, PX4/SITL/HIL, field behavior, QGC media
  validation, service/deployment action, alias retirement, MCP promotion, or
  real-aircraft behavior was performed or claimed.
- Residual slice-specific gaps: unit tests mock the restart executor rather
  than validating the full legacy dispatch path, and no browser-flow test
  submits tracker restart from the dashboard UI in this slice.
- Remaining PXE-0008 tracker API work: typed tracker configuration mutation
  design, fallback telemetry/deprecation tracking for legacy tracker
  catalog/config reads, and compatibility-retirement planning.

## 2026-07-01 Dashboard Tracker Compatibility Fallback Telemetry

- Added structured client-side dashboard telemetry for legacy tracker fallback
  from typed APIs. The dashboard now emits
  `pixeagle:tracker-compatibility-fallback`, records a bounded in-memory
  history of the last 50 attempted fallback events, and logs a structured
  warning when:
  - typed tracker catalog fallback serves selector/current-config metadata from
    legacy `/api/tracker/available-types` and `/api/tracker/current-config`;
  - typed available-trackers catalog fallback uses legacy
    `/api/tracker/available`;
  - typed current-tracker catalog fallback uses legacy
    `/api/tracker/current`;
  - typed tracker-switch action fallback uses legacy `/api/tracker/switch`.
- Kept fallback limited to missing or explicitly unsupported typed routes
  (`404`, `405`, `501`, or existing typed unavailable/no-entry handling).
  Auth, policy, and malformed typed payload failures still do not fall back.
- Updated API/tracker/developer docs and the modernization issue register to
  state that deprecated `/api/tracker/set-type` is compatibility-only and that
  new tracker-selection clients should use
  `POST /api/v1/actions/tracker-switch`.
- Focused validation passed:
  - dashboard tracker hook test: 1 suite, 14 tests passed.
- Broader validation passed:
  - dashboard tracker hook/endpoint/selector/status-card suites: 4 suites, 21
    tests passed;
  - dashboard production build passed;
  - docs-link gate passed;
  - `git diff --check` passed;
  - final `PYTHON=.venv/bin/python make phase0-check` passed with schema
    current, generated API/MCP candidate inventory current, 393 tests passed,
    and one existing Starlette/httpx deprecation warning.
- Independent read-only blocker review found no blockers. It confirmed fallback
  telemetry is recorded only after the compatibility fallback gate and noted
  residual gaps around per-hook negative cases, volatile client-side storage,
  and the existing typed unavailable/no-entry fallback policy. Added direct
  50-event-cap coverage and clarified that the event records an attempted
  fallback, not legacy route success.
- No backend route changes, server-side deprecation counters, alias retirement,
  tracker runtime success, PX4/SITL/HIL, field behavior, QGC media validation,
  service/deployment action, MCP promotion, or real-aircraft behavior was
  performed or claimed.
- Remaining PXE-0008 tracker API work: typed tracker configuration mutation
  design, backend/server-side deprecation counters for legacy tracker
  catalog/config routes, and compatibility-retirement planning.

## 2026-07-02 Backend Tracker Compatibility Deprecation Counters

- Added process-local backend usage counters for attempted public legacy
  `/api/tracker/*` compatibility route handling in
  `src/classes/api_legacy_tracker_routes.py`.
- The counter snapshot records route key, method, path, intended replacement
  `/api/v1` path, deprecated status, compatibility-alias status, count, and
  last-used timestamp. Deprecated `POST /api/tracker/set-type` is explicitly
  marked deprecated.
- Embedded the snapshot in typed `GET /api/v1/tracking/catalog` as
  `legacy_compatibility` with a claim boundary that keeps it to volatile
  process-local observability. It is not a durable audit log and does not prove
  tracker runtime, follower response, PX4, SITL/HIL, field, QGC media, or
  real-aircraft behavior.
- Later update: the 2026-07-03 schema/capabilities retirement removed the
  tracker legacy counter contract because no public legacy tracker diagnostic
  route remained.
- Kept typed tracker-restart action execution from inflating legacy-route
  counters by calling the shared restart helper with
  `record_compatibility_usage=false`.
- Regenerated the non-callable API/MCP candidate inventory after the contract,
  snapshot, handler, and legacy helper source hashes changed.
- Focused validation passed:
  - legacy tracker helper suite: 32 tests passed;
  - typed tracker catalog tests: 3 passed, 107 deselected;
  - route inventory plus API/MCP candidate tests: 50 tests passed after
    regenerating the candidate inventory.
- Broader validation passed:
  - schema check current;
  - docs-link gate: 1 passed;
  - touched Python module compile check passed;
  - typed API contract import smoke passed and confirmed
    `classes.api_v1_contracts` does not import
    `classes.api_legacy_tracker_routes`;
  - `git diff --check` passed;
  - final `PYTHON=.venv/bin/python make phase0-check` passed with schema
    current, generated API/MCP candidate inventory current, 393 tests passed,
    and one existing Starlette/httpx deprecation warning.
- Independent read-only review found one blocker: typed API contracts imported
  the legacy tracker route helper only for the compatibility claim-boundary
  constant, which would pull legacy route dependencies and AI availability
  probes into typed contract imports. Fixed by moving the constant ownership
  into `api_v1_contracts.py` and having the legacy route helper reuse it.
- No route inventory change, alias retirement, durable audit log, tracker
  runtime success, PX4/SITL/HIL, field behavior, QGC media validation,
  service/deployment action, MCP promotion, or real-aircraft behavior was
  performed or claimed.
- Remaining PXE-0008 tracker API work: typed tracker configuration mutation
  design and compatibility-retirement planning.

## 2026-07-02 Retired Tracker Set-Type Alias

- Retired public `POST /api/tracker/set-type` after typed
  `POST /api/v1/actions/tracker-switch` and dashboard typed-action adoption
  were already in place.
- Removed the route registration, `FastAPIHandler` wrapper/import, direct
  legacy `set_tracker_type()` helper, security-policy classification, frozen
  route inventory entry, dashboard endpoint constant, and active
  `legacy_compatibility` counter metadata for `set_type`.
- Kept `/api/tracker/switch` registered as the rolling legacy compatibility
  fallback for clients that cannot yet call the typed action. New clients
  should use `POST /api/v1/actions/tracker-switch`.
- Updated API, tracker integration, schema-development, route-inventory, phase
  map, and issue-register docs so active guidance no longer teaches the
  retired route as an available endpoint.
- No typed tracker configuration mutation redesign, remaining tracker alias
  retirement, tracker runtime success, PX4/SITL/HIL, field behavior, QGC media
  validation, service/deployment action, MCP promotion, or real-aircraft
  behavior was performed or claimed.
- Remaining PXE-0008 tracker API work: typed tracker configuration mutation
  design and compatibility-retirement planning for still-registered tracker
  catalog/config, switch/restart, and diagnostic aliases.

## 2026-07-02 Retired Tracker Switch Alias

- Retired public `POST /api/tracker/switch` after typed
  `POST /api/v1/actions/tracker-switch` and dashboard typed-action adoption
  were already in place.
- Removed the route registration, `FastAPIHandler` wrapper/import, public
  request-parsing compatibility helper, security-policy classification, frozen
  route inventory entry, dashboard endpoint constant, dashboard fallback path,
  and active `legacy_compatibility` counter metadata for `switch`.
- Kept the internal `switch_tracker_to_type()` executor because the typed
  tracker-switch action still uses it to preserve AppController validation and
  switch behavior without exposing a legacy HTTP mutation alias.
- Updated API, tracker integration, schema-development, route-inventory, phase
  map, and issue-register docs so active guidance no longer teaches
  `/api/tracker/switch` as an available endpoint.
- No typed tracker configuration mutation redesign, remaining tracker alias
  retirement, tracker runtime success, PX4/SITL/HIL, field behavior, QGC media
  validation, service/deployment action, MCP promotion, or real-aircraft
  behavior was performed or claimed.
- Remaining PXE-0008 tracker API work: typed tracker configuration mutation
  design and compatibility-retirement planning for still-registered tracker
  catalog/config, restart, and diagnostic aliases.

## 2026-07-02 Retired Tracker Restart Alias

- Retired public `POST /api/tracker/restart` after typed
  `POST /api/v1/actions/tracker-restart` and dashboard typed-action adoption
  were already in place.
- Removed the route registration, `FastAPIHandler` wrapper, security-policy
  classification, frozen route inventory entry, dashboard legacy endpoint
  constant, hidden config badge restart URL, and active
  `legacy_compatibility` counter metadata for `restart`.
- Kept the internal `restart_tracker()` helper because the typed
  tracker-restart action still uses it to preserve reload, rate-limit, and
  reinitialize behavior without exposing a legacy HTTP mutation alias.
- Updated API, tracker integration, schema-development, hot-reload,
  route-inventory, phase-map, and issue-register docs so active guidance no
  longer teaches `/api/tracker/restart` as an available endpoint.
- No typed tracker configuration mutation redesign, remaining tracker catalog
  or diagnostic alias retirement, tracker runtime success, PX4/SITL/HIL, field
  behavior, QGC media validation, service/deployment action, MCP promotion, or
  real-aircraft behavior was performed or claimed.
- Remaining PXE-0008 tracker API work: typed tracker configuration mutation
  design and compatibility-retirement planning for still-registered tracker
  catalog/config and diagnostic aliases.

## 2026-07-02 Retired Tracker Catalog/Config Read Aliases

- Retired public `GET /api/tracker/available`, `GET /api/tracker/current`,
  `GET /api/tracker/available-types`, and
  `GET /api/tracker/current-config` after typed
  `GET /api/v1/tracking/catalog` and dashboard typed-catalog adoption were
  already in place.
- Removed route registrations, `FastAPIHandler` wrapper imports/methods,
  obsolete legacy helper bodies/tests, security-policy classifications, frozen
  route inventory entries, reverse-proxy e2e allowlist entries, dashboard
  endpoint constants, dashboard fallback branches, browser fallback telemetry
  code, and active `legacy_compatibility` counter metadata for those routes.
- Dashboard tracker selector/current/config metadata now requires
  `GET /api/v1/tracking/catalog`. Missing or unsupported typed catalog
  responses surface as operator-visible errors instead of silently calling
  stale aliases.
- Independent review found stale dashboard error-visibility and historical-doc
  wording blockers; both were fixed in this slice. A follow-up reviewer found no
  blockers and flagged a non-blocking generic `/api/system/schema_info`
  automatic-fallback claim; this slice also set that metadata to
  `automatic_fallback=false`, listed the then-remaining tracker diagnostic
  routes and retired tracker catalog/config aliases explicitly, and added regression
  coverage.
- At that checkpoint the typed tracker catalog still embedded
  `legacy_compatibility`, but the snapshot then only listed currently
  registered legacy tracker diagnostic routes: `output`, `capabilities`,
  `schema`, and `current_status`. The 2026-07-03 schema/capabilities
  retirement removed that counter contract.
- Regenerated the non-callable API/MCP candidate inventory after route counts
  and source hashes changed; current declared route pairs are 129 total, 127
  HTTP, with 71 GET routes.
- Focused validation passed:
  - legacy tracker helper, route inventory, API security policy, API/MCP
    candidate coverage, and focused tracking-catalog safety test: 87 passed;
  - schema-info compatibility metadata plus route inventory/security focused
    regression: 61 passed;
  - dashboard tracker hook, endpoint, selector, and status-card tests: 4 suites,
    24 tests passed;
  - regenerated API/MCP candidate inventory checked current; candidate tests: 10
    passed;
  - touched Python compile check and frontend syntax checks passed;
  - schema check current;
  - generated API/MCP candidate inventory check current;
  - markdown local-link gate: 1 passed;
  - dashboard production build passed;
  - `PYTHON=.venv/bin/python make phase0-check`: 393 passed with one existing
    Starlette/httpx deprecation warning.
- No typed tracker configuration mutation redesign, remaining diagnostic alias
  retirement, tracker runtime success, PX4/SITL/HIL, field behavior, QGC media
  validation, service/deployment action, MCP promotion, or real-aircraft
  behavior was performed or claimed.
- Remaining PXE-0008 tracker API work: typed tracker diagnostic/schema
  replacement design, typed tracker configuration mutation design, and
  compatibility-retirement planning for still-registered diagnostic aliases.

## 2026-07-02 Retired Tracker Runtime/Output Diagnostic Aliases

- Retired public `GET /api/tracker/current-status` and
  `GET /api/tracker/output` after typed
  `GET /api/v1/tracking/runtime-status`, typed
  `GET /api/v1/tracking/telemetry`, and dashboard typed telemetry adoption were
  already in place.
- Removed route registrations, `FastAPIHandler` wrapper imports/methods,
  obsolete legacy helper bodies/tests, security-policy classifications, frozen
  route inventory entries, reverse-proxy e2e allowlist entries, dashboard
  endpoint constants, and active `legacy_compatibility` counter metadata for
  those routes.
- Dashboard tracker status/output views now require typed
  `GET /api/v1/tracking/telemetry`; malformed typed telemetry fails visibly and
  does not fall back to retired aliases. Typed
  `GET /api/v1/tracking/runtime-status` remains the runtime-read contract.
- `/api/system/schema_info` now lists `current-status` and `output` under
  retired tracker diagnostic routes. At that checkpoint the typed tracker
  catalog `legacy_compatibility` snapshot listed only currently registered
  legacy tracker diagnostics: `capabilities` and `schema`. The 2026-07-03
  schema/capabilities retirement removed that counter contract.
- Regenerated the non-callable API/MCP candidate inventory after route counts
  and source hashes changed; current declared route pairs are 127 total, 125
  HTTP, with 69 GET routes.
- Validation passed:
  - touched Python compile check and frontend syntax checks passed;
  - legacy tracker helper, route inventory, API security policy, and focused
    tracking-catalog/schema-info tests: 72 passed;
  - dashboard tracker hook, endpoint, and status-card tests: 3 suites, 22 tests
    passed;
  - generated API/MCP candidate inventory checked current;
  - schema check current;
  - API/MCP candidate plus markdown local-link tests: 11 passed;
  - whitespace diff check clean;
  - dashboard production build passed;
  - `PYTHON=.venv/bin/python make phase0-check`: 393 passed with one existing
    Starlette/httpx deprecation warning.
- Independent read-only review found no blockers. It confirmed backend
  registrations/security/counters keep only `schema` and `capabilities`,
  dashboard status/output hooks use typed telemetry, old endpoint constants are
  gone, route/candidate counts align, and active docs describe
  `current-status`/`output` as retired. The reviewer's residual dashboard-build
  gap was covered by the successful dashboard production build above.
- No typed tracker configuration mutation redesign, tracker runtime success,
  PX4/SITL/HIL, field behavior, QGC media validation, service/deployment
  action, MCP promotion, or real-aircraft behavior was performed or claimed.
- At this checkpoint, remaining PXE-0008 tracker API work included typed
  schema/capabilities replacement design and broader typed tracker configuration
  mutation design. The next 2026-07-03 entry records the schema/capabilities
  retirement; broader typed tracker configuration mutation design remains open.

## 2026-07-03 Retired Tracker Schema/Capabilities Diagnostic Aliases

- Retired public `GET /api/tracker/schema` and
  `GET /api/tracker/capabilities` after typed
  `GET /api/v1/tracking/catalog` began carrying tracker data-type schemas as
  `data_type_schemas`.
- Dashboard tracker schema metadata now reads the typed tracker catalog through
  `useTrackerSchema()` without fallback to retired aliases. Malformed typed
  catalog payloads fail visibly instead of silently reaching a legacy route.
- Removed route registrations, `FastAPIHandler` wrapper imports/methods,
  obsolete legacy helper bodies/tests, security-policy classifications, frozen
  route inventory entries, reverse-proxy e2e allowlist entries, dashboard
  endpoint constants, active `legacy_compatibility` counter metadata for
  schema/capabilities, and the obsolete tracker legacy counter contract.
- `GET /api/v1/tracking/catalog` no longer returns `legacy_compatibility`
  because no public legacy tracker diagnostic routes remain. The internal
  `switch_tracker_to_type()` and `restart_tracker()` executors remain for typed
  tracker actions.
- Regenerated the non-callable API/MCP candidate inventory after route counts
  and source hashes changed; current declared route pairs are 125 total, 123
  HTTP, with 67 GET routes.
- Validation passed:
  - touched Python compile check and frontend syntax checks passed;
  - legacy tracker helper, route inventory, API security policy, focused
    tracking-catalog/schema-info tests: 68 passed after removing the obsolete
    tracker legacy counter contract;
  - dashboard tracker hook, endpoint, and status-card tests: 3 suites, 24 tests
    passed.
  - generated API/MCP candidate inventory check current;
  - schema check current;
  - API/MCP candidate plus markdown local-link tests: 11 passed;
  - whitespace diff check clean;
  - dashboard production build passed;
  - `PYTHON=.venv/bin/python make phase0-check`: 393 passed with one existing
    Starlette/httpx deprecation warning.
- Independent read-only review found one blocker: the empty tracker legacy
  counter contract still existed after route retirement. Fixed by removing the
  counter helpers, typed catalog field, tests, and docs before final validation.
- No typed tracker configuration mutation redesign, tracker runtime success,
  PX4/SITL/HIL, field behavior, QGC media validation, service/deployment
  action, MCP promotion, or real-aircraft behavior was performed or claimed.
- Remaining PXE-0008 tracker API work: broader typed tracker configuration
  mutation design and any future route-boundary debt discovered by static
  guards. No public legacy tracker diagnostic aliases remain.

## 2026-07-03 VPS Browser Test Readiness Estimate

- Added a checkpoint report for the first VPS/browser handoff question:
  `checkpoints/2026-07-03-phase-4-vps-browser-test-readiness-estimate.md`.
- Reviewed the current setup/browser access contracts after tracker alias
  retirement and separated three lanes:
  - controlled VPS/browser smoke through SSH tunnel, private overlay, trusted
    private address, or the lab `demo_lan_browser` profile;
  - public HTTPS/WSS browser access through the guarded `production_remote`
    profile and operator-managed reverse proxy/TLS/firewall evidence;
  - later tester/funder handoff after clean walkthrough, stale-doc cleanup, and
    accepted deployment evidence.
- Estimate for the first controlled browser review: 2 focused slices in the
  best case, 3 if the VPS needs package/browser dependency cleanup, port
  conflict resolution, or Playwright/browser installation.
- Estimate for public production remote after the controlled smoke: 2
  additional slices in the best case, 3 to 5 if TLS/proxy/firewall/service
  evidence needs adjustment.
- Estimate for browser/operator handoff without PX4/SITL claims: 4 to 7
  focused slices after the controlled smoke. Any PX4/SITL-backed handoff adds
  the relevant validation-ladder slices.
- Next recommended slice: PXE-0074 controlled browser-readiness walkthrough on
  the VPS from a clean temporary checkout, followed by local-only posture or
  profile selection, bind/port verification, browser smoke, sanitized evidence,
  and credential handoff outside the repository when credentials are generated.
- No service start, remote exposure, credential generation, PX4/SITL/HIL, field
  behavior, QGC media validation, deployment action, MCP promotion, or
  real-aircraft behavior was performed or claimed.

## 2026-07-03 Clean VPS Browser Readiness Walkthrough

- Ran the PXE-0074 controlled clean temp-directory walkthrough on the VPS after
  the readiness estimate.
- First clone attempt failed because the VPS root filesystem was nearly full;
  reproducible npm/pip cache cleanup freed enough space for the walkthrough.
- Discovered that `scripts/lib/common.sh` was missing from the repository
  because the broad `lib/` ignore pattern hid `scripts/lib/`. Added the shared
  helper, root-scoped the ignore rule, validated shell helper consumers, and
  pushed commit `03927605`.
- From a fresh clean clone at `03927605`, Core-profile init passed without a
  local `configs/config.yaml`; setup-profile dry-runs, binary download dry-run,
  schema check, minimum backend/API tests, dashboard tests, and dashboard build
  passed.
- Ran a local-only browser/API smoke with `bash scripts/run.sh --no-attach -m
  -k`, intentionally skipping MAVLink2REST and MAVSDK Server. Backend
  `127.0.0.1:5077`, dashboard `127.0.0.1:3040`, typed runtime status, typed
  tracker catalog, and typed media-health responded; services were stopped and
  listener cleanup was verified afterward.
- Closed walkthrough startup hazards found during the smoke: direct dashboard
  component startup no longer kills unknown port owners, npm audit/fund noise
  is suppressed in setup/runtime install paths, init prefers `npm ci` and
  primes the dashboard dependency cache, run.sh uses longer service-specific
  readiness waits for first-run dashboard builds, and invalid readiness retry
  overrides now fall back to safe positive defaults.
- Validation after the startup fixes passed: shell syntax/help checks, retry
  override normalization probe, focused setup-profile regressions, docs
  local-link gate, minimum backend/API gate, schema check, dashboard tests,
  dashboard build, and whitespace diff check. Dashboard build still emits the
  known CRA/Node `[DEP0176] fs.F_OK` deprecation warning tracked under
  PXE-0021.
- Independent read-only review first flagged stale PXE-0074 reporting as a
  blocker; the new checkpoint, journal, phase map, and issue-register updates
  close that blocker. The review's low-risk retry override concern was fixed
  before commit.
- Claim boundary remains strict: no public browser access, credential handoff,
  MAVSDK Server runtime behavior, MAVLink2REST runtime behavior,
  MavlinkAnywhere routing, tracker/follower closed-loop behavior, PX4/SITL/HIL,
  QGC playback, field, deployment, or real-aircraft behavior is claimed.
- Estimate update: if SSH/local-only or private-overlay access is acceptable,
  the first controlled user browser test is now about one focused handoff slice
  away after temp-checkout cleanup and service restart. Public HTTPS/WSS
  production remote still needs the separate TLS/proxy/firewall/credential and
  adversarial evidence slices.

## 2026-07-04 Demo LAN Browser VPS Handoff

- Prepared the super-quick beginner HTTP/IP browser demo using the documented
  `demo_lan_browser` profile on the VPS private-overlay interface
  `100.82.207.49`.
- Applied the profile with an external hashed browser-session user file under
  `/home/alireza/.config/pixeagle/secrets/` and captured the generated
  plaintext password only in the owner-readable local handoff file
  `/home/alireza/PIXEAGLE_DEMO_BROWSER_HANDOFF_PRIVATE.txt`.
- Found and fixed a quick-demo launcher blocker: `scripts/run.sh` and
  `scripts/components/main.sh` assumed `venv`, but this workspace used
  `.venv`. Both launchers now resolve `.venv/bin/python` then
  `venv/bin/python`, and a regression guard was added.
- Repaired the active VPS `.venv` environment by replacing
  `opencv-python-headless` with `opencv-contrib-python-headless`, restoring
  CSRT/KCF symbols required by the default tracker. This was local environment
  repair; project requirements already declare OpenCV contrib for normal init.
- Started the demo with `bash scripts/run.sh --no-attach -m -k`, intentionally
  skipping MAVLink2REST and MAVSDK Server. Backend and dashboard readiness
  passed on `0.0.0.0:5077` and `0.0.0.0:3040`.
- Verified overlay dashboard static response, protected API `401` before
  login, authenticated browser-session API access after login, runtime status,
  media-health, and a headless Chromium login smoke that reached
  `http://100.82.207.49:3040/dashboard` with the auth gate gone.
- Left the `pixeagle` tmux session running for user testing. Claim boundary
  remains limited to browser login and typed API smoke over private overlay:
  no public HTTP, production HTTPS/WSS, MAVSDK/MAVLink2REST runtime, PX4/SITL,
  QGC playback, field, or real-aircraft behavior is claimed.

## 2026-07-04 Quick Browser Demo Public Firewall Follow-Up

- User testing of `http://204.168.181.45:3040/` exposed a real handoff gap:
  PixEagle was listening on `0.0.0.0`, but active UFW rules did not allow the
  new public demo ports from outside clients.
- Opened temporary public demo UFW rules for `3040/tcp` and `5077/tcp` on the
  VPS. External check-host probes then returned HTTP 200 for the dashboard and
  backend auth-session endpoint. The current demo password was not rotated after
  the user asked to keep it stable for this session.
- Converted the operational miss into setup automation: added
  `scripts/setup/quick-browser-demo.sh` and `make quick-browser-demo` so the
  beginner path applies `demo_lan_browser`, writes a private credential handoff,
  handles active UFW for trusted private CIDR demos when inferable, and starts
  the minimal dashboard/backend demo.
- Extended `demo_lan_browser` to support an owner-only credential handoff JSON
  and to keep public HTTP rejected unless `--allow-public-http-demo` is supplied
  explicitly. Public HTTP remains temporary bench-only and sends credentials
  without TLS.
- Updated README/setup-profile docs and regression tests for default public-IP
  rejection, explicit public override warnings, handoff-file generation, and
  quick-wrapper dry-run.
- Validation passed: shell syntax/import checks, `tests/test_setup_profiles.py`
  (136 tests), docs infrastructure consistency (23 tests), route inventory plus
  parameter reload (50 tests), and schema check.
- Next slice moved to user-observed runtime/UI defects: WebRTC fallback,
  responsive Settings UX, and operator-focused Tracker Data/Follower Data pages.

## 2026-07-04 WebRTC Auto Public HTTP Policy

- Diagnosed the user-observed "WebRTC falls back to WebSocket" report.
  Host-local forced WebRTC validation connected, received a video track, reached
  ICE `connected`, and rendered 640x480, but Auto on the public HTTP/IP demo was
  making a bad default decision by selecting WebRTC only because
  `RTCPeerConnection` existed.
- Changed dashboard Auto mode so non-local plain HTTP origins use WebSocket JPEG
  with a visible `Auto: WebSocket for HTTP demo` chip. Localhost HTTP and HTTPS
  can still use WebRTC automatically, and manual WebRTC remains available for
  reviewed networks.
- Increased evidence-backed WebRTC fallback behavior: when Auto does choose
  WebRTC, fallback waits 15 seconds after signaling opens and only falls back
  when no video track arrives or signaling/ICE fails. Auto no longer opens a
  transient WebSocket before selecting WebRTC.
- Updated WebRTC docs and setup-profile docs to state that constructor support
  is not media proof; WebRTC needs an ICE/TURN/firewall/TLS path, and public
  demos must not broaden random UDP ranges as a shortcut.
- Validation passed: VideoStream Jest tests (9), docs infrastructure consistency
  (23), dashboard production build, live public Auto browser check, live manual
  WebRTC host-local browser check, and whitespace diff check.

## 2026-07-04 Setup Bootstrap UX Consolidation

- Closed the PXE-0078 setup/bootstrap UX slice that came from the WebRTC,
  bootstrap-choice, and logging planning review.
- Split Python dependency roles into `requirements-core.txt`,
  `requirements-ai.txt`, and `requirements-dev.txt`; kept `requirements.txt` as
  an aggregate developer convenience instead of the bootstrap source of truth.
- Updated `scripts/init.sh` so Core installs from `requirements-core.txt`
  directly, with the old grep behavior kept only as a fallback for damaged or
  partial checkouts. The Full profile still offers PyTorch setup first, then
  calls the AI helper.
- Updated `scripts/setup/install-ai-deps.sh` so the required AI package list is
  centralized in `requirements-ai.txt`, while `pnnx` remains best-effort for
  NCNN export and cannot break the core AI runtime install.
- Fixed the stale optional dlib install path by moving guidance to
  `scripts/setup/install-dlib.sh`.
- Fixed the quick browser demo wrapper so `DRY_RUN=1` does not create
  credential directories, write files, open firewall ports, or start services.
  The wrapper also no longer chmods existing parent directories such as `/tmp`
  when the operator supplies custom credential paths.
- Expanded the quick-demo printed scenario before changes happen: mode, host
  scope, dashboard/API URLs, hashed credential store, one-time handoff path,
  minimal service scope, WebRTC/WebSocket expectation, and cleanup are visible.
- Updated README, installation, and setup-profile docs with the role-based
  install flow, setup choice matrix, public HTTP WebRTC boundary, and dry-run
  semantics.
- Validation passed: `bash -n scripts/setup/quick-browser-demo.sh
  scripts/init.sh scripts/setup/install-ai-deps.sh`, `git diff --check`,
  focused setup/docs tests, and `PYTHONPATH=src .venv/bin/pytest
  tests/test_setup_profiles.py tests/test_docs_infrastructure_consistency.py
  -q` with 160 passed. The minimum backend/schema gates also passed:
  `PYTHONPATH=src .venv/bin/pytest tests/test_api_route_inventory.py
  tests/unit/core_app/test_parameters_reload.py -q` with 50 passed, and
  `bash scripts/check_schema.sh` reported `Schema is up-to-date`.
- Claim boundary: no new PX4/SITL/HIL, QGC receiver, production deployment, or
  real-aircraft behavior is claimed. Production-target evidence remains
  PXE-0068/PXE-0074; unified runtime logging remains PXE-0079.

## 2026-07-04 Quick Demo Admin Default Follow-Up

- Adjusted the quick browser demo role policy after public-demo feedback: the
  first generated `demo_lan_browser`/`quick-browser-demo` account is now
  `admin` by default so a maintainer can open Settings and the runtime Logs
  page immediately.
- Kept the production remote profile `operator` by default. The quick demo now
  documents `SESSION_ROLE=operator` or `SESSION_ROLE=viewer` for less-privileged
  bench accounts.
- Updated `scripts/setup/apply-setup-profile.py`,
  `scripts/setup/quick-browser-demo.sh`, README/install/setup-profile docs, and
  setup-profile tests for generated user files, one-time handoff files, and
  dry-run output.
- Updated the active VPS `pixeagle-demo` user role from `operator` to `admin`
  without rotating or printing the password, then restarted the minimal public
  demo. The one-time handoff file was absent, so authenticated curl verification
  was not run; unauthenticated logs still returned 401 and stored metadata shows
  the enabled demo user has `role=admin` with a password hash.
- Validation passed: quick-demo shell syntax, focused setup/API/config gate
  with 54 tests, schema check, whitespace diff check, and the broader
  docs/setup suite with 160 passed.

## 2026-07-05 Runtime Logging Pane Capture Follow-Up

- Continued PXE-0079 after the quick-demo admin checkpoint and closed the
  dashboard/sidecar stdout capture sub-slice.
- Added `tools/runtime_log_pipe.py`, a small Python helper that reuses
  `RuntimeLogSessionManager` validation, redaction, retention, and manifest
  paths to capture line-oriented tmux pane output into component JSONL files.
- Extended `RuntimeLogSessionManager` with component registration and sanitized
  component-message append support. Runtime sessions can now list started
  component logs such as `main_app`, `dashboard`, `mavlink2rest`, and
  `mavsdk_server` in addition to structured `backend` logs.
- Updated `scripts/run.sh` to prepare component log files, pass
  `PIXEAGLE_RUN_ID`/`PIXEAGLE_RUNTIME_LOG_DIR` explicitly to every pane command,
  and wrap launched components with a launcher pipe that mirrors output to tmux
  while appending sanitized component JSONL.
- Updated the Logs page title/boundary text from backend-only wording to
  Runtime Component Logs and displayed pane `stream`/`source` chips when
  present.
- Updated logging/API docs, issue register, phase map, and runtime logging
  checkpoint. At this pane-capture checkpoint, remaining PXE-0079 work was
  frontend error ingestion, live stream/export evidence bundle, and final setup
  walkthrough evidence; frontend ingestion was closed in the follow-up entry
  below.
- Focused validation passed before broader gates: launcher shell syntax,
  runtime logging tests, runtime pipe tests, log route tests, launcher static
  tests, and Python compile checks.

## 2026-07-05 Runtime Logging Frontend Ingestion Follow-Up

- Continued PXE-0079 and closed the frontend browser error ingestion sub-slice.
- Added write-only `runtime:report` scope for authenticated dashboard clients.
  Viewer/operator/admin sessions can report their own browser errors, while
  only admin receives `debug:read` for reading stored runtime logs.
- Added typed `POST /api/v1/logs/frontend-errors` with strict request/response
  models, CSRF policy, fixed server-owned `frontend` component, server-side
  context byte limit, per-principal rate limiting, bounded rate-limit key
  growth, and runtime redaction before storage.
- Added `dashboard/src/services/frontendErrorReporter.js` and focused tests.
  The reporter waits until auth mode is known, checks `runtime:report`, strips
  query/hash values from URL/route fields, uses the central API client for
  credentials/CSRF, and rate-limits client submissions.
- Updated the Logs page so frontend error entries display compact browser
  diagnostics instead of leaving stack/context data visible only in raw `extra`.
- Regenerated the API/MCP candidate inventory. The frontend report route is
  represented as blocked, non-callable, and not MCP-exposed. Added
  `api_v1_log_routes.py` to candidate provenance.
- Updated logging/API docs, API modernization provenance wording, issue
  register, phase map, and added checkpoint
  `2026-07-05-phase-4-runtime-logging-frontend-ingestion.md`.
- Focused validation passed:
  - backend log/security/route/candidate/docs focused gate: 113 passed;
  - dashboard frontend reporter/API client/Logs page tests: 12 passed.
  - schema check reported up to date;
  - parameter reload tests: 10 passed;
  - dashboard production build passed as `main.c2803765.js`;
  - whitespace diff check passed.
- Restarted the existing public quick demo without rotating credentials.
  Active runtime run is `pixeagle_20260705T023718Z_787814`; local/public
  dashboard returned 200; unauthenticated log read and frontend report write
  returned 401; the served dashboard manifest references `main.c2803765.js`.
- At this checkpoint PXE-0079 still needed live log streaming/export evidence
  bundle and final setup evidence. Runtime logs remain process-local
  diagnostics, not PX4/SITL/HIL/field proof.
  Superseded: live tail, export, and setup evidence are closed as of the
  2026-07-07 handoff walkthrough.

## 2026-07-05 Runtime Logging Evidence Export Follow-Up

- Continued PXE-0079 and implemented sanitized runtime log evidence export.
- Added `RuntimeLogSessionManager.export_session_bundle()`, which creates a
  temporary `tar.gz` bundle with `README.txt`, sanitized component JSONL files,
  the session manifest, and an `export_manifest.json` containing skipped
  malformed-line counts.
- Added typed `GET /api/v1/logs/sessions/{run_id}/export` with `FileResponse`
  route metadata, `LOGS_EXPORT_RESPONSES`, `debug:read` security policy,
  `no-store` cache headers, run ID, bundle size, SHA-256 digest, and runtime-log
  claim-boundary headers.
- Updated the dashboard Logs page with an authenticated evidence-bundle download
  action and endpoint registry entry.
- Regenerated the API/MCP candidate inventory. The export route is present as
  blocked, non-callable, and not MCP-exposed; `/api/v1` candidate count is now
  33 with 26 blocked/guarded candidates, 7 approved-for-review-only candidates,
  and 5 deferred candidates.
- Updated logging/API docs, API modernization provenance wording, issue
  register, phase map, and added checkpoint
  `2026-07-05-phase-4-runtime-logging-evidence-export.md`.
- Validation passed:
  - Python compile for touched backend/API/generator modules;
  - backend runtime-log/API security/route inventory focused gate: 84 passed;
  - dashboard Logs page focused gate: 2 passed;
  - API candidate/docs focused gate: 33 passed;
  - schema check up to date;
  - parameter reload gate: 10 passed;
  - dashboard production build passed as `main.97292de5.js`.
- Restarted the existing public quick demo without rotating credentials.
  Active runtime run is `pixeagle_20260705T025407Z_795121`; local/public
  dashboard returned 200; served dashboard manifest references
  `./static/js/main.97292de5.js`; unauthenticated export returned 401;
  authenticated login using the private handoff credential returned 200; export
  returned 200; exported SHA-256 and size headers matched the downloaded
  archive; archive listing contained README, manifest, export manifest, and
  backend/dashboard/main_app component logs.
- At this checkpoint PXE-0079 still needed live log streaming and final setup
  evidence.
  Superseded: live tail and setup evidence are closed as of the 2026-07-07
  handoff walkthrough.

## 2026-07-05 Runtime Logging Live Tail Follow-Up

- Continued PXE-0079 and implemented bounded runtime log live tailing without
  adding a separate unaudited streaming surface.
- Added `RuntimeLogSessionManager.read_entry_window()`, which returns redacted
  entries plus `offset`, `next_offset`, `tail`, `matched_total`, and
  `has_more` metadata. Existing `read_entries()` remains as a compatibility
  wrapper.
- Extended typed `GET /api/v1/logs/sessions/{run_id}` with `tail=true`.
  Tail reads return the latest bounded matching window and an exact cursor;
  normal reads return `next_offset` for bounded follow-up polling.
- Closed independent reviewer findings by making reads rotation-aware across
  the retained `.jsonl.1` backup plus current file, fixing the generated
  candidate artifact so query-only log reads no longer show
  `request_model: Optional[str]`, guarding stale in-flight dashboard poll
  responses with request generations, disabling Live tail without `debug:read`,
  and scrolling live mode to newest appended rows.
- Updated the dashboard Runtime Component Logs page with a Live tail switch,
  cursor-aware polling from `next_offset`, compact live/cursor chips, and a
  disabled manual offset field while live tail owns the cursor.
- Regenerated the API/MCP candidate inventory after backend source changes.
  No callable MCP/log-stream runtime surface was introduced.
- Updated logging/API docs, issue register, phase map, and added checkpoint
  `2026-07-05-phase-4-runtime-logging-live-tail.md`.
- Validation passed:
  - Python compile for touched backend/API/generator modules;
  - backend/API/log/security/docs focused gate: 121 passed;
  - parameter reload gate: 10 passed;
  - schema check up to date;
  - dashboard Logs page focused gate: 5 passed;
  - dashboard production build passed as `main.f7c7ec4a.js`;
  - whitespace diff check passed.
- Restarted the existing public quick demo without rotating credentials.
  Active runtime run is `pixeagle_20260705T091338Z_881812`; local/public
  dashboard returned 200; served dashboard manifest references
  `./static/js/main.f7c7ec4a.js`; unauthenticated live-tail read returned 401;
  authenticated login using the private handoff credential returned 200;
  authenticated `tail=true` read returned 200 with `next_offset=112`; follow-up
  read from that cursor returned 200 with no new entries; headless Chromium at
  `390x844` opened `/logs`, toggled Live tail, observed one `tail=true`
  request, showed cursor status, had no horizontal overflow, and reported no
  browser console/page errors.
- At this historical checkpoint PXE-0079 still needed final setup proof.
  Runtime logs remain process-local diagnostics, not PX4/SITL/HIL/field proof.
  Superseded: setup evidence is closed as of the 2026-07-07 handoff
  walkthrough.

## 2026-07-05 User Feedback Intake And Optional Setup Runtime Readiness

- Resumed after usage-limit interruption, checked the modernization plan,
  current branch, and worktree, and restarted/closed the read-only reviewer
  agents for setup/deps, dashboard polish, and SIH/SITL.
- Triaged the user's eight new feedback items into tracked issues:
  - PXE-0081 browser user management and password recovery;
  - PXE-0082 OSD/video overlay status polish;
  - PXE-0083 runtime log bundle UX and future import/viewer contract;
  - PXE-0084 typed About/System/update-status surface;
  - PXE-0085 SIH Dev/Training validation surface;
  - PXE-0086 safe demo cleanup/rotation and update workflow.
- Closed PXE-0080 as the immediate setup/runtime-readiness slice:
  `scripts/lib/common.sh` now provides shared venv resolution through
  `PIXEAGLE_VENV_DIR`, `.venv/`, then `venv/`; optional AI, PyTorch,
  AI-deps, OpenCV-GStreamer, dlib, and reset-config helpers use it.
- Extended `scripts/setup/check-ai-runtime.sh` so it reports dlib, OpenCV
  version, OpenCV GStreamer/FFMPEG build flags, and CSRT/KCF tracker API
  availability in addition to torch/Ultralytics/lap/NCNN/pnnx and model-path
  status.
- Updated AI-deps constraints to protect `opencv-python-headless` and
  `opencv-contrib-python-headless` in addition to non-headless OpenCV packages,
  so later AI installs do not silently replace a known-good OpenCV runtime.
- Added `tests/test_setup_venv_resolution.py` to guard resolver preference,
  absolute/relative overrides, and optional setup scripts against hardcoded
  legacy `venv/` regressions.
- Updated installation, troubleshooting, and OpenCV/GStreamer docs to explain
  fresh `venv/`, existing `.venv/`, explicit `PIXEAGLE_VENV_DIR`, and what the
  runtime diagnostic answers.
- VPS readiness result from the fixed diagnostic: active Python is
  `/home/alireza/PixEagle/.venv/bin/python`; OpenCV 4.13.0 is installed with
  FFMPEG and CSRT/KCF tracker APIs; OpenCV GStreamer is `NO`; torch,
  torchvision, torchaudio, Ultralytics, lap, ncnn, pnnx, and dlib are not
  installed. This is correct for the quick public browser demo but not for
  AI/YOLO, dlib, or GStreamer-specific workflows.
- Initial validation passed: shell syntax for touched scripts, fixed
  `check-ai-runtime.sh`, focused venv resolver tests, and quick-browser demo
  dry-run regression test. Broader focused gates and checkpoint publication
  continue in the current slice.

## 2026-07-05 Browser User Management CLI

- Continued the user-feedback plan and closed PXE-0081's first gate with an
  offline browser-session user management CLI instead of adding a remote
  web-admin API before audit/session-revocation design.
- Added `scripts/setup/manage-browser-users.py` for external
  `API_SESSION_USER_FILE` JSON files. It supports `list`, `verify`, `add`,
  `set-password`, `set-role`, `enable`, `disable`, and `remove`.
- The tool reuses the existing `classes.api_auth_runtime` PBKDF2 user-record
  format and role validation, writes owner-only JSON atomically, creates
  owner-only backups by default, refuses symlink writes, supports one-time
  owner-only credential handoff files, and never stores plaintext passwords in
  the runtime user file.
- Added `tests/test_manage_browser_users.py` covering generated admin creation,
  password verification, redacted list/JSON output, password reset, role
  changes, enable/disable/remove, handoff-file behavior, and missing user-file
  fail-closed behavior.
- Updated `docs/apis/api-security-policy.md`, `docs/setup/setup-profiles.md`,
  `docs/TROUBLESHOOTING.md`, and `docs/INSTALLATION.md` with demo and
  production reset examples using `python3 scripts/setup/manage-browser-users.py`.
- Cleaned the remaining unversioned Python script-command examples in the
  setup-profile tooling section and schema-generator helper docs so host
  environments without a `python` shim do not hit stale instructions.
- Important boundary: the runtime auth object loads users at startup. After
  changing the active user file, restart PixEagle or require affected users to
  log out when immediate enforcement matters. A typed web-admin UI/API remains
  future work and must include admin scope, CSRF, audit records, confirmation
  semantics where needed, and session-revocation design.
- Validation passed: CLI/schema py_compile, CLI help smoke, 143
  user-management plus setup-profile tests, 23 infrastructure docs tests,
  schema check, whitespace diff check, and a stale unversioned Python
  script-command scan.

## 2026-07-05 OSD And Video Overlay Polish

- Closed PXE-0082's code/test gate for the user-observed overlay issues.
- Changed the tracker mode badge from terse `CLASSIC`/`AI MODE` text to
  explicit `Tracker: Classic` and `Tracker: AI` labels.
- Replaced the Auto/WebSocket notice chip with a responsive protocol badge that
  always renders visible text, for example `Video: WebSocket` plus `HTTP demo`.
  Auto mode now resolves from the same page context used by the WebRTC safety
  check, so public HTTP/IP demos consistently choose WebSocket.
- Hardened `OSDToggle` so backend OSD preset and color-mode strings are trimmed,
  deduplicated, displayed with stable labels, and never allowed to create empty
  select/status values. Non-empty unknown values now show as missing instead of
  silently joining the catalog, and the color-mode catalog endpoint is
  best-effort so partial legacy backends still show OSD status.
- Added `dashboard/src/components/OSDToggle.test.js` and extended
  `VideoStream.test.js` plus `BoundingBoxDrawer.test.js` for blank fallback,
  unknown missing states, optional color catalog failure, explicit protocol
  badge text, and explicit tracker mode labels.
- Validation passed: focused dashboard component tests (20 passed), production
  dashboard build, API route inventory plus parameter reload tests (51 passed),
  schema check, and whitespace diff check.
- Live public screenshot evidence was not captured in this slice because only
  the hashed browser user file exists on disk; the active demo password was not
  rotated. Manual public retest remains with the active tester/password session
  or a deliberate credential rotation during a later test handoff.

## 2026-07-05 Runtime Log Bundle Export UX

- Closed PXE-0083's short gate for the Logs page export workflow.
- Added a post-download export summary in `dashboard/src/pages/LogsPage.js`
  showing filename, run ID, size, SHA-256, claim boundary, and download time.
- Updated FastAPI CORS middleware to expose `Content-Disposition`,
  `X-PixEagle-Run-ID`, `X-PixEagle-Log-Export-Sha256`,
  `X-PixEagle-Log-Export-Size`, and `X-PixEagle-Claim-Boundary` so the
  dashboard can read those headers when served from a different port/origin.
- Extended `LogsPage.test.js` and `test_api_exposure_policy.py` to cover the
  metadata panel and exposed-header contract.
- Updated `docs/core-app/03-api/README.md` with the export header contract and
  the boundary that PixEagle does not import or replay runtime log bundles in
  the live UI yet.
- Validation passed: Logs page test (5 passed), API exposure plus route
  inventory tests (124 passed), production dashboard build, schema check, docs
  consistency tests (23 passed), backend syntax check, and whitespace diff
  check.

## 2026-07-06 Typed System About Status

- Closed PXE-0084 with a typed read-only `GET /api/v1/system/about` contract.
- Added system/about Pydantic models for repository identity, local git
  metadata, backend status, runtime metadata, and a read-only update-status
  placeholder. The claim boundary states that this is process-local metadata
  only and not update, deployment, PX4, SITL, HIL, field, follower-response, or
  vehicle-response proof.
- Added a process-local snapshot builder in `api_v1_snapshots.py` with bounded
  local git commands, unknown-safe fallbacks, backend process/video status,
  uptime/run ID/Python version, and no runtime fetch/pull/restart behavior.
- Registered the route in the typed `/api/v1` route registry with
  `APISystemAboutResponse`, `SYSTEM_ABOUT_ERROR_RESPONSES`, operation ID
  `get_system_about`, and `system` tag. `api_security_policy.py` classifies the
  route under authenticated `system:read`.
- Updated route inventory, typed error-envelope path coverage, API/MCP
  candidate generation, docs-stage `agent_tools.yaml`, and generated
  non-callable candidate inventory. The new candidate is
  `pixeagle.system.about.read`, review-only, non-callable, and
  `mcp_exposure: none`.
- Updated the dashboard endpoint registry and `NavigationDrawer` About dialog
  to consume `/api/v1/system/about` first. Legacy `/api/system/config` fallback
  is kept only for missing/unsupported typed routes during rolling updates.
  The dialog now shows version, git commit/branch/date/dirty state, backend
  status, uptime, run ID, update status, repository link, and claim boundary.
- Updated API and agent-context docs so system/about appears with the current
  reviewed read-only process-local candidate set and without any MCP promotion
  or update-workflow overclaim.
- Added tests:
  - `tests/unit/core_app/test_api_v1_system_about.py`;
  - `dashboard/src/components/NavigationDrawer.test.js`;
  - endpoint, route inventory, candidate inventory, and docs guard updates.
- Validation passed:
  - backend syntax/import compile for touched backend/API/generator modules;
  - final focused backend/API/security/route/candidate/docs/parameter gate:
    112 passed;
  - dashboard focused tests: 6 passed;
  - dashboard production build passed as `main.1a5106b3.js`;
  - schema check up to date;
  - whitespace diff check passed.
- Independent reviewer agents completed for backend/API, frontend/operator UX,
  and docs/governance. Their findings were reconciled in
  `checkpoints/2026-07-06-phase-4-typed-system-about-status.md`.

## 2026-07-07 SIH Dev/Training Validation Surface

- Closed PXE-0085 with a typed read-only `GET /api/v1/sitl/status` contract.
- Added SIH validation status models for terminal commands, checked-in plan
  summary, latest local manifest summary, and strict claim boundaries. The
  route requires `debug:read`, is classified as sensitive validation metadata,
  and does not start Docker, PX4, MavlinkAnywhere, MAVLink2REST, PixEagle,
  Gazebo, X-Plane, or any mutation.
- Added a dashboard Validation page under the System navigation group. It shows
  the maintained official-PX4 SIH profile, plan details, latest manifest
  summary, and terminal commands without browser start buttons or raw SITL
  injection controls.
- Kept API/MCP governance fail-closed: generated candidate
  `pixeagle.sitl.status.read` is non-callable, `mcp_exposure: none`, blocked
  from default read-only promotion, and dated to the 2026-07-07 review.
- Reconciled independent reviewer findings:
  - required Phase 2 scenario coverage now uses the same required scenario set
    as the SITL harness, with regression coverage for missing scenarios;
  - free-form manifest text is sanitized so absolute local paths are not
    returned by the status API;
  - `raw_injection_controls_exposed` now describes dashboard controls rather
    than making a misleading API route-registration claim;
  - the dashboard marks previously loaded data stale after a failed refresh,
    wraps long artifact/failure strings on small screens, and visibly marks
    commands that require a prepared operator stack;
  - docs now explicitly say the route/page do not prove SITL runtime success.
- Updated API, SITL setup, agent-context, API modernization, issue register,
  and phase-map docs.
- Validation passed:
  - Python compile for touched backend/API/generator modules;
  - final focused backend/API/security/route/candidate/docs/parameter gate:
    116 passed;
  - final focused dashboard gate: 12 tests passed;
  - schema check up to date;
  - side-effect-free SIH dry-run printed the plan/evidence contract and did
    not start processes;
  - dashboard production build passed as `main.fe82c183.js`;
  - whitespace diff check passed.

## 2026-07-07 Safe Demo Cleanup And Update Workflow

- Closed PXE-0086 for safe quick-demo cleanup and non-destructive update
  workflows.
- Added `scripts/setup/quick-browser-demo-cleanup.sh` plus
  `make quick-browser-demo-cleanup`.
  - `DRY_RUN=1` previews without stopping services, touching files, restoring
    config, or inspecting UFW through sudo.
  - Destructive cleanup requires `CONFIRM=1`.
  - Cleanup stops the tmux demo when requested, removes the generated
    quick-demo credential store and one-time handoff file, preserves backups by
    default, and restores the `local_dev` profile by default so
    `configs/config.yaml` does not stay in browser-session LAN/public-demo
    posture after a bench test.
  - Public HTTP demo firewall cleanup now deletes the same broad UFW rule shape
    that public setup opens. LAN/private-overlay firewall cleanup requires an
    explicit/inferred CIDR and refuses ambiguous broad deletion unless
    `ALLOW_BROAD_FIREWALL_CLEANUP=1` is set.
- Updated `scripts/setup/quick-browser-demo.sh` so startup output prints exact
  cleanup commands including custom `SESSION_USER_FILE`,
  `CREDENTIAL_HANDOFF_FILE`, `DASHBOARD_PORT`, and `BACKEND_PORT`, and states
  that cleanup restores local-only config by default.
- Replaced risky update behavior:
  - `scripts/lib/sync.sh` now requires a clean worktree, fetches the exact
    requested remote branch into the remote-tracking ref, verifies the fetched
    ref, and uses `git merge --ff-only` only.
  - Linux and Windows installers now refuse dirty worktrees or branch mismatch,
    fail on fetch/ref errors, and fast-forward only.
  - Service sync/help text now reflects clean-worktree fast-forward behavior
    and prints post-update validation guidance.
- Updated README, installation, setup-profile, and service-management docs so
  beginner demo cleanup, public HTTP cleanup, config restoration, firewall
  scope, and update validation are consistent.
- Added focused tests for:
  - quick-demo dry-run cleanup and exact printed cleanup variables;
  - public broad-UFW cleanup dry-run;
  - LAN/private firewall cleanup refusal when no CIDR is known;
  - static no-stash/no-reset/no-merge-update contract;
  - live temporary Git dirty-worktree refusal before fetch;
  - live temporary Git fetch-failure behavior without stale update claims.
- Validation passed:
  - `bash -n scripts/setup/quick-browser-demo-cleanup.sh
    scripts/setup/quick-browser-demo.sh scripts/lib/sync.sh
    scripts/service/sync_and_restart.sh install.sh`;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_setup_profiles.py
    tests/test_docs_infrastructure_consistency.py -q`: 167 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `PYTHONPATH=src .venv/bin/python -m pytest
    tests/test_api_route_inventory.py
    tests/unit/core_app/test_parameters_reload.py -q`: 54 passed;
  - `git diff --check`: no whitespace errors, only the existing Git
    LF/CRLF warning for `install.ps1`.
- Independent review:
  - First docs/operator and governance reviewers found cleanup/profile,
    firewall, installer-fetch, reporting, and validation gaps.
  - After fixes, the replacement DevOps reviewer found no blocking issues and
    confirmed the prior script-safety findings were resolved.
- Boundaries:
  - No live UFW deletion was executed.
  - `pwsh` is not installed on this host, so `install.ps1` was reviewed and
    covered by static tests rather than parsed/executed in PowerShell.
  - No full clean temp-directory release walkthrough was claimed; that remains
    PXE-0074.

## 2026-07-07 Clean Setup/Update Handoff Walkthrough

- Advanced PXE-0074 with a repeatable clean-checkout setup/update evidence
  harness: `tools/run_setup_handoff_walkthrough.py`.
- The harness refuses handoff evidence from a dirty source worktree by default,
  clones the selected branch to a temporary checkout, verifies required public
  setup/docs/config files, runs safe documented dry-run/check-only commands, and
  writes command logs plus a manifest under
  `docs/reporting/agent-ops/codex-modernization/evidence/`.
- Default command coverage:
  - `make help`;
  - shell syntax checks for install/init/run/stop/quick-demo/cleanup/sync
    scripts;
  - pinned binary download plan with `--dry-run`;
  - setup-profile dry-runs for `local_dev`, `field_qgc_video`,
    `demo_lan_browser`, `qgc_direct_media`, and `production_remote`;
  - `make quick-browser-demo` with `DRY_RUN=1 START_DEMO=0 OPEN_FIREWALL=0`;
  - `make quick-browser-demo-cleanup` with
    `DRY_RUN=1 STOP_DEMO=0 CLOSE_FIREWALL=0`;
  - clean-worktree fast-forward sync check;
  - schema check and minimum backend/API tests.
- Added `tests/test_setup_handoff_walkthrough.py` so the plan cannot quietly
  grow service install, firewall mutation, binary download, or process-start
  side effects.
- Updated README and installation docs with the maintainer command and clear
  non-claims.
- Independent reviewer fixes applied before final evidence:
  - `git_status_initial` and `git_status_final` now fail when
    `git status --short --branch` contains any modified/untracked file line;
  - source git status is recorded at start before evidence artifacts are
    created;
  - `--include-dashboard` is documented as an optional npm-fetching lane;
  - the QGC firewall example is scoped to a trusted GCS IP/CIDR;
  - README service-install warnings now appear before sudo service commands;
  - `python src/test_Ver.py` is labeled as an OpenCV diagnostic, not release
    verification;
  - stale PXE-0079 phase-map text was removed.
- Captured evidence:
  - manifest:
    `docs/reporting/agent-ops/codex-modernization/evidence/2026-07-07-pxe0074-clean-handoff-walkthrough/manifest.json`;
  - source commit under test:
    `b954ff872da0d88464f66da2a4efee96fda129c5`;
  - result: required files passed, source git status at start empty, 22/22
    commands passed, failed commands none, and both temporary checkout
    `git_status_initial`/`git_status_final` records report
    `git_worktree_clean: true`.
- Closed PXE-0079 because its only remaining listed dependency was clean setup
  evidence, now covered by the PXE-0074 harness. PXE-0068 remains open for
  target deployment evidence only.
- PXE-0074 remains in progress, not done. At this 2026-07-07 checkpoint, the
  remaining gates were active public-demo cleanup/credential rotation after the
  current test session, optional `--include-dashboard` clean-clone
  release-candidate proof, production target evidence when selected, and final
  release/tag dry run. The 2026-07-08 journal entry below closes the dashboard
  clean-clone gate.
- Validation passed:
  - `PYTHONPATH=src .venv/bin/python -m pytest
    tests/test_setup_handoff_walkthrough.py
    tests/test_docs_infrastructure_consistency.py -q`: 26 passed;
  - `.venv/bin/python -m py_compile tools/run_setup_handoff_walkthrough.py
    tests/test_setup_handoff_walkthrough.py`;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `PYTHONPATH=src .venv/bin/python -m pytest
    tests/test_api_route_inventory.py
    tests/unit/core_app/test_parameters_reload.py -q`: 54 passed;
  - clean-checkout harness run: 22/22 commands passed with
    `git_worktree_clean=true` for initial and final temporary-checkout status.

## 2026-07-08 Dashboard Clean Handoff Walkthrough

- Continued PXE-0074 with the optional dashboard-inclusive clean-checkout lane.
- Ran:
  `tools/run_setup_handoff_walkthrough.py --include-dashboard --run-id 2026-07-08-pxe0074-dashboard-clean-handoff-walkthrough`
  from a clean source worktree.
- Captured evidence:
  - manifest:
    `docs/reporting/agent-ops/codex-modernization/evidence/2026-07-08-pxe0074-dashboard-clean-handoff-walkthrough/manifest.json`;
  - source commit under test:
    `2027e779841d13b00dc564fa5849dc7586888812`;
  - source git status at start: empty;
  - command result: 25/25 passed, failed commands none;
  - `git_status_initial.git_worktree_clean`: true;
  - `git_status_final.git_worktree_clean`: true.
- Dashboard clean-clone evidence:
  - `npm ci --no-audit --fund=false` passed and added 1607 packages in 27
    seconds;
  - `npm test -- --runInBand --watchAll=false` passed with 28 suites and 161
    tests;
  - `npm run build` compiled successfully and produced
    `build/static/js/main.fe82c183.js`.
- Evidence hygiene scan found only credential/token option names and inert local
  placeholder file paths in the manifest, not plaintext passwords or bearer
  tokens.
- Read-only DevOps/safety and operator-doc reviewer passes found no blocking
  findings; their stale-resume and wording findings were folded into the
  checkpoint, phase map, and offline report.
- Non-blocking warnings observed:
  - React Router v7 future-flag warnings in `NavigationDrawer.test.js`;
  - Node `fs.F_OK` deprecation warning during dashboard build.
- Boundaries:
  - this lane may fetch npm package artifacts through `npm ci`;
  - no service install, firewall mutation, MAVSDK/MAVLink2REST binary download,
    PX4/SITL/HIL, QGC playback, tracker/follower response, field behavior, or
    real-aircraft readiness is claimed.
- Remaining PXE-0074 gates are active public-demo cleanup/credential rotation
  after the current test session, production target evidence when selected, and
  final release/tag dry run on the exact release branch.

## 2026-07-08 QGC Windows Artifact And Receiver Handoff

- Resumed PXE-0070 after interruption and verified PixEagle and QGC feature
  worktrees were clean, no stale agents were active, and PR #13594 remained
  open/draft at head `b98848b2c`.
- Recovered the host from 99% filesystem use and about 1.2 GiB free to 91% use.
  Cleanup was limited to regenerated caches and named scratch material; source
  repos, reports/evidence, credentials, environments, and active Docker
  project state were preserved.
- Confirmed the upstream successful Windows artifacts had expired and could
  not be rerun or manually dispatched without upstream maintainer permission.
- Added a temporary fork-only Windows artifact workflow. First run
  `28969299585` proved configure/build, clean install, and plugin-check
  behavior but exposed a PowerShell summary parser error before upload.
- Fixed summary writing and the artifact action convention, then run
  `28971178285` passed build, installer creation, clean silent install, 28
  bundled GStreamer plugins, and artifact upload.
- Downloaded
  `/home/alireza/qgc-pr13594-windows-artifacts/run-28971178285/QGroundControl-installer-AMD64.exe`
  with SHA-256
  `3fff77fb0eb63c683a501856e7bc4e7389c365a4c3d955b7a75faacfd7f98856`.
- Removed the temporary workflow from the fork default branch after preserving
  the artifact; the QGC PR feature branch was not changed.
- Added the maintained QGC Windows receiver-test runbook for generic HTTP
  MJPEG, generic WebSocket JPEG, same-host PixEagle, and guarded remote
  PixEagle HTTPS/WSS. The current public browser-session demo is explicitly
  excluded as a native QGC credential target.
- Independent QGC receiver and release/DevOps reviewers confirmed the test
  matrix, evidence boundaries, and cleanup safety. PXE-0070 remains in progress
  until user playback/recording plus authenticated target proxy evidence pass.
- A resumed independent review on 2026-07-09 found that slash-style mismatch
  left the build GStreamer SDK in run `28971178285`'s installed verification
  PATH. The workflow was restored with canonical PATH filtering plus an
  explicit absence assertion. Corrective run `28993788648` failed that new
  guard because GitHub re-injected the build SDK path between steps, so fork
  commit `0952f43f2` moved installed verification into one sanitized
  PowerShell process and rerun `28998523729` was started. Packaged-runtime
  verification is not claimed until the corrected run passes.

## 2026-07-09 Tracker Identifier Normalization

- Resumed PXE-0008 after the QGC artifact gate was put back in progress and
  closed the stale sidecar audit agent. The sidecar audit confirmed that the
  next broader API slice should be typed tracker configuration mutation, but
  the immediate browser-reported `ACTION_TRACKER_SWITCH_INVALID` class of issue
  was narrower: tracker schema keys, factory keys, and class names were being
  treated inconsistently.
- Added schema-manager tracker identifier resolution so API/UI callers can use
  schema keys (`CSRTTracker`, `KCFKalmanTracker`, `DlibTracker`,
  `GimbalTracker`) or existing factory-key/config identifiers (`CSRT`, `KCF`,
  `dlib`, `Gimbal`). Successful AppController switches now store the canonical
  schema-manager key while retaining the factory key for tracker construction.
- Typed tracker catalog entries now expose both `request_tracker_type` and
  `factory_key`. Dashboard tracker selector sends `request_tracker_type`, while
  backend validation remains compatible with existing factory-key defaults.
- Updated tracker/API docs to describe the identifier contract and preserved
  the boundary that tracker switch/restart action resources are local PixEagle
  control-path records, not tracker-runtime, follower-response, PX4, SITL, HIL,
  field, or real-aircraft proof.
- Regenerated the generated API/MCP candidate inventory because typed contract
  and tracker helper provenance hashes changed. Candidates remain
  non-callable/unpromoted.
- Focused and broad validations passed so far:
  - `git diff --check`;
  - focused tracker catalog/action regressions: 5 passed;
  - `tests/unit/core_app/test_api_legacy_tracker_routes.py`: 6 passed;
  - `tests/unit/core_app/test_app_controller_offboard_safety.py` plus legacy
    tracker helper tests: 120 passed;
  - `tests/test_api_route_inventory.py`,
    `tests/test_api_tool_candidates.py`, and `tests/test_api_security_policy.py`:
    79 passed after regenerating candidates;
  - `tests/test_docs_infrastructure_consistency.py`: 23 passed;
  - `bash scripts/check_schema.sh`: schema current, 41 sections and 549
    parameters;
  - dashboard `CI=true npm test -- --runInBand --watchAll=false`: 28 suites
    and 161 tests passed;
  - dashboard `CI=true npm run build`: compiled successfully and produced
    `build/static/js/main.77a9c7d5.js`;
  - minimum API/config reload gate
    `tests/test_api_route_inventory.py tests/unit/core_app/test_parameters_reload.py`:
    54 passed.
- Corrected QGC Windows artifact run `28998523729` was still in progress when
  this tracker slice was checkpointed; no QGC packaged-runtime, playback,
  PX4/SITL/HIL, field, or real-aircraft success is claimed here.

## 2026-07-09 QGC Corrected Windows Package Evidence

- Corrective QGC fork run `28998523729` completed successfully at
  2026-07-09 06:51:13 UTC for workflow commit
  `0952f43f28f54f4c4a565ef2a7558537188b432c`.
- The run passed build, pre-install executable verification, NSIS installer
  creation, silent install, installed executable verification in the same
  sanitized PowerShell process without the build GStreamer SDK in `PATH`,
  installer locate, and artifact upload.
- Log evidence includes `Installed verification PATH excludes build GStreamer
  SDK` followed by `[OK] Tests passed`, closing the specific defect found in
  runs `28971178285` and `28993788648`.
- Downloaded corrected artifact `8191101989` to
  `/home/alireza/qgc-pr13594-windows-artifacts/run-28998523729/QGroundControl-installer-AMD64.exe`.
  Local size is `144012786` bytes and SHA-256 is
  `686b8fc07d8fabd0a64d59794ec554e3a4c27ccec9bc97cc599e7f48852479ef`;
  `sha256sum -c SHA256SUMS` passed.
- Fork commit `1fb98c85d` removed the temporary artifact workflow from the fork
  default branch after evidence was preserved. QGC PR #13594 remains open/draft
  and its feature branch remains unchanged at
  `b98848b2c5e9afb5109bd49200c1d9aaa0185e5c`.
- PXE-0070 remains open for Windows receiver playback/recording, authenticated
  PixEagle HTTPS/WSS proxy playback, negative auth/Origin/TLS tests, reconnect,
  redaction, sustained playback, and operator acceptance. No QGC receiver
  playback, PixEagle remote interoperability, PX4/SITL/HIL, field, or
  real-aircraft success is claimed.

## 2026-07-09 QGC Tester Handoff Source

- Added `tools/qgc_media_test_source.py`, a lab-only anonymous generated HTTP
  MJPEG and WebSocket JPEG source using Python standard-library networking plus
  an embedded JPEG fixture. The tool prints `http://.../mjpeg` and
  `ws://.../ws` URLs for QGC receiver tests and also serves `/still.jpg` and
  `/health`.
- Updated `docs/video/04-streaming/qgc-windows-receiver-test.md` with the
  optional lab-source commands for same-host loopback and trusted lab-network
  source-host testing. The docs explicitly warn not to expose the anonymous
  source publicly or treat it as PixEagle production media evidence.
- Created tester handoff copy:
  `/home/alireza/PIXEAGLE_QGC_WINDOWS_RECEIVER_TEST_HANDOFF_2026-07-09.md`.
- Validation passed:
  - `tests/test_qgc_media_test_source.py`: 3 passed;
  - `py_compile tools/qgc_media_test_source.py`: passed;
  - local endpoint smoke returned startup URLs, `/health` 200, and JPEG SOI
    bytes from `/still.jpg`.
- Clarified tester handoff networking: `127.0.0.1` is only for same-host QGC
  and media source; VPS tests use `204.168.181.45:8095`, and real Raspberry Pi
  or companion tests use the Pi/companion IP reachable from the GCS/QGC device.
- Started the temporary VPS bench source in tmux session
  `pixeagle-qgc-media-source`, opened UFW TCP `8095` with comment
  `TEMP QGC receiver lab media source`, and verified
  `http://204.168.181.45:8095/health` returns the expected URLs. This is an
  anonymous short bench source only and must be stopped/closed after receiver
  testing.

## 2026-07-09 QGC Lab Source Receiver Fix

- Investigated tester feedback that the QGC HTTP lane showed only a small
  orange-looking placeholder, the WebSocket lane stayed waiting, and VLC could
  not open `ws://204.168.181.45:8095/ws`.
- Root causes and clarifications:
  - the initial lab source repeated one static JPEG, so it was visually
    misleading as a "video" source;
  - the handler did not support `HEAD /mjpeg`, which can break receiver probes;
  - raw `ws://` binary JPEG streams are for QGC/WebSocket clients, not browser
    address bars or VLC.
- Updated `tools/qgc_media_test_source.py` to keep the runtime dependency-free
  while cycling three small generated JPEG frames, use HTTP/1.1 for WebSocket
  handshakes, answer `HEAD` for supported probe endpoints, expose
  `/ws-viewer` for browser WebSocket verification, and keep the original JPEG
  only as a fallback fixture.
- Updated the QGC Windows receiver runbook and offline handoff copy with the
  correct tester URLs and expected behavior:
  `http://204.168.181.45:8095/mjpeg` for browser/VLC/QGC HTTP MJPEG,
  `http://204.168.181.45:8095/ws-viewer` for browser WebSocket verification,
  and `ws://204.168.181.45:8095/ws` for QGC WebSocket JPEG mode.
- Restarted the temporary public VPS tmux source
  `pixeagle-qgc-media-source` after the fix and verified:
  - public `/health` advertises the corrected URLs;
  - public `HEAD /mjpeg` returns `HTTP/1.1 200 OK`;
  - public `/ws-viewer` serves the browser viewer page;
  - a raw WebSocket client receives a complete binary JPEG frame;
  - public MJPEG parsing sees distinct consecutive JPEG frames.
- Validation passed:
  - `python3 -m py_compile tools/qgc_media_test_source.py`;
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_qgc_media_test_source.py -q`;
  - `git diff --check`.
- No QGC receiver playback/recording success, PixEagle production media
  interoperability, PX4/SITL/HIL, field, or real-aircraft success is claimed
  from this fix.

## 2026-07-09 QGC Actual PixEagle Feed Bench

- Investigated tester feedback that the HTTP/VLC test still showed a generated
  placeholder instead of the video visible in the PixEagle browser dashboard.
- Clarified the architecture distinction:
  - port `8095` is a standalone anonymous generic lab source for QGC receiver
    smoke tests;
  - the actual PixEagle dashboard feed is served by backend media endpoints on
    port `5077` and requires `media:read` authorization.
- Generated an owner-only, out-of-repo temporary `media:read` bearer token file
  for the active VPS bench:
  `/home/alireza/.config/pixeagle/secrets/qgc-media-tokens.json`.
- Generated an owner-only plaintext tester handoff outside the repo:
  `/home/alireza/PIXEAGLE_QGC_ACTUAL_FEED_HANDOFF_2026-07-09.json`.
- Updated the live local `configs/config.yaml` to keep
  `API_AUTH_MODE: browser_session` for dashboard users while also loading the
  bearer token file for QGC actual-feed testing. This local config is ignored
  and was not committed.
- Restarted PixEagle. The restart downloaded the pinned MAVSDK Server binary
  because it was missing; SHA-256 verification passed and local provenance was
  appended under `bin/binary-provenance.jsonl`. Added that generated log path
  to `.gitignore`.
- Verified the actual-feed behavior:
  - anonymous `http://204.168.181.45:5077/video_feed` returns `401`;
  - authorized HTTP MJPEG with the generated bearer token returns actual
    PixEagle JPEG frames;
  - authorized WebSocket JPEG with bearer token and Origin
    `http://204.168.181.45:3040` returns an actual PixEagle binary JPEG frame.
- Updated the QGC receiver runbook, offline tester handoff, and quick test note
  so testers do not confuse the generic lab source with the actual PixEagle
  feed. The notes explicitly state that normal VLC URL entry is not a valid
  authenticated actual-feed test because it cannot attach the QGC Bearer
  credential.
- No QGC Windows GUI playback/recording, production HTTPS/WSS proxy,
  PX4/SITL/HIL, field, or real-aircraft success is claimed from this bench.

## 2026-07-10 QGC Host Authority Versus Client-IP Clarification

- Responded to operator feedback after successful QGC HTTP/WebSocket playback:
  selected GCS IP allowlisting, request Host authority validation, browser CORS,
  and PixEagle authorization needed a clearer operator-facing split before the
  QGC PR is moved back toward maintainer review.
- Clarified active docs that:
  - `Streaming.API_ALLOWED_HOSTS` validates the request Host authority used in
    the URL or reverse-proxy request;
  - it is not a selected GCS/source-IP allowlist and does not bypass auth;
  - selected GCS devices should be restricted by firewall, VPN/overlay, or
    reverse-proxy source-IP rules;
  - anonymous PixEagle media is controlled only by
    `ALLOW_UNAUTHENTICATED_MEDIA_STREAMING`;
  - authenticated direct QGC HTTP/WS media still requires generic QGC
    Authorization/Origin/TLS support.
- Added the same wording to README onboarding, scoped broad MAVLink firewall
  examples, and setup-profile summaries for `qgc_direct_media` and
  `unsafe_demo_lan_media_only` so command output does not imply that
  `PUBLIC_HOST` or `LAN_HOST` means "the trusted GCS client IP."
- Added docs-regression terms so this distinction remains visible in the QGC
  source plan, remote-media policy, and setup-profile documentation.
- Added focused behavior regressions proving that a configured PixEagle Host
  authority does not authorize a remote GCS client under `local_compat`, and
  that `API_ALLOWED_HOSTS` does not accept the GCS laptop address unless that is
  actually the request Host authority.
- Validation passed:
  - `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_docs_infrastructure_consistency.py tests/test_setup_profiles.py tests/unit/core_app/test_api_exposure_policy.py tests/unit/core_app/test_api_auth_runtime.py`;
  - `python3 -m py_compile scripts/setup/apply-setup-profile.py`;
  - `bash scripts/check_schema.sh`;
  - `git diff --check`.
- No PixEagle runtime behavior, QGC branch code, QGC receiver evidence,
  production proxy/firewall deployment, PX4/SITL/HIL, field, or real-aircraft
  claim is made from this docs clarification.

## 2026-07-09 Unsafe Anonymous Actual Media Bench

- Responded to tester/product feedback that a deliberate no-auth actual-feed
  lane is useful for quick VLC/QGC lab benches when the operator accepts that
  anyone reaching the URL can view the video.
- Added `Streaming.ALLOW_UNAUTHENTICATED_MEDIA_STREAMING`, default `false`, as
  the single source of truth for the exception. When true, it allows anonymous
  access only to `GET /video_feed` and `WS /ws/video_feed`.
- Implemented the explicit `unsafe_demo_lan_media_only` setup profile plus
  `make unsafe-demo-lan-media-profile LAN_HOST=<host>`. The profile writes
  exact Host/CORS allowlists, keeps `API_AUTH_MODE: local_compat`, clears
  bearer/session files for the media-only profile, and prints the HTTP MJPEG
  and WebSocket JPEG URLs with unsafe lab warnings.
- Kept the exception narrower than the route policy: query-string tokens still
  fail, invalid Authorization still fails, wrong WebSocket browser Origin still
  fails, and `/api/v1/streams/media-health`, `/status`, `/ws/webrtc_signaling`,
  dashboard/control/config/log/action routes remain authenticated.
- Updated active docs, schema, setup profile docs, QGC runbook, remote media
  policy, README, and offline handoff files. The current VPS ignored
  `configs/config.yaml` now enables the flag while preserving
  `API_AUTH_MODE: browser_session` and the existing dashboard login.
- Restarted PixEagle. `make run` returned the known non-TTY attach exit code
  after creating the `pixeagle` tmux session; backend `5077` and dashboard
  `3040` were listening.
- Public VPS probes verified:
  - anonymous `http://204.168.181.45:5077/video_feed` returned HTTP 200
    `multipart/x-mixed-replace` and valid JPEG bytes;
  - anonymous `ws://204.168.181.45:5077/ws/video_feed` returned a metadata
    text message followed by a binary JPEG frame;
  - WebSocket with `Origin: http://evil.example` was rejected;
  - unauthenticated `/api/v1/streams/media-health` and `/status` returned 401.
- Validation passed:
  - `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -q tests/unit/core_app/test_api_auth_runtime.py tests/unit/core_app/test_api_exposure_policy.py tests/test_setup_profiles.py tests/test_docs_infrastructure_consistency.py`;
  - `bash scripts/check_schema.sh`;
  - `git diff --check`;
  - `python3 -m py_compile src/classes/api_auth_runtime.py src/classes/api_legacy_media_routes.py scripts/setup/apply-setup-profile.py`.
- No QGC Windows GUI playback/recording, production HTTPS/WSS proxy,
  PX4/SITL/HIL, field, or real-aircraft success is claimed from this bench.

## 2026-07-12 GStreamer Output Runtime And Installer Closure

- Recovered the paused PXE-0040/PXE-0091 slice and reconciled both PixEagle and
  QGC worktrees before changing source.
- Completed validated, bounded GStreamer output scheduling, independent
  browser/GCS OSD state, serialized writer generations, retained asynchronous
  release ownership, retryable cleanup, and typed media-health reporting.
- Hardened the optional OpenCV-GStreamer source builder into a staged,
  manifest-backed replacement transaction with full native/Python backup,
  strict canonical containment, rollback, exact runtime checks, tracker
  instantiation, and an observed local GStreamer sink gate.
- Independent review identified and drove closure of shared renderer sizing,
  symlink containment, stale cleanup errors, tracked-helper rewriting,
  documentation drift, ignored native removal failures, and venv-root path
  aliases. Final review was clean and all subagents were closed.
- Validation passed: 228 focused tests, 430 Phase 0 tests, 54 minimum API/reload
  tests, 19 installer/venv tests, schema (41 sections / 548 parameters), API
  candidate drift, shell syntax, ShellCheck, dashboard 28 suites / 161 tests,
  production dashboard build, and `git diff --check`.
- The VPS active OpenCV 4.13.0 build reports GStreamer `NO`. No live UDP/RTP
  frame, QGC UDP receipt, target deployment, Gazebo, PX4/SITL/SIH/HIL, field,
  or aircraft success is claimed.
- Checkpoint:
  `checkpoints/2026-07-12-phase-4-gstreamer-output-runtime-closure.md`.

## 2026-07-13 Video-File EOF And Replay Safety

- Replaced generic live-source failure handling at normal `VIDEO_FILE` EOF with
  a schema-backed `LOOP`/`STOP` state contract.
- Preserved the consumed initialization probe, added verified seek/atomic
  reopen behavior for OpenCV and GStreamer, distinguished known mid-stream
  decode failures, and bounded unknown-length EOF classification to three
  consecutive empty reads.
- Added replay provenance, policy/state/epoch/loop diagnostics and reset
  deterministic PTS pacing across epochs.
- Kept replay useful for tracking/streaming while denying command freshness and
  Offboard start for video trackers; explicit `requires_video: false` external
  providers retain their independent contract.
- Iterative independent review moved from NO-GO on three findings to final GO.
- Validation passed: 225 focused tests, 430 Phase 0 tests, schema/API candidate
  drift, compile/whitespace checks, and a real 1613-frame OpenCV/MP4 boundary
  transition. A full-suite discovery run exposed two unrelated harness debts,
  now tracked as PXE-0093.
- Checkpoint:
  `checkpoints/2026-07-13-phase-4-video-file-eof-replay-safety.md`.

## 2026-07-13 Full-Suite Test Harness Closure

- Closed the two unrelated harness failures found during the video-file slice:
  the static WebSocket guard now follows the extracted exposure helper, and the
  recording overflow test deterministically blocks/releases its writer.
- Focused tests passed, the overflow test passed 20 repeated runs, and the
  complete maintained non-hardware/non-SITL gate passed with 2615 tests, 40
  optional dlib skips, one explicit deselection, and zero failures.
- No production source or runtime behavior changed.
- Checkpoint:
  `checkpoints/2026-07-13-phase-4-full-suite-test-harness-closure.md`.

## 2026-07-14 Configuration Authority And Transactional Runtime Sync

- Replaced absence-based config deletion with contract v2 exact-path
  operations and an explicit versioned retirement registry. Unknown
  extension/plugin keys are reported by type and preserved.
- Made the nested safety schema exact: complete strict global limits and sparse
  strict follower overrides now derive from the checked-in default contract.
- Added opaque process-local HMAC preview tokens, source-state checks, exact
  write receipts, final persistence preconditions, owned-artifact rollback, and
  operator-recovery behavior that preserves lock-ignoring external edits.
- Serialized persistent config mutation, runtime Parameters/Safety/Follower
  publication, follower-sensitive mutation, tracker model replacement,
  SmartTracker lifecycle, and target selection under explicit fail-closed
  barriers with post-validation target recheck.
- Added staged owner-controlled pre-update defaults for Linux and Windows,
  `.venv`-first lifecycle resolution, fail-closed launchers, native Windows CI
  contracts, and removal of the duplicate legacy OpenCV-GStreamer installer.
- Updated the Settings defaults-sync/import UX, production browser fixture,
  active setup/config/tracker/model docs, generated schema, API candidate
  provenance, and clean-clone/setup guards.
- Initial adversarial review found three P1 races/redaction gaps. Exact write
  receipts, malformed-query/userinfo detection, and the model-target barrier
  closed them with regressions.
- The final local completion audit additionally put per-frame SmartTracker
  inference/target loss and cancellation inside that barrier, moved receipt
  ownership to immediately after atomic replacement, retained audit ownership
  through exception paths, and classified credentials in URL fragments.
- Two delegated final re-review attempts exhausted the separate subagent quota
  before verdict. No independent final GO is claimed; the earlier independent
  findings plus local audit and exact broad gates are recorded instead.
- Validation passed: 210 focused tests; 448 Phase 0 tests; complete backend
  suite 2760 passed/47 skipped; dashboard 30 suites/181 tests, lint, and build;
  schema 40 sections/548 parameters; API candidate drift; Python compile; shell
  syntax; ShellCheck; and `git diff --check`.
- The ignored VPS config remained byte-identical at SHA-256
  `88c3fc36e07b3942a41858f1bc227b708c72b7a964ba9f043052a18cb286ea9d`,
  mode 0600, owner `alireza:alireza`, and 126661 bytes. No demo restart,
  native Windows result, dlib/GStreamer target proof, QGC receiver proof,
  PX4/SITL/SIH/HIL, field, or aircraft claim is made.
- Checkpoint:
  `checkpoints/2026-07-14-phase-4-config-authority-transactional-runtime-sync.md`.
- Next: commit/push PixEagle, complete exact QGC CI for the source-EOS
  recording-finalization repair, perform controlled ignored-config migration
  and public-demo probes, then update the QGC PR head/Windows artifact while
  keeping the PR draft through exact-artifact receiver testing.

## 2026-07-14 Public Demo And QGC Receiver Candidate

- Committed and pushed PXE-0094 plus three narrow truthfulness/docs follow-ups
  through clean PixEagle commit `cf16411a`.
- Preserved the ignored live config and existing browser credential, then used
  the authenticated contract-v2 preview/apply API to add two defaults and
  remove five explicitly registered retirements. The post-report has zero
  actionable operations and the managed backup is retained.
- Restarted the temporary public lab from `cf16411a` as run
  `pixeagle_20260714T132818Z_558897`. Dashboard, auth denial/login, anonymous
  actual-feed MJPEG/WS, wrong-Origin rejection, responsive SPA navigation, and
  at least 16 normal VIDEO_FILE loop boundaries passed. Optional AI,
  OpenCV-GStreamer, MAVLink2REST, and PX4 remain intentionally absent/degraded.
- Exact QGC head `ab5213f4f` passed 183 unit and 49 integration tests, including
  the source-EOS recording-finalization regression. Exact Windows CI built all
  variants and installed/verified the AMD64 package with the build SDK removed
  from PATH; macOS and the pre-commit/security/docs gates passed.
- Promoted only that head to PR #13594 with an explicit force-with-lease after
  confirming it was zero commits behind upstream. Replaced the stale title and
  body, posted exact evidence, and kept the mergeable PR draft pending manual
  Windows receiver evidence.
- Downloaded the new 144790567-byte Windows installer, verified PE/NSIS type and
  SHA-256
  `c84467e514c4db696e3431248471d912d3933ab40de69fd778445a618e6de6e2`,
  removed the superseded July 9 binary, and exposed the exact candidate plus
  checksum through the temporary dashboard. A full public-URL download
  reproduced the hash.
- Checkpoint:
  `checkpoints/2026-07-14-phase-4-public-demo-qgc-receiver-candidate.md`.
- Next: manual Windows HTTP/WS playback, reconnect/source-switch and playable
  MKV/MOV evidence; then PR evidence update. Fresh Raspberry Pi setup,
  production TLS/firewall evidence, public-demo cleanup/credential rotation,
  tag, and release remain later gates. No PX4/SITL/HIL/field claim is made.

## 2026-07-16 Runtime, Installer, And Model Release Hardening

- Recovered the intentional post-PXE-0095 worktree without resetting or
  touching the running public demo, ignored config, browser credential, or QGC
  artifact.
- Closed the OpenCV provider transaction and model admission/load lifecycle
  review findings, including exact provider/source evidence, lease-owned model
  names, legacy same-artifact evidence upgrade, private AI probe result
  transport, and delegated service cgroups.
- Replaced duplicate sync/restart surfaces with one stopped-runtime
  `make update` transaction. Runtime components retain shared source/venv locks;
  start/stop/update use deterministic lifecycle ownership; service generation
  is verified and atomically published; uninstall fails closed.
- Fixed a startup normalization defect where a legacy alias passed validation
  but the unnormalized mapping remained in memory and blocked unrelated config
  writes.
- The bounded independent updater review initially returned NO-GO with six
  reproducible findings. Added and proved collision guards for ignored operator
  paths, truthful partial-merge state, lifecycle-first lock ordering,
  stderr-aware `lsof` failure handling, hardened public service uninstall, and
  a stable candidate contract. The same reviewer returned GO after the fixes.
- Validation passed: 102 focused adversarial tests with one root-only skip;
  72 minimum API/reload tests; schema 40 sections/540 parameters; current API
  tool inventory; 36 docs/inventory tests; Python compile, Bash syntax,
  ShellCheck, and whitespace checks; final maintained suite 3275 passed,
  48 expected optional/platform skips, one deliberate deselection, zero
  failures. Earlier in this candidate, dashboard 49 suites/296 tests, lint, and
  production build passed; installer/runtime/model combined gates also passed.
- Deferred only nonblocking model internals cleanup as PXE-0097. Target
  OpenCV-GStreamer, trusted `.pt`/NCNN inference, delegated Full service, clean
  exact-commit checkout, controlled VPS migration, RPi5, QGC manual receiver,
  PX4/SITL/HIL, and field evidence remain separate gates.
- Checkpoint:
  `checkpoints/2026-07-16-phase-4-runtime-installer-model-release-hardening.md`.
- Committed and pushed exact candidate
  `b64d6c2817f21068073f63c66131258561f90125`. The isolated handoff then passed
  26/26 commands with clean initial/final state, dashboard `npm ci`, 49
  suites/296 tests, and production build. Its updater dry-run was deliberately
  skipped because the verified old public demo was still active; no live state
  was changed. Evidence:
  `evidence/2026-07-16-pxe0096-release-candidate/manifest.json`.

## 2026-07-16 Exact-Candidate VPS Handoff

- Backed up ignored config, sync metadata, audit state, credentials, models,
  service/runtime state, and config backups into one owner-only hashed bundle.
- Applied the 21-operation config contract-v2 migration with a managed backup;
  post-state is zero-actionable and existing browser/QGC credentials are
  byte-identical.
- Live startup exposed and closed a self-observing orphan PID pipeline and two
  false nested-lock warnings in dependency/log preparation. The fix stayed
  within `scripts/run.sh` plus executable regressions; no lifecycle abstraction
  or unrelated refactor was added.
- Focused 39/54/150-test gates, Bash syntax, ShellCheck, diff hygiene, and a
  bounded independent release-blocker review passed (`GO`).
- Pushed `9b1b6f6ce74106a93e4ee3d12872950c89dc6cca`; exact run
  `pixeagle_manual_97590a57-ba07-4781-a99e-5acf76e0d456` is healthy and passed
  public dashboard 200, unauthenticated API 401, MJPEG, WebSocket JPEG,
  wrong-Origin 403, and desktop/mobile pre-login checks.
- Maintainer authenticated UI/action review is next. RPi, AI/GStreamer target,
  SIH/PX4, QGC, production TLS, cleanup, tag, and release remain separate gates.
- Checkpoint:
  `checkpoints/2026-07-16-phase-4-exact-candidate-vps-handoff.md`.

## 2026-07-18 Beta.4 Tracker And Config Acceptance Slice

- Recovered the interrupted worktree at the published beta.3 commit without
  resetting or touching ignored runtime configuration, credentials, or the
  public process. The slice was narrowed to concrete operator feedback: the
  point-click ROI, manual retarget identity, detector threshold enforcement,
  and CSRT configuration drift.
- Set the point-click ROI authority to `0.08` in both the dashboard default and
  JavaScript fallback, with a repository consistency test. Drag selection still
  submits the exact operator-drawn region.
- Added the detector `initialize_target` contract with frame/ROI validation and
  replaced TemplateMatching identity state atomically on every explicit target
  selection. Recovery now resets score state, rejects non-finite values, and
  enforces method-aware configured thresholds.
- Routed CSRT/KCF/dlib startup through the shared detector contract. Mapped
  CSRT schema names to runtime/OpenCV fields, made its appearance-update gate
  effective, and aligned missing/unknown fallback with the checked-in robust
  mode.
- Removed unreachable detector prototypes and registered their four unused
  FeatureMatching/ORB/RANSAC keys for explicit schema `1.4.0` retirement.
  Generated schema descriptions/options and tracker configuration docs were
  corrected; existing operator config remains under the normal backup-based
  sync workflow.
- Validation: focused Python `456 passed, 39 skipped`; dashboard click/retarget
  `9 passed`; schema `40 sections / 534 parameters`; compile and diff checks
  passed. The broad backend gate then passed `3352 passed, 48 skipped` after
  updating the intentional schema-version assertion. Isolated dashboard CI
  passed `npm ci`, `53 suites / 339 tests`, lint, production build, and
  `npm audit --omit=dev` with zero runtime vulnerabilities. No independent reviewer GO is claimed because the prior delegated
  review quota was exhausted; local bounded CV/safety/config/hygiene review
  found no release-blocking issue in this slice.
- Checkpoint:
  `checkpoints/2026-07-18-phase-5-beta4-tracker-acceptance-readiness.md`.
- Next: broad release gates, beta.4 version/tag/public refresh, then operator
  VPS retest. Fresh Ubuntu remains after public acceptance; recorded-video
  tracker benchmarks, WebRTC ICE/TURN, PX4/SIH/SITL/HIL, and Raspberry Pi
  evidence remain separately tracked.

## 2026-07-18 Beta.4 VPS Handoff

- Reconciled the exact published beta.4 source (`f16133de`) with the live
  manual browser bench run `pixeagle_manual_f6549d79-e917-454c-86ee-d12a2dfb9230`.
- Confirmed the dashboard is public HTTP 200 while the unauthenticated typed
  About route remains 401. Public MJPEG returns multipart JPEG data and public
  WebSocket negotiation returns metadata followed by a binary JPEG frame.
- Confirmed runtime streaming counters continue with zero sampled drops. The
  only persistent degraded status is the intentional absence of MAVLink2REST/
  PX4 sidecars in the browser-only profile.
- Applied the four registered detector-key retirements through the normal
  authenticated config-sync transaction while stopped; the owner-only backup
  remains available and credential bytes were preserved.
- Ran the clean no-touch setup handoff against beta4 with a private example
  LAN host: 23/23 checks passed, including schema, profile dry-runs, shell
  syntax, minimum API tests, and clean initial/final checkout state. The
  updater check was separately refused while the live runtime was active, as
  required by the stopped-runtime safety contract.
- The VPS AI checker reports CPU PyTorch/Ultralytics and a verified local
  `yolo26n.pt`; GStreamer, dlib, NCNN, PNNX, CUDA, PX4, and MAVLink sidecars
  are not claimed for this bench.
- Checkpoint:
  `checkpoints/2026-07-18-phase-5-beta4-vps-handoff.md`.
- Offline handoff:
  `/home/alireza/PIXEAGLE_BETA4_VPS_TEST_HANDOFF.md`.
- Next: maintainer VPS retest. Only after acceptance, run the documented exact
  commit or beginner Ubuntu bootstrap and record fresh-host evidence.

## 2026-07-20 Beta.13 Profile-Driven Python Compatibility

- Resumed the bounded installer-recovery slice after a usage-limit pause. The
  worktree was reconciled against `fbe4cc8`; no ignored runtime config,
  credentials, models, or public-process state was reset.
- Replaced the stale global PyTorch/Python gate with the schema-v2
  `scripts/setup/pytorch_matrix.json` policy and the shared
  `check-python-compatibility.py` validator. Core, Linux CPU, CUDA/macOS, and
  Jetson profiles now own their compatibility ranges and maintenance tracks.
- `scripts/init.sh` now resolves an explicit interpreter, reuses a valid
  existing PixEagle venv during repair, creates new venvs with that interpreter,
  offers an intent-preserving Core fallback for interactive incompatible Full
  setup, and fails unattended Full setup closed. Automatic PyTorch setup can
  select a compatible CPU profile and removes stale torchaudio when the
  selected profile does not use it.
- Focused installer/policy tests passed 53/53; the broader setup/docs suite
  passed 233/233. The clean official Ubuntu 26.04 image (Python 3.14.4)
  installed Core, PyTorch CPU (`2.12.1+cpu`/`0.27.1+cpu`), and the verified
  Ultralytics wheel (`8.4.95`) with successful imports and OpenCV provider
  preservation. The project pip policy accepted only the documented
  Ultralytics `opencv-python` metadata-name mismatch; unrelated pip failures
  remain fatal.
- Existing host Python 3.12.3 passed the AI runtime diagnostic with verified
  local YOLO first inference and CPU fallback. The isolated container's heavier
  model probe was not counted after its constrained memory limit killed it;
  this is explicitly recorded in the evidence manifest.
- Committed candidate `d1a11bf2383de992fa949bdb9235d976fb24af1a` then
  passed the maintained dashboard-inclusive clean-checkout handoff 26/26 with
  clean source state, strict `npm ci`, 53 suites/348 dashboard tests,
  production build, schema, profile dry-runs, and minimum backend/API checks.
- Checkpoint:
  `checkpoints/2026-07-20-phase-5-python314-full-ai-compatibility.md`.
- Next: release-gate validation, beta.13 publication, credential-preserving
  VPS refresh, and maintainer fresh Ubuntu/Raspberry Pi handoff. QGC, PX4/SIH/
  SITL/HIL, GStreamer target, and field evidence remain separate slices.

## 2026-07-20 Beta.13 Publication And Handoff

- Published the completed PXE-0114 installer/policy slice after the evidence-log
  continuation was normalized and committed. `main` and annotated tag
  `v7.0.0-beta.13` now point to `985ecbd3ed18c0da79de15c7ec49ed97ceb28354`.
- GitHub prerelease:
  <https://github.com/alireza787b/PixEagle/releases/tag/v7.0.0-beta.13>.
- The release is intentionally bounded: it records Core/Full dependency and
  clean-checkout evidence, but does not claim Raspberry Pi, PX4/SIH/SITL/HIL,
  QGC, GStreamer target, production networking, field, or aircraft readiness.
- Next operational step is a credential-preserving refresh of the public
  browser-only VPS bench, followed by the maintainer's fresh Ubuntu run from
  the published beta. Minor nonblocking script-parity debt remains deferred in
  PXE-0111; QGC remains a separate later slice.

## 2026-07-21 Beta.14 Guided Installer And GStreamer Recovery

- Recovered the beta.13 maintainer transcript and isolated two concrete
  defects: Bash dynamic-scope shadowing discarded explicit yes/no input in
  both bootstrap layers, and Python helper bytecode mutated the pinned OpenCV
  source export after an otherwise successful GStreamer build.
- Replaced the colliding input buffers, made terminal loss fail closed, used
  nonblocking unattended sudo validation, and added pseudo-terminal coverage
  for explicit yes/no, Enter defaults, one-line input forwarding, and outer
  checkout-update refusal.
- Simplified the guided UI. Enter now means Core plus only the shell shortcut;
  dlib, OpenCV/GStreamer, service, and auto-start remain explicit. Ambiguous
  `none` combinations are rejected.
- Disabled Python bytecode writes in the OpenCV builder while preserving the
  strict source-tree digest, private staging, prior-provider protection, and
  atomic replacement order. Real source mutation remains a tested failure.
- Kept model acquisition outside setup. PXE-0116 tracks a later bounded,
  default-No, immutable-manifest installer; the existing digest-pinned manual
  lab example remains the supported path.
- Focused and broad installer/setup/update/docs tests, required Phase 0,
  schema, Bash/ShellCheck/diff gates, dashboard lint, 53 suites/348 tests, and
  production build passed. Exact candidate `a1bce296` then passed the
  dashboard-inclusive clean-checkout handoff 26/26 with clean initial/final
  state, and an independent release-blocker review returned GO.
- Checkpoint:
  `checkpoints/2026-07-21-phase-5-beta14-guided-installer-gstreamer-recovery.md`.
- Next: publish beta.14, refresh and smoke the browser-only public bench, then
  have the maintainer rerun the complete optional GStreamer path on disposable
  Ubuntu. No target GStreamer, QGC, Raspberry Pi, PX4, field, or aircraft claim
  is made before its separate evidence gate.

## 2026-07-21 Beta.14 Publication And Browser Bench

- Committed the exact clean-handoff manifest and checkpoint as `f4a87fbe`,
  pushed `main`, created annotated tag `v7.0.0-beta.14`, and published the
  prerelease:
  <https://github.com/alireza787b/PixEagle/releases/tag/v7.0.0-beta.14>.
- Replaced the old public browser-only process without changing ignored
  config, credential-store, model, or audit data. The refreshed run is
  `pixeagle_manual_a81bc1b5-7ac9-4fa0-85b7-7eb889574ffa` at commit `f4a87fbe`.
- Public dashboard returned 200; anonymous lab MJPEG returned multipart JPEG;
  WebSocket delivered metadata followed by a JPEG; authenticated login/About
  returned beta.14; unauthenticated API and wrong-Origin requests remained
  denied. MAVSDK Server, MAVLink2REST, PX4, and simulation sidecars stayed off.
- Runtime logs contained no unexpected errors or tracebacks. The remaining
  warnings are bounded and expected for this bench: absent MAVLink2REST,
  source/configured video dimension mismatch, OSD auto-degrade, and the
  intentional public lab exposure warning.
- Evidence:
  `evidence/2026-07-21-pxe0115-beta14-public-browser-smoke/manifest.json`.
- Next operator gate: rerun the optional GStreamer build on the disposable
  Ubuntu host through `make check-gstreamer-runtime`; only then update PXE-0115.

## 2026-07-21 - PXE-0118 beta.15 service readiness closure

- Used authorized key-based access to repair the disposable Ubuntu 24.04 Full
  AI host without wiping config, credentials, models, logs, binaries, or the
  verified source-built OpenCV/GStreamer provider.
- Fixed tracked launcher mode mutation, no-PX4 MAVSDK gRPC readiness,
  service/reboot actions inside update reconciliation, automatic restart
  bounding, explicit start-limit recovery, exact tmux window reporting, and
  optional legacy port status.
- Exact `ea725c76` passed update/reconciliation, start/restart, systemd settle,
  four-pane ownership, loopback listener, dashboard/About/MJPEG, clean-checkout,
  and active JSONL log gates. `NRestarts=0`; no error-like active log entries.
- The maintained GStreamer check passed OpenCV 4.13.0, all required QGC
  H.264/RTP/UDP elements, and effective `x264enc`; CSRT and dlib 20.0.1 also
  loaded. This closes PXE-0115's disposable-host gate without claiming receiver
  playback or target-board performance.
- The corrected release-gate run `29809434623` passed all required jobs,
  including the full backend suite (`3417 passed, 49 skipped, 1 deselected`),
  dashboard tests/lint/build, Windows setup contracts, schema/infrastructure
  checks, and coverage. The annotated prerelease `v7.0.0-beta.15` was
  published at `c34c8165a22ab142c60103247926403e8d2701b4`.
- Next: run fresh Ubuntu beginner and exact-tag Raspberry Pi Core-first
  acceptance. No PX4, QGC, remote WebRTC, field, or aircraft success is
  implied.

## 2026-07-21 - PXE-0119 GitHub Actions pin cleanup

- Audited every maintained workflow and the gimbal simulator CI example after
  GitHub reported Node 20 action deprecation warnings.
- Replaced mutable major tags with reviewed immutable commits for checkout,
  Python, Node, artifact upload, and Codecov. Added a monthly Dependabot lane
  so future action revisions are reviewable rather than silently forgotten.
- Added an infrastructure regression test that rejects unapproved/mutable
  action references and requires the Dependabot GitHub-Actions configuration.
- YAML parsing and the full infrastructure documentation test passed (`26
  passed`). That run exposed two stale `upload-artifact@v4` assertions in the
  SIH/Gazebo contract tests; both were replaced with the immutable
  SHA/version-comment contract and the focused combined suite passed `100`.
  The corrected release-gate run `29809434623` then passed, including the full
  backend suite (`3417 passed, 49 skipped, 1 deselected`), dashboard,
  Windows, schema, infrastructure, and coverage jobs. The annotated
  prerelease `v7.0.0-beta.15` was published at
  `c34c8165a22ab142c60103247926403e8d2701b4`. The earlier runtime candidate
  remains valid evidence for VPS service behavior.

## 2026-07-21 - PXE-0124 beta.16 browser-ready bootstrap recovery

- Reproduced the fresh-service failure from the disposable-host evidence: setup
  held the exclusive source/environment resource while the service's four
  runtime components correctly requested shared access. The later systemd retry
  succeeded only after setup exited.
- Moved service onboarding past lock release and removed runtime start/reboot
  from that onboarding. Added asynchronous managed start/restart with visible,
  bounded exact-runtime readiness and interrupt diagnostics.
- Made `make setup-status` distinguish an exclusive setup lease from a shared
  runtime/reader instead of suggesting lock deletion.
- Joined the one-line bootstrap to the existing browser-lab profile: host and
  credentials are guided, Enter keeps admin/admin, public HTTP is visibly
  warned, active UFW is handled, dashboard HTTP is checked, and the exact URL is
  printed. Only lab TCP 3040/5077 are exposed by this path.
- Replaced active ambiguous MAVSDK `udp://` examples/defaults with explicit v3
  `udpin://`/`tcpout://`; generic video/GStreamer UDP URLs were not changed.
- Local gates passed: Phase 0 `482`, API/reload `72`, dashboard touched tests
  `11`, production dashboard build, schema `40/535`, shell/static checks, and
  the focused installer/setup/service/PX4 slice (`679` passed, `1` skipped),
  plus the SIH/SITL plan-contract suite (`74` passed). Fresh remote VPS
  acceptance, CI, beta.16 publication, and Raspberry Pi remain next.

## 2026-07-21 - PXE-0125 beta.17 model selection and replay UX

- Live VPS API and log evidence showed that selecting the uploaded VisDrone or
  YOLO26-OBB model changed persisted config but not the `Parameters` runtime
  snapshot. Smart Mode consequently retried stale untrusted default paths.
- Replaced generic publication with one transactional publication of both
  validated model variants and any explicit GPU/CPU preference. Generic path
  edits retain their honest system-restart tier. The Models UI distinguishes
  configured `selected` state from runtime-proven `active` state and preserves
  actionable switch outcomes.
- Made GStreamer video-file capture timing-mode aware. Real-time replay uses the
  media clock and drops stale frames; deterministic/throughput modes preserve
  frames. Main-loop pacing now accounts for capture plus processing time.
- Reduced the final bootstrap handoff to a compact network/local/address-change
  choice, reports the resulting login accurately, and prints the exact cleanup
  command including UFW reversal; `VERBOSE=1` retains diagnostics.
- Two independent reviews found nine concrete issues; all blocking and bounded
  findings were repaired. Final local gates passed: focused slice `432`, required
  API/reload `72`, docs `27`, dashboard model tests `9`, schema `40/535`, Bash/
  Python/diff checks, and production dashboard build. Exact VPS refresh/smoke
  remains before maintainer browser acceptance.
- Initial GitHub run `29839704958` caught only test-lint formatting and stale
  generated API source digests. The assertions and inventory were corrected;
  dashboard lint, focused UI tests, inventory check, and the exact `145`-test
  Phase 0 command then passed locally before the follow-up push.
- Checkpoint:
  `checkpoints/2026-07-21-phase-5-beta17-model-selection-replay-ux.md`.

## 2026-07-21 - PXE-0126 beta.18 operator handoff controls

- Replaced the combined optional-component selection with separate dlib,
  OpenCV/GStreamer, and shell-shortcut questions. Enter keeps dlib and the long
  source build off, installs the shortcut, installs service controls, and keeps
  boot auto-start and SSH login hints off unless explicitly selected.
- Added one shared interface-aware IPv4 discovery helper for the outer installer
  and browser-lab launcher. Operators can select a detected LAN, overlay/shared,
  or public address, choose loopback, or enter an explicit address; bridge/veth
  noise is suppressed when a usable host interface exists.
- Added optional bounded model display names to trusted provenance metadata.
  Artifact filenames, paths, digests, and validation remain the execution
  identity. The Models page now uses conventional selected radio state and the
  compact status bar reports the model actually loaded by Smart Mode.
- Added a small Follower Test toggle beside the circuit breaker. It changes the
  canonical `Follower.FOLLOWER_EXECUTION_MODE` value to `COMMAND_PREVIEW` only
  while the circuit breaker is active; preview records local command intent and
  has no MAVSDK/PX4 publisher.
- Rechecked the maintained RTSP/GStreamer, Gimbal tracker, Topotek SIP UDP
  provider, and `gm_velocity_vector` contracts. Replaced stale follower tuning
  documentation and produced an external merge-only client profile. Historical
  repository evidence says `.110`; the user's `.108`/`.109` recollection has
  not been verified on hardware.
- Focused backend, installer, dashboard, RTSP/gimbal, schema, syntax, lint, and
  production-build gates passed. The final clean backend suite passed `3,444`
  tests with `48` expected skips and one deferred Starlette/httpx test-toolchain
  warning; the full dashboard passed `54` suites / `358` tests, lint, and build.
- Independent review found three bounded defects in active-session safety truth,
  duplicate display-label selection, and installation wording. All were fixed
  with regression coverage, and the independent re-review returned `GO`.
- Initial GitHub run `29862000612` passed its lint, Windows, dashboard, unit,
  and integration stages, then exposed three installer tests that accidentally
  resolved the new address helper through the developer's real `~/PixEagle`.
  The shared Bash-test harness now pins `PIXEAGLE_HOME` to the checkout unless a
  test explicitly overrides it; all `38` installer UX tests pass with a
  deliberately nonexistent `HOME`.
- Corrected GitHub CI/tag/publication and all physical gates remain separate.

## 2026-07-21 - PXE-0128 beta.19 PX4 connectivity handoff

- Consolidated PX4/MAVLink onboarding under one canonical connectivity guide.
  The deployment router must fan the same vehicle network to local UDP `14540`
  for MAVSDK and `14569` for MAVLink2REST; PixEagle owns both local consumer
  processes but not the physical UART, radio, Ethernet, SITL source, or router.
- Corrected stale `14570`, duplicate MAVLink2REST startup, external-versus-
  embedded MAVSDK, and MavlinkAnywhere dashboard/router-mode documentation.
  UDP `14550` is now explicitly mode-dependent and cannot be two listeners on
  the same address.
- Verified that pinned MAVSDK Server v3.12.0 has no gRPC bind-host option and
  listens on `0.0.0.0:50051`. PixEagle connects through `127.0.0.1`; installer,
  runtime, and docs now require an untrusted-interface firewall boundary and do
  not imply that the listener is loopback-only.
- Kept beginner browser behavior simple: Enter enables the network lab,
  dashboard/backend bind to `0.0.0.0`, and the selected requested or
  primary-route device address is printed as the browser URL. Wildcard binds
  are never presented as navigable URLs. The lab opens only TCP `3040` and
  `5077`; its bundled-video runtime disables MAVSDK Server and MAVLink2REST.
- Final gates passed: focused setup/runtime/docs `281`; API/reload `72`; schema
  `40` sections / `535` parameters; backend `3,448` passed / `48` skipped with
  the one PXE-0127 warning; dashboard `54` suites / `358` tests, lint, and
  production build; Bash syntax, ShellCheck, Python compile, diff checks, and
  independent release-blocker re-review `GO`.
- Exact clean code candidate `da22d1dd` passed the setup handoff walkthrough
  `24/24`; the manifest and bounded logs are stored under
  `evidence/2026-07-21-pxe0074-clean-handoff-walkthrough-232742Z/`.
- No PX4/SITL/HIL, physical camera/gimbal, Raspberry Pi, QGC, field, or aircraft
  success is claimed. Maintainer fresh-host and target execution are the next
  gates.
- Release commit `638fcbf3` passed GitHub Actions run `29877355992`; annotated
  prerelease `v7.0.0-beta.19` was published from that exact commit.
- MavlinkAnywhere `v3.0.14` still exposes the serial/UDP source, explicit
  endpoint, and dashboard options used by the guide. Fresh-host and target
  execution remain unclaimed.

# Codex Modernization Journal - 2026-05

## 2026-05-07 - Resume And Phase 0 Dashboard Debt Closure

- Resumed after a long pause by rereading the modernization plan, architecture
  blueprint, API blueprint, issue register, and current worktree state.
- Confirmed the repository is intentionally dirty from earlier Phase 0 slices;
  no unrelated local changes were reverted.
- Rechecked companion tools before continuing:
  - `mavlink-anywhere` is current at `fd80c48`, tag `v3.0.8`.
  - local `mavsdk_drone_show` is behind `origin/main` by 26 commits; remote
    head is `3c42c7b2`, tag `v5.3.56-connectivity-profile-altitude`.
  - Smart Wi-Fi Manager remote tags now include `v2.1.8`; no local clone was
    present under `/home/alireza`.
- Read `mavsdk_drone_show` remote `origin/main` connectivity docs directly
  instead of trusting the stale local checkout. Key drift: environment registry,
  Fleet Ops connectivity posture, Smart Wi-Fi sidecar integration, safe Wi-Fi
  secret handling, network transport status, and current companion pins.
- Finished PXE-0009/PXE-0010 dashboard debt slice:
  - removed unused dashboard imports/locals that were producing ESLint
    warnings;
  - added an explicit dashboard `lint` script and CI lint step;
  - upgraded or moved dashboard dependencies so runtime audit is clean;
  - removed unused `styled-components`;
  - mocked backend status in the dashboard shell test so tests do not make live
    backend requests;
  - enabled React Router future flags to remove migration warnings.
- Closed PXE-0009 and PXE-0010.
- Added PXE-0021 for the remaining deprecated CRA/react-scripts dev-toolchain
  audit debt and PXE-0022 for reconciling PixEagle API/MCP/devops plans with
  newer companion environment/connectivity standards.
- Validation run for the dashboard slice:
  - `npm ci` passed.
  - `npm run lint -- --format unix` passed.
  - `CI=true npm test -- --watchAll=false` passed, 1 test.
  - `npm run build` passed.
  - `npm audit --omit=dev --json` reported zero vulnerabilities.
  - full `npm audit --json` still reports 26 dev-only vulnerabilities rooted
    in `react-scripts`; `npm audit fix --force` would install
    `react-scripts@0.0.0`, so the correct fix is a planned build-tool
    migration, not a forced audit rewrite.

## 2026-05-21 - Resume, Companion Drift, And Slice Map

- Resumed after another pause and re-read the operating docs, modernization
  plan, architecture blueprint, API blueprint, issue register, journal, and
  checkpoint list.
- Confirmed the interrupted PXE-0016 gimbal provider patch did not leave a
  partial `src/classes/gimbal_provider.py` file.
- Refreshed companion repositories:
  - `mavlink-anywhere` remote is now `v3.0.10`; local checkout is behind four
    commits.
  - `mavsdk_drone_show` remote is now `v5.5.6-sitl-image-refresh`; local
    checkout is behind 74 commits.
  - cloned `/home/alireza/smart-wifi-manager` for local review; latest checked
    tag is `v2.1.11`.
- Recorded companion drift in
  `docs/reporting/agent-ops/codex-modernization/audits/2026-05-21-resume-companion-drift.md`.
- Added `docs/reporting/agent-ops/codex-modernization/phase-slice-map.md` as
  the durable resume anchor for future pauses and context compaction.
- Updated PXE-0022 with current companion pins and sidecar requirements:
  token-gated remote mutations, dry-run/apply profile workflows,
  `fleet-merge` defaults, safe Wi-Fi secret handling, and current MavlinkAnywhere
  fleet profile APIs.
- Active slice remains PXE-0016: gimbal provider abstraction.

## 2026-05-21 - Phase 1 Gimbal Provider Boundary

- Closed PXE-0016 by moving the current Topotek SIP-over-UDP path behind a
  normalized `GimbalInputProvider` and `SipUdpGimbalProvider` factory.
- Added shared neutral DTOs in `src/classes/gimbal_types.py` so provider
  adapters are not forced to import the Topotek network client.
- Updated `GimbalTracker` to select `GimbalTracker.PROVIDER`, expose provider
  metadata/capabilities, and distinguish `has_output`, `gimbal_tracking_active`,
  and `usable_for_following`.
- Fixed a safety issue where stale `TRACKING_ACTIVE` state could be reused
  while fresh angle packets were still arriving.
- Fixed a follower safety issue where unusable/stale external gimbal output
  could skip command dispatch and leave the last command path alive; the
  follower now emits zero body velocity/yaw commands for that condition, and
  AppController plus the `Follower` manager wrapper route this explicit
  fail-closed case through to PX4 command dispatch.
- Fixed a discovered `GimbalInterface` lock re-entry deadlock by using
  `threading.RLock`; the issue was exposed by the new provider health test.
- Updated backend tracker status and dashboard display so stale or inactive
  angle output can be inspected by operators while remaining unusable for
  following.
- Updated config defaults/schema and docs for the provider boundary, current
  Topotek-only support, simulator examples, and external provider integration.
- Added PXE-0023 to track future MAVLink Gimbal v2/vendor provider adapters
  separately from the now-complete provider boundary.
- Added PXE-0024 to track dashboard emphasis for degraded/stale visible tracker
  output during the later dashboard/API normalization slice.
- Validation completed:
  - focused backend gimbal/follower/AppController/telemetry tests: 125 passed,
    2 skipped;
  - touched-module `py_compile`: passed;
  - `scripts/check_schema.sh`: schema up-to-date;
  - `make phase0-check`: 22 passed;
  - dashboard lint/test/build: passed;
  - `git diff --check`: passed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-05-21-phase-1-gimbal-provider.md`.
- Next active slice is Phase 2 Offboard commander and safety truth
  (PXE-0007/PXE-0013).

## 2026-05-21 - Phase 2 Offboard Safety Audit Kickoff

- Started Phase 2 PXE-0007/PXE-0013 with a read-only audit of command
  publication, Offboard start/stop, setpoint sender behavior, MAVSDK command
  errors, target loss, video stalls, and operator abort paths.
- Confirmed the current Offboard stream is frame/tracker-loop coupled:
  `SetpointSender` validates/logs but does not publish MAVSDK commands.
- Decomposed the audit into explicit tracked issues PXE-0025 through PXE-0033.
- Recorded findings in
  `docs/reporting/agent-ops/codex-modernization/audits/2026-05-21-phase-2-offboard-safety-audit.md`.
- Corrected the new gimbal fail-closed regression test to assert the real
  `velocity_body_offboard` send path for `gm_velocity_vector`.

## 2026-05-21 - Phase 2 Fail-Open Fixes

- Closed PXE-0025: Offboard start errors returned by
  `PX4InterfaceManager.start_offboard_mode()` now abort `connect_px4()` instead
  of marking following active.
- Closed PXE-0026: PX4 send methods now return explicit booleans and
  `AppController.follow_target()` returns false on failed MAVSDK publication.
- Closed PXE-0028: Offboard-exit callbacks from the MAVLink polling thread now
  schedule cleanup through the captured app event loop or clear local follow
  state synchronously if no loop is available.
- Closed PXE-0029: `SetpointSender.get_status()` now exists for disconnect and
  health/status paths.
- Added regression tests in:
  - `tests/unit/core_app/test_app_controller_offboard_safety.py`
  - `tests/unit/drone_interface/test_px4_interface_manager.py`
  - `tests/unit/drone_interface/test_setpoint_sender.py`
- Validation completed:
  - targeted Phase 2 tests: 71 passed;
  - broader affected backend tests: 195 passed, 2 skipped;
  - `make phase0-check`: 22 passed;
  - touched-module `py_compile`: passed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-05-21-phase-2-offboard-fail-open-fixes.md`.

## 2026-05-21 - Phase 2 Operator Abort Fix

- Closed PXE-0027 by routing operator cancel/stop-tracking through an async
  safe path that disconnects PX4 following before tracker state is cleared.
- Covered external tracker stop requests so manual tracker-stop rejection no
  longer leaves PX4 following active.
- Updated FastAPI stop/cancel endpoints to return operation details from the
  safe path.
- Validation completed:
  - affected Phase 2 tests: 73 passed;
  - broader affected backend tests: 197 passed, 2 skipped;
  - `make phase0-check`: 22 passed;
  - touched-module `py_compile`: passed;
  - `git diff --check`: passed.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-05-21-phase-2-operator-abort-fix.md`.

## 2026-05-22 - Phase 2 Target-Loss Safe Publication

- Resumed after interruption by rereading the phase/slice map, issue register,
  modernization plan, architecture/API blueprints, and latest Phase 2
  checkpoints.
- Closed PXE-0031 by making inactive target-loss command publication an
  explicit follower opt-in instead of an accidental compatibility bypass.
- Updated `BaseFollower` with a shared inactive-output predicate and kept the
  default behavior fail-closed.
- Updated target-loss behavior so:
  - `GMVelocityVectorFollower` initializes failure counters, zeroes yaw as well
    as body velocity on stop/timeout, and remains robust when stale external
    gimbal input arrives.
  - `GMVelocityChaseFollower` publishes only explicit callback-generated loss
    commands, stops publication when RTL is requested, and otherwise emits a
    deterministic zero velocity/yaw command.
  - `MCVelocityChaseFollower` and `MCAttitudeRateFollower` route inactive
    vision output to explicit stop/hover commands instead of normal pursuit
    math, even if the output still carries last-known coordinates.
  - `FWAttitudeRateFollower` routes inactive vision output into fixed-wing
    target-loss policy without normal pursuit math.
- Added regression coverage for follower opt-ins, actual safe command fields,
  AppController dispatch after real `Follower` manager forwarding, and both
  `velocity_body_offboard` and `attitude_rate` PX4 send branches.
- Independent review gate found blockers in the first patch; those blockers
  were fixed before checkpointing.
- Validation completed:
  - focused PXE-0031 tests: 16 passed;
  - broader affected Phase 2 suite: 207 passed, 2 skipped;
  - `make phase0-check`: schema up-to-date and 22 passed;
  - touched-module `py_compile`: passed;
  - `git diff --check`: passed.
- No SITL, HIL, real-aircraft, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-05-22-phase-2-target-loss-safe-publication.md`.

## 2026-05-24 - Phase 2 Command Freshness

- Resumed after multiple interruptions by rereading `AGENTS.md`, the
  modernization plan, architecture/API blueprints, phase map, issue register,
  and latest Phase 2 checkpoints.
- Closed PXE-0032 by making video/frame freshness, tracker measurement
  freshness, and inactive follower dispatch part of one command-freshness
  contract.
- Updated `VideoHandler` and mock video fixtures so fresh, cached, and missing
  frames expose explicit `usable_for_following` metadata. Cached frames remain
  available for operator/video continuity but are command-stale.
- Updated `FlowController` so hard frame stalls while following call
  `AppController.handle_video_frame_unavailable()` instead of silently skipping
  the fail-closed target-loss command path.
- Updated `AppController` so:
  - stale/cached/prediction-only tracker output is converted to inactive
    fail-closed `TrackerOutput`;
  - inactive output is rejected unless the concrete follower explicitly opts
    in through `should_process_inactive_tracker_output()`;
  - first classic tracker update failure, recovery prediction, timeout, and
    always-reporting gimbal provider inactive outputs reach follower/PX4 safe
    publication paths.
- Updated tracker/follower behavior so:
  - `BaseTracker` and `SmartTracker` mark prediction-only or tentative selected
    targets as visible but unusable for command generation;
  - `GimbalTracker` clears active state when angle packets lack a fresh
    tracking status;
  - stale SmartTracker `MULTI_TARGET` outputs can publish only explicit
    fail-closed follower commands, never normal pursuit math;
  - multicopter velocity, multicopter attitude-rate, fixed-wing attitude-rate,
    and gimbal followers publish deterministic stop/hold/hover/orbit/cruise
    commands for their supported inactive inputs.
- Expanded docs and CI guardrails:
  - active docs now describe cached-frame and SmartTracker/gimbal freshness
    semantics;
  - the docs consistency guard checks local Markdown links across `README.md`
    and `docs/**/*.md`, ignoring code fences and inline code;
  - CI Phase 0 guardrail tests now include `test_parameters_reload.py`.
- Independent review gate found and drove fixes for:
  - first classic tracker loss skipping safe publication;
  - gimbal provider hard-stall inactive output not reaching followers;
  - stale docs and broken component links;
  - stale SmartTracker `MULTI_TARGET` output not matching follower inactive
    opt-ins.
- Validation completed:
  - touched-module `py_compile`: passed;
  - focused PXE-0032 suite: 68 passed;
  - broader affected Phase 2 suite: 357 passed, 2 skipped;
  - `make phase0-check`: schema up-to-date and 22 passed;
  - `git diff --check`: passed;
  - dashboard `npm run lint -- --format unix`, `CI=true npm test -- --watchAll=false`,
    and `npm run build`: passed.
- No SITL, HIL, real-aircraft, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-05-24-phase-2-command-freshness.md`.

## 2026-05-29 - Phase 2 Rate/Cadence Truth

- Resumed after interruption by rereading `AGENTS.md`, the modernization
  blueprint/API blueprint, phase map, issue register, latest journal, and
  Phase 2 checkpoints.
- Closed PXE-0030 by separating actual runtime timing contracts from future
  Offboard commander goals:
  - `FOLLOWER_DATA_REFRESH_RATE` is now treated as Hz, validated, clamped, and
    converted to `1 / rate` seconds before telemetry-loop sleep;
  - `SETPOINT_PUBLISH_RATE_S` is now a bounded SetpointSender monitor period,
    not a MAVSDK publish cadence;
  - `CONTROL_UPDATE_RATE` is documented/schemaed as follower math cadence, not
    Offboard heartbeat cadence.
- Updated `SetpointSender` status to expose the current negative publication
  contract: `sends_mavsdk_commands: false` and
  `command_publication_source: app_controller.follow_target`.
- Updated config schema generation overrides and regenerated
  `configs/config_schema.yaml` so rate units, bounds, defaults, and reload tier
  are checked from one source of truth.
- Updated drone/follower docs that still implied fixed 20 Hz command publishing
  from current PixEagle runtime, and added docs guard patterns to prevent that
  stale claim from returning before PXE-0007 lands.
- Independent review gate found blockers in tests/docs:
  - integration tests treated `SETPOINT_PUBLISH_RATE_S` as PX4 heartbeat;
  - docs still claimed current 20 Hz command cadence;
  - SetpointSender diagram pointed at the wrong dispatch method;
  - runtime accepted SetpointSender periods above schema max.
  All blockers were fixed before checkpointing.
- Validation completed:
  - touched-module `py_compile`: passed;
  - focused PXE-0030 suite: 167 passed;
  - broader affected Phase 2/backend suite: 237 passed;
  - `PYTHON=/tmp/pixeagle-audit-venv/bin/python make phase0-check`: schema
    up-to-date and 23 passed;
  - `git diff --check`: passed;
  - dashboard `npm run lint -- --format unix`, `CI=true npm test -- --watchAll=false`,
    and `npm run build`: passed.
- No SITL, HIL, real-aircraft, or field validation was run.
- PXE-0007/PXE-0013 remain open for the actual independent Offboard commander
  and unified safety truth.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-05-29-phase-2-rate-cadence.md`.

## 2026-05-29 - Phase 2 Safety Truth

- Resumed after interruption by checking the branch/worktree, rereading the
  phase map, issue register, journal, modernization blueprint, API blueprint,
  and active safety/offboard docs.
- Closed PXE-0033 by adding a shared command safety boundary in
  `src/classes/command_safety.py` and routing both `SetpointHandler` and final
  PX4 command sends through the same finite-value and limit-validation logic.
- Changed PX4 command gating so unavailable circuit-breaker infrastructure,
  circuit-breaker exceptions, and audit/logging failures block or explicitly
  degrade command dispatch unless the operator enables
  `FOLLOWER_ALLOW_COMMANDS_WITHOUT_SAFETY_MODULES`.
- Kept the bypass default fail-closed in `configs/config_default.yaml` and
  regenerated `configs/config_schema.yaml`.
- Fixed stale limit truth by moving pitch, roll, yaw, and deprecated alias
  mappings into `FIELD_LIMIT_MAPPING` and using follower-aware
  `Parameters.get_effective_limit(...)` at runtime.
- Fixed `BaseFollower.check_safety()` so `SafetyManager` exceptions produce an
  emergency-stop safety violation instead of returning `SafetyStatus.ok()`,
  except for the existing explicit circuit-breaker test bypass path.
- Updated safety, PX4, setpoint, follower, and no-drone testing docs so they no
  longer teach fail-open behavior, stale `Parameters.SafetyLimits` sources, or
  pitch/roll rate mapping through the yaw-rate limit.
- Added regression coverage for circuit-breaker unavailable/exception paths,
  operator bypass, non-finite command rejection, final PX4 command validation,
  pitch/roll/yaw mapping consistency, docs staleness guards, and
  SafetyManager-exception fail-closed behavior.
- Fixed an unrelated Python 3.12 test debt in `test_smart_click.py` that the
  full backend suite exposed after the safety slice was otherwise green.
- Independent review gate identified one remaining architecture debt: concrete
  followers still mutate shared `SetpointHandler` state field by field and some
  call sites can ignore failed field updates. The final PX4 gate now blocks
  unsafe values, but the atomic command-intent boundary is tracked as PXE-0034
  before the dedicated Offboard commander lands.
- Validation completed:
  - focused PXE-0033 suite: 233 passed;
  - Phase 0 API/reload gate: 13 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - `make phase0-check`: schema up to date and 24 passed;
  - broader affected schema/follower hygiene suite: 62 passed;
  - `tests/unit/core_app/test_smart_click.py`: 11 passed after the async test
    fix;
  - full backend suite: 1718 passed, 40 skipped;
  - touched-module `py_compile`: passed;
  - `git diff --check`: passed.
- No dashboard gates were required because this slice did not touch frontend
  code. No SITL, HIL, real-aircraft, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-05-29-phase-2-safety-truth.md`.
- Next active slice is the atomic command-intent and dedicated Offboard
  commander boundary under PXE-0007/PXE-0013/PXE-0034.

## 2026-05-30 - Phase 2 Command Intent Atomicity

- Resumed after interruption by checking the branch/worktree, rereading the
  phase map, issue register, journal, modernization blueprint, API blueprint,
  and the Phase 2 safety-truth checkpoint.
- Closed PXE-0034 by adding a frozen `CommandIntent` contract and an atomic
  `SetpointHandler.set_fields(...)` API that validates a staged field snapshot
  before committing it to live setpoint state.
- Migrated concrete followers from repeated `set_command_field(...)` mutation to
  `BaseFollower.set_command_fields(...)`, so follower output is now a complete
  command snapshot with source, reason, control type, and UTC/monotonic timing
  metadata.
- Preserved the single-field helper as a legacy/low-level path for tests and
  debug use, while docs and hygiene tests prevent active follower guidance from
  teaching field-by-field publication.
- Fixed the full-suite regression where the external-gimbal fail-closed test
  still used a pre-PXE-0034 handler stub and asserted field-by-field mutation;
  it now asserts the atomic zero-velocity command intent.
- Independent review gate findings addressed in this slice:
  - stale-field carryover is blocked when an atomic update is invalid or
    incomplete;
  - concrete followers no longer contain direct single-field publication calls;
  - follower and setpoint docs teach complete command snapshots;
  - unusable external gimbal output still reaches PX4 dispatch through the
    fail-closed zero-command path.
- Validation completed:
  - Phase 0 API/reload gate: 13 passed;
  - `bash scripts/check_schema.sh`: schema up to date;
  - affected follower/drone-interface/docs suite: 261 passed;
  - targeted fail-closed/command-intent rerun: 8 passed;
  - full backend suite: 1729 passed, 40 skipped;
  - touched-module `py_compile`: passed;
  - `git diff --check`: passed.
- No dashboard gates were required because this slice did not touch frontend
  code. No SITL, HIL, real-aircraft, or field validation was run.
- Checkpoint recorded in
  `docs/reporting/agent-ops/codex-modernization/checkpoints/2026-05-30-phase-2-command-intent.md`.
- Next active slice is the dedicated Offboard commander boundary under
  PXE-0007/PXE-0013.

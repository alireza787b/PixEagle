# Codex Modernization Journal - 2026-04

## 2026-04-30 - Phase 0 Started

- Reconfirmed approved plan from `/home/alireza/pixeagle_codex_improvement_plan_2026-04-29.md`.
- Created repo-local modernization reporting structure.
- Began Phase 0/Slice 1: clean-clone imports, route inventory, schema/frontend CI gates, and governance docs.
- Confirmed `classes.parameters` failed to import when `configs/config.yaml` was absent.
- Patched config loading to fall back to `configs/config_default.yaml` for clean-clone reads.
- Confirmed route inventory should be collected statically from `FastAPIHandler.define_routes()` to avoid runtime side effects.
- Added static API route inventory tests for the current 117-route FastAPI surface.
- Replaced detector/estimator audit stubs with real contract tests.
- Added a test hygiene guard for placeholder/audit-stub patterns.
- Made schema generation deterministic and changed schema check mode to compare via a temp file instead of mutating the working tree.
- Added CI gates for schema drift, route/hygiene/config guardrails, stricter backend pytest, full syntax checks, and dashboard install/test/build.
- Ran full Python tests: 1613 passed, 40 skipped, 7 warnings.
- Ran dashboard gates: `npm ci` succeeded, `npm test -- --watchAll=false` passed, and `npm run build` passed with existing ESLint warnings.

## 2026-04-30 - Phase 0 Slice 2 Infrastructure Docs

- Resumed after pause from the Phase 0 Slice 1 checkpoint and reread the
  approved plan, root operating guide, architecture blueprint, and API
  blueprint.
- Ran expert subagent reviews for docs staleness, DevOps/bootstrap service
  guidance, and companion-project alignment.
- Confirmed current MavlinkAnywhere defaults from `/home/alireza/mavlink-anywhere`:
  MAVSDK `127.0.0.1:14540`, MAVLink2REST input `127.0.0.1:14569`,
  local MAVLink `127.0.0.1:12550`, QGC `gcs_listen` on `14550/udp`, TCP
  server `5760`, and dashboard `127.0.0.1:9070`.
- Rewrote PixEagle's source-of-truth infrastructure docs around current
  MavlinkAnywhere instead of the stale `14541`/`14551` split.
- Corrected MAVLink2REST docs to use `/v1/mavlink/...` endpoints and current
  `MAVLink.*` config keys.
- Added a real `bash scripts/run.sh --no-dashboard` flag and corrected README
  guidance that previously claimed `-d` skipped the dashboard.
- Changed PixEagle's MAVLink2REST launcher default HTTP bind to
  `127.0.0.1:8088`, keeping network exposure explicit.
- Made launcher and port helpers fall back to `configs/config_default.yaml`
  when `configs/config.yaml` is absent.
- Added docs consistency and shell syntax guardrails to CI and `make phase0-check`.
- Recorded remaining stale secondary docs as PXE-0012 instead of treating the
  source-of-truth docs cleanup as a complete repo-wide docs rewrite.

## 2026-04-30 - Phase 0 Slice 3 Secondary Drone-Interface Docs

- Resumed from the Slice 2 checkpoint and continued PXE-0012 instead of
  restarting the audit.
- Used a focused expert subagent review to enumerate stale secondary docs in
  SITL, hardware, companion-computer, custom telemetry, no-drone testing, and
  troubleshooting guides.
- Updated secondary docs to current MavlinkAnywhere routing defaults:
  MAVSDK `127.0.0.1:14540`, MAVLink2REST input `127.0.0.1:14569`, and local
  MAVLink2REST HTTP `127.0.0.1:8088`.
- Replaced stale `/mavlink/...` examples with `/v1/mavlink/...`.
- Replaced lowercase config examples with current schema-backed keys:
  `PX4.SYSTEM_ADDRESS`, `MAVLink.*`, `Safety.GlobalLimits`,
  `Setpoint.SETPOINT_PUBLISH_RATE_S`, and `FOLLOWER_CIRCUIT_BREAKER`.
- Removed stale direct `python main.py`, Docker-first MAVLink2REST, fake
  `localhost:8000`, and old circuit-breaker API examples from the covered docs.
- Extended `tests/test_docs_infrastructure_consistency.py` to guard secondary
  drone-interface docs and actual current API examples.
- Confirmed manual stale-pattern scan now only finds explicit legacy notes in
  the source-of-truth port/PX4 docs.
- Closed PXE-0012 and added PXE-0014 for typed MAVLink polling timeout/retry
  configuration uncovered while cleaning the telemetry docs.
- Ran validation: docs guard 3 passed, `make phase0-check` 20 passed, full
  Python suite 1616 passed/40 skipped/7 existing warnings, and `git diff --check`
  passed.

## 2026-04-30 - Phase 0 Slice 4 Test Hygiene

- Resumed from Slice 3 and targeted PXE-0011, the remaining pytest warning debt.
- Confirmed `tests/test_gimbal_vector_body.py` was returning boolean status
  values from pytest test functions, which allowed stale tests to pass while
  emitting `PytestReturnNotNoneWarning`.
- Treating the warning as an error exposed that the old
  `GimbalVectorBodyFollower` module no longer exists and that the active profile
  is `gm_velocity_vector`.
- Reworked the smoke test to exercise the current `GMVelocityVectorFollower`
  implementation and verify that the removed `gimbal_vector_body` alias is not
  registered.
- Added a test hygiene guard that fails any pytest test function returning
  `True` or `False`.
- Marked the old gimbal vector developer summary as historical/superseded and
  added PXE-0015 for full cleanup of stale developer guidance.
- Ran validation: targeted PXE-0011 tests 9 passed, `make phase0-check`
  21 passed, full Python suite 1617 passed/40 skipped with no warnings, and
  `git diff --check` passed.

## 2026-04-30 - Phase 0 Slice 5 Legacy And Gimbal Contract Cleanup

- Resumed from Slice 4 and completed the stale gimbal/follower cleanup that was
  still open after the test hygiene pass.
- Removed the superseded gimbal-vector developer summary instead of keeping a
  historical file in active docs, renamed the smoke test to
  `tests/test_gm_velocity_vector_smoke.py`, and updated the docs index to point
  at current `gm_velocity_vector` guidance.
- Converted `configs/follower_commands.yaml` from `deprecated_profile_aliases`
  to `removed_profile_aliases`, matching the current fail-fast
  `FollowerFactory` behavior.
- Removed stale config backup files that still taught old flat gimbal keys and
  removed follower aliases.
- Audited the current gimbal implementation with an expert subagent and primary
  protocol references. The current protocol is Topotek SIP-series UDP
  (`GAC`/`GIC`/`TRC`/`OFT`), not MAVLink Gimbal v2.
- Corrected `GimbalInterface`, `GimbalTracker`, gimbal simulator docs, tracker
  docs, and tracker integration docs so followers remain protocol-agnostic and
  consume only normalized `TrackerOutput(GIMBAL_ANGLES, angular=(yaw,pitch,roll))`.
- Removed the old flat gimbal config fallback from `GimbalTracker`; active
  configuration now has one source of truth under `GimbalTracker.*`.
- Cleaned active runtime docs from old backend/OSD ports, stale
  `run_pixeagle.sh` entrypoints, and stale core-app `http.port: 8000` snippets.
- Extended `tests/test_docs_infrastructure_consistency.py` to guard active
  runtime docs against old ports, old flat gimbal keys, and incorrect SIP/passive
  wording.
- Closed PXE-0015 and PXE-0017; added PXE-0016 for the next runtime phase:
  extracting a typed `GimbalInputProvider` abstraction and moving the Topotek
  implementation behind `SipUdpGimbalProvider`.
- Ran validation: targeted gimbal/docs/follower checks 61 passed,
  schema check passed, `make phase0-check` 22 passed, full Python suite
  1618 passed/40 skipped, and `git diff --check` passed.

## 2026-04-30 - Phase 0 Slice 6 PX4/SITL Validation Scout

- Resumed from Slice 5 and kept scope to scouting/planning, not runtime SITL
  implementation.
- Used read-only specialist reviews to audit PixEagle's PX4/SITL/X-Plane docs,
  CI/Makefile, follower/tracker mocks, gimbal tests, and command-flow tests.
- Confirmed PixEagle has strong mock/unit foundations but no executable
  PX4-in-loop CI, no checked-in SITL runner, no scenario plan library, no
  artifact contract, and no full tracker/follower visual validation loop.
- Rechecked current `mavsdk_drone_show` validation practice: checked-in SITL
  scenario plans, stable evidence artifacts, API-first control paths, and
  explicit same-host/split-root workflows should be adopted in PixEagle.
- Rechecked official PX4/MAVSDK guidance: PX4 has prebuilt headless/Gazebo
  container images and Offboard requires continuous setpoint proof-of-life;
  MAVSDK Offboard requires a setpoint before start and resends setpoints at a
  fixed rate.
- Researched the user-suggested `jonasvautherin/px4-gazebo-headless` image. It
  is useful as an optional reference because it exposes MAVSDK/QGC ports and an
  RTSP camera mode, but it is unofficial; the PixEagle primary path should use
  official PX4 images or an internally pinned image.
- Added a target validation ladder to the modernization blueprint and expanded
  the issue register with PXE-0018, PXE-0019, and PXE-0020.
- Corrected one remaining gimbal docs contradiction: old flat gimbal keys are
  not supported after Slice 5.
- Ran `git diff --check`; result passed.
